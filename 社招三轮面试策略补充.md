# 字节 TikTok PGC/OGC 大模型算法岗 · 社招三轮面试策略补充

> 适用对象：已进入社招三轮、岗位为 TikTok PGC/OGC 大模型算法（多模态内容理解 + 视频配乐推荐 + Agentic AI + 大模型微调 + 训练推理加速 + 搜推 + 算法引擎工程化）。
> 定位：三轮面的是"你能否独立负责一个方向"，不是"你还会不会做题"。本文在原大纲基础上，把偏薄和缺失的模块展开成可落地的段落，每个模块给具体例子和踩坑点。

---

## 0. 先校准认知：三轮到底面什么

社招前两轮（或笔试+一面二面）通常已经验证了"你会不会"——基础算法、模型原理、coding 能力。能进三轮，说明招聘方认为你的**技术匹配度基本过关**。三轮的本质是压强测试另一个问题：**把一件模糊的事交给你，你能不能自己把它跑通**。

具体区别在于：
- 一二轮考"给定 X，你会不会做 Y"；三轮考"没有 X，你能不能把问题定义出来，并把它做成 Z"。
- 三轮面试官（通常是未来 Leader 或跨团队负责人）看的是五件事你是否主动补齐：**目标（做什么）、约束（不能做什么）、指标（怎么算赢）、风险（会怎么炸）、验证（怎么证明成了）**。缺任意一块，答案就会显得"像个执行者"。
- 底层原理和现场编码**不会停**。公开面经里"主管面突然让你手撕接雨水/最长上升子序列"是真实存在的。招聘方明显把"候选人的血压"也当成一个可观测指标——你被追问到卡壳时是否还能结构化拆解，本身就是评分项。

一个容易踩的坑：以为三轮是"聊愿景和软实力"，于是全讲业务价值、避谈技术细节。结果被追问"那你这个多模态系统具体怎么对齐、显存怎么估"时答不上来，反而暴露了深度不够。正确姿态是：**软的（目标/指标/协作）和硬的（原理/编码/部署）都要能接住**。

---

## 1. JD 写明但原大纲偏薄的高概率块

### 1.1 视频配乐推荐（检索 vs 生成 + 版权）

JD 明确列了"视频配乐推荐"，但原大纲的高概率问题里没有专块。这一块在 TikTok 这种强音乐心智的产品里权重不低，需要讲清完整链路，而不只是"用 embedding 召回"。

**任务定义**：输入是视频的多模态表征（视觉情绪/动作节奏、ASR 文本情绪、OCR、标题、发布者画像），输出是适配的 BGM。先要回答是**检索**还是**生成**：生产系统里几乎都是**检索为主、生成为辅**——因为版权可控、延迟低、结果可解释；生成式配乐目前更多用于无曲库匹配的兜底或特定营销场景。

**召回**：把视频内容表征和音乐语义标签（mood / tempo / BPM / energy / genre / 人声占比）对齐到统一向量空间，用 ANN（HNSW / IVF）召回候选。视频侧 embedding 来自多模态理解模型，音乐侧 tag 来自音频分析（BPM 检测、能量包络）加人工标注。关键是**两个空间怎么对齐**——常用方案是双塔对比学习，用"创作者最终采用的配乐"作为弱监督正样本。

**排序多目标**：这是最容易讲浅的地方。别只说 CTR。要列出至少四个目标并说明冲突：内容匹配度（情绪/节奏一致）、完播率、音乐跳过率、创作者采用率。匹配度高但抢人声的 BGM，跳过率会飙升；商业上更看重**创作者采用率**（因为采用 = 真认可）。多目标融合可用 ESMM 或 MMOE，并强调"采用率"作为主目标的业务合理性。

**冷启动**：新视频无播放信号时，靠内容理解特征（视觉节奏、ASR 情绪）直接进向量空间检索，不依赖协同信号；新歌冷启用 acoustic 特征（BPM/能量/流派）做内容召回。

**评估"配乐是否真的合适"**：离线用人工评测维度（情绪一致 / 节奏同步 / 不抢人声 / 不突兀），线上 AB 看音乐相关指标（配乐采用率、含音乐视频完播率、音乐相关互动），而不是只看整体 DAU。

**版权约束的工程化位置**（高频踩坑）：版权（地区可用、时长限制、独家授权、是否允许商用）**必须作为召回/过滤层前置**，而不是最后再过滤。踩坑案例：只在精排后过滤版权，导致某些地区候选枯竭、相关性骤降。正确做法是版权作为 mask 进 ANN 检索和粗排，地区维度进特征。

**生成式方向（趋势题）**：被问"能否用大模型生成配乐"时，讲清 tradeoff——生成的优势是无限曲库、风格可控、零版权纠纷（若自研模型）；劣势是推理成本高、风格一致性难控、商用版权界定仍模糊、长音频质量不稳。结论通常是"生成做兜底/长尾，检索做主力"。

### 1.2 搜索与推荐能力

JD 列了"搜推能力"，原大纲只在系统设计和 Agent 里顺带提。需讲清一个推荐/搜索系统的主干。

**多路召回融合**：内容召回（标签/类目）、协同召回（item-CF / user-CF）、向量召回（双塔 ANN）、标签召回。融合不是简单拼接——常用**级联**（向量召回万级 → 粗排千级 → 精排百级）或 **GBDT 融合**（把各路分数+特征喂给轻量模型学融合权重）。

**粗排 + 精排的算力分配**：精排用重 DNN 但只能跑百级候选，所以前置粗排（双塔/轻量模型）把万级打到百级。要讲清为什么粗排用双塔——因为 user 和 item 塔可离线预计算 item embedding，线上只做点积，延迟低。粗排的目标是"保召回率"而非"保精度"，所以允许一定误差。

**偏差处理**（容易漏）：位置偏差用 PAL（位置作为模块输入、不进入最终打分）或浅层位置塔消偏；选择偏差用 IPS（逆概率加权）纠正曝光不公；辛普森悖论——整体涨、分层掉，要分层看指标，不能只看总池。

**E&E**：新内容/新用户探索用 bandit（Thompson Sampling / UCB），与模型迭代共存的方式是"探索流量独立桶 + 探索结果回流训练"。

**冷启动**：用户冷启用设备/上下文特征兜底；item 冷启用内容特征（封面/标题/首帧）进向量空间，或用 look-alike 找相似老 item 的协同信号。

### 1.3 算法引擎工程化

这块原大纲只在结论提了一句，但 JD 写了"算法引擎工程化"，senior 面会问你怎么保证线上效果不崩。

**离线在线一致性**：最典型的事故是 **training-serving skew**——离线用 pandas 算特征、线上用 C++ 算，结果不一致导致指标掉。防护是**特征计算同一份代码**（特征 SDK 双端共用），并做离线在线特征对账（抽样比对）。**特征穿越（leakage）** 常见来源是用"未来信息"（如用点击后的统计特征训练），防护用 point-in-time correctness——每个样本只用其事件时间之前的特征快照。

**特征 store**：在线低延迟取特征（Redis / 自研 KV），离线回溯用历史特征表。要讲清"实时特征 vs 落库特征"的区别，以及特征版本如何随模型版本绑定。

**版本管理与灰度**：模型上线走 shadow mode（双跑不生效）→ canary（小流量）→ 全量。AB 实验平台对接、回滚触发条件（某核心指标跌破阈值自动回滚）。这直接呼应原大纲"业务方催上线但置信度不够"那题——答案之一就是"用 shadow + canary 先放过风险、用统计显著性决定是否全量，而不是硬上或硬拒"。

---

## 2. 系统设计框架的"交付物"补强（让答案从"说"变"画"）

原大纲给了九段式框架，但面试官要看你**画得出来**。补三个能直接写白板的模板。

### 2.1 延迟预算表

不要只说"要低延迟"。给出总预算（如 200ms，由产品 SLA 倒推）逐段拆解，并标清每段**可缓存吗、必须同步吗**：

| 环节 | 预算 | 可缓存？ | 必须同步？ | 压延迟手段 |
|---|---|---|---|---|
| 视频抽帧/解码 | 20ms | 部分（按 video_id） | 是 | 抽帧结果缓存复用 |
| ASR/OCR | 50ms | 否 | 是 | 流式分段、模型蒸馏 |
| VLM 理解 | 100ms | 否 | 是（瓶颈） | 量化/蒸馏/批处理/更小底座 |
| 重排/融合 | 30ms | 部分（预计算） | 是 | 重排异步预计算 |
| 网络往返 | 20ms | — | 是 | 就近部署、连接复用 |

讲清：抽帧结果可按 video_id 缓存复用；VLM 是瓶颈，靠量化/蒸馏/批处理压；重排可异步预计算。**踩坑**：只算模型推理时间忘了网络往返和序列化，实际 P99 超 SLA——预算表必须含端到端。

### 2.2 成本测算公式

给出可算的数字，而不是"要控制成本"：
- 单次推理 GPU 成本 =（单卡小时价 × 占用卡时）/ 该时段产出请求数。例：A100 约 $1–2/小时，一次请求占 0.1s 卡时，单请求算力成本约 $0.00003 量级，再乘 QPS 得日成本。
- 区分输入/输出 token 单价（输出通常贵 2–3 倍，因为要逐 token 生成）。
- 量化/蒸馏/KV Cache 复用的边际改善：INT8 推理吞吐约 ×1.5–2、显存约 ×0.5；KV Cache 复用（同 session 多轮）省掉重复 prefill。
- 模型迭代 ROI：精度提升带来的业务收益（如采用率↑→留存↑→LTV↑）− 推理成本增量，是否划算。**踩坑**：只报精度涨、不报成本涨，被问"值不值"时答不出。

### 2.3 数据闭环（不止"bad case 回流"一句）

讲清最小闭环的每一步和周期：
- **hard case 回流**：线上低置信/人工标记的错误样本自动入库。
- **主动学习**：用不确定性采样（熵/margin）+ 多样性采样挑高价值难例，而不是全量重标。
- **自动/半自动标注**：模型预标 + 人工抽检（如 95/5 比例），标注准确率门槛卡在进训练前。
- **再训练触发**：模型 drift 检测（预测分布偏移 / 业务指标掉）超阈值触发，周期从天级（高频场景）到周级。
- **踩坑**：闭环周期太长（月级）导致分布漂移后模型已严重退化；或自动标注比例过高引入噪声正样本，反而伤模型。

---

## 3. 底层技术的"深度自检"（面试官会追问到实现）

原大纲点名了 FlashAttention / ZeRO / TP / PP / Attention 切分。补"讲到什么程度才算过关"——这决定了你显得是真懂还是背过。

### 3.1 FlashAttention

不能只说"省显存"。要讲：标准 attention 把 N×N 的 softmax 中间矩阵写回 HBM，HBM 带宽成了瓶颈。FlashAttention 用 **tiling** 把 Q/K/V 分块（块大小按 SRAM 容量定），在 SRAM 内完成块间 attention 计算，**不把完整 N×N 矩阵落 HBM**；配合 **online softmax**（增量更新最大值和指数和，无需看整行），把 HBM 读写从 O(N²) 降到接近 O(N)。thread block 按 Q 的块划分，每个 block 流式扫 K/V 块。结果是显存 O(N) 且速度快数倍。

### 3.2 ZeRO 三阶段

- ZeRO-1：优化器状态分片（每张卡只存 1/N 的 optimizer state），通信量小。
- ZeRO-2：+ 梯度分片（反向时梯度 reduce-scatter 后只留本地分片）。
- ZeRO-3：+ 参数分片（前向/反向时按需 all-gather 参数，用完即弃）。
每张卡显存随阶段递减，但通信量递增（ZeRO-3 通信最多，接近 DP 的两倍）。ZeRO-3 可与 TP/PP 叠加——ZeRO 管数据并行内的分片，TP/PP 管单卡内的模型切分。

### 3.3 vLLM PagedAttention

KV Cache 传统按最大长度连续分配，导致内部碎片（实际长度 << 预留）。PagedAttention 像 OS 虚拟内存**分页**：KV 按 block（如 16 token）分配，逻辑连续、物理不连续，block 表映射。显存碎片化大幅下降，吞吐提升明显。要能讲清 block 粒度 attention 怎么算（按 block 索引取 KV）。

### 3.4 Attention 的 TP 切分

QKᵀV 里，Q/K/V 的 head 维度切到各卡（每张卡算一部分 head），各卡算完自己的 head 输出后，**在输出投影之前 all-reduce**（因为输出投影的权重是按隐藏维度，需要完整 head 拼接）。这是 TP 在 attention 上的标准切法，通信发生在每个 attention 层末尾。

### 3.5 编码题清单（原大纲只列接雨水/LIS，对 LLM 岗不够）

- **通用**：编辑距离、LCA、二叉树序列化/反序列化、拓扑排序、滑动窗口最大值、二分、堆 TopK、回溯（子集/排列）、接雨水、最长上升子序列。
- **ML 专属（高相关，强烈建议准备，因为它们能暴露你是否真懂原理而非背过）**：
  - 数值稳定的 softmax（减 max 防溢出）
  - 单头 attention（含 causal mask 的广播实现）
  - beam search（含长度归一化，避免偏好短句）
  - RoPE 旋转位置编码（奇偶维度旋转、复数视角）
  - label smoothing 的 cross-entropy（平滑后的目标分布）
  - two-tower 相似度（双塔点积 + 温度）
  - KV cache 增量计算（prefill vs decode 的区别）
  - cosine warmup scheduler（warmup + 余弦衰减）

### 3.6 现场编码：不只是 AC

面试官看四步，边写边讲比一次写对更重要：
1. **澄清约束**：输入规模（N 多大）、是否允许额外空间、边界（空输入/重复/溢出）。
2. **给复杂度**：先报时间/空间复杂度，再写。
3. **写测试/边界**：主动列测试用例（正常/边界/异常）。
4. **再写实现**：边写边解释每一步在干什么。
踩坑：闷头写、写完了才说"我没考虑空输入"，不如开场就把边界说清楚。

---

## 4. 完全没覆盖的主题

### 4.1 内容安全与版权合规

TikTok 是海外产品，senior 面几乎必问合规，且和"视频配乐推荐"的版权约束直接呼应。要讲进系统设计：
- **多模态内容审核**：色情/暴力/违禁/仇恨言论的分级流水线（模型初筛 → 风险分级 → 人工复审），误杀 vs 漏放的权衡（不同风险等级阈值不同）。
- **音乐版权**：地区可用表、独家/非独家、时长限制、商用授权——进召回/过滤层（见 1.1）。
- **地区监管**：EU 的 DSA、美国的 COPPA（未成年保护）、音乐 licensing 差异。怎么在系统里落地——合规 gate 作为前置检查，地区维度进特征与过滤。
- **踩坑**：把合规当"运营的事"而非"系统架构的事"，结果上线后某地区被下架。

### 4.2 Oncall / SRE

原大纲没提，但"线上指标下降你怎么判"这道题本质就是 SRE。准备：
- **SLO/SLI**：SLI 是可量化指标（延迟 P99、可用性），SLO 是目标（P99<200ms 的 99.9% 请求），error budget 消耗速度决定能否发版。
- **事故响应**：SEV 分级、war room、postmortem（无指责、写时间线+根因+action）。
- 把 ASR 流式项目的"线上故障定位"升华：你是怎么从 RTF 异常、内存涨、并发掉里定位到 VAD 分段 bug 的——这就是工程成熟度的证据。

### 4.3 A/B 统计深度

原大纲"置信度不够是否上线"要能讲出为什么不够：
- **MDE（最小可检测效应）**：样本量由 MDE 反推，效应太小需要海量流量。
- **peeking problem**：实验中途反复看 p 值，一类错误率被 inflate（应用序贯检验或固定看板节奏）。
- **SRM（样本比失衡）**：分流后两组人数显著不等，说明分流有 bug（用卡方检验查），此时任何结论都不可信。
- **多重比较**：看多个指标要校正（Bonferroni / Hochberg），否则假阳性堆积。

### 4.4 影响他人无职权

跨团队协作（原大纲"你如何推动数据、后端、产品和算法团队协作"）的具体抓手：
- **RACI**：明确谁 Responsible / Accountable / Consulted / Informed，避免责任模糊。
- **design doc**：写一份能被其他团队 adopt 的文档（背景、目标、方案、权衡、迁移计划），而不是口头推动。
- **用数据推动**：用"不做的代价"量化（如现状每天浪费多少 GPU、bad case 多少），而不是靠职级压。

### 4.5 技术路线图与容量规划

直接呼应原大纲"人力只有两个人，半年规划怎么排"：
- **路线图**：对齐业务 OKR，拆成季度里程碑，每个里程碑有可验证产出。
- **容量估算**：团队人日 = 任务数 × 单任务估时 × 缓冲；用 RICE（Reach/Impact/Confidence/Effort）排优先级，两人半年只能做高 RICE 的少数几件。
- **踩坑**：排满不预留 buffer，一个线上事故就全乱；或只排技术债不排业务产出，被业务方质疑价值。

---

## 5. 行为面补漏

### 5.1 反向提问环节

原大纲完全没提"你有什么要问我的"，但高层面试的反问质量很关键（显示你在想 ownership，不是在求录用）。准备 3–5 个有深度的问题，并想清楚为什么问：
- 这个岗位对应的业务下一年的核心目标是什么？（判断业务是否在上升期）
- 团队当前最大的技术痛点 / 技术债是什么？（判断你进去能不能立刻有价值）
- 这个岗位的 success 定义是什么（半年/一年看什么指标）？（对齐预期，避免入职才发现标准模糊）
- 为什么这个位置空缺（新设 / 前任去了哪）？（判断是增量岗还是填坑岗）
- 团队在训练/推理资源上的边界在哪里？（判断落地自由度）

### 5.2 行为案例扩维

原大纲要求准备 3 个（失败 / 冲突 / 主动）。建议再加三类，且每个案例用"情境 → 行动 → 结果 → 反思"结构讲：
- **技术决策权衡**：精度 vs 延迟 vs 成本，你怎么选、选完的后果。
- **带人 / 带项目**：不是写代码，是让别人把事做成（分工、对齐、兜底）。
- **数据推翻直觉**：你以为 A 对，实验打脸后怎么归因、怎么改。
技巧：同一项目可同时答技术和行为问题，准备深度比数量重要——把一个项目讲到能答 5 种问题，胜过准备 5 个浅项目。

### 5.3 压力测试应对口径

原大纲点出"血压可观测"但没给预案。准备三句通用口径：
- **被当场质疑方案**：承认边界 + 给备选 + 说验证方式，**别硬顶**。例："这个方案在 X 约束下确实不是最优，我备选是 Y，可以用 Z 指标验证哪个更好。"
- **被要求白板改架构**：先复述约束（延迟/成本/数据），再给最小改动方案，而不是推倒重来。
- **时间不够**：先交能跑的 baseline，再标"若加 10 分钟我会补 X"——展示优先级感。

---

## 6. PGC/OGC 业务特殊性

- **数据特征**：PGC/OGC 元数据质量高、授权清晰、噪声结构更规范，但长尾更明显（头部内容海量、腰部稀疏）。相比 UGC，标签更可信但覆盖不均。
- **商业化指标权重**：靠广告/订阅/创作者基金，完播、互动、订阅转化比纯曝光更受关注；推荐系统优化目标要往"采用率/订阅/时长"倾斜，而非只追 CTR。
- **LIVE 实时性**：TikTok 有直播，若岗位沾边要准备流式/实时理解（低延迟、partial 结果、断线重连、实时配乐切换）。

---

## 7. 三轮前最低准备清单（合并版 + 执行节奏）

能连续讲 10 分钟、并经受追问的模块：
1. 一个项目的完整技术闭环（含失败方案和量化结果）；
2. 一个多模态 / 大模型系统设计题（从数据讲到线上监控 + 延迟预算表 + 成本测算）；
3. 一道 Medium 编码题（边写边讲复杂度与边界）；
4. 三个行为案例（失败 / 跨团队冲突 / 主动推动）+ 扩维案例备用；
5. 3–5 个高质量反问。

**执行节奏**（建议）：每天挑 1 个模块，按"讲 10 分钟 → 自录回听 → 找卡壳点补"循环；编码题每天 1 道边写边讲；底层原理每天深度过 1 个（FlashAttention/ZeRO/PagedAttention 轮流）。三轮前至少完整走 2 轮。

---

## 8. 一句话收尾

三轮 = "独立负责"的压强测试。底层原理和编码不要停，但决定成败的是你能不能把模糊目标变成**可度量、可验证、可兜底**的闭环——并且被追问到实现细节时，还能接得住。

---

## 附：与题库的对应关系

本文补的"软模块"（系统设计交付物、行为面、合规、路线图）偏向策略，未全部转成题卡；技术缺口已落卡：
- `cards/系统设计/sys-content-safety-compliance.js` — 海外短视频多模态内容安全 + 音乐版权 + 地区合规
- `cards/搜索推荐/rec-music-generation.js` — 视频配乐生成式 vs 检索式权衡 + 版权闸门
技术深度（搜推/Agent/推理/FlashAttention/ZeRO/PagedAttention）在题库已有专卡，本文第 3 节是"讲到什么程度过关"的深度自检，与卡互补。

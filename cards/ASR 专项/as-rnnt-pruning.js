export default {
  id: 'as-rnnt-pruning', category: 'ASR 专项', difficulty: 'Hard', kind: 'code',
  title: 'RNN-T 解码剪枝到底省了什么',
  prompt: 'RNN-T 解码中 beam、状态合并、blank 阈值和词表 shortlist 分别削减哪部分计算？',
  quickAnswer: 'beam pruning 减少需要保留和继续扩展的文本历史；相同前缀可共享 predictor 状态；blank 阈值可让高置信 blank 帧少做标签扩展。仅在完整 vocab logits 之后取 top-k，并没有省掉这次词表投影；若要省词表计算，还需要 shortlist、分层 softmax 或专门 kernel。',
  beginnerSummary: '剪枝不是一个按钮。少留几条句子能省搜索，复用相同前缀能省 predictor，遇到明显静音少展开能省 joiner；但“先算完所有词再取前几个”省不了刚才那次全词表计算。',
  explanationFocus: '先把 encoder、predictor、joiner/词表投影和 beam 管理四类成本分开，才能知道某种剪枝真正优化哪一段。',
  approach: '逐项消融 beam width、每帧最大标签数、blank skip、前缀状态缓存和 shortlist，并记录各模块耗时，而不是只报告总 RTF。',
  derivation: ['为什么需要：RNN-T 会在同一声学帧上多次发标签，候选历史和 joiner 调用可能迅速增长。', '怎么实现：按累计 log score 截断 beam，缓存 predictor 状态，并在可靠 blank 帧减少非 blank 展开。', '有什么代价：剪得过早可能永久丢掉正确前缀，罕见词和含噪语音更敏感。', '怎么评测：画 WER/CER—RTF 曲线，并分解 encoder、predictor、joiner、beam 管理耗时。'],
  prerequisites: ['RNN-T 与 Transducer', 'Beam Search 与 top-k'],
  workedExample: ['示意：beam 从 8 降到 4 后 predictor 状态数减少，但每个保留状态若仍算完整词表，词表投影成本仍在。', '高 blank 后验帧跳过标签扩展可加速静音段；噪声段阈值过激则可能漏字。'],
  code: "def prune_hypotheses(hypotheses, beam_size, score_margin):\n    ranked = sorted(hypotheses, key=lambda h: h.score, reverse=True)\n    best = ranked[0].score\n    return [h for h in ranked[:beam_size] if h.score >= best - score_margin]",
  lineByLine: ['先按累计分数排序候选历史。', '同时用最大条数和相对最佳分数差剪枝。', '这段只省搜索状态，并未实现词表 projection shortlist。'],
  complexity: '复杂度不能只写 O(T·k·B)：每帧可能发多个 label，且完整词表投影是否被计算取决于实现。应以 joiner 调用数、活跃假设数和实际 profile 为准。',
  diagram: 'encoder frame + predictor state ─▶ joiner / vocab scores ─▶ 扩展候选 ─▶ beam pruning\n                       ▲ predictor cache                         └▶ blank skip',
  edgeCases: ['blank 分支不能因 top-k 实现错误而丢失。', '一个声学帧连续发很多 label 时要限制最大展开数。', '罕见词可能长期分数偏低，对小 beam 更敏感。'],
  pitfalls: ['承诺剪枝后仍一定保留全局最优路径——近似 beam search 没有这种保证。', '把 logits 计算后的 top-k 写成“把 O(V) 降成 O(k)”。'],
  followUps: [{ question: '量化和剪枝可以叠加吗？', answer: '可以，但量化会改变分数间隔，必须重新校准 beam margin、blank 阈值和准确率。' }, { question: '怎样知道瓶颈在 joiner 还是 beam 管理？', answer: '用 profiler 记录各模块耗时、调用次数与活跃假设数；不同硬件和 batch 下结论可能相反。' }],
  references: [{ title: 'Sequence Transduction with Recurrent Neural Networks', url: 'https://arxiv.org/abs/1211.3711' }],
};

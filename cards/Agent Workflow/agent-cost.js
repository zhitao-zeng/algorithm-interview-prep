export default {
  "id": "agent-cost",
  "kind": "concept",
  "category": "Agent Workflow",
  "difficulty": "Medium",
  "title": "Agent 的成本控制（token 消耗）",
  "prompt": "怎么控制 Agent 的 token 与调用成本？",
  "code": "def budget_guard(state, budget):\n    if state.tokens > budget:\n        raise BudgetExceeded(state.tokens)\n    if state.steps > state.max_steps:\n        return finalize(state)               # 硬截断\n\n# 配合 cache: get_cached(query) or retrieve(query)",
  "diagram": "Budget --guard--> stop; Cache --hit--> skip retrieve",
  "explanationFocus": "是什么：Agent 成本控制是一套工程实践，通过限制调用次数（max_steps）、压缩/裁剪上下文、模型路由（简单任务用小模型）、结果缓存（TTL）与预算硬熔断，把 token 与工具费用压到『任务价值可接受』的范围内。其本质是『质量-延迟-成本』三角中的主动取舍，而非无限调用 LLM。",
  "quickAnswer": "Agent 成本主要来自多轮模型调用与膨胀的上下文。控制手段：限制步数与重试（max_steps）、压缩/摘要历史、简单决策用小模型（模型路由）、缓存重复检索与中间结果（TTL）、批处理工具调用。本质是在『质量-延迟-成本』三角中按任务价值取舍，并用预算上限做硬熔断。",
  "beginnerSummary": "控成本像家庭做预算：给每次任务设花销上限（预算护栏），重复买的东西先看看家里有没有（缓存），小事自己办、大事才请贵专家（模型路由），月底超支就停卡（熔断）。不加约束的 Agent 就像没有预算的小孩，几轮对话就把 token 和钱烧光。",
  "walkthrough": "某客服 Agent 优化前：平均 14 步、上下文 ~8k token、单均成本 ¥0.12。加 max_steps=6、历史每 8 轮做一次摘要（压到 ~2k token）、FAQ 检索结果缓存 60s、简单意图分类路由到 7B 小模型（复杂才上 70B）。优化后单均成本 ¥0.054（降 55%），用户满意度（CSAT）持平在 92%。",
  "approach": "按『限步数 → 压上下文 → 模型路由 → 缓存 → 预算熔断』五步推进：先用 max_steps 与预算兜底防失控，再用摘要/裁剪降上下文单价，接着把简单子任务分流到便宜模型，最后对高重复调用加 TTL 缓存。",
  "bruteForce": "无限制循环调用：Agent 每轮反思都再调一次 LLM、每次都带全量历史，几轮对话就烧掉上万 token 与多次调用费用，长任务直接超预算。",
  "invariant": "任何单任务成本不得超过预算硬上限，超限即安全终止（finalize），绝不因为『再调一次就好』而突破预算。",
  "complexity": "优化后单任务成本 ≈ 步数 × 路由单价（小模型便宜、大模型贵）+ (1-缓存命中率) × 检索成本；上下文压缩把每步的 token 单价降下来。整体成本随步数线性、随缓存命中率下降，缓存命中率高时可降一个数量级。",
  "derivation": [
    "为什么需要：Agent 天然多轮（思考-行动-观察循环），冗余反思/重试/超长上下文会让 token 与调用费失控，必须系统性约束而非事后惊讶。",
    "怎么实现：设 max_steps 与 token 预算；定期对历史做摘要降长；简单子任务路由到小模型、复杂才上大模型；检索与只读工具结果加 TTL 缓存；高延迟调用批处理合并。",
    "有什么代价：压缩/摘要可能损失细节影响质量；小模型误判需回退大模型反而增成本；缓存有陈旧风险（TTL 内返回旧答案）。",
    "怎么评测：跟踪单位任务平均成本、成本方差、以及成本-质量帕累托前沿（如成本降 50% 时质量掉多少），用 A/B 验证降本不降质。"
  ],
  "edgeCases": [
    "缓存命中了但答案已过时（如股价、库存变化），返回陈旧结果误导用户，需合理 TTL + 关键数据绕过缓存。",
    "预算截断发生在任务关键步，导致任务未完成就终止，需要 finalize 给一个『尽力而为』的答复而非直接报错。",
    "小模型误判把复杂问题当简单处理，回退大模型补救，反而比一开始就用大模型更贵。",
    "批处理工具调用若其中一项失败，需设计部分成功处理，避免整批重跑浪费。"
  ],
  "pitfalls": [
    "只砍步数不顾质量，步数压太低导致任务做不完、质量崩坏，省了钱丢了口碑。",
    "缓存不设 TTL 或 TTL 过长，返回陈旧结果（如过时的优惠信息）造成事实错误。",
    "模型路由阈值拍脑袋，简单任务误上大模型白花钱、或复杂任务误用小模型反复重试更贵。"
  ],
  "prerequisites": [
    "能统计 token 消耗与每次调用的单价（知道钱花在哪）。",
    "有模型路由能力（能按难度把请求分给不同尺寸模型）。",
    "可在 Agent 循环里插入缓存与预算钩子（拦截点）。",
    "能衡量任务质量（CSAT/成功率），否则降本不知是否伤质。"
  ],
  "workedExample": [
    "max_steps=6 与 token 预算双硬限制：超步数直接 finalize，避免无限反思烧钱。",
    "FAQ 检索结果加 60s TTL 缓存：同一问题百次询问只检索 1 次，检索成本降 ~99%。",
    "意图分类路由：『查订单状态』这类简单意图走 7B 小模型，仅 5% 复杂意图上 70B，整体成本降 55%。"
  ],
  "lineByLine": [
    "统计累计 token 与步数：每轮更新 state.tokens、state.steps，作为护栏输入。",
    "超预算立即终止：state.tokens > budget 抛 BudgetExceeded，进入安全收尾。",
    "超步数硬截断：state.steps > max_steps 调 finalize 返回尽力结果，不再调用。",
    "简单决策路由小模型、重复结果走缓存：分层降本，贵操作仅用于必要处。"
  ],
  "codeNotes": [
    "预算 Hard stop 是最后防线，防止任何逻辑漏洞导致成本爆炸。",
    "缓存必须带 TTL 且对时效敏感数据设更短 TTL 或绕过，防返回陈旧答案。",
    "finalize 应返回一个『部分完成』的安全答复，而不是裸奔报错，保证用户体验。"
  ],
  "followUps": [
    {
      "question": "小模型路由会降质量吗？",
      "answer": "对简单决策（意图分类、格式校验、抽取）影响极小，因为这些任务大模型和小模型差距不大；对复杂推理/生成，路由错了会出错，这时用『置信度』决定是否升级——小模型输出置信度低或触发回退规则，就转给大模型。关键是路由判据要准，路由成本本身远低于大模型调用。"
    },
    {
      "question": "缓存最适合哪类调用？",
      "answer": "高重复、低时效的调用最划算：比如 FAQ 检索、知识库查询、只读工具（如查汇率的历史快照）、以及 Agent 中间结果（同一子问题已被算过）。给这类调用加 TTL（如 30~300s），在『新鲜度』与『省钱』间平衡；对实时数据（股价、库存）则缩短 TTL 或干脆不缓存。"
    }
  ],
  "followUpAnswers": [
    "对简单决策（意图分类、格式校验、抽取）影响极小，因为这些任务大模型和小模型差距不大；对复杂推理/生成，路由错了会出错，这时用『置信度』决定是否升级——小模型输出置信度低或触发回退规则，就转给大模型。关键是路由判据要准，路由成本本身远低于大模型调用。",
    "高重复、低时效的调用最划算：比如 FAQ 检索、知识库查询、只读工具（如查汇率的历史快照）、以及 Agent 中间结果（同一子问题已被算过）。给这类调用加 TTL（如 30~300s），在『新鲜度』与『省钱』间平衡；对实时数据（股价、库存）则缩短 TTL 或干脆不缓存。"
  ]
};

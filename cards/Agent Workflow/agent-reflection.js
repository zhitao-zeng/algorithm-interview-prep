export default {
  "id": "agent-reflection",
  "kind": "concept",
  "category": "Agent Workflow",
  "difficulty": "Medium",
  "title": "反思（reflection）/ self-critique",
  "prompt": "Agent 的反思（reflection）/ self-critique 机制是什么？",
  "code": "def reflect(llm, task, draft, rounds=3):\n    cur = draft\n    for i in range(rounds):\n        feedback = llm(f\"Critique:\\n{task}\\n{cur}\")  # 批评者视角\n        if feedback.is_pass():\n            break\n        cur = llm(f\"Revise per feedback:\\n{feedback}\\n{cur}\")\n    return cur",
  "diagram": "Draft -> Critique -> Revise -> Critique -> ... -> Pass",
  "explanationFocus": "是什么：反思（reflection / self-critique）是 Agent 在产出答案或计划后，主动『以评论者视角』审视自己的结果，找出错误、偏差或遗漏，再迭代修订的机制。它把『一次性输出』升级为『生成 → 批评 → 改进』的闭环，显著提升复杂任务（写代码、做数学、长文规划）的质量。",
  "quickAnswer": "反思让 Agent 产出后『当自己老师挑毛病』：生成初稿，再用批评提示（或独立 critic 模型）指出错误/偏差/遗漏，然后照反馈修订，可多轮直至通过或达上限。它把『一次交付』变成『生成-批评-改进』闭环，复杂任务质量明显提升。",
  "beginnerSummary": "反思像你写完东西后自己当校对：先写一版，再假装是老师挑毛病（『这里漏了空值处理』『那段逻辑有漏洞』），然后照着改，直到挑不出错。比『写完就交』可靠得多，尤其写代码、做推理这类容易藏小错的活。",
  "walkthrough": "以『写函数』任务为例：v1 生成未处理空输入；critic 指出『未处理 None/空列表』；v2 补了边界判断；critic 再检查『还漏了负数情况』；v3 补负数，critic 通过。共 3 轮、2 条关键反馈，最终函数覆盖所有边界。相比一次交付，v3 通过单测率从 60% 升到 100%。",
  "approach": "按『生成初稿 → 自我/他者批评 → 指出问题 → 修订 → 再评估』循环；critic 可用同一模型换批评提示，也可用独立更强的模型降低偏袒；需明确『通过信号』与『停止条件（rounds 上限）』防无限改。",
  "bruteForce": "只生成一次就交付：模型首稿常含逻辑漏洞、事实错误或偏离目标，尤其多步推理会累积小错，且无二次校验，错误直接暴露给用户。",
  "invariant": "每轮修订必须显式对照『上一条批评意见』来改，不能凭空重写；且必须保留 best-so-far，防止越改越差。",
  "complexity": "成本 = 轮数 ×（生成 + 批评）两次调用；通常 2~4 轮收敛。若用独立 critic 模型（如小模型批评、大模型生成）可降本；轮数受 rounds 上限约束，最坏 O(rounds) 倍单次成本。",
  "derivation": [
    "为什么需要：模型首稿常含逻辑漏洞、事实错误或偏离任务目标，尤其长任务多步推理会累积小错，需要一次『二次校验』来兜底。",
    "怎么实现：把『原答案 + 任务要求』交给批评提示（或独立 critic 模型）产出反馈，再把反馈拼回让模型修订；可多轮循环直至 critic 判通过或达 rounds 上限。",
    "有什么代价：每多一轮就多一次模型调用，成本与延迟上升；critic 本身也可能误判（漏错或冤枉对的）；无停止条件会无限改烧 token。",
    "怎么评测：看修订前后质量提升（如单测通过率、人工评分）、收敛步数、以及是否引入新错；用 best-so-far 保留最优，避免越改越差。"
  ],
  "edgeCases": [
    "critic 与生成者用同一模型，容易『互相包庇』——生成犯的错 critic 也意识不到，需换独立更强模型或加外部校验。",
    "过度反思：critic 挑刺上瘾，把原本正确的地方改坏，质量反而下降，需 best-so-far + 客观校验兜底。",
    "无停止条件：模型对批评永远不满意，陷入改不完的循环，必须设 rounds 上限或『通过即停』。",
    "critic 反馈太空泛（『还可以更好』），模型无法据此有效修订，需让 critic 给具体、可执行的意见。"
  ],
  "pitfalls": [
    "没有停止条件，反思无限循环烧 token，直到预算耗尽。",
    "critic 与 actor 同源，缺乏独立判断，错误被『自己人』放行。",
    "不保留 best-so-far，某轮 critic 误判导致把好的改坏，最终交付反而更差。"
  ],
  "prerequisites": [
    "模型能扮演『批评者』角色（给反馈而非只生成）。",
    "有可量化的质量判据（单测、评分、校验），否则不知改好没改好。",
    "需明确的停止条件（rounds 上限或『通过信号』），防无限循环。",
    "理解 best-so-far / 客观校验，避免越改越差。"
  ],
  "workedExample": [
    "代码 v1 被 critic 指出『缺空值/None 处理』；v2 补上；critic 再指出『负数未处理』；v3 补齐后通过，单测 60%→100%。",
    "数学题：v1 代入算错；critic 指出『第 2 步代入符号错』；v2 修正得正确答案，避免交错解。",
    "长文规划：v1 漏了风险章节；critic 提示后 v2 补『风险评估』，完整性评分从 7/10 到 9/10。"
  ],
  "lineByLine": [
    "生成初稿 draft：先让 actor 产出第一版结果，不急着交付。",
    "用批评提示产出 feedback：把任务 + 初稿喂给 critic，得到具体可执行的改进意见。",
    "对照反馈修订 cur：把 feedback 拼回，让模型针对性修改而非重写。",
    "循环至通过或达 rounds 上限：每轮都重新批评，避免无限改。"
  ],
  "codeNotes": [
    "critic 与 actor 可用不同模型（如小模型批评、大模型生成）降低偏袒与成本。",
    "需明确的『通过/停止』信号，否则循环不会自己停。",
    "实际部署建议保留 best-so-far，并优先用客观校验（单测/评分）决定留哪版。"
  ],
  "followUps": [
    {
      "question": "反思和 ReAct 有什么区别？",
      "answer": "ReAct 是在『行动循环』里边想边做边观察，用于推进任务执行、纠偏工具调用；反思是在『产出之后』做质量评审，用于提升最终答案/代码的正确性。二者不冲突、可叠加：外层用 ReAct 推进任务、调工具，内层在 key 产出后插一轮反思把关。简单说：ReAct 管『把事做成』，反思管『把结果做对』。"
    },
    {
      "question": "怎么避免越改越差？",
      "answer": "三招：①保留 best-so-far——每轮修订后和上一版比，只保留更优的；②用客观校验决定去留（如跑单测、算评分、比对事实），而不是盲信 critic 的主观判断；③设停止条件（rounds 上限或『通过即停』），critic 一通过就交，不贪图『再改改』。这样即使某轮 critic 误判，也不会让最终交付变差。"
    }
  ],
  "followUpAnswers": [
    "ReAct 是在『行动循环』里边想边做边观察，用于推进任务执行、纠偏工具调用；反思是在『产出之后』做质量评审，用于提升最终答案/代码的正确性。二者不冲突、可叠加：外层用 ReAct 推进任务、调工具，内层在 key 产出后插一轮反思把关。简单说：ReAct 管『把事做成』，反思管『把结果做对』。",
    "三招：①保留 best-so-far——每轮修订后和上一版比，只保留更优的；②用客观校验决定去留（如跑单测、算评分、比对事实），而不是盲信 critic 的主观判断；③设停止条件（rounds 上限或『通过即停』），critic 一通过就交，不贪图『再改改』。这样即使某轮 critic 误判，也不会让最终交付变差。"
  ]
};

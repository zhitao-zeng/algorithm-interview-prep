export default {
  "kind": "concept",
  "id": "agent-planning-algo",
  "category": "Agent Workflow",
  "difficulty": "Hard",
  "title": "Agent 的规划算法（tree / search / ToT）",
  "prompt": "Agent 的规划算法（tree / search / Tree-of-Thoughts）是怎样的？",
  "quickAnswer": "这类算法把推理/行动建模为搜索问题：ToT 让模型在每步生成多个候选「思路」(thought)，用模型自评估对每条打分/剪枝，沿有希望的分支继续展开，像树搜索（BFS/DFS/beam）找最优解。相比线性 CoT/ReAct，它支持探索、回溯与全局择优，适合需要多路径试错的难题（谜题、规划），但调用数与成本更高。",
  "approach": "多候选展开 → 评估打分 → 剪枝/回溯 → 择优深入。具体：在搜索树的每个节点让 LLM 产出 k 个 thought；用 critic（可同模型或专用小模型）给状态打分；按 beam/BFS/DFS 选 top 分支展开；到达终态或死路则回溯到未穷尽节点继续，直到找到解或超预算。",
  "explanationFocus": "是什么：ToT 等规划算法把 Agent 的推理/行动建模成树状搜索问题——每步让模型生成多个候选「思路」(thought)，用模型自评对每条打分/剪枝，仅沿有希望的分支继续展开，支持探索、回溯与全局择优。相比线性 CoT/ReAct 一条路走到黑，它适合需要多路径试错的难题。",
  "bruteForce": "线性 ReAct/CoT 一条路走到黑：第一步选错就再无回头机会，遇到分支与死路会一错到底，对需要试错的谜题/规划任务成功率很低。",
  "derivation": [
    "为什么需要：很多任务（谜题、数学、多步规划）有分支与死路，单链推理无回溯会一错到底；人类解题也是「试几条路、不行就换」，ToT 把这种探索形式化。",
    "怎么实现：每节点让 LLM 产 k 个 thought；用 critic 给状态/思路打分（可行性、进展）；按 beam/BFS 选 top 展开；到达终态返回并 backtrack 重建路径，死路剪枝。也可用价值网络替代 LLM 评分降本。",
    "有什么代价：候选×评估导致 LLM 调用数激增、成本高、延迟大；评估函数若不准会剪掉正解或留错分支；树管理（回溯、去重）增加工程复杂度。",
    "怎么评测：看难题成功率（相对 CoT/ReAct 的提升）、探索效率（有效分支比 = 通向解的分支/总展开）、以及单任务平均成本与延迟，权衡收益与开销。"
  ],
  "invariant": "只有被评估为「有前景」（高于阈值或进入 top-k）的分支才展开；死路必须可回溯而非硬撑，否则退化为穷举或单链。",
  "walkthrough": "以 24 点游戏为例：给定数字 {4,7,8,8}，根节点生成 3 个候选算式（thought），critic 给每个打可行分（0~1），保留 top-2（beam=2）；对每个分支再生成 3 个后续，再打分保留 top-2；某分支算到 (8-4)=4 后无后继（死路），则回溯到上一节点换另一算式。共展开约 9~12 个节点、调用 LLM 约 30+ 次（含评估），最终找到解。对比线性 ReAct 单链常卡死在死路。",
  "edgeCases": [
    "评估函数误判：把正确分支打低分剪掉，导致无解——需留一定探索余量或多次评分取平均。",
    "分支爆炸：候选多、深度大时节点指数增长，需用 beam 限宽与最大节点预算封顶。",
    "回溯过深：反复回溯导致成本飙升且可能陷入循环，需记录已访问状态去重。",
    "无可行解：整棵树穷尽仍无解，应早停并返回「无法确定」而非无限展开。"
  ],
  "code": "def tot(llm, root, beam=2, depth=4):\n    frontier = [root]\n    for d in range(depth):\n        cands = [t for n in frontier for t in llm.thoughts(n)]\n        scored = [(t, llm.score(t)) for t in cands]\n        frontier = topk(scored, beam)          # 仅留高分分支\n        if any(is_solved(n) for n in frontier):\n            return backtrack(frontier)\n    return None",
  "codeNotes": [
    "beam 宽度控制分支爆炸，是成本与覆盖率的关键旋钮；depth 控制探索深度。",
    "评估函数质量决定上限：可用专用小模型评分、多次采样平均，或接价值网络。",
    "backtrack 用于从解节点重建完整路径，便于解释、回放与归因。"
  ],
  "complexity": "LLM 调用数 ≈ 节点数 ×（候选数 k + 评估数），呈树级增长，beam 宽度与深度直接决定成本。例如 beam=2、depth=4、k=3、每节点评分 1 次，约 2^4×3×2 ≈ 近百次调用，需靠小模型评分与 beam 限宽压成本。",
  "followUps": [
    {
      "question": "ToT 和 ReAct 怎么选？",
      "answer": "看任务是否需要「多路径试错」。需要探索、回溯、在分支中择优的难题（谜题、数学证明、复杂规划）用 ToT，成功率高但贵；顺序执行、单链即可的任务（「查天气→播报」）用 ReAct 更省、更稳。也可混合：ReAct 做主干，遇到卡点再启动局部 ToT 搜索。"
    },
    {
      "question": "成本怎么压？",
      "answer": "几招：用更小的模型做分支评估（critic 不必和生成同级别）；beam 限宽、depth 限深、设最大展开节点数封顶；对同一节点多次采样评分取平均以减少误剪；用价值网络/启发式替代 LLM 自评。核心是在「探索收益」与「调用成本」间找平衡。"
    },
    {
      "question": "ToT 一定会比 CoT 好吗？",
      "answer": "不一定。在线性、无分支的任务上 ToT 只增加成本；只有在存在死路/多解、需要回溯的探索型任务上才显著占优。研究也表明 ToT 收益高度依赖评估函数质量——评估差反而更差。所以要先判断任务是否需要搜索，再决定是否上 ToT。"
    }
  ],
  "followUpAnswers": [
    "看任务是否需要「多路径试错」。需要探索、回溯、在分支中择优的难题（谜题、数学证明、复杂规划）用 ToT，成功率高但贵；顺序执行、单链即可的任务（「查天气→播报」）用 ReAct 更省、更稳。也可混合：ReAct 做主干，遇到卡点再启动局部 ToT 搜索。",
    "几招：用更小的模型做分支评估（critic 不必和生成同级别）；beam 限宽、depth 限深、设最大展开节点数封顶；对同一节点多次采样评分取平均以减少误剪；用价值网络/启发式替代 LLM 自评。核心是在「探索收益」与「调用成本」间找平衡。",
    "不一定。在线性、无分支的任务上 ToT 只增加成本；只有在存在死路/多解、需要回溯的探索型任务上才显著占优。研究也表明 ToT 收益高度依赖评估函数质量——评估差反而更差。所以要先判断任务是否需要搜索，再决定是否上 ToT。"
  ],
  "pitfalls": [
    "无剪枝/无 beam 限制导致分支爆炸，烧钱且延迟失控。",
    "评估函数质量差（同模型自评易自满）剪掉正解或留错分支。",
    "把 ToT 用在简单顺序任务，白白增加成本无收益。"
  ],
  "beginnerSummary": "ToT 像走迷宫时同时在几个路口都探一步，记下哪条更有戏（打分），死路就退回换边。比只认一条道走到死胡同聪明——你不会在死胡同里硬撞，而是试点别的方向。但探得多也费时间费钱：每多探一个分支就要多问一次模型「这步靠谱吗」，所以得用 beam 宽度控制别疯探。",
  "prerequisites": [
    "模型能生成多个候选思路（thought），而非只给一个确定性输出。",
    "具备自评/打分能力（或外接 critic/价值模型）以判断分支前景。",
    "能维护搜索树、记录已访问状态并支持回溯。",
    "有预算上限（最大节点数/调用数）防止失控。"
  ],
  "workedExample": [
    "每步展开：24 点游戏每节点 3 个候选、beam=2，4 层共展开约 9~12 节点、约 30+ 次 LLM 调用找到解。",
    "死路回溯：某分支算到 (8-4)=4 后无后继，回溯到上一节点换算式，避免卡死。",
    "成本对比：同任务 ToT 成功率比单链 ReAct 高约 30%，但调用数多 5~10 倍，需按任务价值决定是否值得。"
  ],
  "lineByLine": [
    "`frontier = [root]`：初始化搜索 frontier 为根节点。",
    "`cands = [t for n in frontier for t in llm.thoughts(n)]`：对每个前沿节点生成多个候选思路。",
    "`scored = [(t, llm.score(t)) for t in cands]`：用 critic 给每个候选打分。",
    "`frontier = topk(scored, beam)`：只保留高分分支，控制爆炸。",
    "`if any(is_solved(n) ...): return backtrack(frontier)`：找到解则回溯重建路径。"
  ],
  "diagram": "root -> {a,b,c} -> score -> keep a,c -> expand ..."
};

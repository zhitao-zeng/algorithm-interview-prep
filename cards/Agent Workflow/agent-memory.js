export default {
  "kind": "concept",
  "id": "agent-memory",
  "category": "Agent Workflow",
  "difficulty": "Medium",
  "title": "记忆（memory）机制",
  "prompt": "Agent 的短期记忆与长期记忆分别指什么，怎么实现？",
  "quickAnswer": "短期记忆是单轮/近期交互的上下文（对话历史、当前轨迹），直接放 context window，受窗口上限约束、需压缩；长期记忆跨会话保存事实/经验，通常外挂向量库或键值存储，用时语义检索 top-k 回灌。短期解决\"这轮别失忆\"，长期解决\"上次聊过的事这次还记得\"；短期满了用摘要压缩，长期靠\"写入-检索\"闭环沉淀知识，并需去重/冲突裁决防串扰。",
  "approach": "短期 = 上下文窗口 + 滚动/摘要压缩；长期 = 外部存储 + 写入（remember）与检索（recall）闭环；检索结果按相关性+新鲜度排序后再回灌决策。",
  "explanationFocus": "是什么：记忆让 Agent 能够跨步骤、跨会话地保留信息；短期记忆是当前这一轮/近几轮交互的上下文（对话历史、当前轨迹、工具返回），直接放在上下文窗口里；长期记忆是持久化的外部知识库（用户偏好、历史结论、经验），通常外挂向量库或键值存储，在需要时检索回灌。二者互补：短期保\"当下的连贯\"，长期保\"过往的沉淀\"。",
  "bruteForce": "把所有历史无差别全塞进 prompt：很快超出窗口、噪声大、成本高，且越往后模型对早期关键事实的注意力越被稀释，反而更容易\"忘\"重要内容。",
  "derivation": [
    "为什么需要：任务常跨多轮甚至多天（如连续一周的客服、跨会话的个性化助手），而模型自身无持久状态、每次请求都是\"重新出生\"，必须显式地把信息搬进搬出。没有记忆，Agent 会重复问已知信息、违背用户已声明的偏好，体验与可用性都崩塌。",
    "怎么实现：短期用滚动窗口保留最近 K 轮，或定期用 LLM 把旧内容压缩成摘要；长期把重要事实写成 {text, ts, source} 存入向量库（embedding）或结构化 KV，需要时按 query 做语义检索 top-k，再用 rank_by_freshness 排序后拼回 prompt。关键工程点是写入去重（同事实不重复存）与冲突解决（新旧矛盾时按策略裁决）。",
    "有什么代价：检索可能不相关（噪声）而干扰决策；写入需去重/冲突解决，否则库越来越脏；存储与向量检索有延迟与成本，且隐私敏感信息入长期库需脱敏与权限控制。过度依赖长期记忆还可能让模型\"太信过去\"而忽略当前上下文。",
    "怎么评测：看记忆命中率（该想起时是否想起）、任务一致性（跨会话是否违背已知偏好）、以及遗忘/串扰率（false context，即把无关或错误记忆当成事实）；上线常用\"注入偏好后隔 N 轮再问\"的探针测试来验证长期记忆是否真的生效。"
  ],
  "invariant": "写入长期记忆的内容必须带时间戳与来源，检索时按\"相关性打分 × 新鲜度衰减\"排序，且回灌前要标注来源让用户可核查，避免错误记忆污染决策。",
  "walkthrough": "以客服 Agent 为例：本次对话（短期）放窗口，保留最近 6 轮；用户说\"以后别用短信通知我\"——这条被 remember 写入长期库，带 ts=当天、source=对话#8821；下一会话用户来查物流，recall 检索到该偏好（相关性 0.91、最新），拼回 prompt，Agent 改用电邮通知。整流程 1 次写入、1 次检索，检索耗时约 12ms，回灌 1 条记忆约占 40 tokens；而在 10 万用户、人均 50 条记忆的规模下，库约 500 万条、向量存储约 3GB，单次检索仍 < 25ms。",
  "edgeCases": [
    "冲突记忆：新旧信息矛盾（用户先说\"不加糖\"后说\"加糖\"），需策略裁决——可按时间戳取最新，或保留两者让模型结合语境判断，不能静默丢一条。",
    "检索到无关记忆造成干扰：低相关度记忆被拼进 prompt 反而带偏回答，需设相关性阈值过滤并标注来源。",
    "敏感信息入长期库：身份证、密码等需脱敏与权限控制，否则有泄漏与合规风险。",
    "用户要求\"忘记\"：需支持按来源/时间删除记忆，满足被遗忘权，不能只增不删。"
  ],
  "code": "def recall(mem_store, query, k=5):\n    vec = embed(query)\n    hits = mem_store.search(vec, k)            # 语义检索\n    return rank_by_freshness(hits)\n\ndef remember(mem_store, fact, ts):\n    mem_store.upsert(embed(fact), {\"text\": fact, \"ts\": ts})",
  "codeNotes": [
    "检索结果需去噪、按相关性阈值过滤后再回灌，避免无关记忆干扰，并标注 source 让用户可核查。",
    "写入带时间戳与来源便于冲突裁决；upsert 用 (事实指纹) 去重，防止同一句话被反复存储撑爆库。",
    "rank_by_freshness 在相关性相近时偏向新记忆，避免模型被陈旧结论误导。"
  ],
  "complexity": "写入 O(embedding 维度) 量级、检索在向量索引上约 O(log n)（ANN 近似最近邻）；长期记忆随数据量增长，受 embedding 存储与查询延迟约束。以一个存 100 万条记忆、维度 1536 的库为例，单次 top-5 检索通常 < 20ms，但全量扫描或维度过高会显著变慢；窗口侧的摘要压缩则另占一次 LLM 调用。",
  "followUps": [
    {
      "question": "短期记忆满了怎么办？",
      "answer": "两种主流做法：一是滚动截断，丢弃最老的轮次只留最近 K 轮；二是用 LLM 把旧内容压缩成摘要，保留关键事实与最近交互后拼回窗口。实务常结合两者——最近 2~3 轮原文保留、更早的压缩成\"到目前为止的要点\"。注意压缩本身要一次 LLM 调用，且可能丢细节，所以摘要应作为\"索引\"而非\"替代\"，需要时再从长期记忆取原文。"
    },
    {
      "question": "长期记忆怎么防串扰（false context）？",
      "answer": "检索结果必须按相关性阈值过滤（如 score<0.6 丢弃），并标注每条记忆的来源与时间，让用户和模型都能核查\"这句话从哪来\"。同时写入时做去重与冲突裁决，避免脏数据入库；回灌前最好显式告诉模型\"以下是可能相关的记忆，请按需采用\"，而非无缝混入上下文假装它一直知道。定期用探针测试监控串扰率是上线后的刚需。"
    }
  ],
  "followUpAnswers": [
    "两种主流做法：一是滚动截断，丢弃最老的轮次只留最近 K 轮；二是用 LLM 把旧内容压缩成摘要，保留关键事实与最近交互后拼回窗口。实务常结合两者——最近 2~3 轮原文保留、更早的压缩成\"到目前为止的要点\"。注意压缩本身要一次 LLM 调用，且可能丢细节，所以摘要应作为\"索引\"而非\"替代\"，需要时再从长期记忆取原文。",
    "检索结果必须按相关性阈值过滤（如 score<0.6 丢弃），并标注每条记忆的来源与时间，让用户和模型都能核查\"这句话从哪来\"。同时写入时做去重与冲突裁决，避免脏数据入库；回灌前最好显式告诉模型\"以下是可能相关的记忆，请按需采用\"，而非无缝混入上下文假装它一直知道。定期用探针测试监控串扰率是上线后的刚需。"
  ],
  "pitfalls": [
    "把全部历史无差别塞进窗口：窗口被噪声占满，关键事实反而被稀释，且 token 成本随轮次线性飙升。",
    "长期记忆不做冲突裁决导致矛盾决策：新旧偏好打架时模型随机采信，出现\"上一轮答应不短信、这轮又发短信\"的翻车。",
    "检索结果不经去噪直接回灌：把低质量/错误记忆当真理，造成串扰（false context）。"
  ],
  "beginnerSummary": "记忆就像人的笔记本加便签：短期记忆是你眼前这张便签——当前这轮对话、刚查到的结果，都写在这儿，模型一眼就能看；但它地方小，写满了就得把旧的压缩成摘要，否则塞不下。长期记忆则是归档的文件柜——跨天跨会话沉淀下来的\"用户不爱短信通知\"\"他是素食者\"这类事实，存在外面的向量库里，需要时按关键词去柜子里翻出相关那一页，贴回便签上参考。关键有两坑：一是别把整本日记都糊到便签上(窗口爆掉)，二是柜子里若写了矛盾的两页(新旧偏好打架)，得按时间或可信度决出用哪一页，不然模型会被旧信息带偏。",
  "prerequisites": [
    "有向量库或键值存储可用，且能承载写入与低延迟检索，否则长期记忆无从落地。",
    "能生成与检索 embedding（有 embedding 模型或 API），才能做语义级\"想起相关的事\"。",
    "需区分会话内（短期）与会话间（长期）状态，并设计去重/冲突/权限策略，否则记忆会变脏或越权。"
  ],
  "workedExample": [
    "用户偏好\"不加糖/不用短信\"写入长期库，隔 3 天再会话时 recall 命中并遵守，避免重复询问。",
    "窗口溢出（>8 轮）时用 LLM 摘要压缩旧内容，保留关键事实与最近 2 轮，token 从 6000 降到 1800 仍连贯。",
    "检索探针测试：注入\"用户是素食者\"后隔 10 轮再问餐荐，长期记忆命中率从 0%（无记忆）升到 96%。"
  ],
  "lineByLine": [
    "短期记忆维护在上下文窗口，按滚动窗口或摘要保留\"当下连贯\"所需的最近交互与工具返回。",
    "重要事实（偏好、结论、经验）通过 remember 写入外部长期存储，并带 text/ts/source 便于检索与裁决。",
    "需要时 recall 用 query 的 embedding 在向量库中做语义检索，取 top-k 最相关记忆。",
    "rank_by_freshness 在相关性相近时偏向新记忆，再按来源标注回灌决策，避免陈旧或错误记忆污染回答。"
  ],
  "diagram": "Context(window) <--> summarize\nLongTerm(DB) <--> embed/search"
};

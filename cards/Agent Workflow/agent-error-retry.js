export default {
  "kind": "concept",
  "id": "agent-error-retry",
  "category": "Agent Workflow",
  "difficulty": "Easy",
  "title": "错误处理与重试",
  "prompt": "Agent 里怎么做错误处理与重试？",
  "quickAnswer": "Agent 的失败分三类：模型输出非法（如坏 JSON）、工具执行异常（如空指针）、外部超时/限流。处理策略是分类捕获→把错误文本作为 Observation 回传模型让其自我纠正，配合有限重试与指数退避；不可恢复则升级到 human-in-the-loop 或安全终止。关键是把错误变成可学习信号，而不是让一次失败直接整轮崩掉。",
  "approach": "把「出错」当作控制流的一环：① 用 try/except 包裹每个工具/模型调用，按异常类型分类（瞬时/确定性/未知）；② 把错误文本与上下文作为 Observation 回灌 prompt，让模型分析并改进下一步动作（如调参、换工具）；③ 设最大重试次数与指数退避，避免瞬时限流被瞬间打满；④ 连续不可恢复则升级人工或安全终止，并保留 trace 便于复盘。",
  "explanationFocus": "是什么：错误处理与重试是 Agent 在面对模型输出非法、工具执行异常、外部超时等失败时，进行分类捕获、把错误作为 Observation 回传给模型让其自我纠正，并配合有限次重试与退避（必要时升级 human-in-the-loop 或安全终止）的机制。它的目标是让错误成为可学习的信号而非直接崩溃，从而提升任务韧性。",
  "bruteForce": "最朴素的做法是异常直接抛出：一次工具超时或坏 JSON 就让整轮 Agent 运行崩溃，用户只看到 500。这等于把「一次偶发网络抖动」放大成「整个任务失败」，在外部依赖不稳定的生产环境里几乎不可用。",
  "derivation": [
    "为什么需要：LLM 输出不稳定（可能坏 JSON）、外部工具/API 也不稳定（超时、限流、偶发 5xx），若没有任何容错，一次偶发失败就导致整轮任务失败，生产环境极其脆弱。",
    "怎么实现：用 try/except 包裹工具调用，按异常类型分流；把错误文本作为 Observation 回灌 prompt；设 max_retry 与指数退避（backoff）；连续失败转人工或安全终止。还可让模型基于错误做「反思」后再行动（reflect-then-retry）。",
    "有什么代价：重试增加端到端延迟与 token/调用成本；过度重试会掩盖根因、拖垮下游（重试风暴）；需准确区分「可重试的瞬时错误」与「不可重试的确定性错误」，否则要么无谓重试要么错失补救。",
    "怎么评测：看任务成功率提升幅度、平均/最大重试次数、误重试率（对不可重试错误还去重试的比例），以及重试带来的额外成本与尾延迟（p99）是否在可接受范围。"
  ],
  "invariant": "回传给模型的错误必须是真实且附带足够上下文（哪一步、什么参数、什么异常）的信息，模型才能有效改进行动；同时任何退避/重试都必须有上限，避免无限循环消耗资源。",
  "walkthrough": "支付 Agent 调第三方支付 API：第 1 次调用超时（TransientError，504），safe_call 捕获后指数退避 1s 并把 obs='error:timeout; retry 1' 回传给模型，模型把超时从 30s 降到 10s 重试；第 2 次仍 504，退避 2s，模型改走备用通道再试；第 3 次成功返回。共 2 次重试 + 1 次退避累积约 3s 额外延迟；若连续 3 次失败则 raise EscalateToHuman 转人工。在并发 100 的压测下，重试机制把任务成功率从 82% 提升到 96%，平均重试 0.4 次。",
  "edgeCases": [
    "区分 4xx（参数非法/权限不足）与 5xx（服务端错误）：4xx 是确定性失败，重试无意义，应直接纠正或升级；5xx/超时才是可重试的。",
    "模型持续产出非法 JSON：说明 schema 理解有问题，需格式修复（如用 json5 容错解析）或换模型/加 few-shot，而不是无限重试同一 prompt。",
    "重试风暴：上游限流时若所有并发一起退避重试，会在退避结束后同时打满下游，需全局令牌桶限流 + 抖动（jitter）。",
    "幂等性：重试写操作（如扣款）必须保证幂等，否则重试导致重复副作用。"
  ],
  "code": "def safe_call(tool, args, max_retry=3):\n    for i in range(max_retry):\n        try:\n            return tool(**args)\n        except TransientError as e:\n            backoff(i)                         # 指数退避\n            obs = f\"error:{e}; retry {i+1}\"\n            args = llm_fix(args, obs)          # 让模型纠正\n    raise EscalateToHuman(\"persistent failure\")",
  "codeNotes": [
    "只重试瞬时错误（TransientError/5xx/超时），对 4xx 等确定性失败不重试，直接纠正或升级。",
    "把错误 obs 回灌给 llm_fix，让模型基于真实错误调整参数或换策略，而非盲目重发。",
    "退避建议加随机抖动，避免大量并发同时重试形成尖峰，配合全局限流防重试风暴。"
  ],
  "complexity": "最坏情况下延迟 ≈ max_retry ×（单次调用耗时 + 退避时间），退避使额外延迟呈指数（如 1s、2s、4s）；重试还会放大下游 QPS，因此需配合全局限流。正确性与可用性收益通常远大于这点延迟，但重试风暴会反噬服务，所以必须区分可重试与不可重试错误。",
  "followUps": [
    {
      "question": "哪些错误根本不该重试？",
      "answer": "参数非法、权限拒绝、资源不存在等 4xx/确定性失败不应重试——重试同样会失败且浪费资源，正确做法是直接纠正参数、补充权限或升级人工。只有超时、限流、5xx 这类瞬时/服务端错误才值得重试。另外非幂等的写操作即使瞬时失败也不应盲目重试，需先确认幂等或做去重。"
    },
    {
      "question": "重试和反思（reflection）怎么配合？",
      "answer": "重试是执行层的兜底（再试一次），反思是决策层的改进（先想清楚为什么错再行动）。好的做法是重试前先让模型分析错误 Observation、定位根因并调整策略（reflect-then-retry），而不是无脑重发同一请求；仍失败才退避或升级。两者结合能显著降低无效重试。"
    },
    {
      "question": "重试会不会引发雪崩？怎么防？",
      "answer": "会。上游限流时若所有并发在同一时刻退避后同时重试，会在退避结束瞬间打满下游（重试风暴）。防护手段：指数退避叠加随机抖动（jitter）、全局令牌桶限流控制重试点、对可重试错误做熔断（失败率过高时暂停重试）、以及下游做幂等保护。"
    }
  ],
  "followUpAnswers": [
    "参数非法、权限拒绝、资源不存在等 4xx/确定性失败不应重试——重试同样会失败且浪费资源，正确做法是直接纠正参数、补充权限或升级人工。只有超时、限流、5xx 这类瞬时/服务端错误才值得重试。另外非幂等的写操作即使瞬时失败也不应盲目重试，需先确认幂等或做去重。",
    "重试是执行层的兜底（再试一次），反思是决策层的改进（先想清楚为什么错再行动）。好的做法是重试前先让模型分析错误 Observation、定位根因并调整策略（reflect-then-retry），而不是无脑重发同一请求；仍失败才退避或升级。两者结合能显著降低无效重试。",
    "会。上游限流时若所有并发在同一时刻退避后同时重试，会在退避结束瞬间打满下游（重试风暴）。防护手段：指数退避叠加随机抖动（jitter）、全局令牌桶限流控制重试点、对可重试错误做熔断（失败率过高时暂停重试）、以及下游做幂等保护。"
  ],
  "pitfalls": [
    "无差别重试一切错误，导致重试风暴与限流雪崩，反而降低可用性。",
    "把异常吞掉（catch 后不打日志、不回传），模型拿不到错误信息无法学习，问题被静默掩盖。",
    "重试非幂等操作（如重复下单），造成真实资损。"
  ],
  "beginnerSummary": "错误处理像教小孩拧瓶盖。第一次拧不开（工具超时），你不是把她骂哭，而是告诉她「用力点、换个角度再试」（把错误回传给模型），她试两次还不行，就找大人帮忙（升级人工）。这样一次失误不会变成全盘崩溃。但也要注意：如果她是在做「把糖放进微波炉」这种根本不该做的事（4xx 参数非法），重试没用，应该直接纠正或叫停，而不是一遍遍试。",
  "prerequisites": [
    "能区分错误类型（瞬时 vs 确定性 vs 不可恢复），这是决定重试策略的前提。",
    "工具/API 抛出的异常可被 try/except 捕获，且能序列化出错上下文。",
    "底层 LLM 能根据错误 Observation 改进下一步动作（否则回传也无用，只能直接升级）。",
    "关键写操作具备幂等设计，才能安全重试。"
  ],
  "workedExample": [
    "支付超时重试：调用支付 API 第 1 次 504，退避 1s 后模型将超时 30s→10s 重试；第 2 次仍 504，退避 2s 改备用通道成功。共 2 次重试、累积约 3s 延迟，并发 100 下成功率从 82%→96%。",
    "连续失败升级：连续 3 次 5xx，超过 max_retry=3，safe_call 抛出 EscalateToHuman，工单流转人工；过程中每步错误都写入 trace 便于复盘。",
    "4xx 不重试：模型给出非法金额（400），直接走纠正分支让模型改参数，不进入重试计数。"
  ],
  "lineByLine": [
    "`for i in range(max_retry)`：设置重试上限，防止无限循环。",
    "`try: return tool(**args)`：正常则直接返回结果，跳出重试循环。",
    "`except TransientError`：只捕获瞬时错误才进入重试，其余错误据此分流。",
    "`backoff(i); obs=...; args=llm_fix(args, obs)`：指数退避并把错误回传给模型纠正参数或换策略。",
    "循环外 `raise EscalateToHuman`：超过重试上限则升级人工，安全终止。"
  ],
  "diagram": "call -> ok? -> yes:return / no:backoff+retry -> human"
};

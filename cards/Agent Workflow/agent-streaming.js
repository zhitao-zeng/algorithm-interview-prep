export default {
  "kind": "concept",
  "id": "agent-streaming",
  "category": "Agent Workflow",
  "difficulty": "Easy",
  "title": "流式输出与用户体验",
  "prompt": "Agent 的流式输出与用户体验该如何设计？",
  "quickAnswer": "Agent 多步执行慢，需以流式（SSE/WebSocket）逐步回传：先即时显示「思考中/正在调用 X 工具」，再增量吐字、工具状态可视化，最终给完整答案与引用。这把「等待」变成「可见的进展」，显著降低感知延迟；同时要能中途打断与取消，避免资源浪费与用户等待已无意义的结果。",
  "approach": "即时占位（先发 status）→ 增量 token（边生成边推）→ 工具状态可视化（tool_call 事件）→ 可打断取消（cancel 信号清理在途调用）。前后端约定统一事件协议（thought/tool_call/token/done）。",
  "explanationFocus": "是什么：流式输出（Streaming）让 Agent 在执行与生成过程中，实时增量地把进展（思考、工具调用、token）推送给用户，把「空白等待」变成「可见的进展」，从而显著降低感知延迟与焦虑。它是多步 Agent 体验的关键，而非可选项。",
  "bruteForce": "等全部跑完再一次性返回：用户面对空白页数秒到数十秒，易以为卡死、重复提交或离开，体验与留存都差。",
  "derivation": [
    "为什么需要：Agent 多轮（检索+思考+生成）延迟高，无反馈用户会流失或重复提交，且长任务需证明「还活着」。",
    "怎么实现：服务端用 SSE/WebSocket 推送事件（thought/tool_call/token/done）；前端渲染进度条/状态；提供停止按钮触发取消信号。",
    "有什么代价：前后端状态同步复杂；增量渲染需防抖动（合并/节流）；取消需清理在途工具与模型调用，否则泄漏。",
    "怎么评测：看感知延迟（首 token 时间）、中断成功率、用户满意度/留存，而非仅端到端总时长。"
  ],
  "invariant": "推送给用户的中间状态必须真实反映后端进度，不能伪造「假思考」；取消后必须真正终止在途调用并释放资源，不能只停前端。",
  "walkthrough": "问答 Agent：0.1s 推「检索中」→0.8s 推「已查 3 篇文献」→从 1s 起逐字吐答案→2s 完成。用户全程可见进展，感知延迟远低于实际 2s。若用户在 0.5s 点取消，后端在事件循环检查到 ws.cancelled，立即 agent.cancel() 中止在途检索与生成，释放 GPU，避免白算 1.5s。",
  "edgeCases": [
    "用户中途取消：需中止在途模型与工具调用，释放 GPU/连接。",
    "工具慢：需阶段性占位（「检索中」）不空白，否则像卡死。",
    "增量渲染闪烁：需合并/节流事件，避免每 token 重排抖动。",
    "网络重连：断线后如何续传或重发已推内容。"
  ],
  "code": "async def stream(agent, q, ws):\n    await ws.send(\"status: thinking\")\n    for ev in agent.run_stream(q):           # 逐步产出事件\n        await ws.send(serialize(ev))         # 增量推送\n        if ws.cancelled:\n            agent.cancel(); break            # 支持打断",
  "codeNotes": [
    "状态事件应先于内容推送，让用户秒级感知在进行。",
    "取消要清理在途工具/模型调用，否则资源泄漏。"
  ],
  "complexity": "流式本身几乎零额外计算成本，主要是长连接（WebSocket/SSE）管理与前端增量渲染开销；取消需清理在途模型/工具调用，避免资源泄漏。",
  "followUps": [
    {
      "question": "流式会影响正确性吗？",
      "answer": "不影响，流式只是传输方式，最终答案由模型一次生成决定，与是否分片推送无关。但需保证「最终态」与中间事件一致——不能先说一个结论、最后吐出另一个，否则用户困惑。前端应展示「暂定→最终」或只在 done 时确定结论。"
    },
    {
      "question": "如何支持中途打断？",
      "answer": "前端发取消信号（如 WebSocket close 或 cancel 帧），后端在事件循环每次迭代检查 cancelled 标志，终止在途模型推理与工具调用并释放资源。关键是取消要「真终止」而非仅停前端渲染，否则 GPU/连接仍被占用、成本照计。"
    }
  ],
  "followUpAnswers": [
    "不影响，流式只是传输方式，最终答案由模型一次生成决定，与是否分片推送无关。但需保证「最终态」与中间事件一致——不能先说一个结论、最后吐出另一个，否则用户困惑。前端应展示「暂定→最终」或只在 done 时确定结论。",
    "前端发取消信号（如 WebSocket close 或 cancel 帧），后端在事件循环每次迭代检查 cancelled 标志，终止在途模型推理与工具调用并释放资源。关键是取消要「真终止」而非仅停前端渲染，否则 GPU/连接仍被占用、成本照计。"
  ],
  "pitfalls": [
    "伪造进度欺骗用户：显示「思考中」却无真实后端活动，失信。",
    "取消不清理在途调用：前端停了后端还在算，资源泄漏、成本虚高。",
    "首 token 太晚仍像卡死：占位要在 100ms 内到达。"
  ],
  "beginnerSummary": "流式像外卖跟踪：下单后立刻显示「商家接单→骑手取餐→配送中」，你不用干等，知道到哪了；想取消也能马上点。空白等待最让人焦虑——你不知道是卡了还是快好了。Agent 也一样，先告诉你「在查资料」比干转圈圈友好十倍。",
  "prerequisites": [
    "有流式通道（SSE/WebSocket）：服务端能主动推。",
    "Agent 能产出阶段事件：每步可 emit。",
    "前端能增量渲染：处理流式帧。"
  ],
  "workedExample": [
    "先推「检索中」再吐答案，用户感知延迟远低于实际 2s。",
    "取消按钮在 0.5s 触发，后端终止在途调用，省 1.5s 计算。",
    "工具调用事件显示「已查 3 篇」，用户知道进展不空白。"
  ],
  "lineByLine": [
    "先发状态占位：ws.send(\"status: thinking\")。",
    "逐步推送事件流：for ev in run_stream 增量发。",
    "前端增量渲染：收到 token 即追加显示。",
    "收到取消则中止：ws.cancelled→agent.cancel()。"
  ],
  "diagram": "Agent --events--> WS --> UI(progress)"
};

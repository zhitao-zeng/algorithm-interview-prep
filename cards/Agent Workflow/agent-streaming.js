export default {
  "kind": "concept",
  "id": "agent-streaming",
  "category": "Agent Workflow",
  "difficulty": "Easy",
  "title": "流式输出与用户体验",
  "prompt": "Agent 的流式输出与用户体验该如何设计？",
  "quickAnswer": "Agent 多步执行慢，需以流式（SSE/WebSocket）逐步回传：先即时显示「思考中/正在调用 X 工具」的状态占位，再增量吐字、把工具状态可视化，最终给完整答案与引用。这把「等待」变成「可见的进展」，显著降低感知延迟；同时要支持中途打断与取消，避免资源浪费与用户等一个已无意义的结果。设计上前后端要约定统一事件协议（thought/tool_call/token/done），并保证「中间状态真实、最终态与中间一致」。",
  "approach": "设计顺序：①即时占位（先发 status 事件让用户秒级感知在进行）；②增量 token（边生成边推，首字控制在 100ms 内到达）；③工具状态可视化（tool_call 事件显示「正在调用 X」）；④可打断取消（cancel 信号触发清理在途调用）。前后端约定统一事件协议（thought / tool_call / token / done），前端按事件类型渲染进度条与状态，并在 done 前明确「暂定→最终」，避免中间结论与终态矛盾。",
  "explanationFocus": "是什么：流式输出（Streaming）让 Agent 在执行与生成的过程中，实时、增量地把进展（思考、工具调用、token）推送给用户，而不是等全部跑完再一次性返回。它把「空白等待」变成「可见的进展」，从而显著降低用户的感知延迟与焦虑感。对多步 Agent 而言，一次回答可能要经历检索、思考、多轮工具调用、生成，耗时数秒到数十秒，流式不是可选项而是体验的关键。",
  "bruteForce": "最糟的「暴力」做法是等 Agent 全部跑完（检索+思考+生成可能数秒到数十秒）再一次性返回。用户面对空白页，无法判断是「卡死了」还是「快好了」，易以为卡死、重复提交或离开，体验与留存都差，且重复提交还会进一步放大后端负载，形成恶性循环。",
  "derivation": [
    "为什么需要：Agent 多轮执行（检索+思考+生成）延迟高，若全程无反馈，用户会误以为卡死、重复提交甚至离开，导致流失与后端负载翻倍。长任务更需要证明「系统还活着」，否则用户没有耐心等到结果。",
    "怎么实现：服务端通过 SSE/WebSocket 推送事件流（thought/tool_call/token/done）；前端把状态渲染成进度条或状态标签；提供停止按钮，触发 cancel 信号。关键是事件协议要统一，前后端对同一事件类型有共识。",
    "有什么代价：前后端状态同步变复杂，要处理乱序/重连；增量渲染需防抖动（合并/节流事件），否则每来一个 token 就重排会闪屏；取消需清理在途工具与模型调用，否则资源泄漏。这些都不是免费午餐。",
    "怎么评测：不要只看端到端总时长，而要看感知延迟（首 token/首状态时间）、中断成功率（取消后资源是否真释放）、以及用户满意度/留存。流式做得好，用户感知延迟应远低于真实耗时，且取消后能立刻释放资源。"
  ],
  "invariant": "推送给用户的中间状态必须真实反映后端进度，不能伪造「假思考」来安抚用户，否则一旦被发现就失信。更重要的是取消语义：用户点取消后，必须真正终止在途的模型推理与工具调用并释放资源（GPU/连接），不能只停前端渲染——否则后端还在算、成本照计、资源泄漏。",
  "walkthrough": "以问答 Agent 为例，用户提问后 0.1s 先推「检索中」状态占位，0.8s 推「已查 3 篇文献」的工具事件，从 1s 起逐字吐答案，2s 完成。用户全程看到进展，感知延迟远低于实际 2s，流失率明显下降。取消场景：用户在 0.5s 点取消，后端在事件循环每次迭代检查到 ws.cancelled，立即调用 agent.cancel() 中止在途检索与生成，释放 GPU，避免白算 1.5s。若不做流式，用户面对 2s 空白页，约 30% 会以为卡死而重复提交，反而把后端压力翻倍。",
  "edgeCases": [
    "用户中途取消：需中止在途模型与工具调用、释放 GPU/连接，否则后端白算、成本照计、资源泄漏。",
    "工具慢：需阶段性占位（如「检索中」）避免空白，否则即使后端在忙，用户侧也像卡死。",
    "增量渲染闪烁：需合并/节流事件（如每 50ms 批量刷一次），避免每个 token 触发一次重排导致闪屏。",
    "网络重连：断线后如何续传或重发已推内容，需前端用事件序号做去重与补帧。"
  ],
  "code": "async def stream(agent, q, ws):\n    await ws.send(\"status: thinking\")\n    for ev in agent.run_stream(q):           # 逐步产出事件\n        await ws.send(serialize(ev))         # 增量推送\n        if ws.cancelled:\n            agent.cancel(); break            # 支持打断",
  "codeNotes": [
    "状态事件应先于内容推送（ws.send('status: thinking')），让用户毫秒级感知「在进行」，首状态最好 100ms 内到达。",
    "取消要清理在途工具/模型调用（agent.cancel()），否则仅停前端渲染、后端仍在算，资源泄漏且成本虚高。",
    "agent.run_stream 是生成器式逐步产出，真实实现里每个 ev 都带类型字段（thought/tool_call/token/done）。"
  ],
  "complexity": "流式本身几乎零额外计算成本（只是把一次返回拆成多次推送），主要开销在长连接（WebSocket/SSE）的管理与前端增量渲染。取消机制需要在事件循环里周期性检查 cancelled 标志，并在每次迭代清理在途的模型/工具调用，否则会造成 GPU 与连接泄漏。连接数随并发线性增长，需配合连接池与心跳保活。",
  "followUps": [
    {
      "question": "流式会影响正确性吗？",
      "answer": "不影响。流式只是传输方式——最终答案由模型『一次生成』决定，与是否分片推送无关。但要注意一致性：必须保证『最终态』和中间事件不矛盾，不能先说一个结论、最后吐出另一个，否则用户会困惑。工程上的做法是前端只在 done 事件时确定最终结论，中间态标注为『暂定/生成中』，或仅把流式用于展示、结论以最终帧为准。"
    },
    {
      "question": "如何支持中途打断？",
      "answer": "前端发取消信号（如 WebSocket close 或独立的 cancel 帧），后端在事件循环每次迭代检查 cancelled 标志，一旦置位就终止在途的模型推理与工具调用并释放资源。关键是取消要『真终止』而非仅停前端渲染——否则 GPU/连接仍被占用、成本照计。实践中 cancel 信号要能穿透到每一个在途的子调用（检索、DB、模型），否则会有残留任务继续跑。"
    }
  ],
  "followUpAnswers": [
    "不影响。流式只是传输方式——最终答案由模型『一次生成』决定，与是否分片推送无关。但要注意一致性：必须保证『最终态』和中间事件不矛盾，不能先说一个结论、最后吐出另一个，否则用户会困惑。工程上的做法是前端只在 done 事件时确定最终结论，中间态标注为『暂定/生成中』，或仅把流式用于展示、结论以最终帧为准。",
    "前端发取消信号（如 WebSocket close 或独立的 cancel 帧），后端在事件循环每次迭代检查 cancelled 标志，一旦置位就终止在途的模型推理与工具调用并释放资源。关键是取消要『真终止』而非仅停前端渲染——否则 GPU/连接仍被占用、成本照计。实践中 cancel 信号要能穿透到每一个在途的子调用（检索、DB、模型），否则会有残留任务继续跑。"
  ],
  "pitfalls": [
    "伪造进度欺骗用户：显示「思考中」却无真实后端活动，一旦用户察觉就失信，比无反馈更糟。",
    "取消不清理在途调用：前端停了后端还在算，GPU/连接仍被占用、成本照计，资源泄漏。",
    "首 token 太晚仍像卡死：即便最终流式，若首个状态超过 1s 才到，用户已失去耐心，占位必须在 100ms 内到达。"
  ],
  "beginnerSummary": "流式就像外卖跟踪：下单后立刻显示「商家接单→骑手取餐→配送中」，你不用干等，随时知道到哪了；想取消也能马上点。空白等待最让人焦虑——你不知道是卡了还是快好了。Agent 也一样，先告诉你「在查资料」比干转圈圈友好十倍。而且如果骑手已经取消，系统就该真停送，而不是前台显示取消、后台还在路上白跑——这就是「取消要真终止」。",
  "prerequisites": [
    "有流式通道（SSE/WebSocket）：服务端能主动推送，而非只能等请求结束才回。",
    "Agent 能产出阶段事件：每一步（思考/工具/生成）可 emit 带类型的事件。",
    "前端能增量渲染：能处理流式帧、做节流与取消按钮的状态管理。"
  ],
  "workedExample": [
    "先推「检索中」再吐答案，用户感知延迟远低于实际 2s，重复提交率从 30% 降到 5%。",
    "取消按钮在 0.5s 触发，后端终止在途调用并释放 GPU，省下 1.5s 无效计算。",
    "工具调用事件显示「已查 3 篇文献」，用户知道进展不空白，留存明显提升。"
  ],
  "lineByLine": [
    "await ws.send('status: thinking')：先发状态占位，让用户秒级感知 Agent 已启动，这是流式的第一优先级。",
    "for ev in agent.run_stream(q)：逐步产出事件（思考/工具/生成），边产生边推，而非等全部完成。",
    "await ws.send(serialize(ev))：把每个事件增量推给前端，前端按类型渲染进度或追加 token。",
    "if ws.cancelled: agent.cancel(); break：每次迭代检查取消标志，真终止在途调用并释放资源，而非只停前端。"
  ],
  "diagram": "Agent --events--> WS --> UI(progress)"
};

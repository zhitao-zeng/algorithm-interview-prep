export default {
  "kind": "concept",
  "id": "agent-workflow-vs-agent",
  "category": "Agent Workflow",
  "difficulty": "Medium",
  "title": "Workflow 和 Agent 有什么区别？什么场景不应该使用自主 Agent？",
  "prompt": "Workflow（工作流）和 Agent（自主智能体）有什么区别？什么场景不应该使用自主 Agent？",
  "quickAnswer": "Workflow 是路径预定义、每步确定性、易测试、适合固定业务；Agent 由模型动态决定下一步、灵活但状态空间大、不稳定、难测试。不应盲目用自主 Agent 的场景：路径固定的批量任务、对正确性/可审计要求高、成本敏感、延迟敏感、或失败代价高的写操作。应把确定性节点（校验、入库、支付）交给代码，只在真正开放/不确定的决策点用模型。还要限制最大步数、防循环、保证写操作幂等。",
  "approach": "按“确定性 vs 开放性”选择；能用 Workflow 就用 Workflow，Agent 只用于开放决策点。具体落地：固定步骤用 DAG/状态机串起确定代码；开放步骤用“规划+工具调用+观察”循环，设 max_steps、循环检测、Replan 而非无限 Retry、写操作带唯一键保证幂等。",
  "explanationFocus": "是什么：Workflow（工作流）是路径预定义、每步确定性的管线（DAG/状态机），易测试、可复现、成本低；Agent（自主智能体）由模型在运行中动态决定下一步（规划+工具循环），灵活但状态空间大、不稳定、难测试、贵。二者不是“越自主越好”的递进关系，而是按任务“确定性 vs 开放性”选型：固定业务用 Workflow，开放决策点才用 Agent。",
  "bruteForce": "把所有任务都套 Agent 自主循环：模型自由调工具、自由决定步数，成本高（多步 LLM 调用）、难控（易跑偏/死循环）、难测（非确定性），本可用 10 行代码搞定的事烧了几十次推理。",
  "derivation": [
    "为什么需要：不是所有任务都该让模型自由发挥；固定流程用代码更稳更省，盲目上 Agent 是过度工程与成本黑洞。",
    "怎么实现：Workflow 用 DAG/状态机串起确定步骤；Agent 用规划+工具循环，模型自行选下一步（ReAct/Plan-and-Execute 等）。",
    "怎么约束：限制 max_steps、检测循环、Tool 失败走 Replan 而非无限 Retry、写操作幂等带唯一键。",
    "怎么评测：看成功率、平均/尾步数、延迟、成本，以及“错误工具/错误参数”的发生率，对比 Workflow 基线判断是否“Agent 真有必要”。"
  ],
  "invariant": "选型原则：能用确定性 Workflow 解决的，就不应上自主 Agent；Agent 只用在开放性决策点。写操作/支付/发送等正确性与安全攸关节点，无论哪种架构都应由确定性代码执行，不交给模型自由调用。",
  "walkthrough": "固定报表生成用 Workflow（取数→模板→发送，三步确定）；开放的研究问答用 Agent（检索→读→再检索→写，步数不定）。支付/入库节点在两种里都应是确定性代码，不受模型随意调用。举例：日跑 10 万份报表用 Agent 会贵到离谱且不可审计，用 Workflow 成本可忽略；而“调研竞品并写摘要”这种没固定路径的才适合 Agent。",
  "edgeCases": [
    "高合规/可审计：自主 Agent 难解释每步，Workflow 每步可日志回放，更合适。",
    "成本敏感：Agent 多步调用贵（每步一次 LLM），固定流程几乎零推理成本，批量任务差几个数量级。",
    "失败代价高：自主写操作（删库、付款）易出错，需强约束、幂等与人工确认。",
    "延迟敏感：Agent 步数不定，尾延迟难控，交互场景可能超 SLA。"
  ],
  "code": "# Python\ndef route(task):\n    if task.type in ('report', 'etl'):\n        return run_workflow(task)          # 确定性路径\n    if task.type == 'open_research':\n        return run_agent(task, max_steps=8) # 开放决策\n    raise ValueError('unknown task')\n# 写操作节点应始终由确定性代码执行",
  "codeNotes": [
    "真实含步数限制（max_steps）、循环检测、幂等写，伪代码只展示路由。",
    "工具失败优先 Replan（换思路）而非无限 Retry（同错反复）。",
    "route 函数本质是“能确定就确定，不确定才交模型”的护栏。"
  ],
  "complexity": "Workflow 成本随步骤固定（O(步数)）；Agent 成本随步数与工具调用数增长（O(步数×工具数×LLM 成本)），需上限保护。工程上 Agent 还多出规划/记忆/观测的复杂度与可观测性成本。",
  "followUps": [
    {
      "question": "Agent 是否越自主越好？",
      "answer": "不是。自主带来不确定性与成本，固定任务用 Workflow 更稳更省；Agent 只用于开放决策点（路径未知、需检索推理）。判断标准：能否在写代码时把步骤列全？能→Workflow；不能→才考虑 Agent。很多“看似要 Agent”的场景其实是固定流程套了层皮。"
    },
    {
      "question": "如何限制最大步数？",
      "answer": "设 max_steps（如 8~15），每轮规划后计数，超过即终止并交回确定逻辑或人工；防止无限循环烧钱与超 SLA。步数上限也是安全护栏：模型卡住时不会永远空转。"
    },
    {
      "question": "如何防止循环调用？",
      "answer": "记录已访问的状态/动作序列（如最近 k 步的哈希），检测重复或回环则截断，或加衰减/惩罚避免原地打转。也可限制同一工具连续调用次数，避免“调了失败又调”的同错循环。"
    },
    {
      "question": "Tool 失败后 Retry 还是 Replan？",
      "answer": "简单瞬时错误（超时、限流）可有限 Retry；语义/策略失败（参数错、思路错）应 Replan（换思路/换工具），避免同错反复。经验：连续 2 次同一错误就转 Replan，而不是第 3 次还硬 Retry。"
    },
    {
      "question": "如何保证写操作幂等？",
      "answer": "写操作带唯一键/版本号/状态机，重复调用不产生重复副作用（如“订单已存在则更新而非新建”）；关键写（付款、删数据）加人工确认或双签。幂等是 Agent 接写操作的前提，否则一次重试就出事故。"
    },
    {
      "question": "哪些节点应由确定性代码完成？",
      "answer": "校验、入库、支付、发送等正确性与安全攸关的节点，应由确定性代码执行，不交给模型自由调用。模型只负责“生成建议/决策文本”，真正产生副作用的动作由护栏后的代码落地，可审计、可回滚。"
    }
  ],
  "followUpAnswers": [
    "不是。自主带来不确定性与成本，固定任务用 Workflow 更稳更省；Agent 只用于开放决策点（路径未知、需检索推理）。判断标准：能否在写代码时把步骤列全？能→Workflow；不能→才考虑 Agent。很多“看似要 Agent”的场景其实是固定流程套了层皮。",
    "设 max_steps（如 8~15），每轮规划后计数，超过即终止并交回确定逻辑或人工；防止无限循环烧钱与超 SLA。步数上限也是安全护栏：模型卡住时不会永远空转。",
    "记录已访问的状态/动作序列（如最近 k 步的哈希），检测重复或回环则截断，或加衰减/惩罚避免原地打转。也可限制同一工具连续调用次数，避免“调了失败又调”的同错循环。",
    "简单瞬时错误（超时、限流）可有限 Retry；语义/策略失败（参数错、思路错）应 Replan（换思路/换工具），避免同错反复。经验：连续 2 次同一错误就转 Replan，而不是第 3 次还硬 Retry。",
    "写操作带唯一键/版本号/状态机，重复调用不产生重复副作用（如“订单已存在则更新而非新建”）；关键写（付款、删数据）加人工确认或双签。幂等是 Agent 接写操作的前提，否则一次重试就出事故。",
    "校验、入库、支付、发送等正确性与安全攸关的节点，应由确定性代码执行，不交给模型自由调用。模型只负责“生成建议/决策文本”，真正产生副作用的动作由护栏后的代码落地，可审计、可回滚。"
  ],
  "pitfalls": [
    "以为 Agent 越自主越好：自主带来不确定性与成本，固定任务用 Workflow 更稳更省。",
    "把高合规任务交给不可审计的自主 Agent：出了错无法回放每步决策。",
    "写操作不幂等，重试产生副作用：如重复扣款、重复发消息。"
  ],
  "beginnerSummary": "Workflow 像一条流水线：第一步干啥、第二步干啥都提前定好，稳、快、好测试，适合固定业务（比如每天自动出报表）。Agent 像一个能自己想办法的员工：每一步干什么由它看着办，灵活但“想法”可能飘、成本高、还难复现。所以不是越自主越好——能走流水线的就别派自由人。尤其是要付钱、要写数据库、要发消息这些“后果严重”的节点，必须交给写死的代码，不能让模型随意点按钮；还要给 Agent 设步数上限、防它绕圈、保证重复执行不出事。",
  "prerequisites": [
    "Workflow 路径预定义、确定性，输出可复现。",
    "Agent 由模型动态决策，灵活但不稳定、状态空间大。",
    "写操作需安全与可审计（幂等、人工确认）。"
  ],
  "workedExample": [
    "报表生成=Workflow（取数→模板→发送）；开放研究问答=Agent（检索→读→再检索→写）。",
    "支付/入库节点在两者中都是确定性代码，模型只提“建议”，真正执行由代码落库。",
    "Agent 设 max_steps=8，超过即终止交人工；同状态回环检测截断防烧钱。"
  ],
  "lineByLine": [
    "Workflow: 预定义路径，每步确定，易测、可复现、成本低。",
    "Agent: 模型动态决策，灵活但不稳、状态空间大、难测、贵。",
    "固定任务用 Workflow；开放决策点才用 Agent。",
    "写操作/支付等节点永远用确定性代码，不交模型自由调用。",
    "Agent 需限步数、防循环、保证幂等。"
  ],
  "diagram": "Workflow vs Agent:\nWorkflow: 固定路径 -> 可预测/易测/省成本\nAgent:    模型动态规划 -> 灵活/状态大/不稳/贵\n原则: 能 Workflow 就别 Agent\n写操作/支付: 永远确定性代码"
};

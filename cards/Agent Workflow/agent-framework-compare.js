export default {
  "kind": "concept",
  "id": "agent-framework-compare",
  "category": "Agent Workflow",
  "difficulty": "Medium",
  "title": "Agent 框架选型：LangGraph / CrewAI / AutoGen / 自研",
  "prompt": "在 LangGraph、CrewAI、AutoGen 和自研之间，应该怎么为业务选型 Agent 框架？",
  "quickAnswer": "选型看「控制流形态」：需要显式、可持久化、可断点恢复的有向图流程选 LangGraph（状态机+checkpoint）；需要多角色分工协作、强调角色与任务委派选 CrewAI；需要多 Agent 自由对话/协商编排选 AutoGen。当流程高度定制、对延迟/可观测/合规有强约束且团队有工程能力时，自研薄编排层更划算。MCP 是「工具接入标准」，与框架正交——框架负责编排，MCP 负责把工具统一暴露，混用可降低对单一框架生态的锁定。",
  "approach": "选型四步：①先定控制流形态——是确定图、角色分工还是自由对话；②匹配框架原语（图式→LangGraph、角色→CrewAI、对话→AutoGen）；③评估定制与合规需求，若强合规/低延迟/高度定制则倾向自研；④用 MCP 作为统一工具层接入工具，使框架与后端解耦。最后用真实业务流做 PoC，对比开发速度、可观测性、断点恢复与 p99 延迟后再拍板，不要仅凭功能清单长短决定。",
  "explanationFocus": "是什么：Agent 框架选型是根据任务的控制流形态（图式状态机、角色分工、对话协商）与团队的定制/合规诉求，在 LangGraph、CrewAI、AutoGen 与自研之间做权衡，并用 MCP 作为统一的工具接入层。选型的本质不是「哪个框架功能最多」，而是「哪个框架的原语最贴合我的控制流」——框架只解决编排，工具/数据的接入应走与框架解耦的标准协议（如 MCP）。",
  "bruteForce": "最笨的做法是不管业务形态，一律用通用 while 循环硬写多 Agent 协作：流程不可视、难恢复、难复用，每加一个分支就要改一堆胶水代码。迭代成本随规模指数爆炸，且出错了无法回放「当时走到了哪一步」。这等于亲手重新实现了一个劣质框架，却没享受到任何生态红利。",
  "invariant": "框架只解决「编排」，工具/数据的接入应当走与框架解耦的标准协议（如 MCP），避免把业务逻辑焊死在某个框架的适配器里。更具体：无论选哪个框架，工具都应经 MCP server 统一暴露，这样未来换框架时工具层可复用；同时控制流形态必须和评估结论一致——别用对话框架去硬撑本应确定图的流程。",
  "walkthrough": "以「周报生成」业务为例：流程固定为 取数→汇总→润色→审核（4 节点、顺序+1 个条件分支），并发约 100 个用户任务/天，要求失败可断点续跑。这明显是图式控制流且有持久化需求，选 LangGraph，把节点画成图、状态落库（checkpoint），某节点挂了能从断点恢复而不是重跑全链。另一「市场调研」任务需要 研究员/写手/审校 三角色循环互审，选 CrewAI 的 role/task 模型最顺手，少写大量协作样板。若业务是客服多 Agent 自由协商（如退款 bot 与物流 bot 对话），AutoGen 的群聊编排更合适。当公司要求所有工具调用走自研审计网关、且延迟 p99<200ms，现成框架适配成本高，则自研薄编排层 + MCP 接工具，既满足合规又可控延迟。",
  "derivation": [
    "为什么需要：手搓多 Agent 流程不可视、难恢复、难复用，随着规模变大迭代成本爆炸。框架把常见编排模式（图、角色、群聊）沉淀成可组合原语，让团队专注业务逻辑而非重复造轮子。",
    "怎么实现：按控制流选框架——图式用 LangGraph（节点+边+状态 checkpoint）、角色用 CrewAI（role/task/crew）、对话用 AutoGen（群聊/代理）；自研则抽象 DAG/状态机 + 事件总线；工具统一经 MCP server 暴露，与编排解耦。",
    "有什么代价：框架有学习曲线，且存在抽象泄漏（某些需求绕过框架原语）；重框架引入依赖体积与少量延迟；选错形态后期迁移成本极高；自研要养维护团队。需要在「开箱即用」与「完全可控」之间权衡。",
    "怎么评测：用真实业务流做 PoC，横向比开发速度、可观测性、断点恢复能力、p99 延迟与团队上手成本。关键指标是「同样需求谁写得更少、跑得更稳、出问题更好查」，而不是框架名气或功能清单长度。"
  ],
  "edgeCases": [
    "流程需长时间挂起等人工（如等审批）：LangGraph 的 checkpoint 更关键，CrewAI 在这块较弱，选型时要重点验证持久化能力。",
    "角色间循环依赖导致死锁：多角色互审若无限循环，需在框架层设最大轮次上限，否则任务永不结束。",
    "框架默认工具适配不满足内网合规：需自研 MCP 桥接把工具调用导到审计网关，框架只负责编排、不碰合规细节。",
    "超高并发低延迟场景：现成框架的抽象层引入的 p99 开销若超阈值，只能自研薄层绕过。"
  ],
  "code": "def choose_framework(control_flow, needs):\n    if control_flow == \"graph\" and needs.resumable:\n        return \"LangGraph\"          # 状态机+checkpoint\n    if control_flow == \"roles\":\n        return \"CrewAI\"             # role/task 分工\n    if control_flow == \"dialogue\":\n        return \"AutoGen\"            # 多代理群聊\n    if needs.compliance and needs.low_latency:\n        return \"self-built + MCP\"   # 薄编排+标准工具层",
  "codeNotes": [
    "框架选型的核心是「控制流形态匹配」，而非功能清单长短——选错形态后期迁移成本极高。",
    "MCP 与编排框架正交，可作为统一工具层降低锁定风险：换框架时工具 server 可复用。",
    "needs.resumable 对应 LangGraph 的 checkpoint 能力，断点续跑是企业级 Agent 的刚需。"
  ],
  "complexity": "框架本身运行开销很小（图遍历/事件分发是 O(节点数)），主要成本在框架带来的抽象与依赖体积，以及 Agent 自身的 LLM/工具调用。选错形态的后隐性成本极高：用错框架会导致大量适配代码、调试困难、迁移时被锁死。自研则要持续养维护成本，但换来对延迟与合规的完全掌控。MCP 接入会引入一次性的 server 改造成本，但长期降低锁定风险。",
  "followUps": [
    {
      "question": "MCP 和 Agent 框架是什么关系，能混用吗？",
      "answer": "MCP 是工具/数据接入的标准协议，与编排框架完全正交。任何框架（LangGraph/CrewAI/AutoGen）都能通过 MCP server 把工具统一暴露给 Agent，二者可以也推荐混用。混用的好处是降低对单一框架工具生态的锁定——今天用 LangGraph、明天换 CrewAI，只要工具都走 MCP，工具层无需重写。一句话：框架管『怎么编排』，MCP 管『怎么接工具』，职责分离。"
    },
    {
      "question": "什么时候一定要自研而不是用现成框架？",
      "answer": "当控制流高度定制、对 p99 延迟/可观测/合规审计有强约束，且现成框架的抽象带来不可接受的适配成本时，应自研。或者团队已有成熟的编排/事件基础设施（如自研状态机、消息总线），在其上写一层薄编排反而更快更可控。自研的代价是要养维护成本，所以只有当『框架适配成本 + 锁定风险』超过『自研维护成本』时才划算。"
    }
  ],
  "followUpAnswers": [
    "MCP 是工具/数据接入的标准协议，与编排框架完全正交。任何框架（LangGraph/CrewAI/AutoGen）都能通过 MCP server 把工具统一暴露给 Agent，二者可以也推荐混用。混用的好处是降低对单一框架工具生态的锁定——今天用 LangGraph、明天换 CrewAI，只要工具都走 MCP，工具层无需重写。一句话：框架管『怎么编排』，MCP 管『怎么接工具』，职责分离。",
    "当控制流高度定制、对 p99 延迟/可观测/合规审计有强约束，且现成框架的抽象带来不可接受的适配成本时，应自研。或者团队已有成熟的编排/事件基础设施（如自研状态机、消息总线），在其上写一层薄编排反而更快更可控。自研的代价是要养维护成本，所以只有当『框架适配成本 + 锁定风险』超过『自研维护成本』时才划算。"
  ],
  "pitfalls": [
    "为「赶时髦」用对话框架（AutoGen）去做本应确定图的流程（周报生成），结果是调试地狱——流程不可视、状态难恢复。",
    "把业务强耦合进某框架专属 API，后续迁移被锁死，换框架等于重写；应把工具层经 MCP 解耦。",
    "只看功能清单长短而非控制流匹配，选了「功能最多」却「原语最不贴合」的框架，开发反而更慢。"
  ],
  "beginnerSummary": "选框架像选交通工具：固定路线通勤（确定性流程）骑共享单车画好路线最稳（LangGraph）；多人分工搬货（角色协作）用货运队（CrewAI）；几个人自由商量办事（多 Agent 对话）用微信群（AutoGen）。还有一类情况是公司规定必须用自家审计网关、且要求极速响应，那就自己造辆定制车（自研）+ 统一充电桩（MCP）。MCP 是「统一插座标准」，不管开啥车都能接同一个充电桩，避免被某家车厂的配件锁死。",
  "prerequisites": [
    "理解状态机/DAG 与事件驱动编排：才能判断业务是图式、角色还是对话形态。",
    "了解 MCP 等工具接入标准：明白工具层应与编排解耦，降低框架锁定。",
    "清楚业务的控制流形态与合规要求：这是选型的输入，含糊不得。"
  ],
  "workedExample": [
    "周报流水线（4 节点顺序图+1 条件分支）选 LangGraph，状态落库支持断点续跑，节点挂了从 checkpoint 恢复而非重跑。",
    "三角色互审的调研任务选 CrewAI，用 role/task 原语少写大量协作样板，开发提速明显。",
    "强合规+低延迟（p99<200ms）场景选自研薄编排 + MCP 接审计网关，既满足合规又控住延迟。"
  ],
  "lineByLine": [
    "if control_flow == 'graph' and needs.resumable：图式且需断点恢复→LangGraph，其 checkpoint 能把状态落库、从断点续跑。",
    "if control_flow == 'roles'：多角色分工协作→CrewAI，用 role/task/crew 原语表达委派最自然。",
    "if control_flow == 'dialogue'：多 Agent 自由协商→AutoGen，群聊编排擅长代理间对话。",
    "if needs.compliance and needs.low_latency：强合规且低延迟→自研薄编排 + MCP，既可控又解耦工具层。"
  ],
  "diagram": "控制流形态 ──▶ 图式 ──▶ LangGraph\n     │\n     ├──▶ 角色分工 ──▶ CrewAI\n     │\n     ├──▶ 对话协商 ──▶ AutoGen\n     │\n     └──▶ 强合规/低延迟 ──▶ 自研 + MCP\n工具层：任意框架 ──▶ MCP server ──▶ 统一工具/数据"
};

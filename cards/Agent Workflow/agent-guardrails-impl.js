export default {
  "kind": "concept",
  "id": "agent-guardrails-impl",
  "category": "Agent Workflow",
  "difficulty": "Hard",
  "title": "Guardrails 工程落地：给 Agent 套上安全护栏",
  "prompt": "如何给生产环境的 Agent 套上一套可落地的 Guardrails（安全护栏）？",
  "quickAnswer": "Guardrails 是一套位于 Agent 与外部环境之间的「过滤+约束」层，工程落地分四块：①输入侧做内容审核、敏感词与注入检测、PII 识别脱敏；②输出侧做格式校验、违规拦截与脱敏改写；③工具侧用沙箱与最小权限隔离副作用，高危动作走人工 gate；④用 red-teaming 持续攻击发现漏网之鱼，用熔断/降级在护栏自身异常时保底。目标是把「聪明但不可控」的模型，约束进「有用且安全」的运行边界。落地时要做成可插拔中间件，异常时降级到最小规则集而非整体不可用。",
  "approach": "落地顺序建议是『纵深、可插拔、可降级』：①输入过滤（内容审核+注入检测+PII 脱敏）；②输出校验（格式合规+泄露扫描+脱敏改写）；③工具沙箱与最小权限（凭证代理、高危动作 human gate）；④注入防护（把不可信检索内容当数据而非指令）；⑤red-teaming 持续攻击发现漏洞；⑥熔断降级保证护栏自身故障时系统仍可用。每一层都做成独立可插拔中间件，串联在 Agent 的输入/输出与 tool 边界，避免把护栏逻辑焊死在业务代码里。",
  "explanationFocus": "是什么：Guardrails 是围绕 Agent 的一整套纵深防御体系，通过输入过滤、PII 脱敏、工具沙箱与最小权限、注入防护、红队演练和熔断降级，把不可控的模型推理约束在安全的执行边界内。它既不是单一的『敏感词过滤器』，也不是事后审计，而是串联在『用户输入→模型→输出→工具调用』全链路的显式校验层。其核心目标是把「聪明但不可控」的模型，约束进「有用且安全」的运行边界，让 Agent 既能发挥能力，又不会越权、泄密或作恶。",
  "bruteForce": "最简陋也最危险的『暴力』做法是把用户原话直接喂给 LLM，并允许它任意调用工具、读写外部系统：一次成功的提示注入就能让 Agent 窃取隐私、越权操作、或把机密写进对外回复。另一种伪防护是『只在输入做一层关键词黑名单』，但机密仍可能从模型生成的回复里绕出来，且注入可以绕过静态词表。没有沙箱与最小权限，工具一旦被诱导调用就是真实损失。",
  "invariant": "任何越过 Agent 边界的「输入解析」与「工具执行」都必须先经过一层显式、可审计的校验与授权，绝不能直接透传。更具体地说：输入在进 LLM 前必须过审核与脱敏；输出在离站前必须过泄露扫描；工具调用必须按风险分级，高危动作必须有人工 gate 或显式授权。护栏自身也要有熔断降级，不能因为护栏服务故障就让整个 Agent 不可用或失去防护。",
  "walkthrough": "以峰值并发 100 路会话的客服 Agent 为例。每分钟约 1000 条用户消息先过输入审核模型（p99 延迟 80ms），命中 PII（手机号/身份证）自动掩码，例如把 13800131234 替换成 138****1234。模型生成回复后，输出护栏做泄露扫描，检查是否把原始手机号写进了对外回复（漏检率目标<0.1%）；若发现则脱敏改写后再发出。工具调用走沙箱，删单/退款类高危动作需人工 gate 确认才执行。一次异常流量打满审核服务时，熔断器打开，护栏降级为『仅关键字黑名单 + 最小工具集』，保证主体可用性不中断。整条链路每月拦截越权调用约 200 次、PII 泄露 0 起。",
  "derivation": [
    "为什么需要：Agent 具备读写数据、调用工具、对外发声的能力，一次错误或恶意的指令就可能造成隐私泄露与真实财产损失（如误删订单、泄密、越权退款）。把风险关进可控边界，是 Agent 能上生产的先决条件，否则「能力越强、危害越大」。",
    "怎么实现：输入侧接内容审核与注入检测、做 PII 识别与脱敏；输出侧接格式校验与泄露扫描，发现明文敏感信息就拦截或改写；工具侧用沙箱+最小权限+高危动作 human gate；配套 red-teaming 持续发现漏洞；护栏自身异常时熔断降级到最小规则集。",
    "有什么代价：每一层护栏都增加 p99 延迟（累计 100~300ms）与单请求成本；过度拦截损害体验、误杀正常用户；规则与模型需要持续维护以跟上新型攻击。护栏本身也可能成为单点故障，必须有熔断降级设计。",
    "怎么评测：用红队数据集测输入/输出护栏的拦截率与误拦率；压测看护栏本身的开销与熔断是否生效（护栏挂了系统是否仍可用）；线上长期观测 PII 泄露事件数与越权调用数，作为护栏有效性的最终指标。"
  ],
  "edgeCases": [
    "注入指令藏身检索文档或用户上传附件：不可信内容必须当数据而非指令执行，需内容/指令隔离与结构化解析。",
    "PII 以变体呈现（如『手机 138 0013 1234』带空格、或写成『一三八零零幺三一二三四』）：脱敏正则与模型需覆盖多种变体，否则漏脱敏。",
    "护栏服务自身故障：审核模型超时或崩溃时，必须熔断降级到关键字黑名单+最小工具集，而非让整条 Agent 不可用或失去全部防护。",
    "多语言/方言注入：用英文或方言写的注入指令绕过中文词表，需要多语言审核模型覆盖。"
  ],
  "code": "def guardrails_pipeline(message, model_reply, tool_call):\n    # 输入：审核 + PII 脱敏\n    risk = content_moderation(message)\n    safe_in = mask_pii(message)                 # 138****1234\n    if risk == \"block\":\n        raise Blocked(\"input policy violation\")\n    # 输出：泄露扫描\n    if leaks_pii(model_reply):\n        model_reply = redact(model_reply)       # 改写后再发\n    # 工具：最小权限 + 高危人工 gate\n    if tool_call.risk == \"high\" and not human_confirmed(tool_call):\n        raise NeedApproval(tool_call)\n    return sandbox_exec(tool_call, scoped_creds(tool_call))",
  "codeNotes": [
    "护栏应作为可插拔中间件，串联在 Agent 的输入/输出与 tool 三处边界，与业务解耦，便于单独升级与熔断。",
    "熔断降级策略要可配置（如审核 p99>200ms 即降级），避免护栏自身成为单点故障、拖垮主链路。",
    "safe_in 才是真正进 LLM 的上下文，原始 message 不应保留明文 PII，防止后续步骤意外泄露。"
  ],
  "complexity": "每层护栏的单次开销是 O(1) 的推理或正则匹配，主要成本在附加的审核/分类模型调用（单条 p99 约 50~150ms，输入+输出两层叠加约 100~300ms），可通过批处理与缓存摊薄。脱敏正则与规则维护是持续的运营成本，且要在『拦截率』与『误杀率』之间平衡——过严会误伤正常用户、过松会漏放风险。部署上护栏应作为独立服务，方便单独扩容与熔断。",
  "followUps": [
    {
      "question": "输入护栏和输出护栏为什么要都做？",
      "answer": "只做输入防护是远远不够的：机密仍可能从模型自己生成的回复里『绕出来』——比如模型在推理时记住了上下文里的手机号，并在对外回复中明文写出。输出护栏专门负责拦截回复中的明文 PII 与违规内容，与输入防护形成闭环，二者缺一不可。实践上常见失误就是『以为挡住输入就安全了』，结果输出侧漏了，照样泄密。"
    },
    {
      "question": "怎么平衡安全与体验？",
      "answer": "应按风险分级施加摩擦，而不是一视同仁地严管：低风险自动通过、中风险轻提示、高风险才强制人工确认，把用户的等待与摩擦集中在真正危险的动作上。同时把护栏做成可降级的中间件——当护栏服务异常时退到最小规则集（如关键字黑名单）而非整体不可用，这样既保安全又保可用性。平衡的本质是『在风险最高的地方加锁，在风险低的地方放行』。"
    }
  ],
  "followUpAnswers": [
    "只做输入防护是远远不够的：机密仍可能从模型自己生成的回复里『绕出来』——比如模型在推理时记住了上下文里的手机号，并在对外回复中明文写出。输出护栏专门负责拦截回复中的明文 PII 与违规内容，与输入防护形成闭环，二者缺一不可。实践上常见失误就是『以为挡住输入就安全了』，结果输出侧漏了，照样泄密。",
    "应按风险分级施加摩擦，而不是一视同仁地严管：低风险自动通过、中风险轻提示、高风险才强制人工确认，把用户的等待与摩擦集中在真正危险的动作上。同时把护栏做成可降级的中间件——当护栏服务异常时退到最小规则集（如关键字黑名单）而非整体不可用，这样既保安全又保可用性。平衡的本质是『在风险最高的地方加锁，在风险低的地方放行』。"
  ],
  "pitfalls": [
    "把不可信检索内容当系统指令执行，导致间接提示注入（indirect prompt injection），这是最常见的护栏失效点。",
    "只在输入做防护、忽略输出泄露，机密仍可能从模型生成的回复里『绕出来』，形成防护盲区。",
    "护栏无熔断，一旦审核服务抖动就整链卡死或整体不可用，反而制造新的可用性事故。"
  ],
  "beginnerSummary": "Guardrails 就像小区门禁加快递柜的组合：进门先刷脸登记（输入审核），包裹里暴露的身份证号先打码（PII 脱敏），外卖小哥只能把餐放进柜子、不能进你家（工具沙箱+最小权限），遇到可疑人员保安先拦下问清楚（熔断降级）。这样住户（用户）既方便，又防住了坏人（注入/泄密/越权）。而且门禁是「一道道关卡」而不是一扇门——前面没拦住，后面还有机会，这就是纵深防御。",
  "prerequisites": [
    "理解提示注入与最小权限原则：知道『不可信内容不能当指令』以及『工具应只拿必要凭证』。",
    "具备内容审核/分类模型与 PII 识别能力：这是输入/输出护栏的执行器。",
    "工具调用走沙箱与凭证代理：确保即使越权调用也被限制在最小权限内。"
  ],
  "workedExample": [
    "用户消息含身份证号 110...1990，护栏识别并掩码为 ****，模型全程只看到脱敏值，回复里也不会泄露。",
    "回复草稿误带客户手机号 13800131234，输出护栏拦截并改写为 138****1234 后再发出，漏检率<0.1%。",
    "删单工具调用被标为 high 风险，无人确认时抛出 NeedApproval，阻止了一次潜在的越权操作。"
  ],
  "lineByLine": [
    "content_moderation(message)：入站消息先送审核，标记风险等级（正常/嫌疑/拦截）。",
    "mask_pii(message)：命中 PII 按策略脱敏（掩码/替换），safe_in 才是进 LLM 的上下文，原始明文不保留。",
    "leaks_pii(model_reply)：模型输出回程再过泄露扫描，发现明文敏感信息则 redact 改写后再离站。",
    "tool_call.risk=='high' 且未确认 → raise NeedApproval：高危工具动作路由到人工 gate，scoped_creds 只给最小权限凭证。"
  ],
  "diagram": "User ─▶ [Input Guard: 审核+脱敏] ─▶ LLM ─▶ [Output Guard: 泄露扫描] ─▶ User\n                                      │\n                                      ▼\n                              [Tool Guard: 沙箱+gate] ─▶ 外部API"
};

export default {
  "id": "agent-failure-modes",
  "kind": "concept",
  "category": "Agent Workflow",
  "difficulty": "Hard",
  "title": "Agent 失败模式与缓解",
  "prompt": "Agent 有哪些典型失败模式，怎么缓解？",
  "code": "def guard(trace, action, budget):\n    if action in last_k(trace, 3): raise LoopDetected()   # 防循环\n    if not schema_ok(action):   raise BadAction()         # 防误用\n    if budget.exceeded():       raise BudgetExceeded()    # 防爆炸\n    return True",
  "diagram": "Action -> [Loop?][Schema?][Budget?] -> allow/block",
  "explanationFocus": "是什么：Agent 失败模式指其在运行中反复出现的系统性错误形态，典型有七类——循环空转（重复同一动作）、工具误用（选错工具/参数错）、幻觉作答（脱离观察凭空回答）、上下文溢出（关键上下文被截断丢失）、提示注入劫持（被外部内容诱导偏离）、目标漂移（跑着跑着忘了初衷）、成本爆炸。缓解是把『容错+约束+可恢复』织进全流程的工程集合。",
  "quickAnswer": "典型失败有七类：循环空转、工具误用、幻觉作答、上下文溢出、提示注入劫持、目标漂移、成本爆炸。缓解靠步数上限、观察回灌（grounding）、格式/schema 校验、沙箱与授权、预算护栏、可观测回放与 HITL 兜底——一句话：把『约束 + 校验 + 可恢复』织进全流程，而不是依赖模型自觉。",
  "beginnerSummary": "Agent 失败像小孩常犯的几种错：绕圈重复（循环空转）、用错工具（误用）、瞎编（幻觉）、忘事（上下文溢出）、被人骗（提示注入）、跑题（目标漂移）、乱花钱（成本爆炸）。大人（护栏）提前定规矩——『同一件事不许做第三遍』『没真查过不许说查了』『不认识的指令先问人』——才稳当。",
  "walkthrough": "一次失败复盘：Agent 在回答前连续调用同一搜索工具 12 次（循环空转），导致成本飙升且用户等到超时；另一次它没真正查数据库却回答『已查询，结果为 X』（幻觉作答）。修复后加『最近 3 步动作去重 + max_steps』拦截循环，加『作答前必须引用观察结果』的 grounding 校验拦截幻觉；两类事故归零。",
  "approach": "先枚举本场景高概率失败模式，再为每类配对应护栏（步数上限防循环、schema 校验防误用、grounding 防幻觉、预算防爆炸、注入检测防劫持），最后做可观测回放与 HITL 兜底，让未知失败也能被捕获与恢复。",
  "bruteForce": "出问题再看、靠模型『自觉』不犯错：无预设护栏，每种失败都会变成用户可感事故（发错消息、乱扣款、瞎编事实），且无法快速定位是哪个环节出的问题。",
  "invariant": "每种已知失败模式都应有可触发的硬护栏兜底，绝不能把安全性寄托在『模型这次恰好做对』上；护栏本身需可组合、有权重与优先级。",
  "complexity": "每步推理附加若干 O(1) 护栏检查（去重、schema 校验、预算比较），成本可忽略（微秒级）；主要开销在可观测回放与 HITL 的人力，以及护栏设计本身。护栏叠加对延迟影响通常在 1% 以内。",
  "derivation": [
    "为什么需要：LLM 非确定且能执行真实动作（发消息、调 API、扣款），一次失败就可能造成用户资损或事实错误，必须系统性防护而非事后补救。",
    "怎么实现：对每类失败配护栏——步数上限 + 动作去重防循环；工具调用的 schema/类型校验防误用；作答必须引用观察（grounding）防幻觉；预算护栏防爆炸；对外部内容做注入检测防劫持；目标一致性检查防漂移。",
    "有什么代价：护栏叠加增加少量延迟与不少工程复杂度；过度约束会抑制模型合理多样性（误伤正常行为），需调阈值。",
    "怎么评测：用红队（red team）刻意触发各失败模式，统计拦截率与误伤率（正常行为被错拦的比例），追求高拦截、低误伤。"
  ],
  "edgeCases": [
    "多个护栏相互冲突（如预算护栏要停、但 grounding 还没过），需定义优先级与仲裁，避免状态机卡死。",
    "出现训练/设计阶段没覆盖的新失败模式（如新型注入），既有的护栏拦不住，需靠回放+红队持续补。",
    "护栏阈值太严误伤正常多样行为（把合理的多步探索当循环拦掉），需用误伤率指标调参。",
    "HITL 兜底人力不足时，未决样本积压，需有超时自动安全降级。"
  ],
  "pitfalls": [
    "把安全寄托在模型『自觉』而非硬护栏，结果在长尾输入下必然踩雷。",
    "护栏堆砌过度，把正常的多步探索、合理重试也误伤，用户体验反而变差。",
    "只测拦截率不看误伤率，护栏看似很强实则把大量正常请求拦掉。"
  ],
  "prerequisites": [
    "能枚举本场景常见失败模式（循环/误用/幻觉/注入…）。",
    "Agent 框架有可插入的护栏钩子（在动作执行前后拦截）。",
    "有 trace 记录与回放能力，才能复盘失败、补护栏。",
    "理解沙箱/授权/预算等基础安全手段。"
  ],
  "workedExample": [
    "重复动作检测 + max_steps：Agent 连续调同一搜索被去重拦截，并触发步数上限终止，成本从 12 次调用降到 6 次上限。",
    "grounding 校验：作答前检查回答是否引用了真实观察结果，未引用则打回重做，杜绝『已查询』式幻觉。",
    "注入检测：从网页抓取的内容里含『忽略之前指令』，被注入过滤器标记并交 HITL，未被执行。"
  ],
  "lineByLine": [
    "识别循环/误用/幻觉等失败模式：先列清单，明确每类用什么信号检测。",
    "为每类加对应护栏：动作去重、schema 校验、grounding、预算各自守住一类。",
    "超阈值即拦截或升级：命中护栏就 block 或转 HITL，不让错误动作落地。",
    "新失败持续沉淀护栏：红队/回放发现新模式就补进防护，形成闭环。"
  ],
  "codeNotes": [
    "护栏要可组合且有权重/优先级，多个护栏同时触发时能仲裁。",
    "新失败模式需持续补护栏，防护是『活』的清单而非一劳永逸。",
    "拦截动作前最好记录 trace，方便事后复盘是哪条护栏、为何触发。"
  ],
  "followUps": [
    {
      "question": "怎么发现未知失败模式？",
      "answer": "靠三条渠道持续积累：①生产 trace 回放——定期把真实运行记录拉出来人工/自动审查，找异常模式；②用户反馈——把用户投诉、纠错当作信号；③红队演练——专人刻意构造对抗输入（注入、诱导循环、边界参数）主动引爆。发现的新形态就沉淀成一条新护栏，防护清单滚雪球式变厚。"
    },
    {
      "question": "护栏会误伤正常行为吗，怎么处理？",
      "answer": "会。比如『循环检测』可能把合理的『换关键词重试搜索』误判成死循环。处理办法：用误伤率指标监控，把阈值调到『拦截率 vs 误伤率』的帕累托较优点；对边界 case 不硬拒，而是转人工（HITL）兜底；并定期用正常样本回归测试护栏，确保不误伤核心流程。"
    }
  ],
  "followUpAnswers": [
    "靠三条渠道持续积累：①生产 trace 回放——定期把真实运行记录拉出来人工/自动审查，找异常模式；②用户反馈——把用户投诉、纠错当作信号；③红队演练——专人刻意构造对抗输入（注入、诱导循环、边界参数）主动引爆。发现的新形态就沉淀成一条新护栏，防护清单滚雪球式变厚。",
    "会。比如『循环检测』可能把合理的『换关键词重试搜索』误判成死循环。处理办法：用误伤率指标监控，把阈值调到『拦截率 vs 误伤率』的帕累托较优点；对边界 case 不硬拒，而是转人工（HITL）兜底；并定期用正常样本回归测试护栏，确保不误伤核心流程。"
  ]
};

export default {
  "kind": "concept",
  "id": "agent-planning",
  "category": "Agent Workflow",
  "difficulty": "Medium",
  "title": "规划（planning）与任务分解",
  "prompt": "Agent 的规划（planning）与任务分解是怎么做的？",
  "quickAnswer": "规划是把高层目标拆成有序、可执行的子任务，并在执行中按需调整。常见做法：让 LLM 一次性生成步骤清单(plan-then-act)，或每步动态重规划(ReAct 式)；复杂任务可用层级分解（子目标递归拆分）。关键点在于可验证的子目标边界与失败时的回滚/改路，避免长任务中途迷失或漏步。",
  "approach": "生成计划（LLM 出带依赖的步骤图）→ 逐步执行并观察 → 偏差超阈值则重规划或回退 → 已完成步骤状态入栈供复用。",
  "explanationFocus": "是什么：规划是 Agent 把抽象的高层目标分解为有依赖关系的子任务序列，并在执行中根据观察动态调整的过程。它回答\"先做什么、后做什么、卡住了怎么办\"，把模糊目标变成可逐步验证、可回退的执行路径。",
  "bruteForce": "无计划直接开干：遇到长任务易中途迷失、漏步骤、无法回溯，且一步走错要整条重来，完全依赖单步贪心易走入歧路。",
  "derivation": [
    "为什么需要：长任务跨度大、子目标间有依赖，单步贪心看不到全局，极易走入歧路且无法回溯；没有计划，Agent 像没地图乱走，做到一半才发现前提没满足。规划把\"全局结构\"显式建模出来，使每步都有目标与校验点。",
    "怎么实现：用 LLM 产出带依赖的步骤图（plan-then-act），或每步基于当前观察动态决定下一步（ReAct）；复杂任务用层级分解——先拆大阶段，再展开当前阶段的子步骤。执行每步后评估进度，偏差超阈值则重规划；也可用监督式 planner 或搜索（如 MCTS）替代纯 LLM。",
    "有什么代价：规划本身消耗 token 与时间；计划过细则僵化（遇变不敢绕）、过粗则无效（等于没计划）；还需检测\"计划已完成/卡死\"的终止条件。重规划若不带已完成上下文会重复劳动，若太频繁则抖动。",
    "怎么评测：看计划质量（步骤是否完备无环）、任务完成率、重规划频率与最终步数；对照\"无计划基线\"看完成率提升，并监控卡死率（某步反复失败无法推进）。"
  ],
  "invariant": "已完成的子目标状态必须被记录（含产出与观察），后续步骤依赖它而非重新猜测；重规划必须带上已完成列表，避免把已做的事再做一遍。",
  "walkthrough": "以\"写一份调研报告\"为例：1) 列提纲 2) 检索各节资料 3) 撰写 4) 汇总。执行第 2 步发现第 3 节缺数据 → 重规划插入\"先补数据再写第 3 节\"。共 4 个主步骤、发生 1 次重规划、约 9 次 LLM 调用。因为 done 列表记录了\"提纲已完成、第1/2节资料已检索\"，重规划只补第 3 节数据环节，没有把已做的工作推倒重来。端到端比无计划基线少走约 3 个弯路。",
  "edgeCases": [
    "子任务间循环依赖：步骤图出现 A→B→A，需环检测并告警，否则执行死循环。",
    "某步反复失败：如工具持续报错，需换策略或标记阻塞并升级，不能无限重试同一动作。",
    "目标在中途变化：用户改需求，需部分丢弃旧计划、保留已完成部分重新规划，而非推倒重来。",
    "计划已完成但观察说没完成：需一致性校验，避免虚假完成导致后续基于错误前提推进。"
  ],
  "code": "def plan_and_execute(llm, goal, tools):\n    plan = llm_make_plan(goal)                 # 有序子任务\n    done = []\n    while not plan.empty():\n        step = plan.next()\n        ok, obs = execute_step(step, tools)\n        done.append(step)\n        if not ok:\n            plan = llm_replan(goal, done, obs)  # 动态重规划\n    return summarize(done)",
  "codeNotes": [
    "重规划要带上已完成步骤 done 列表与最近观察 obs，避免重复劳动并让新计划承接现状。",
    "计划应可序列化持久化，便于崩溃恢复——重启后从 saved plan + done 继续，而非从头。",
    "对 step 执行加超时与最大重试，卡死时触发 replan 或标记阻塞，防止单点卡整个流程。"
  ],
  "complexity": "规划开销 ≈ 任务复杂度对应的 LLM 调用；每发生一次重规划追加一次模型调用。一个 10 步任务若重规划 2 次，调用约 13 次。重规划频率过高说明计划过粗或观察不准，应监控\"重规划率\"作为计划质量指标。",
  "followUps": [
    {
      "question": "plan-then-act 与 ReAct 区别？",
      "answer": "plan-then-act 先由 LLM 生成完整全局计划再逐步执行，结构清晰、易调试、适合步骤可预见的任务；ReAct 是边走边想——每步根据最新观察决定\"思考+行动\"，更灵活、适合不确定性高的环境，但轨迹可能更发散难复现。实务常结合：用 plan 提供整体骨架保证不漏步，用 ReAct 在每步内填细节、处理突发。简单说，计划是\"导航\"，ReAct 是\"边开边看路况\"。"
    },
    {
      "question": "计划太细怎么办？",
      "answer": "采用层级规划（hierarchical planning）：只展开当前子目标到可执行的粒度，远期的步骤保持粗粒度占位，执行到那一层再展开。这样既保留全局结构又不至于被过细计划绑架——环境一变，未展开的部分可以整体调整而不破坏已展开的执行。配合\"偏差超阈值才重规划\"，能把重规划频率控制在合理范围，避免抖动。"
    }
  ],
  "followUpAnswers": [
    "plan-then-act 先由 LLM 生成完整全局计划再逐步执行，结构清晰、易调试、适合步骤可预见的任务；ReAct 是边走边想——每步根据最新观察决定\"思考+行动\"，更灵活、适合不确定性高的环境，但轨迹可能更发散难复现。实务常结合：用 plan 提供整体骨架保证不漏步，用 ReAct 在每步内填细节、处理突发。简单说，计划是\"导航\"，ReAct 是\"边开边看路况\"。",
    "采用层级规划（hierarchical planning）：只展开当前子目标到可执行的粒度，远期的步骤保持粗粒度占位，执行到那一层再展开。这样既保留全局结构又不至于被过细计划绑架——环境一变，未展开的部分可以整体调整而不破坏已展开的执行。配合\"偏差超阈值才重规划\"，能把重规划频率控制在合理范围，避免抖动。"
  ],
  "pitfalls": [
    "计划写死不参与反馈：生成后从不根据观察调整，遇偏差即失败，变成\"睁眼瞎\"式执行。",
    "不记录已完成步骤导致重复劳动：重规划时忘了已做的事，又把已完成的子目标跑一遍，浪费且可能破坏已产出。",
    "重规划过频致抖动：每步都重想，轨迹来回横跳，永远到不了终点。"
  ],
  "beginnerSummary": "规划就像做菜前先列步骤：洗菜→切菜→下锅→装盘；做到一半发现没盐，就临时改计划\"先去楼下买盐再继续\"，而不是闷头炒到一半才发现缺料。它把\"写一份报告\"这样的大目标，拆成\"列提纲、查资料、写初稿、汇总\"几个小目标，每完成一个就记下来，下一步依赖前面已做的。好处是不会做到一半迷失方向、漏掉步骤；坏处是计划若写死不参与反馈，遇到变化就傻眼——所以好规划是\"有骨架、能临时改路\"，并且永远记得\"已经做完的不用重做\"。",
  "prerequisites": [
    "LLM 能产出结构化步骤（有序列表或依赖图），否则计划不可消费。",
    "能执行每个子任务并观察结果（有工具与反馈通道），否则计划无法落地验证。",
    "需机制记录进度与触发重规划（状态栈 + 偏差检测），否则遇变即崩。"
  ],
  "workedExample": [
    "把\"写报告\"拆成 4 个子任务（提纲/检索/撰写/汇总），done 记录避免重做。",
    "某步失败触发 1 次重规划：插入补数据环节，而非从头再来，省 3 个弯路。",
    "层级规划：先拆 3 大阶段，只展开当前阶段子步骤，避免计划过细僵化；10 步任务重规划 2 次、调用约 13 次。"
  ],
  "lineByLine": [
    "llm_make_plan(goal)：用 LLM 产出有序、带依赖的子任务列表，作为执行骨架。",
    "execute_step(step, tools)：逐条执行子任务，返回是否成功与观察 obs。",
    "done.append(step)：记录已完成步骤与观察，作为后续步骤的依赖与重规划的输入。",
    "if not ok: llm_replan(goal, done, obs)：失败则带上已完成列表与观察动态重规划，承接现状而非推倒。"
  ],
  "diagram": "Goal -> [Plan] -> step1 -> step2 -> (replan?) -> ... -> Done"
};

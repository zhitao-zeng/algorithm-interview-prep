export default {
  "id": "agent-state-machine",
  "kind": "concept",
  "category": "Agent Workflow",
  "difficulty": "Medium",
  "title": "任务调度与状态机",
  "prompt": "Agent 的任务调度与状态机是怎么设计的？",
  "code": "def step(sm, event):\n    nxt = sm.transitions.get((sm.state, event))\n    if nxt is None:\n        raise IllegalTransition(sm.state, event)\n    sm.state = nxt                            # 唯一确定态\n    persist(sm)                               # 落库可恢复\n    return dispatch(nxt, event)",
  "diagram": "IDLE->PLAN->ACT->OBSERVE->(RETRY?)->DONE/ERROR",
  "explanationFocus": "是什么：任务调度与状态机（FSM）是把 Agent 的一次运行抽象成一组『显式状态』（如 IDLE/PLANNING/ACTING/OBSERVING/REFLECTING/DONE/ERROR）加上它们之间的『迁移规则』，由调度器按事件驱动推进。它的价值在于：把原本不确定、不可控的 LLM 自发行为，约束进一个可控、可观测、可暂停、可恢复、可重试的执行框架里。",
  "quickAnswer": "把 Agent 运行抽象成有限状态机：状态如 IDLE→PLANNING→ACTING→OBSERVING→REFLECTING→DONE/ERROR，由调度器根据事件（用户消息、工具回调、超时）驱动状态迁移；状态落库持久化，使任务能暂停、恢复、重试。它把『LLM 不确定行为』装进『可控、可观测、可恢复』的壳里，是生产级 Agent 的骨架。",
  "beginnerSummary": "状态机像十字路口的红绿灯流程：一辆车的任务只能在『停/走/等』几种确定状态间按规矩变，绝不会同时既停又走。Agent 也一样——它当前要么是『在规划』、要么是『在调工具』、要么是『在等结果』，清清楚楚。万一服务器断电，我们记下了它卡在哪一盏灯（持久化状态），恢复供电后从那一灯接着走，而不是从头乱来。",
  "walkthrough": "以『下单』任务为例：IDLE（空闲）→PLANNING（LLM 生成执行计划，约 1 次调用、耗时 ~300ms）→ACTING（调用支付工具，网络往返 ~200ms）→OBSERVING（解析回执）→若回执成功则 DONE，若支付超时则迁移到 RETRY 边（最多 3 次）而非直接 DONE。整条链路共 5 个主状态、1 条重试回边；在 100 QPS 下平均每个任务经历 6~8 次迁移。",
  "approach": "设计分四步：①枚举任务阶段并定义状态枚举（别漏 ERROR/RETRY 这类异常态）；②写迁移表 (当前态, 事件)→下一态，集中管理逻辑；③事件驱动调度器消费用户/工具/超时事件推进状态；④状态变更后立即持久化，并给每个动作带幂等 id，支持断点续跑与重复事件去重。",
  "bruteForce": "用一个大 while 循环硬写流程：状态靠局部变量隐式表示，暂停要自己存一堆零散字段，工具超时没有统一的回边，出错只能整个重来。结果是『遇错难恢复、过程难观测、无法限流、无法水平扩展』，只适合玩具 Demo。",
  "invariant": "任一时刻 Agent 处于唯一确定状态（互斥、穷尽）；所有迁移必须幂等，重复收到同一事件不应导致重复执行副作用，以防重复扣款/重复发消息。",
  "complexity": "每次迁移查表 O(1)；真正的成本在『每个状态的模型/工具调用』（一次 LLM 调用 ~几百 ms、一次工具 ~几十~几百 ms）。状态机本身几乎零开销，主要价值是把这些开销变得可观测、可限流、可恢复。",
  "derivation": [
    "为什么需要：生产环境要求 Agent 可恢复（进程挂了能续）、可观测（知道卡在哪）、可限流（别打爆工具），而把流程写死在 while 循环里无法满足这些，必须用显式状态组织。",
    "怎么实现：定义状态枚举与迁移条件（迁移表）；调度器消费事件（用户/工具回调/超时）推进状态；把当前状态与上下文落库，使进程重启后能恢复；异常走 ERROR/RETRY 回边。",
    "有什么代价：状态设计需完备，迁移条件容易漏（比如漏了超时回边）；持久化增加存储与一次写库开销；事件驱动比线性代码更难调试。",
    "怎么评测：看状态覆盖度（异常态是否齐）、恢复成功率（重启后能否续上）、平均迁移步数、以及重复事件下的幂等性（重复触发不重复副作用）。"
  ],
  "edgeCases": [
    "进程在 ACTING 中途重启，需从持久化状态恢复并『补查』工具结果，而非重发动作造成重复副作用。",
    "同一工具回调因网络重传到达两次，需幂等：用动作 id 查重，已执行则忽略第二次。",
    "出现迁移表里没有的 (状态, 事件) 组合（非法迁移），必须拒绝并记录告警，而不是默默忽略。",
    "并发事件（用户取消 + 工具返回同时到）需定义优先级，避免状态机进入矛盾态。"
  ],
  "pitfalls": [
    "状态设计遗漏异常态（ERROR/RETRY/CANCEL），导致任务卡死在某个中间态永远结束不了。",
    "迁移非幂等（如支付动作重复执行），造成重复扣款、重复发消息等真实资损。",
    "持久化时机错：在状态变更前就写库，或忘了写，导致恢复时拿到旧状态。"
  ],
  "prerequisites": [
    "能枚举并定义任务的宏观阶段（规划/行动/观察/反思/完成/错误）。",
    "有可靠的持久化存储（如 Redis/DB）来保存状态与上下文。",
    "系统以事件/回调驱动（用户消息、工具回调、定时器超时）而非纯阻塞调用。",
    "理解幂等（idempotency）概念，才能给动作去重。"
  ],
  "workedExample": [
    "下单任务走 IDLE→PLAN→ACT→OBSERVE→DONE，含一条支付超时 RETRY 回边（最多 3 次）。",
    "进程在 ACTING 后崩溃，重启从库读出状态=ACTING，补查支付网关拿到回执，继续到 DONE，用户无感。",
    "支付回调因重传到达两次，第二次因动作 id 已存在被幂等层丢弃，未发生重复扣款。"
  ],
  "lineByLine": [
    "nxt = sm.transitions.get((sm.state, event))：用 (当前态, 事件) 查迁移表，得到下一状态。",
    "if nxt is None: raise IllegalTransition：遇到未定义组合直接拒绝，防止进入非法态。",
    "sm.state = nxt：把当前状态更新为唯一确定的下一态（互斥、穷尽）。",
    "persist(sm)：状态变更后立即落库，保证可恢复；再 dispatch(nxt, event) 分发处理。"
  ],
  "codeNotes": [
    "迁移表驱动让所有『状态怎么变』集中在一处，改流程只改表，可读可测。",
    "持久化必须放在状态变更之后，且最好带版本号防并发覆盖。",
    "dispatch 里执行副作用动作时要先查幂等 id，避免重复执行。"
  ],
  "followUps": [
    {
      "question": "状态机和 ReAct 冲突吗？",
      "answer": "不冲突，二者是不同层级。状态机管『宏观阶段』（规划→行动→观察→完成），ReAct 管『ACTING 这个状态内部的思考-行动-观察小循环』。可以把它俩叠起来：外层用 FSM 保证可控可恢复，内层在 ACTING 里跑 ReAct 做多步工具调用。很多生产 Agent 正是『FSM 外壳 + ReAct 内核』。"
    },
    {
      "question": "如何保证迁移幂等？",
      "answer": "给每个会产生副作用的动作分配全局唯一 id（如 action_id=任务id+步骤号），执行前先查去重表：已存在就跳过、不存在才执行并登记；对无法天然幂等的操作（支付、发消息）走事务或加补偿（如先预扣再确认）。重复事件到达时靠这个 id 兜住，绝不二次执行。"
    }
  ],
  "followUpAnswers": [
    "不冲突，二者是不同层级。状态机管『宏观阶段』（规划→行动→观察→完成），ReAct 管『ACTING 这个状态内部的思考-行动-观察小循环』。可以把它俩叠起来：外层用 FSM 保证可控可恢复，内层在 ACTING 里跑 ReAct 做多步工具调用。很多生产 Agent 正是『FSM 外壳 + ReAct 内核』。",
    "给每个会产生副作用的动作分配全局唯一 id（如 action_id=任务id+步骤号），执行前先查去重表：已存在就跳过、不存在才执行并登记；对无法天然幂等的操作（支付、发消息）走事务或加补偿（如先预扣再确认）。重复事件到达时靠这个 id 兜住，绝不二次执行。"
  ]
};

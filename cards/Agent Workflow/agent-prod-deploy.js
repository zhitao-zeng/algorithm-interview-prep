export default {
  "kind": "concept",
  "id": "agent-prod-deploy",
  "category": "Agent Workflow",
  "difficulty": "Hard",
  "title": "Agent 在生产部署的挑战",
  "prompt": "Agent 在生产环境部署面临哪些主要挑战？",
  "quickAnswer": "Agent 生产部署要把「演示可用」变成「稳定、安全、可控、可观测」的体系，核心挑战包括五块：①低延迟与高并发（限流、超时、排队）；②工具与依赖的可用性、限流与熔断降级；③成本与预算治理（防止单用户刷爆）；④安全边界与注入防护；⑤可观测、回放与回滚。本质上是从「能跑」跃迁到「可 SLA 化运营」——任何外部依赖（模型/检索/工具）不可用都必须有降级路径，不能让单点故障拖垮整条链。此外还要有人工程序（human-in-the-loop）作为最后兜底，以及灰度发布与快速回滚能力。优先级建议是「先稳定→再安全→再可观测→再成本→再降级→再运维」逐层补齐，而不是一次到位。",
  "approach": "按风险与收益优先级逐层推进，而不是一次性重构：①先稳定——加队列、限流、超时与重试退避，扛住基本流量；②再安全——沙箱化工具、最小权限授权、注入防护；③再可观测——全链路 trace、指标（QPS/延迟/错误率）与结构化日志；④再成本——预算护栏与用量告警，防单用户刷爆；⑤再降级——熔断、缓存与人工兜底，保证单点故障不致命；⑥再运维——灰度发布与自动回滚。每一层都建议带可量化指标（如 P99 延迟、错误率阈值），用数据判断是否进入下一层。",
  "explanationFocus": "是什么：Agent 生产部署挑战，是把研究原型转化为具备「稳定性、安全性、可观测性、成本可控」的运营系统所面临的一整套工程问题。它远超「能跑通 demo」，必须应对真实用户、SLA 承诺、合规要求与开放攻击面，是体系化工程而非单点优化。这一阶段的核心矛盾是：研究原型假设「网络永远稳、输入永远善、预算永远够」，而生产环境这些假设全部不成立。因此部署挑战横跨限流、降级、安全、可观测、成本治理、版本与回滚等多个维度，任何一环缺失都会让「演示可用」变成「线上事故」。",
  "bruteForce": "最危险的『暴力』做法是把研究 demo 直接暴露到公网：没有限流，并发一高请求就互相争抢、服务直接崩溃；没有成本护栏，一个恶意用户循环调用就能把额度刷爆；没有 trace，一旦出事完全无迹可查、无法回放定位；没有降级，任何一个上游工具（检索/模型）短暂故障就会让整条 Agent 链路雪崩。这种『能跑就行』的上线方式，在真实流量与攻击面前几乎必然翻车。",
  "derivation": [
    "为什么需要：生产环境有真实用户、明确的 SLA 承诺、合规要求与开放攻击面，而研究 demo 的全部假设（网络稳、输入善、预算无限）在这里全部不成立。若直接把 demo 暴露公网，一次限流、一次工具故障、一次恶意注入就可能酿成全面事故，因此必须把『能跑』升级为『可运营』。",
    "怎么实现：在入口加队列/限流/超时与指数退避；对工具做熔断与降级（如检索挂了就走缓存）；用沙箱与最小权限隔离副作用；建立 trace、指标与日志三件套实现可观测；加预算护栏与用量告警治理成本；用灰度发布与自动回滚保障运维；对高危动作保留 human-in-the-loop 兜底。",
    "有什么代价：引入上述每一层都会抬高架构与运维成本，需要额外的基础设施与人力。同时过度防护会损害体验、增加端到端延迟（每层加几十到几百毫秒），必须在「安全/稳定」与「体验/成本」之间做平衡，而不是无脑加锁。",
    "怎么评测：不能只看「能不能跑通」，而要看运营指标——可用性（如 99.5%）、P99 延迟、成本/请求、事故恢复时间（MTTR）、以及注入攻击拦截率。还应做故障演练（chaos）：主动关掉某个上游依赖，验证降级是否真的生效、错误率是否可控。"
  ],
  "invariant": "生产部署必须满足一条硬不变量：任何外部依赖（大模型、检索、第三方工具）不可用时，Agent 都必须有对应的降级路径，绝不能让单点故障拖垮整个系统。同时预算护栏必须是『硬限制』——它不依赖模型或用户的自觉，而是在基础设施层强制生效，确保单个用户或一次异常循环都无法把成本刷爆。此外，任何会改变外部状态的高危动作（退款/发消息/写数据库）都应默认走人工确认，除非有充分的安全论证。",
  "walkthrough": "以一个上线到公网的客服 Agent 为例，峰值并发约 100 路会话、入口限流 100 QPS。某次上游检索服务超时 800ms，熔断器打开，Agent 自动降级到本地缓存的常见问题答案，避免了整条链路因等待检索而雪崩；涉及退款/支付的高危动作一律走人工确认（human-in-the-loop），不直接放行。全链路 trace 记录每一步耗时与工具调用，事后复盘能精确到『哪一步慢』。首月运营数据：可用性 99.5%，平均处理成本 ¥0.03/请求，P99 延迟 2.3s。一次上游模型限流事故中，排队+指数退避机制让错误率仅短时升到 1.2% 即在 30 秒内恢复，没有演变成全面故障。这说明『降级路径 + 限流 + 可观测』三者组合，是把『能跑』变成『能运营』的关键。",
  "edgeCases": [
    "上游模型限流：需排队与指数退避，否则瞬间重试会雪崩——实测在 30 秒内把错误率从 1.2% 压回正常。",
    "工具大面积故障：检索/数据库挂掉时需熔断降级到缓存或模板答案，而不是硬失败把用户请求全拒。",
    "突发流量：需弹性扩容+入口限流双保险，既保护后端不被冲垮，也保护成本不被瞬时刷爆。",
    "注入攻击：恶意用户输入诱导 Agent 调用危险工具或泄露机密，需沙箱、白名单与输出护栏联防。"
  ],
  "code": "def serve(agent, req, limiter, breaker):\n    limiter.acquire()                         # 限流\n    if breaker.open(\"retriever\"):\n        return agent.run(req, use_cache=True) # 降级\n    return agent.run(req)",
  "codeNotes": [
    "依赖故障必须有降级分支（breaker.open 时走 use_cache），绝不能让单点故障变成整链硬失败。",
    "入口 limiter.acquire() 同时保护后端稳定性与成本，防止单个用户用循环调用刷爆额度。",
    "真实代码还会在 agent.run 外加超时与 try/except，超时就返回降级结果而非无限等待。"
  ],
  "complexity": "部署成本主要来自副本数、消息队列与可观测设施，与流量规模成正比，是 O(流量) 的运营开销。架构与运维的复杂度显著更高：一次用户请求往往跨 5~10 个外部调用（模型推理、检索、工具、数据库），端到端延迟是各段之和，因此必须给每一段单独设超时与重试上限，否则一个慢调用会拖垮整条链路。引入限流、熔断、trace 等中间件后，单请求增加几十到几百毫秒的薄开销，换来的是可预测的 SLA 与可定位的事故。",
  "followUps": [
    {
      "question": "和部署普通 API 有何不同？",
      "answer": "最大区别是 Agent 依赖多个外部系统（大模型、检索、各类工具）且行为非确定——同一个输入可能因工具返回不同、或模型采样不同，走完全不同的路径。因此它需要远强于普通 API 的降级策略、预算护栏与可观测能力，而不是单纯扩副本。普通 API 逻辑固定、结果可缓存、副作用可控；Agent 每步都可能调用外部、产生真实副作用（发消息、改数据库、退款），运维面与故障域都大得多，部署时要为『每一步都可能失败』做设计。"
    },
    {
      "question": "如何做灰度发布？",
      "answer": "按流量比例或用户分桶，把新版本与稳定版并行运行，对比成功率、成本/请求、P99 延迟与人工介入率等核心指标；设定自动回滚阈值（例如错误率>2% 且持续 5 分钟立即回滚到稳定版）。对支付、退款等关键动作，建议在灰度阶段仍强制人工确认，验证无误、指标平稳后再逐步放开。灰度期间保留完整 trace，便于对比新旧版本在真实流量下的表现差异。"
    }
  ],
  "followUpAnswers": [
    "最大区别是 Agent 依赖多个外部系统（大模型、检索、各类工具）且行为非确定——同一个输入可能因工具返回不同、或模型采样不同，走完全不同的路径。因此它需要远强于普通 API 的降级策略、预算护栏与可观测能力，而不是单纯扩副本。普通 API 逻辑固定、结果可缓存、副作用可控；Agent 每步都可能调用外部、产生真实副作用（发消息、改数据库、退款），运维面与故障域都大得多，部署时要为『每一步都可能失败』做设计。",
    "按流量比例或用户分桶，把新版本与稳定版并行运行，对比成功率、成本/请求、P99 延迟与人工介入率等核心指标；设定自动回滚阈值（例如错误率>2% 且持续 5 分钟立即回滚到稳定版）。对支付、退款等关键动作，建议在灰度阶段仍强制人工确认，验证无误、指标平稳后再逐步放开。灰度期间保留完整 trace，便于对比新旧版本在真实流量下的表现差异。"
  ],
  "pitfalls": [
    "无降级导致单点故障全崩：一个检索服务挂掉，整个 Agent 直接不可用，所有用户请求失败。",
    "无预算护栏成本爆表：单个用户用循环把 API 额度刷爆，单日成本暴涨百倍。",
    "无 trace 难定位：出事时无法回放『哪一步慢/错』，平均修复时间（MTTR）从分钟级拖到小时级。"
  ],
  "beginnerSummary": "上线一个 Agent 就像开一家餐厅：光菜能做出来（能跑通 demo）远远不够，你还得有排队叫号（并发限流）、备胎食材（依赖降级）、监控摄像头（可观测）、防偷防砸的安保（安全护栏）、以及一本清楚的账（成本治理）。缺了任何一样都开不长久——今天被挤爆、明天被投毒、后天账算不清。而且餐厅真开门后，来的客人不会都善良、网络不会总顺畅、预算也不是无限的，这些『现实假设』正是 demo 阶段被忽略、生产阶段必须补齐的部分。",
  "prerequisites": [
    "稳定的工具与依赖：工具本身可用、有自身 SLA，否则再多降级也兜不住连续故障。",
    "可观测三件套：trace/metrics/log 已就绪，否则出了事只能瞎猜。",
    "预算与降级策略：明确了成本上限与每类故障的 fallback 路径，才能谈生产部署。"
  ],
  "workedExample": [
    "限流 100 QPS + 检索超时 800ms 自动降级到缓存，故障期错误率仅短时升到 1.2% 即恢复。",
    "支付/退款动作走人工确认（human-in-the-loop），避免 Agent 自主误操作造成资金损失。",
    "全链路 trace 把一次 2.3s 的慢请求定位到『检索耗时 1.8s』，针对性优化后降到 0.9s。"
  ],
  "lineByLine": [
    "limiter.acquire()：入口限流，控制 QPS 并保护后端与成本，是生产第一道防线。",
    "breaker.open('retriever')：检查检索服务的熔断器是否打开，判断依赖是否健康。",
    "return agent.run(req, use_cache=True)：依赖不健康时走降级分支，用缓存答案兜底而非硬失败。",
    "正常路径 return agent.run(req)：依赖健康时正常执行，但生产里它外面通常还包着超时与重试。"
  ],
  "diagram": "User -> Limiter -> Agent -> (Breaker/Degrade) -> Tools"
};

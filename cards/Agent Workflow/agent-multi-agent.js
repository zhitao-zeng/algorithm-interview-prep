export default {
  "kind": "concept",
  "id": "agent-multi-agent",
  "category": "Agent Workflow",
  "difficulty": "Hard",
  "title": "多 Agent 协作（角色分工）",
  "prompt": "多 Agent 协作（角色分工）是怎么工作的？",
  "quickAnswer": "多 Agent 把复杂任务拆给专长不同的角色（如规划者/执行者/审查者），通过共享黑板或消息总线通信，各自维护子目标并向下游交接产物。它用\"分工+协作\"替代单体 Agent 的万能推理，特别适合跨领域、需要多重把关或需要并行的任务。其价值在于把不可控的\"黑箱长推理\"变成可观测、可审计、可并行的一组短链路；核心难点则在于清晰的接口协议、冲突仲裁与成本控制。落地时务必记住：并非所有任务都值得多 Agent，简单任务上多 Agent 只会徒增通信与 token 成本，应先以单 Agent 基线对照再决定是否拆分。",
  "approach": "角色定义（明确每个 Agent 的职责、输入/输出 schema、可调工具）→ 任务分派（orchestrator 或黑板把子目标派给合适角色）→ 协作执行（并行或串行推进，产物写入共享区）→ 汇总仲裁（critic/merger 对冲突产物裁决合并）→ 质量闭环（必要时回退重做）。关键设计点是把\"谁产出什么、长什么样\"用强 schema 固定下来，避免口头约定带来的歧义与甩锅。",
  "explanationFocus": "是什么：多 Agent 协作是让多个职责不同、专长各异的 Agent 在统一协议下分工、通信、交接中间产物，从而协同完成单一 Agent 难以胜任的复杂任务的系统架构。它把\"一个全能模型包揽一切\"拆成\"若干专才各管一段\"，本质是用组织分工换取深度、广度与可控性；典型形态包括编排者(orchestrator)调度多个工人 Agent，或基于黑板(blackboard)的松散协作，二者都强调角色边界清晰、接口强约束与产物可追溯。",
  "bruteForce": "单 Agent 包揽全部：把所有上下文、工具、约束都塞进一个超长 prompt，让模型一个人又查又写又审。结果是上下文混杂、注意力分散、易顾此失彼，且无法并行、难以追责；一旦中途出错，整条链路要重来，调试时也很难定位是哪一段出了问题。当任务超过单轮能力边界时，这种\"全能单体\"会迅速退化成不可控的长推理。",
  "derivation": [
    "为什么需要：真实任务常跨多个领域（检索、计算、写作、审查），还要并行与多重校验，单体 Agent 在深度与广度上难以兼顾——它要么上下文过长导致质量下降，要么一次推理过长导致不可控。把任务按能力边界拆给专才，可让每个 Agent 只做自己最擅长的短链路，整体更稳更可控。此外多 Agent 还能天然并行，缩短端到端时延，并在某一环节失败时只回退局部而非整条链路。",
    "怎么实现：先为每个角色写清 prompt 与接口（输入/输出用 JSON schema 约束，例如 Researcher 输出 {sources:[], summary}）；再用 orchestrator 模式（中心调度依次/并行调用）或黑板模式（角色订阅主题、读写共享黑板）分发任务；各角色产物经过 critic/merger 做冲突检测与合并。工程上常用消息队列或内存黑板承载通信，并给每个产物打角色标签，确保下游能按 role+version 精确拉取依赖。",
    "有什么代价：通信开销显著增大（N 个角色间需要约 N×(N-1)/2 量级的交接与格式转换）；角色间易冲突、重复劳动或互相甩锅，需要仲裁与去重；调试困难（错误可能来自任一环节或它们的组合），且 token 与调用成本随角色数倍增。若缺乏可观测性（trace/日志），一次失败的任务定位会非常痛苦，迭代协议也会很慢。",
    "怎么评测：不只看最终产物质量，还要看角色利用率（是否有人闲置或过载）、通信轮数（是否冗余广播）、冲突率（仲裁触发次数）与端到端成本；常用对照实验——同一任务分别用单 Agent 与多 Agent 跑，比较质量、时延与花费，再决定是否值得上多 Agent。上线后建议用 A/B 持续监控：若多 Agent 版本在质量上无显著差异却贵数倍，应果断回退或减角色。"
  ],
  "invariant": "任一 Agent 的输出必须带角色标识(role)、版本(version)与时间戳，并说明其依赖的上游产物 id，便于下游追溯、仲裁与缓存；同一子目标被多角色处理时，最终采用哪一份必须由仲裁者显式记录理由，禁止静默覆盖。",
  "walkthrough": "以\"做一次行业研究并产出报告\"为例：Planner 先把任务拆成 3 个子目标（竞品扫描、数据整理、结论撰写），把任务挂到黑板上；Researcher 订阅\"竞品扫描\"主题，检索 8 篇资料后把摘要回贴黑板；Writer 订阅\"数据整理/竞品扫描\"后起草约 1200 字初稿；Reviewer 订阅\"初稿\"做事实与一致性审查，提出 3 处修改；Merger 汇总为最终报告。整轮共 3 个工人 Agent + 1 个协调者，发生 4 次产物交接，端到端约 40 个 LLM 调用、合计约 90k tokens；相比单 Agent 一次写到底，角色间可并行（Researcher 与 Writer 部分重叠），且每一版都有角色标识便于追责。在并发 100 个此类任务的压测下，多 Agent 流水线把平均时延控制在 p50=6s、p99=14s，而单 Agent 基线 p99 高达 28s 且更易超时。",
  "edgeCases": [
    "两 Agent 结论冲突：例如 Researcher 说市场增长、Writer 引用了收缩数据，需要仲裁者按来源可信度/新鲜度裁决，并保留双方依据供回溯，而不是简单取后者覆盖前者。",
    "某角色卡死或超时（如检索 API 返回 502）：需设超时与替补策略（换备用工具或降级到缓存结果），否则整条链路阻塞，p99 延迟会被单点拖垮。",
    "消息环路：A 改了产物触发 B 重算，B 又触发 A，需用版本号/幂等键防止无限广播，并设最大迭代轮次（如 5 轮）强制收敛。",
    "某角色输出空或格式非法：需做 schema 校验，失败则重试或降级，绝不能让脏数据流入下游导致后续角色连环崩溃。"
  ],
  "code": "def multi_agent(roles, task):\n    bus = Blackboard()\n    bus.post(\"task\", task)\n    for role in roles:\n        msg = bus.subscribe(role.in_topics)\n        out = role.act(msg)                    # 角色专长出力\n        bus.post(role.name, out)               # 交接产物\n    return arbitrate(bus)",
  "codeNotes": [
    "黑板(blackboard)模式让角色之间只依赖共享区而非彼此直连，显著降低耦合、便于替换或并行某个角色，也方便后续加新角色而不动旧代码。",
    "仲裁逻辑(arbitrate)必须可解释：记录每次采纳/否决的依据（来源、版本、打分），否则出问题时无法追责，也难以迭代协作协议。",
    "生产环境应给 post/subscribe 加超时、重试与 schema 校验，防止脏数据或卡死在角色间扩散，并把每次交接写入 trace 便于复盘。"
  ],
  "complexity": "≈ 角色数×单角色开销 + 通信轮数×单次交接成本；并行可把端到端时延从 O(Σ角色) 降到接近 O(关键路径长度)，但协调复杂度与失败面随角色数上升。工程上总成本近似正比于 LLM 调用次数与 token 数：一个 4 角色流水线可能用到 40~100 次调用、数十 k tokens，远高于单 Agent 的 1 次长调用，因此要把\"角色数/交接轮数\"当作核心成本指标监控。",
  "followUps": [
    {
      "question": "何时该用多 Agent 而非单 Agent？",
      "answer": "当任务跨多个领域、需要并行提速、或需要强把关（生成与审查必须分离，如金融/医疗内容）时，多 Agent 收益明显；它还能让每个环节可观测、可审计。反之，简单问答、单轮写作等任务上多 Agent 只会徒增通信与 token 成本，反而更慢更贵。经验法则：先写单 Agent 基线，只有当你发现单 Agent 上下文过长、质量不稳或明显可并行时，再按能力边界拆角色，并用 A/B 验证质量/成本确实改善才保留。"
    },
    {
      "question": "多 Agent 最大的风险是什么，怎么控？",
      "answer": "最大风险是通信混乱与责任不清——角色间冲突、重复、甩锅，最终产物自相矛盾且无法定位问题。控制手段有三：一是用强 schema 固化每个角色的输入输出，减少接口歧义；二是引入可解释的仲裁者并保留每次决策依据；三是加全链路可观测（trace/日志），出问题时能迅速定位到具体角色。成本失控是第二风险，需用角色利用率与通信轮数监控，避免为小任务上过多角色。"
    },
    {
      "question": "Orchestrator 和 Blackboard 两种模式怎么选？",
      "answer": "Orchestrator（中心调度）适合流程固定、依赖清晰的线性/树状任务，控制力强、易调试，但协调者可能成为瓶颈与单点。Blackboard（黑板/订阅）适合角色多、交互松散、需要灵活组合的探索型任务，扩展性好但更难保证收敛。实务中常混合：用 orchestrator 管主干流程，用黑板承载某些角色的并行产物，兼顾可控性与灵活性。"
    }
  ],
  "followUpAnswers": [
    "当任务跨多个领域、需要并行提速、或需要强把关（生成与审查必须分离，如金融/医疗内容）时，多 Agent 收益明显；它还能让每个环节可观测、可审计。反之，简单问答、单轮写作等任务上多 Agent 只会徒增通信与 token 成本，反而更慢更贵。经验法则：先写单 Agent 基线，只有当你发现单 Agent 上下文过长、质量不稳或明显可并行时，再按能力边界拆角色，并用 A/B 验证质量/成本确实改善才保留。",
    "最大风险是通信混乱与责任不清——角色间冲突、重复、甩锅，最终产物自相矛盾且无法定位问题。控制手段有三：一是用强 schema 固化每个角色的输入输出，减少接口歧义；二是引入可解释的仲裁者并保留每次决策依据；三是加全链路可观测（trace/日志），出问题时能迅速定位到具体角色。成本失控是第二风险，需用角色利用率与通信轮数监控，避免为小任务上过多角色。",
    "Orchestrator（中心调度）适合流程固定、依赖清晰的线性/树状任务，控制力强、易调试，但协调者可能成为瓶颈与单点。Blackboard（黑板/订阅）适合角色多、交互松散、需要灵活组合的探索型任务，扩展性好但更难保证收敛。实务中常混合：用 orchestrator 管主干流程，用黑板承载某些角色的并行产物，兼顾可控性与灵活性。"
  ],
  "pitfalls": [
    "角色职责重叠导致重复劳动：例如 Researcher 与 Analyst 都去检索，浪费 token 且结论不一致，应在定义阶段就用接口边界划清，谁负责\"采集\"谁负责\"加工\"要写死。",
    "无仲裁机制致结论互相矛盾：多个角色各写一段、互不校验，最终报告自相矛盾；必须有一个明确的合并/审查角色，且其决策依据要留痕。",
    "过度拆分导致通信成本爆炸：把本可一步完成的任务拆成 6、7 个角色，收益不足以覆盖开销，端到端反而更慢更贵。"
  ],
  "beginnerSummary": "多 Agent 就像一个有分工的项目组：有人专门查资料（研究员）、有人专门写稿（写手）、有人专门挑错（审稿人），大家不抢着干同一件事，而是把各自的成果放到一块公共白板(黑板)上互相接力，最后由组长(仲裁者)把几份成果拼成最终交付。比\"一个人又查又写又审\"专业得多，也更容易并行、追责。代价是大家得约好\"白板上写什么格式、谁接谁的活\"，否则就会互相看不懂、甚至写出互相矛盾的内容；真出了冲突，组长得按依据拍板，不能和稀泥。",
  "prerequisites": [
    "能清晰定义角色边界与输入/输出 schema，否则分工会变成互相甩锅或重复劳动，接口歧义会在下游放大成bug。",
    "有可靠的通信/黑板机制与消息订阅能力，保证产物能可靠交接、不丢失且不串味。",
    "具备仲裁与冲突解决策略（可信度/新鲜度打分或人工兜底），否则多份产物无法收敛成最终答案。",
    "有全链路可观测性（日志、trace、token 计数），来定位是哪个角色出了问题，否则多 Agent 调试如黑盒。"
  ],
  "workedExample": [
    "行业研究：Planner 拆 3 子目标，Researcher 检索 8 篇资料产出摘要，Writer 起草 1200 字，Reviewer 提 3 处修改，Merger 汇总；共 4 次交接、约 90k tokens，p99 从 28s 降到 14s。",
    "客服工单：Classifier 分类（退款/咨询/投诉）→ ToolAgent 查订单 → Writer 拟回复 → Reviewer 做合规审查；并发 100 张工单时，多 Agent 流水线把平均处理时延从 12s 降到 5s。",
    "代码生成：Planner 定方案 → Coder 写实现 → Tester 写用例执行 → Reviewer 审查，发现 2 个边界 bug 被回退修复，避免带病合并。"
  ],
  "lineByLine": [
    "bus = Blackboard()：初始化一块共享黑板，承载所有角色间的产物交接，解耦发送方与接收方，使新增角色无需改动旧角色代码。",
    "bus.post('task', task)：协调者把总任务挂到黑板，作为后续所有角色的起点与统一上下文来源，下游都从这块黑板取依赖。",
    "for role in roles：遍历每个专长角色，按订阅主题依次或并行激活，保证每个子目标都有对口处理者，避免有人闲置或遗漏。",
    "msg = bus.subscribe(role.in_topics)：角色只拉取自己关心的上游产物，按 role+version 精确匹配依赖，避免被无关信息干扰注意力。",
    "out = role.act(msg)：角色用专长出力，产出本环节结果（如检索摘要、初稿），并自动带上自己的 role 与版本标签。",
    "bus.post(role.name, out)：把带角色标签的产物回贴黑板，供下游订阅；最后 arbitrate(bus) 汇总仲裁成最终产出，并留存每次采纳/否决理由。"
  ],
  "diagram": "Planner -> Researcher\nPlanner -> Writer -> Reviewer -> Merger"
};

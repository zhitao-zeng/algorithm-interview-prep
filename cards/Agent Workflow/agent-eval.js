export default {
  "kind": "concept",
  "id": "agent-eval",
  "category": "Agent Workflow",
  "difficulty": "Hard",
  "title": "Agent 的评测",
  "prompt": "怎么评测一个 Agent（正确性 / 效率 / 成本）？",
  "quickAnswer": "Agent 评测是多维的：正确性看任务成功率与轨迹质量(是否走合理路径)，效率看步数/延迟，成本看 token 与工具调用费用；还需评「鲁棒性」(扰动下是否仍对)与「安全性」。常用 held-out 基准 + LLM-as-judge 评轨迹，配合离线回放与在线 A/B。难点在于任务多样、路径非唯一、需过程与结果双评，以及对齐评测口径使结论可复现。",
  "approach": "定指标（成功率/步数/token/鲁棒性）→ 构造基准 → 跑轨迹(工具 Mock 可复现) → 结果+过程双评 → 成本归因与版本对比。",
  "explanationFocus": "是什么：Agent 评测是用正确性、效率、成本、鲁棒性等多维指标，对 Agent 的最终结果与中间轨迹进行量化衡量的体系。它不只看「最终答案对不对」，还要看「走了几步、花了多少 token、绕没绕路、换种问法还答对不」。因为 Agent 行为路径开放、成本高，单看结果无法指导优化，必须过程与结果双评。",
  "bruteForce": "只看最终答案对错：忽略走了 50 步绕路、烧了天价 token、或工具调用了危险操作，结论严重失真。",
  "derivation": [
    "为什么需要：Agent 行为路径开放、工具调用成本高，单看结果既无法定位问题（是规划差还是工具差），也无法在多个版本间做公平对比，必须量化多维指标。",
    "怎么实现：建任务基准；用 Mock 工具记录完整轨迹（含每步动作、观测、token）；用规则 + LLM-judge 评正确性；统计步数/延迟/token 做效率与成本归因；做扰动与对抗测试鲁棒性。",
    "有什么代价：构造高质量、覆盖边缘情形的基准贵；LLM-judge 有位置偏见与评分漂移，需校准；过程评标注成本高，且 judge 自身也消耗 token。",
    "怎么评测：看指标稳定性（多次运行方差小）、与人工评分的相关性（≥0.7 较可信）、以及能否稳定区分好坏 Agent（好版本指标显著更优）。"
  ],
  "invariant": "同一任务多次运行应可复现统计结论，评测需固定随机种子、固定 prompt 模板、并对外部工具做 Mock，避免工具非幂等导致不可复现。",
  "walkthrough": "评 100 个任务、每任务跑 3 次取平均：v1 成功率 82%、平均 6.3 步、平均 4.1k token、扰动后掉到 70%；v2 把规划拆更细，平均步数降到 5.0（降 20%）、成功率持平 82%、token 略升到 4.3k；综合看 v2 效率更优，且在并发 100 的压测下 p99 延迟从 12s 降到 9s，于是采纳 v2。",
  "edgeCases": [
    "任务无唯一正确答案（如「写一封邮件」）：需 LLM-judge 语义评判，而非精确字符串匹配。",
    "工具非幂等（如发短信）导致重复运行结果不同，必须用 Mock 或沙箱固化。",
    "成本指标被缓存/复用掩盖：真实线上每次都重算，评测里的命中会低估真实成本。",
    "长轨迹截断：超步数上限的任务被强制终止，成功率被低估，需区分「真失败」与「超预算」。"
  ],
  "code": "def evaluate(agent, bench, runs=3):\n    stats = {\"succ\":0,\"steps\":0,\"tokens\":0}\n    for case in bench:\n        for _ in range(runs):\n            traj = agent.run(case, mock_tools=True)   # 工具 Mock 可复现\n            stats[\"succ\"]  += judge_correct(traj)\n            stats[\"steps\"] += len(traj.actions)\n            stats[\"tokens\"]+= traj.tokens\n    return normalize(stats)",
  "codeNotes": [
    "工具用 Mock（mock_tools=True）保证可复现，且能安全触发危险操作路径而不真执行。",
    "过程(每步动作/观测)与结果(最终答案)都要记录，否则无法做过程质量评测与成本归因。"
  ],
  "complexity": "评测总成本 = 任务数 × 运行次数 × 单次开销（含 LLM 推理 token 与工具调用）；100 任务 × 3 次 × 约 4k token ≈ 1.2M token 单次评测，需控制规模并缓存复用。",
  "followUps": [
    {
      "question": "为什么 Agent 评测需要 LLM-as-judge，规则匹配不够吗？",
      "answer": "Agent 路径开放、答案非唯一（如写邮件、做规划），规则精确匹配覆盖面太小；LLM-judge 能做语义级评判，给轨迹合理性与结果质量打分。但要注意校准：用人工标注集检查 judge 与人工一致性（目标 ≥0.7），并固定 judge 模型与提示，避免评分漂移；也可对关键指标保留规则校验做交叉验证。"
    },
    {
      "question": "效率和成本冲突时怎么权衡，部署怎么选？",
      "answer": "画成功率-成本帕累托前沿，选「同等成功率下成本最低」或「同等成本下成功率最高」的点；对高价值任务（如关键决策）放宽预算换成功率，对高频低价值任务压成本。线上用 A/B 验证评测结论，监控真实 p99 延迟与单价是否与评测一致。"
    }
  ],
  "followUpAnswers": [
    "Agent 路径开放、答案非唯一（如写邮件、做规划），规则精确匹配覆盖面太小；LLM-judge 能做语义级评判，给轨迹合理性与结果质量打分。但要注意校准：用人工标注集检查 judge 与人工一致性（目标 ≥0.7），并固定 judge 模型与提示，避免评分漂移；也可对关键指标保留规则校验做交叉验证。",
    "画成功率-成本帕累托前沿，选「同等成功率下成本最低」或「同等成本下成功率最高」的点；对高价值任务（如关键决策）放宽预算换成功率，对高频低价值任务压成本。线上用 A/B 验证评测结论，监控真实 p99 延迟与单价是否与评测一致。"
  ],
  "pitfalls": [
    "只评结果忽略过程成本：一个答对但绕了 50 步烧光 token 的 Agent 其实不可用。",
    "评测不可复现致结论漂移：seed/工具未固定，两次跑出不同结论，无法判断版本优劣。"
  ],
  "beginnerSummary": "评 Agent 像考员工不只看交付物：还看他用了几步、花了多少钱、绕没绕路、换种问法还答对不。综合打分才知道谁真「又好又省」。只看最终答案就像只看交没交卷，却不管他抄了多久、花了多少资源。",
  "prerequisites": [
    "有任务基准与判据：知道什么算「对」，最好有可程序化校验的 oracle。",
    "能记录完整轨迹：包括动作、工具调用、观测、token 数，便于事后分析。",
    "工具可 Mock 复现：外部依赖（搜索/数据库）需可重放，保证多次运行可比。"
  ],
  "workedExample": [
    "100 任务跑 3 次取统计：成功率 82%±3%、平均步数 6.3、平均 4.1k token，作为基线。",
    "对比 v1/v2：v2 步数降 20% 且成功率持平，在并发 100 压测下 p99 延迟 9s < v1 的 12s，故采纳 v2。"
  ],
  "lineByLine": [
    "用 Mock 工具跑基准：agent.run(case, mock_tools=True) 在受控环境重放，保证可复现。",
    "记录轨迹与 token：每步动作长度、工具返回值、累计 traj.tokens 都存入 stats。",
    "judge 评正确性与过程：judge_correct 用规则或 LLM 判定最终与中间质量，累加 succ。",
    "归一化出多维指标：把累加值除以 case×runs 得到成功率/平均步数/平均 token。"
  ],
  "diagram": "Bench -> Agent(run) -> Trajectory -> Judges -> Metrics"
};

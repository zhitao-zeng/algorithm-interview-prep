export default {
  "kind": "concept",
  "id": "perf-why-eval",
  "category": "服务性能评测",
  "difficulty": "Easy",
  "title": "为什么需要做 LLM 服务性能评测",
  "prompt": "上线一个大模型服务前，为什么必须先做性能评测？",
  "quickAnswer": "性能评测把「模型能跑」变成「能稳定、便宜、按时地服务用户」。它量化延迟、吞吐、显存与成本，帮助选型、定容量、设 SLA 并发现瓶颈，避免线上卡顿或预算失控。没有评测就上线，等于凭体感赌运气，一旦流量上来要么延迟爆炸要么账单爆炸。",
  "approach": "用统一、贴近真实的负载把主观「快不快」转成可比较数字：① 明确要测的指标（延迟分位、吞吐、显存、成本）；② 构造与线上同分布的输入长度/输出长度/并发；③ 运行压测逐请求采集；④ 统计 p50/p95/p99 与达标率，横向对比不同框架/批大小/卡型的性价比，驱动架构与资源决策。",
  "explanationFocus": "是什么：LLM 服务性能评测是用可重复、可控的负载去测量延迟（TTFT/TPOT）、吞吐（TPS）、显存占用与单位成本等指标，并把结果用于模型/框架选型、容量规划、SLA 制定和瓶颈定位的过程。它把主观的「快不快」变成可比较、可复现的数字，是「能跑」到「能稳定服务用户」的必经环节。",
  "bruteForce": "直接上线靠用户体感发现问题：等线上卡顿、OOM 或账单超支后才排查，此时已影响真实用户且问题难以复现（线上流量不可重放），定位成本极高。",
  "derivation": [
    "为什么需要：大模型推理贵且慢，服务化之后「能出结果」≠「能服务用户」——还要满足延迟 SLA、并发吞吐与成本约束。没有量化就无法选型、无法定容量、无法发现瓶颈，上线即赌博。",
    "怎么实现：固定输入/输出长度分布与并发档位，跑压测；采集 TTFT（首 token 延迟）、TPOT（每输出 token 延迟）、TPS（吞吐）、GPU 利用率、显存峰值与单请求成本。常用工具如 vLLM 的 benchmark、开源 loadgen。",
    "有什么代价：要构造贴近真实的负载与数据，否则结论失真（短样本乐观）；需投入 GPU 机时与脚本开发；长上下文档位尤其费卡。还要固定随机种子保证可复现。",
    "怎么评测：在固定负载下对比不同框架/批大小/并发/量化的指标，结合 p50/p95/p99 与 SLA 达标率做决策；同时画「并发-延迟」「并发-吞吐」曲线找到拐点（饱和点）。"
  ],
  "invariant": "在相同负载（相同输入分布、并发、模型、硬件）下，评测指标必须可复现；资源不变时吞吐上限由 decode 算力与显存（KV cache）共同决定，不能凭空超过物理上限。",
  "walkthrough": "某 7B 模型部署在单张 A100 上做 7×24 推理，评测用合成负载：输入 512 token、输出 256 token、并发从 1 爬到 64。采集到 p50 TTFT=0.35s、p99 TTFT=1.2s，而产品 SLA 要求 p99 TTFT<1s，不达标。进一步看显存峰值 78GB/80GB，说明并发 64 时 KV cache 已接近上限。结论：要么降并发保延迟，要么换更大显存卡或开 prefix cache/量化来扩容。",
  "edgeCases": [
    "负载分布偏离真实流量：用固定短文本评测出的延迟/吞吐过于乐观，线上长文档直接 OOM 或超时。",
    "仅测单请求忽略并发：单请求 p99 很漂亮，但并发 64 时排队与 KV 显存使延迟翻数倍，掩盖容量瓶颈。",
    "忽略成本维度：吞吐高但用了更贵的大卡，单位 token 成本反而更高，商业模式亏本。",
    "冷启动/预热：框架与 kernel 有预热期，不预热直接测会低估稳态性能。"
  ],
  "code": "# Python\ndef need_eval_sla(metric_p99, sla_limit):\n    return metric_p99 <= sla_limit            # 是否达标\ndef cost_per_hour(gpu_price, hours):\n    return gpu_price * hours                  # 评测期间的算力成本",
  "codeNotes": [
    "评测要同时看体验（延迟分位）与成本（单位 token 价格），二者常此消彼长，单看一个会误判。",
    "结论必须可复现：固定随机种子、固定负载与硬件，否则同一配置前后两次数字对不上。",
    "need_eval_sla 只是达标判断的一小步，真正的评测是曲线与分位数分析，需结合饱和点定容量。"
  ],
  "complexity": "评测本身采集成本为 O(压测样本数) 的统计开销，可忽略；真正成本是跑压测占用的 GPU 时间与构造真实负载的人力。指标统计（分位数、均值）是线性处理，不随负载规模变复杂。",
  "followUps": [
    {
      "question": "性能评测和质量评测有什么区别？",
      "answer": "质量评测看「回答对不对」（准确率、胜率、幻觉率），回答的是正确性；性能评测看「多快、多省」（延迟、吞吐、显存、成本），回答的是服务可行性。二者正交且都要做：质量好但 p99 超时 5s 没法上线，性能好但答非所问也没用。常先质量后性能，或并行。"
    },
    {
      "question": "什么时候可以不做性能评测？",
      "answer": "仅做离线 demo、无 SLA 与成本约束的内部实验可以暂缓；但只要是对外提供服务、有并发或预算约束，都应评测。即便是内部工具，若并发上来也可能因未评测而雪崩。一句话：凡牵涉真实用户或真实算力账单，先评测再上线。"
    },
    {
      "question": "怎么选压测的并发档位？",
      "answer": "依据线上预估峰值 QPS 与单请求时长倒推所需并发，并在该值附近取若干档（如 50%/100%/150% 峰值）观察拐点。重点看饱和点——延迟开始陡升、吞吐不再增长的那个并发，它就是容量上限，配合 p99 与成本定 SLA 与扩容线。"
    }
  ],
  "followUpAnswers": [
    "质量评测看「回答对不对」（准确率、胜率、幻觉率），回答的是正确性；性能评测看「多快、多省」（延迟、吞吐、显存、成本），回答的是服务可行性。二者正交且都要做：质量好但 p99 超时 5s 没法上线，性能好但答非所问也没用。常先质量后性能，或并行。",
    "仅做离线 demo、无 SLA 与成本约束的内部实验可以暂缓；但只要是对外提供服务、有并发或预算约束，都应评测。即便是内部工具，若并发上来也可能因未评测而雪崩。一句话：凡牵涉真实用户或真实算力账单，先评测再上线。",
    "依据线上预估峰值 QPS 与单请求时长倒推所需并发，并在该值附近取若干档（如 50%/100%/150% 峰值）观察拐点。重点看饱和点——延迟开始陡升、吞吐不再增长的那个并发，它就是容量上限，配合 p99 与成本定 SLA 与扩容线。"
  ],
  "pitfalls": [
    "用不真实（过短/过简单）的负载得出乐观结论，线上翻车。",
    "只看均值忽略长尾 p99 与成本，SLA 实际不达标。",
    "没有固定随机种子与负载，导致结论不可复现、无法对比前后优化。"
  ],
  "beginnerSummary": "新店开业前要试营业：看上菜快不快（延迟 TTFT/TPOT）、一天能接几桌（吞吐 TPS）、厨师工资划不划算（成本）。不试就直接开张，可能客人等太久走人（体验差），或者月底算账发现亏本（成本失控）。性能评测就是这套「试营业 + 算账」。",
  "prerequisites": [
    "理解大模型推理分 prefill（处理输入）与 decode（逐 token 生成）两段，二者延迟构成不同。",
    "服务有两个关注面：延迟（用户等多久）与吞吐（单位时间服务多少请求）。",
    "GPU 资源有限且按卡/按时计费，因此容量与成本强相关。",
    "有可量化的 SLA 目标（如 p99 TTFT<1s）才能判定达标与否。"
  ],
  "workedExample": [
    "SLA 不达标：单卡 A100 上 7B 模型 p99 TTFT=1.2s，超过产品要求的 1s；显存峰值 78/80GB 接近上限，需降并发或换框架/量化扩容。",
    "框架取舍：框架 A 在并发 32 下吞吐比 B 高 20%，但单位 token 成本高 15%；结合预算与 SLA 选 A（若成本可接受）或 B（若成本敏感）。",
    "长上下文翻车：用 512 token 短样本评测通过，但真实输入 32k 时 KV 显存溢出 OOM，暴露短样本失真。"
  ],
  "lineByLine": [
    "明确要测哪些指标（延迟分位/吞吐/显存/成本）——先定目标再测。",
    "构造贴近真实的输入长度与并发负载（含长短档位），避免乐观偏差。",
    "运行压测并逐请求采集 TTFT/TPOT/TPS/显存。",
    "统计 p50/p95/p99 与 SLA 达标率，结合成本画曲线输出决策。"
  ],
  "diagram": "离线模型 ──▶ 性能评测 ──▶ 选型/容量/SLA\n                │\n        延迟 吞吐 显存 成本"
};

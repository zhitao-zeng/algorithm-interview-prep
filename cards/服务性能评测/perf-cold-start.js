export default {
  "kind": "concept",
  "id": "perf-cold-start",
  "category": "服务性能评测",
  "difficulty": "Easy",
  "title": "压测中的冷启动 / 预热问题",
  "prompt": "压测 LLM 服务时，冷启动和预热会对指标造成什么干扰，怎么处理？",
  "quickAnswer": "冷启动指进程/模型刚加载时 CUDA 上下文、kernel 编译、显存分配、JIT/图捕获未就绪，首批请求异常慢。预热（warmup）是先跑一批丢弃请求让系统进入稳态，再正式采集，避免把冷启动延迟算进评测结果（否则 p99 被首批 3s 拉爆）。warmup 要覆盖真实 batch 形状与并发，否则正式阶段仍可能触发首次编译（隐性冷启动）。弹性扩容产生的新实例也需各自预热，且 K8s 就绪探针应在真正接流前完成预热，避免把真用户打到冷实例。",
  "approach": "正式压测前先 warmup 数十到数百请求（覆盖真实 batch 形状与并发），待指标平稳（连续样本方差<阈值，is_warm 为真）后再计时，并剔除前若干冷启动样本；弹性扩容产生的新实例也需各自预热。warmup 形状必须与正式一致，否则仍会触发首次编译。实践中建议用“预热 + 稳态检测”双保险：固定丢 N 个 + 再校验连续样本方差，比单纯丢固定数量更稳（不同硬件收敛快慢不同）。",
  "explanationFocus": "是什么：冷启动指服务刚加载时，CUDA 上下文初始化、kernel 编译、显存分配、JIT/图捕获尚未就绪，导致首批请求异常慢的现象。预热（warmup）是在正式测量前先跑一批丢弃的请求，使系统进入稳态，再开始采集，避免把冷启动延迟算进评测结果、抬高均值与 p99。它是压测正确性的基本卫生（hygiene），不做会导致严重失真的结论。",
  "bruteForce": "一启动就压：首批几百请求全是冷启动慢值，拉高均值与 p99，结论严重失真，误判服务很慢；或把冷启动当“正常性能”去定容量，线上扩容后全崩。",
  "derivation": [
    "为什么需要：CUDA 初始化、kernel 编译、KV 分配、图捕获在首请求时发生，不代表稳态性能，若不剔除会系统性高估延迟、误判容量。",
    "怎么实现：加载后发 N 个 warmup 请求丢弃，再开始正式压测与计时；warmup 需覆盖真实 batch/形状/并发，使所有代码路径都“被编译过”。",
    "有什么代价：warmup 要覆盖真实形状，否则仍可能在正式阶段触发首次编译（隐性冷启动）；弹性扩容会不断产生新冷启动，需对每个新实例预热。",
    "怎么评测：对比 warmup 前后 p50，确认稳态后再采数；对扩容新实例单独预热后再计入；记录冷启动段与稳态段分别的指标。"
  ],
  "invariant": "稳态指标应显著低于冷启动首批；warmup 后连续样本方差应平稳（is_warm 为真）；warmup 形状需与正式一致否则仍会触发首次编译。任何“新进程/新实例/新形状”首次出现都伴随冷启动，评测与上线都要处理。",
  "walkthrough": "模型服务加载完成后，冷启动首批 TTFT=3s（CUDA 初始化+图捕获）。warmup 50 个请求丢弃后，指标稳定在 TTFT=0.2s；正式评测 1 万请求测得 p99=0.35s。若 warmup 只用 batch=1 而正式用 batch=32，正式首批会触发 batch=32 的 kernel 首次编译，又出现 2s spikes——所以 warmup 形状必须与正式一致。弹性扩容新副本同样要先 warmup 再接流：假设 HPA 从 2 副本扩到 4 副本，新 2 个副本若不预热直接接流，前 ~50 请求各 3s，整体 p99 从 0.35s 飙到 1.2s，容量看似翻倍实则被冷启动吃光。",
  "edgeCases": [
    "自动扩缩容产生新实例冷启动：扩出的副本首批又慢，需各自预热（就绪探针先 warmup 再接流）。",
    "特定输入形状首次触发 kernel 编译：warmup 未覆盖该形状则正式阶段 spikes（隐性冷启）。",
    "CUDA graph 捕获在首 batch 发生：未捕获走慢路径（逐 op 调度），需以目标形状先捕获。",
    "流水线/张量并行首 token：通信建链首请求慢，多卡部署冷启动更明显。"
  ],
  "code": "# Python\ndef trim_cold(samples, warmup=50):\n    return samples[warmup:]                    # 丢弃冷启动样本\ndef is_warm(prev, cur, tol=0.1):\n    return abs(cur - prev) / prev < tol        # 是否已稳态",
  "codeNotes": [
    "warmup 要覆盖真实 batch/形状/并发，否则隐性冷启（正式首批又触发编译）。",
    "弹性扩容会引入新冷启动，新实例须各自预热后再计入统计，否则 p99 被污染。",
    "is_warm 用连续样本相对变化 < 阈值判断，比“固定丢 N 个”更稳（不同硬件收敛快慢不同）。"
  ],
  "complexity": "O(warmup+采样) 丢弃前段；判定稳态 O(样本)。额外成本仅是多跑的 warmup 请求，与正式评测同量级，几乎可忽略；真正的代价是“不做 warmup 导致错误结论”而非 warmup 本身。",
  "followUps": [
    {
      "question": "如何避免线上冷启动？",
      "answer": "常驻预热（实例启动后自动跑一批废请求再接流）、滚动更新保留热实例（先起新再下旧，避免全冷）、或用就绪探针在真正接流前先 warmup。配合就绪/存活探针，K8s 不会把流量打到未热好的实例；弹性扩容的新副本也必须先 warmup 再进负载均衡池。"
    },
    {
      "question": "CUDA graph 为何要预热？",
      "answer": "图捕获在首 batch 进行且绑定输入形状，未捕获时走慢路径（逐 op 调度，每次都要 kernel launch 开销）；需先以目标形状捕获一次，后续同形状请求才走高效图执行（一次 launch 跑整图）。若形状变化需重新捕获，所以 warmup 必须覆盖所有真实形状（不同 batch、不同 seq）。"
    }
  ],
  "followUpAnswers": [
    "常驻预热（实例启动后自动跑一批废请求再接流）、滚动更新保留热实例（先起新再下旧，避免全冷）、或用就绪探针在真正接流前先 warmup。配合就绪/存活探针，K8s 不会把流量打到未热好的实例；弹性扩容的新副本也必须先 warmup 再进负载均衡池。",
    "图捕获在首 batch 进行且绑定输入形状，未捕获时走慢路径（逐 op 调度，每次都要 kernel launch 开销）；需先以目标形状捕获一次，后续同形状请求才走高效图执行（一次 launch 跑整图）。若形状变化需重新捕获，所以 warmup 必须覆盖所有真实形状（不同 batch、不同 seq）。"
  ],
  "pitfalls": [
    "把冷启动值计入评测：首批 3s 拉高 p99，误判服务慢、错误定容量。",
    "warmup 形状与正式不一致：正式首批又触发编译，隐性冷启，p99 出现 spikes。",
    "忽略弹性扩容引入的新冷启动：新副本接流即被压，整体 p99 被拉高。"
  ],
  "beginnerSummary": "汽车：冬天刚发动时引擎冷、转速不稳（冷启动慢），开出去溜几圈热车后（预热）才进入正常状态。评测不能把「刚发动那几脚油门」算进百公里油耗，得热完车再测——否则油耗数字虚高一大截。线上也一样：新实例刚起来别立刻灌流量，先用就绪探针跑一批废请求热车，K8s 才不会把真用户打到没热好的实例上。更隐蔽的是“自动扩容”：流量一涨系统自动加新机器，新机器也是冷的，若不先热车就接客，整体反而更慢——所以扩容和评测都要把“新机器先热车”当成标准动作。",
  "prerequisites": [
    "CUDA 初始化与 kernel 编译：理解首请求为何慢（懒编译/懒分配）。",
    "CUDA graph / JIT：图捕获绑定输入形状，未捕获走慢路径。",
    "稳态 vs 瞬态：区分临时（冷启动）与持续（真实负载）性能，评测只取稳态。",
    "弹性伸缩机制（HPA/K8s）：扩容会产生新进程，新进程必有冷启动，需纳入评测与上线设计。"
  ],
  "workedExample": [
    "冷启动首批 TTFT=3s，warmup 50 后稳定 0.2s，正式 p99=0.35s。",
    "warmup 用 batch=1、正式 batch=32 → 正式首批又 2s spike，形状须一致。",
    "HPA 从 2 扩到 4 副本，新 2 副本不预热接流 → 前 50 请求各 3s，整体 p99 由 0.35s 飙到 1.2s。",
    "修复：就绪探针先 warmup 再进负载均衡池，扩容后 p99 仅微升到 0.4s。"
  ],
  "lineByLine": [
    "服务加载完成：模型与计算图就位，但 CUDA 上下文/编译未触发。",
    "发 warmup 请求丢弃：跑 N 个废请求，触发所有 kernel 编译与图捕获。",
    "确认指标平稳：is_warm（连续样本方差<阈值）为真，进入稳态。",
    "正式计时采集：剔除冷启动段后统计 p50/p95/p99。",
    "弹性扩容的新副本重复上述预热，否则并入即污染整体 p99。"
  ],
  "diagram": "时间→  [冷启动慢][warmup][稳态平稳]\n                 丢弃      采集"
};

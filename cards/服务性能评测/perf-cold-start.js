export default {
  "kind": "concept",
  "id": "perf-cold-start",
  "category": "服务性能评测",
  "difficulty": "Easy",
  "title": "压测中的冷启动 / 预热问题",
  "prompt": "压测 LLM 服务时，冷启动和预热会对指标造成什么干扰，怎么处理？",
  "quickAnswer": "冷启动指进程/模型刚加载时 CUDA 上下文、kernel 编译、显存分配、JIT/图捕获未就绪，首批请求异常慢。预热（warmup）是先跑一批丢弃请求让系统进入稳态，再正式采集，避免把冷启动延迟算进评测结果。warmup 要覆盖真实 batch 形状，否则正式阶段仍可能触发首次编译。",
  "approach": "正式压测前先 warmup 数十到数百请求（覆盖真实 batch 形状与并发），待指标平稳（连续样本方差<阈值）后再计时，并剔除前若干冷启动样本；弹性扩容产生的新实例也需各自预热。",
  "explanationFocus": "是什么：冷启动指服务刚加载时，CUDA 上下文初始化、kernel 编译、显存分配、JIT/图捕获尚未就绪，导致首批请求异常慢的现象。预热（warmup）是在正式测量前先跑一批丢弃的请求，使系统进入稳态，再开始采集，避免把冷启动延迟算进评测结果、抬高均值与 p99。",
  "bruteForce": "一启动就压：首批几百请求全是冷启动慢值，拉高均值与 p99，结论严重失真，误判服务很慢。",
  "derivation": [
    "为什么需要：CUDA 初始化、kernel 编译、KV 分配、图捕获在首请求时发生，不代表稳态性能，若不剔除会系统性高估延迟。",
    "怎么实现：加载后发 N 个 warmup 请求丢弃，再开始正式压测与计时；warmup 需覆盖真实 batch/形状/并发。",
    "有什么代价：warmup 要覆盖真实形状，否则仍可能在正式阶段触发首次编译（隐性冷启动）；弹性扩容会不断产生新冷启动。",
    "怎么评测：对比 warmup 前后 p50，确认稳态后再采数；对扩容新实例单独预热后再计入。"
  ],
  "invariant": "稳态指标应显著低于冷启动首批；warmup 后连续样本方差应平稳（is_warm 为真）；warmup 形状需与正式一致否则仍会触发首次编译。",
  "walkthrough": "模型服务加载完成后，冷启动首批 TTFT=3s（CUDA 初始化+图捕获）。warmup 50 个请求丢弃后，指标稳定在 TTFT=0.2s；正式评测 1 万请求测得 p99=0.35s。若 warmup 只用 batch=1 而正式用 batch=32，正式首批会触发 batch=32 的 kernel 首次编译，又出现 2s spikes——所以 warmup 形状必须与正式一致。",
  "edgeCases": [
    "自动扩缩容产生新实例冷启动：扩出的副本首批又慢，需各自预热。",
    "特定输入形状首次触发 kernel 编译：warmup 未覆盖该形状则正式阶段 spikes。",
    "CUDA graph 捕获在首 batch 发生：未捕获走慢路径，需以目标形状先捕获。",
    "流水线并行首 token：通信建链首请求慢。"
  ],
  "code": "# Python\ndef trim_cold(samples, warmup=50):\n    return samples[warmup:]                    # 丢弃冷启动样本\ndef is_warm(prev, cur, tol=0.1):\n    return abs(cur - prev) / prev < tol        # 是否已稳态",
  "codeNotes": [
    "warmup 要覆盖真实 batch/形状/并发，否则隐性冷启。",
    "弹性扩容会引入新冷启动，新实例须各自预热。"
  ],
  "complexity": "O(warmup+采样) 丢弃前段；判定稳态 O(样本)。额外成本仅是多跑的 warmup 请求，与正式评测同量级。",
  "followUps": [
    {
      "question": "如何避免线上冷启动？",
      "answer": "常驻预热（实例启动后自动跑一批废请求再接流）、滚动更新保留热实例（先起新再下旧，避免全冷）、或用就绪探针在真正接流前先 warmup。配合就绪/存活探针，K8s 不会把流量打到未热好的实例。"
    },
    {
      "question": "CUDA graph 为何要预热？",
      "answer": "图捕获在首 batch 进行且绑定输入形状，未捕获时走慢路径（逐 op 调度）；需先以目标形状捕获一次，后续同形状请求才走高效图执行。若形状变化需重新捕获，所以 warmup 必须覆盖所有真实形状。"
    }
  ],
  "followUpAnswers": [
    "常驻预热（实例启动后自动跑一批废请求再接流）、滚动更新保留热实例（先起新再下旧，避免全冷）、或用就绪探针在真正接流前先 warmup。配合就绪/存活探针，K8s 不会把流量打到未热好的实例。",
    "图捕获在首 batch 进行且绑定输入形状，未捕获时走慢路径（逐 op 调度）；需先以目标形状捕获一次，后续同形状请求才走高效图执行。若形状变化需重新捕获，所以 warmup 必须覆盖所有真实形状。"
  ],
  "pitfalls": [
    "把冷启动值计入评测：首批 3s 拉高 p99，误判服务慢。",
    "warmup 形状与正式不一致：正式首批又触发编译，隐性冷启。",
    "忽略弹性扩容引入的新冷启动。"
  ],
  "beginnerSummary": "汽车：冬天刚发动时引擎冷、转速不稳（冷启动慢），开出去溜几圈热车后（预热）才进入正常状态。评测不能把「刚发动那几脚油门」算进百公里油耗，得热完车再测——否则油耗数字虚高一大截。",
  "prerequisites": [
    "CUDA 初始化与 kernel 编译：理解首请求为何慢。",
    "CUDA graph / JIT：图捕获绑定形状。",
    "稳态 vs 瞬态：区分临时与持续性能。"
  ],
  "workedExample": [
    "冷启动首批 TTFT=3s，warmup 后稳定 0.2s，正式 p99=0.35s。",
    "warmup 用 batch=1、正式 batch=32 → 正式首批又 2s spike，形状须一致。",
    "弹性扩容新实例需重新预热，否则并入后拉高整体 p99。"
  ],
  "lineByLine": [
    "服务加载完成：模型与图就位。",
    "发 warmup 请求丢弃：跑 N 个废请求。",
    "确认指标平稳：is_warm 连续样本方差<阈值。",
    "正式计时采集：剔除冷启动段后统计。"
  ],
  "diagram": "时间→  [冷启动慢][warmup][稳态平稳]\n                 丢弃      采集"
};

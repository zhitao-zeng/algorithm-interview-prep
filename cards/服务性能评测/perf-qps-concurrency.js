export default {
  "kind": "concept",
  "id": "perf-qps-concurrency",
  "category": "服务性能评测",
  "difficulty": "Easy",
  "title": "QPS 与并发用户数",
  "prompt": "QPS 和并发用户数(Concurrent Users)有什么区别，怎么换算？",
  "quickAnswer": "QPS 是\"速率\"(每分钟过多少辆车),并发是\"状态\"(此刻收费亭前排着几辆车),二者由平均响应时长连接:并发 ≈ QPS × RT(Little 定律)。评测需同时给二者:网关按 QPS 限流,资源按并发占显存/连接——只看 QPS 会不知道系统同时扛多少连接,容易把\"能处理\"误当\"能并发\";只看并发又不知吞吐。换算时务必用真实平均 RT(含流式生成全程),否则并发会被严重低估或高估。",
  "approach": "用 Little 定律 并发 ≈ QPS × RT 把速率与时长连起来;先定 SLA 下的目标 RT,再据资源(显存/KV cache/连接数)反推能扛多少并发,进而得可持续 QPS。压测时阶梯加 QPS,记录各档稳定并发与 p99,找到\"并发撞资源上限\"的饱和点。",
  "explanationFocus": "是什么：QPS(每秒查询/请求数)是吞吐视角下\"单位时间到达或完成的请求速率\";并发用户数是某一时刻\"在途未完成\"的请求数量(状态)。二者描述同一系统的不同侧面,由平均响应时长(RT)连接:并发 ≈ QPS × RT(Little 定律)。评测必须同时给出二者,否则容量规划会失真。",
  "bruteForce": "只报 QPS:以为\"能处理 100 QPS\"就等于\"能并发 100\",完全忽略每个请求占用连接/KV cache 的时长。结果并发悄悄撞显存上限,排队延迟恶化,还误判为\"算力不足\"去加卡,白花钱。",
  "derivation": [
    "为什么需要：网关按 QPS 限流、资源按并发占显存/连接,两个视角缺一不可。只看 QPS 不知同时占多少资源,只看并发不知吞吐,容量规划会失真(要么资源耗尽要么算力浪费)。",
    "怎么实现：QPS = 完成请求数 / 时间窗;并发 = 在途请求计数,或用 Little 定律 并发≈QPS×平均 RT 估算。RT 要取真实平均(含流式生成全程),压测时阶梯加 QPS 记录稳定并发。",
    "有什么代价：并发高会占更多 KV cache 与连接,往往先撞显存而非算力;流式下 RT 很长使并发虚高,若误用短 RT 估算会严重低估资源。突发尖峰使瞬时并发远超均值,需按峰值规划。",
    "怎么评测：阶梯加压记录各 QPS 下的稳定并发与 p99;定位\"并发撞 KV cache/连接上限\"的饱和点;用 并发≈QPS×RT 互验实测并发,偏差大说明 RT 口径或统计有误。"
  ],
  "invariant": "并发 ≈ QPS × 平均响应时长(Little 定律);给定 RT,QPS 与并发线性相关。一个铁律:在 RT 固定时,堆 QPS 会线性推高并发,并发受资源(显存/连接)上限约束,先撞并发上限而非算力才是常态。",
  "walkthrough": "对话模型服务:平均 RT=2s(含 decode 生成全程),目标 QPS=50。按 Little 定律,平均并发 ≈ 50×2 = 100 个在途请求。每个请求占 KV cache 约 0.4GB,100 并发需 40GB KV cache;而单卡 80GB 显存减权重 14GB 后剩 66GB,故 100 并发安全。若 RT 因长上下文升到 4s,同 QPS 下并发变 200,需 80GB KV cache 超显存→必须降 QPS 到 25 或加卡。可见并发受 KV cache 先于算力撞顶。",
  "edgeCases": [
    "长连接/流式让并发定义模糊:decode 逐 token 生成 30s,这 30s 内请求都\"在途\",并发==QPS×30,极大,需按峰值规划而非均值。",
    "突发尖峰:大促瞬时 QPS 5×均值,瞬时并发远超平均,队列爆,需令牌桶/排队兜底。",
    "QPS 二八分布:20% 时间占 80% 流量,尾延迟被拉高,平均 RT 估并发偏低。",
    "RT 口径不一:用 TTFT(首 token)当 RT 算并发,漏掉 TPOT 生成时长,并发被低估数倍。"
  ],
  "code": "# Python\ndef concurrency(qps, avg_rt):\n    return qps * avg_rt                       # Little 定律近似\ndef qps(done, window):\n    return done / window                      # 窗口内请求速率",
  "codeNotes": [
    "并发受 KV cache 与连接数限制,生成式服务常先撞显存而非算力,容量规划要看并发而非只 QPS。",
    "流式响应下 RT 含整个生成过程(数十秒),用 TTFT 当 RT 会低估并发数倍,务必用端到端 RT。",
    "Little 定律给的是平均并发,突发尖峰需按峰值(如 5×均值)规划,配令牌桶/排队防队列爆。"
  ],
  "complexity": "计算 O(时间窗样本):QPS=完成数/窗口,并发=在途计数或用 Little 定律估。关键是 RT 的口径——流式响应下 RT 含整个生成过程(可能数十秒),此时\"并发=QPS×RT\"会很大,连接/KV cache 是瓶颈而非算力。还需区分\"瞬时并发\"(突发尖峰)与\"平均并发\",容量按峰值并发规划。",
  "followUps": [
    {
      "question": "并发上不去通常是哪受限？",
      "answer": "生成式服务里,并发上不去大多不是算力,而是 KV cache 占满显存或连接/worker 数到顶。每并发请求占一份 KV cache(随 seq 长度增长),当并发×单请求 KV > 剩余显存,新请求只能排队或拒,此时加算力(GPU)没用,要加显存(更大卡/更多卡/更短上下文)或降单请求占用(限长、量化 KV cache)。所以\"并发顶\"常是显存顶。"
    },
    {
      "question": "QPS 能无限堆吗？",
      "answer": "不能。超过容量后,排队使延迟恶化、错误率上升,存在饱和点。QPS 与并发线性相关(并发≈QPS×RT),堆 QPS 会线性推高并发,并发撞 KV/连接上限后系统反而更慢、错误更多。正确做法是找到\"p99 达标下的最大可持续 QPS\",而非无限加压;超饱和点后加 QPS 只减体验。"
    },
    {
      "question": "RT 口径选错有什么后果？",
      "answer": "后果严重。流式服务若用 TTFT(首 token 延迟,常 0.2s)当 RT 算并发,会得到并发≈QPS×0.2,而真实含生成 RT=2s,实际并发是被估的 10 倍——容量规划严重不足,上线即 OOM/排队爆。正确 RT 必须是端到端(从请求进到完整响应出,含全部 decode)。区分\"速率维度 QPS\"和\"时长维度 RT\"并正确相乘,是并发估算的命脉。"
    }
  ],
  "followUpAnswers": [
    "生成式服务里,并发上不去大多不是算力,而是 KV cache 占满显存或连接/worker 数到顶。每并发请求占一份 KV cache(随 seq 长度增长),当并发×单请求 KV > 剩余显存,新请求只能排队或拒,此时加算力(GPU)没用,要加显存(更大卡/更多卡/更短上下文)或降单请求占用(限长、量化 KV cache)。所以\"并发顶\"常是显存顶。",
    "不能。超过容量后,排队使延迟恶化、错误率上升,存在饱和点。QPS 与并发线性相关(并发≈QPS×RT),堆 QPS 会线性推高并发,并发撞 KV/连接上限后系统反而更慢、错误更多。正确做法是找到\"p99 达标下的最大可持续 QPS\",而非无限加压;超饱和点后加 QPS 只减体验。",
    "后果严重。流式服务若用 TTFT(首 token 延迟,常 0.2s)当 RT 算并发,会得到并发≈QPS×0.2,而真实含生成 RT=2s,实际并发是被估的 10 倍——容量规划严重不足,上线即 OOM/排队爆。正确 RT 必须是端到端(从请求进到完整响应出,含全部 decode)。区分\"速率维度 QPS\"和\"时长维度 RT\"并正确相乘,是并发估算的命脉。"
  ],
  "pitfalls": [
    "把峰值 QPS 当可持续并发:误以为能处理 100 QPS 就能长期扛 100 并发,忽略每个请求占用时长推高的真实并发。",
    "忽略流式下 RT 很长导致并发虚高:用首 token 延迟当 RT,低估 KV cache 占用数倍,上线即 OOM。",
    "只盯算力不看连接/KV:并发撞显存上限却去加卡,资源错配。"
  ],
  "beginnerSummary": "收费站比喻:QPS 是一分钟过了多少辆车;并发是此刻收费亭前排着几辆车。车过站要 2 秒,每分钟想放 50 辆,那平均就约有 100 辆在排队等待——这就是并发。只数\"过了多少辆\"不知道亭前堵不堵;只数\"排了几辆\"不知道每分钟处理能力。两个数字一起看才懂系统忙不忙。",
  "prerequisites": [
    "吞吐与延迟概念:理解 QPS(速率)与 RT(时长)是两个独立维度,乘积才有意义。",
    "流式请求长时间占用连接:知道生成式服务 RT 含整个 decode,并发会被放大。",
    "KV cache 占用与并发正相关:每并发请求占固定 KV cache,并发上限常由显存决定。",
    "Little 定律:并发≈QPS×RT,能把速率与时长互转。"
  ],
  "workedExample": [
    "RT=2s, QPS=50 → 平均并发≈100;每请求 KV 0.4GB → 需 40GB,单卡 80GB 余 66GB 安全。",
    "RT 升到 4s,同 QPS 并发变 200 需 80GB KV 超显存 → 降 QPS 到 25 或加卡,并发受显存先于算力撞顶。",
    "流式反例:用 TTFT=0.2s 当 RT 算并发=50×0.2=10,实际含生成 RT=2s 并发=100,低估 10 倍致 OOM。"
  ],
  "lineByLine": [
    "统计窗口内完成请求得 QPS:QPS = done / window,反映吞吐速率。",
    "统计在途请求得住并发:或用 Little 定律 并发≈QPS×平均 RT,把速率与时长连起来。",
    "用 并发≈QPS×RT 互验:偏差大说明 RT 口径错(如漏掉流式生成)或统计漏在途。",
    "按 SLA 与资源设并发上限:KV cache/连接数定并发顶,反推可持续 QPS,找到饱和点。"
  ],
  "diagram": "QPS ──×── RT ──▶ 并发\n 速率      时长     在途数\n(车/分)   (秒/车)  (排队车)"
};

export default {
  "kind": "concept",
  "id": "perf-percentiles",
  "category": "服务性能评测",
  "difficulty": "Easy",
  "title": "p50 / p95 / p99 分位数的意义",
  "prompt": "为什么评测延迟要看 p50/p95/p99 而不是只看平均值？",
  "quickAnswer": "平均延迟会把少数极慢请求\"平均\"掉,显得一切正常,但那 1% 最慢用户可能等了 8 倍时长、直接流失。p50 反映典型体验(一半用户比它快),p95/p99 反映长尾(最差 5%/1%),SLA 通常按 p95/p99 设定。评测必须报告分位数:至少 p50/p90/p95/p99 加最大值,才能既看典型又看最差,避免\"均值良好、长尾崩\"的误判。",
  "approach": "对延迟样本升序排序取分位点,重点盯 p95/p99 长尾——它决定 SLA 达标率。实践中报告\"p50/p90/p95/p99 + 最大值 + 达标率(≤SLA 的比例)\",并配合足够样本量(数千以上)让 p99 稳定。可视化用右偏分布图标出各分位位置。",
  "explanationFocus": "是什么：pXX 分位数表示\"有 X% 的请求延迟低于该值\"——p50 是中位数(典型用户体验)、p95/p99 是长尾(最差 5%/1% 用户的体验)。延迟分布高度右偏,平均值对离群值不敏感,会掩盖少数极慢请求;而用户体验、口碑与 SLA 恰恰由最差的那部分决定,所以评测必须报告分位数而非均值。",
  "bruteForce": "只报平均延迟:一个快九个慢被平均成\"还行\",长尾用户实际崩溃,监控报警却不响(因为均值没超阈),直到客诉爆发才发现。",
  "derivation": [
    "为什么需要：延迟分布高度右偏(多数快、少数极慢),均值对离群不敏感——一个 2s 的请求被 99 个 100ms 平均成 118ms,完全掩盖。而 SLA 与口碑由最差体验决定,所以必须用语义更准的分位数。",
    "怎么实现：收集全部延迟样本→升序排序→p99 取第 99% 位置的值(最近秩法 k=ceil(0.99n)-1,或线性插值)。工程上小数据直接排序,大数据用 t-digest/HDR 近似分位,边采边估。",
    "有什么代价：精确分位需存全量并排序,内存随样本增长;小样本时 p99 方差大(100 样本估 p99 只有 1 个数据点,极不稳)。需足够样本(数千+)并统一分位算法,否则跨次对比失真。",
    "怎么评测：固定负载跑大量请求(数千~万),报告 p50/p90/p95/p99 与 max、以及达标率(延迟≤SLA 的比例);用 p99 判 SLA 达标,用 max 查偶发异常,同时报避免只看均值。"
  ],
  "invariant": "分位单调:p50 ≤ p95 ≤ p99 ≤ max;样本量越大,长尾估计越稳。任何 p99<p95 或样本极少却给精确 p99 都是统计错误。",
  "walkthrough": "某 API 压测 10000 请求:均值 120ms,但分布右偏——p50=95ms、p90=180ms、p95=320ms、p99=800ms、max=2100ms。若只看均值 120ms 会判\"健康\",但 p99=800ms 已超 SLA(设 500ms),意味着 1%(约 100 个)用户每次等 0.8s 以上,口碑受损。再细看 max=2100ms 是 GC 抖动偶发,需单独治理。结论:用均值放行会漏掉真实违约。",
  "edgeCases": [
    "样本太少 p99 抖动：仅 100 请求时 p99 只代表 1 个样本,换批数据可能从 600ms 跳到 1500ms,不可信,需累积到数千。",
    "混合请求类型使分位失真：把\"短查询\"与\"长报表\"混算,p99 被长任务拉爆,应分类型分别报分位。",
    "GC/调度抖动制造偶发长尾：max 偶尔 2s 但 p99 正常,属偶发,需单独看 max 与抖动频次而非并入 p99。",
    "分位算法不一致：最近秩法与线性插值差几个 ms,跨团队对比前需统一算法。"
  ],
  "code": "# Python\ndef percentile(vals, p):\n    s = sorted(vals)\n    k = max(0, min(len(s) - 1, int(round(p / 100 * len(s)) - 1)))\n    return s[k]                                # 最近秩法",
  "codeNotes": [
    "样本量建议 ≥ 数千(经验数千~万级),否则 p99 因只有 1~2 个数据点而剧烈抖动。",
    "不同分位算法(线性插值/最近秩)结果略差,团队间对比前必须统一,否则数字不可比。",
    "流式/海量场景用 t-digest 或 HDR Histogram 近似分位,内存 O(1) 且误差可控,不必存全量。"
  ],
  "complexity": "精确分位 O(n log n)(排序后取秩);海量数据可用 t-digest/HDR Histogram 近似,内存 O(1) 且误差可控。关键是样本量:估算 p99 至少需要数千样本(经验数千~万级),否则分位波动大、不可信;流式场景用近似结构边采边估,避免存全量。",
  "followUps": [
    {
      "question": "p99 和最大值哪个更该关注？",
      "answer": "决策看 p99,监控同时报 max。p99 反映\"稳定长尾\"——100 次里有 1 次慢,是可行动、可优化的常态问题(如缓存未命中、偶发排队);max 可能是一次 GC 抖动或网络抖动的偶发异常,单看容易被误导(一次 2s 不代表系统常态)。正确做法:p99 作为 SLA 达标判据与优化目标,max 作为\"异常探测\"信号,超阈就查根因但不直接当容量依据。"
    },
    {
      "question": "样本不足怎么办？",
      "answer": "三招:①延长压测时间/提高并发,累积到数千以上样本让 p99 稳定;②用近似分位结构(t-digest/HDR Histogram)在内存受限下边采边估,误差可控;③若只能小样本,则报告时标注置信区间(如 p99 在 600~1500ms),不给出过于精确的单一数字误导决策。核心是别拿 50 个样本的 p99 当容量依据。"
    },
    {
      "question": "为什么不用平均值就够了？",
      "answer": "平均会把长尾\"抹平\"。举例:99 个 100ms + 1 个 2000ms,均值 119ms 看似优秀,但那 1 个用户等了 20 倍时长、直接流失并差评。平均值对右偏分布几乎不反映最差体验,而 SLA 与口碑恰恰由最差体验决定。分位数(尤其 p99)才是\"用户实际能感知的最差常态\"。"
    }
  ],
  "followUpAnswers": [
    "决策看 p99,监控同时报 max。p99 反映\"稳定长尾\"——100 次里有 1 次慢,是可行动、可优化的常态问题(如缓存未命中、偶发排队);max 可能是一次 GC 抖动或网络抖动的偶发异常,单看容易被误导(一次 2s 不代表系统常态)。正确做法:p99 作为 SLA 达标判据与优化目标,max 作为\"异常探测\"信号,超阈就查根因但不直接当容量依据。",
    "三招:①延长压测时间/提高并发,累积到数千以上样本让 p99 稳定;②用近似分位结构(t-digest/HDR Histogram)在内存受限下边采边估,误差可控;③若只能小样本,则报告时标注置信区间(如 p99 在 600~1500ms),不给出过于精确的单一数字误导决策。核心是别拿 50 个样本的 p99 当容量依据。",
    "平均会把长尾\"抹平\"。举例:99 个 100ms + 1 个 2000ms,均值 119ms 看似优秀,但那 1 个用户等了 20 倍时长、直接流失并差评。平均值对右偏分布几乎不反映最差体验,而 SLA 与口碑恰恰由最差体验决定。分位数(尤其 p99)才是\"用户实际能感知的最差常态\"。"
  ],
  "pitfalls": [
    "用均值掩盖长尾：均值 120ms 但 p99=800ms,靠均值放行漏掉真实 SLA 违约。",
    "小样本下 p99 不可信：用 50 请求估 p99 当容量依据,波动大到无法决策。",
    "只报 p99 忽略达标率：p99=480ms<500ms 看似达标,但达标率仅 92%(8% 超阈),仍违约。"
  ],
  "beginnerSummary": "网约车例子:平均等待 5 分钟听起来不错,但 p99 是\"最慢那 1% 的人等了 40 分钟\"——正是这批人会投诉、卸载。看平均分看不出问题,要看\"最倒霉那批人\"等了多久。平均值像\"全班平均分\",掩盖了\"有几个孩子不及格\";分位数像\"排名\",能揪出垫底的那几个。",
  "prerequisites": [
    "延迟分布通常右偏：理解多数请求快、少数极慢的长尾形态,均值会失真。",
    "SLA 常按分位定义：行业惯例用 p95/p99 设延迟阈值(如 p99<500ms),而非均值。",
    "统计采样基础：知道分位定义、样本量对估计稳定性的影响、近似分位结构(t-digest)。",
    "有稳定压测负载：能复现真实流量分布来得到可信分位。"
  ],
  "workedExample": [
    "1 万请求:均值 120ms、p50=95、p90=180、p95=320、p99=800、max=2100。均值\"健康\"但 p99 超 500ms SLA,1% 用户受损。",
    "样本量对比:100 请求估 p99 仅 1 个数据点(波动±700ms),1 万请求 p99 稳定(±30ms),证明需数千样本。",
    "达标率补充:即使 p99=480<500,若达标率 92% 仍违约,需同时报分位与达标率。"
  ],
  "lineByLine": [
    "采集所有延迟样本：在请求路径打点,记录每次请求的端到端耗时,存全量或近似结构。",
    "升序排序：得到经验分布,准备取分位。",
    "按百分比取分位点：p50=中位、p95=第95%秩、p99=第99%秩(最近秩或线性插值),并取 max。",
    "报告分位+达标率：至少 p50/p90/p95/p99/max 与 ≤SLA 比例,用 p99 判达标、max 查偶发。"
  ],
  "diagram": "延迟分布(右偏):\n |▇▇▇▇▇▇▇▇▇\n |        ▇\n |         ▇  ← p99/max 长尾\n p50   p95  p99"
};

export default {
  "kind": "concept",
  "id": "perf-cost",
  "category": "服务性能评测",
  "difficulty": "Medium",
  "title": "成本评测（每千 token 成本）",
  "prompt": "如何评测算大模型服务的成本，常用哪些单位？",
  "quickAnswer": "成本评测把 GPU 小时价摊到产出上，常用「每千输入/输出 token 成本」或「每百万 token 成本」。计算 = GPU 单价 × 占用卡时 / 产出 token 数，再结合命中缓存、量化、批处理优化来降本。关键要区分输入与输出 token 单价（输出通常更贵，因为逐 token 生成、无法并行）。",
  "approach": "以单卡小时成本除以该卡单位时间产出的 token 数，得单位 token 成本；分别计输入/输出单价；再在固定负载下比较不同框架、量化、批配置、卡型的 cost/token，选出 SLA 内最低单位成本的组合。",
  "explanationFocus": "是什么：成本评测把 GPU 等算力支出折算为每千 token 或每百万 token 的价格，并区分输入与输出单价，用于横向比价、选型与优化。它把「性能好不好」与「花多少钱」绑定，是商业模式可行性的核心指标。",
  "bruteForce": "只看吞吐不看单价：某配置吞吐高但用了更贵的大卡，单位 token 成本反而更高，表面「快」实则「贵」，商业模式可能亏本。",
  "derivation": [
    "为什么需要：成本决定商业模式能否成立，不能只说「模型能跑」，要算清「产一个 token 花多少钱」，才能定价、选卡、定 SLA。",
    "怎么实现：cost_per_1k = GPU每小时价 × 卡数 × 时长 / (产出 token / 1000)；分别计输入/输出；结合缓存命中（输入免费）、量化（提吞吐）、批处理（摊薄）优化。可在压测时同步采集 token 计数与卡时。",
    "有什么代价：需准确归因显存占用与共享资源；缓存命中使输入 token 近乎免费、统计需特殊处理；多副本/弹性伸缩时卡时分摊复杂；输出比输入贵数倍需分项，否则被平均掩盖。",
    "怎么评测：固定负载下比较各框架/量化/批配置/卡型的单位成本，结合 SLA 与质量选最优；持续监控线上单位成本随流量波动。"
  ],
  "invariant": "单位成本 ∝ GPU 单价 × 卡时 / 产出 token；吞吐越高或卡越便宜，单位成本越低。输入/输出分项计价时，总成本 = 输入token/1000×p_in + 输出token/1000×p_out。",
  "walkthrough": "单张 A100 约 $2/h，某 7B 模型在该卡上 1 小时稳定产出 300 万 token（含输入+输出，吞吐受 batch 与输出长度影响）。则 cost_per_1k = 2×1×1 / (3,000,000/1000) = $2 / 3000 ≈ $0.00067/千 token。若开启 prefix cache 命中 40% 输入，则输入近乎免费、实际单位成本再降；若换成更便宜的 A10（约 $0.8/h）但吞吐降至 120 万 token/h，单位成本 ≈ $0.00067 反而相近——说明不能单看卡价，要看「卡价/吞吐」。",
  "edgeCases": [
    "输入缓存命中：prefix cache 命中使该部分输入 token 近乎零成本，统计时要剔除或标记。",
    "空闲副本仍计费：为保低延迟预留的空闲副本按卡时计费，拉高单位成本，需计入常驻成本。",
    "输出比输入贵数倍：分项计价时若混算会掩盖，须分别统计 in/out。",
    "多租户共享：一张卡服务多业务时卡时需按 token 或时长分摊，否则成本归属失真。"
  ],
  "code": "# Python\ndef cost_per_1k(gpu_price_h, cards, hours, tokens):\n    return gpu_price_h * cards * hours / (tokens / 1000)\ndef cost_split(in_tok, out_tok, p_in, p_out):\n    return in_tok/1000*p_in + out_tok/1000*p_out",
  "codeNotes": [
    "输入/输出通常不同单价（输出更贵），cost_split 分项计价才能看清优化重点。",
    "缓存命中降低输入成本——prefix cache 是降本利器，评测时应单开一档对比。",
    "cost_per_1k 是 O(1) 折算，但分子（卡时）与分母（token）都要来自真实压测，不能拍脑袋。"
  ],
  "complexity": "折算本身是 O(1) 的算术；难点在准确归因——显存占用时长、共享副本的分摊、缓存命中带来的输入成本归零，都需要精细计量，否则单位成本失真。",
  "followUps": [
    {
      "question": "为什么输出 token 更贵？",
      "answer": "每生成一个输出 token 都要跑一次完整前向（访存密集的 decode 步），且必须逐 token 串行生成、无法像输入 prefill 那样并行吃下整段，因此单位 token 的算力成本高。输入 token 在 prefill 阶段可并行处理，单位成本低得多。这也是为什么压缩输出长度、用投机解码提速能直接降本。"
    },
    {
      "question": "如何用成本反选配置？",
      "answer": "在 SLA（延迟/质量）约束内，选单位 token 成本最低的组合：优先量化（提吞吐）、开 prefix cache（降输入）、大 batch 摊薄卡时、选性价比卡型（如 A10 vs A100 看「卡价/吞吐」而非单看卡价），并用成本-质量-延迟三维权衡，而非只追最快或最便宜。"
    },
    {
      "question": "缓存命中怎么影响成本评测？",
      "answer": "prefix cache 命中意味着相同前缀的 prefill 只算一次，命中部分的输入 token 成本近乎归零，会显著拉低单位成本。评测时应分别测「无缓存」与「典型缓存命中率」两档，报告缓存带来的降本幅度——很多线上场景缓存命中率 30%~60%，对成本影响巨大，不可忽略。"
    }
  ],
  "followUpAnswers": [
    "每生成一个输出 token 都要跑一次完整前向（访存密集的 decode 步），且必须逐 token 串行生成、无法像输入 prefill 那样并行吃下整段，因此单位 token 的算力成本高。输入 token 在 prefill 阶段可并行处理，单位成本低得多。这也是为什么压缩输出长度、用投机解码提速能直接降本。",
    "在 SLA（延迟/质量）约束内，选单位 token 成本最低的组合：优先量化（提吞吐）、开 prefix cache（降输入）、大 batch 摊薄卡时、选性价比卡型（如 A10 vs A100 看「卡价/吞吐」而非单看卡价），并用成本-质量-延迟三维权衡，而非只追最快或最便宜。",
    "prefix cache 命中意味着相同前缀的 prefill 只算一次，命中部分的输入 token 成本近乎归零，会显著拉低单位成本。评测时应分别测「无缓存」与「典型缓存命中率」两档，报告缓存带来的降本幅度——很多线上场景缓存命中率 30%~60%，对成本影响巨大，不可忽略。"
  ],
  "pitfalls": [
    "忽略输入/输出不同单价，用单一均价误判优化方向（如猛堆输入缓存却不知输出才是大头）。",
    "不计空闲副本/常驻成本，单位成本被低估，预算崩。",
    "只看吞吐选配置，忽略卡价，单位成本反而高。"
  ],
  "beginnerSummary": "打车比喻：成本评测像算「每公里多少钱」。但上车起步（输入 token）和每多开一公里（输出 token）单价不同——输出每公里更贵，因为司机得一笔一划开（逐 token 生成、串行）。而且车空着等客（空闲副本）也按时间计费，所以光看「跑得多快」不够，得算单位成本才知划不划算。",
  "prerequisites": [
    "理解吞吐与 GPU 计费（按卡·小时）的关系，单位成本 = 卡价/产出。",
    "理解输入/输出 token 的计价差异（输出更贵，因逐 token 串行生成）。",
    "了解 prefix cache、量化、批处理等影响单位成本的手段。",
    "有真实负载下的 token 计数能力，才能算准分母。"
  ],
  "workedExample": [
    "A100 $2/h 产出 300 万 token/h → $0.00067/千 token；换 A10 $0.8/h 但吞吐 120 万 → 单位成本相近，说明卡价低不等于更省。",
    "输入缓存命中 40%：输入成本近乎归零，单位成本再降约 20%~40%（视输出占比）。",
    "分项计价：输入 p_in=$0.0005/千、输出 p_out=$0.002/千，一个 1k 输入+2k 输出的请求成本 = 0.5+4 = $4.5/千次请求量级——输出主导成本。"
  ],
  "lineByLine": [
    "`gpu_price_h * cards * hours`：算该时段 GPU 总花费（分子）。",
    "`tokens / 1000`：把产出 token 换算成千 token 单位（分母）。",
    "`return gpu_price_h * cards * hours / (tokens / 1000)`：得每千 token 成本。",
    "`cost_split`：按输入/输出不同单价分别计价，看清成本结构。"
  ],
  "diagram": "成本 = GPU单价 × 卡时\n          ────────────────\n             产出 token 数\n  (输入/输出分项计价)"
};

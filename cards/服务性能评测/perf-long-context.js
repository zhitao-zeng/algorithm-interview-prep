export default {
  "kind": "concept",
  "id": "perf-long-context",
  "category": "服务性能评测",
  "difficulty": "Medium",
  "title": "长上下文对评测的影响",
  "prompt": "输入/输出变长会如何改变 LLM 服务的性能评测结论？",
  "quickAnswer": "长输入主要拉长 Prefill（耗时 ∝ 输入长度）并膨胀 KV Cache 显存，限制可并发数；长输出拉长 Decode 总时长与尾延迟。评测必须覆盖短/中/长多档上下文，否则在真实长文场景会严重失真甚至 OOM。长输入更易先撞显存上限、限制并发。",
  "approach": "构造不同输入长度档（如 1k/8k/32k）与输出长度档，分别测量 TTFT、TPOT、TPS、显存峰值与最大并发，报告各档指标并画随长度变化的曲线，找出显存/延迟拐点，从而给出真实的容量与 SLA 建议。",
  "explanationFocus": "是什么：长上下文指长输入与长输出，它们分别拖累 prefill 与 decode，并放大 KV cache 显存占用，使评测结论强烈依赖输入/输出长度分布。只测短样本得到的「很快很省」在真实长文档场景会严重失真，甚至直接 OOM。",
  "bruteForce": "只用 512 token 短样本评测：上线长文档问答即 OOM 或延迟爆炸，因为短样本完全没触发 KV 显存瓶颈，结论失效且危险。",
  "derivation": [
    "为什么需要：真实流量常含长文档、RAG 拼接的长上下文、长输出报告，短样本评测过于乐观且危险——线上一上长文就 OOM 或超时，事前毫无预警。",
    "怎么实现：设输入长度档（1k/8k/32k/128k）与输出档，逐档测 TTFT、TPOT、TPS、峰值显存与最大并发；若模型支持需配相应 rope/窗口与长上下文推理后端。",
    "有什么代价：长上下文显存与耗时都大，评测占卡成本高；需框架支持相应长度（否则截断或外推失真）；KV 量化/prefix cache 等优化会改变曲线，需在同配置下对比。",
    "怎么评测：报告各长度档的 TTFT/TPOT/最大并发，画随长度变化曲线；重点看「显存拐点」（并发骤降处）与「TTFT 拐点」，据此定容量与长上下文 SLA。"
  ],
  "invariant": "Prefill 耗时 ∝ 输入长度；KV 显存 ∝ batch × seq_len；端到端 ∝ 输入 + 输出长度。长上下文主要压显存（限并发）与 prefill（拉 TTFT），这两点必须进评测。",
  "walkthrough": "某 7B 模型在 A100（80GB）上，输入长度从 2k 增到 32k：TTFT 由 0.3s 升到 4.5s（prefill ∝ 输入），KV cache 峰值从 18GB 涨到 72GB，使最大并发从 64 降到 8（显存限制）。输出长度从 256 增到 2048 token：单请求总时长由约 5s 升到约 40s（decode 累加），p99 TPOT 基本不变但尾延迟显著拉长。可见评测若只用 2k/256 档，会完全低估真实长上下文的成本与容量瓶颈。",
  "edgeCases": [
    "超过训练上下文：需外推（NTK/PI）或截断，否则长输入效果/性能失真，要单独测。",
    "稀疏/滑动窗口注意力：长输入未必线性变慢，评测需贴合实际注意力机制，不能套用全注意力假设。",
    "长输出占连接极久：单请求 decode 数十秒，连接池被长期占用，压测需考虑连接数而非仅并发。",
    "KV 量化影响：开 KV cache 量化后显存降、并发升，需在同配置下重测。"
  ],
  "code": "# Python\ndef prefill_growth(in_len, base=0.00015):\n    return in_len * base                      # TTFT 近似随输入线性\ndef max_concurrent(vram, kv_per_seq):\n    return int(vram / kv_per_seq)             # 显存决定并发",
  "codeNotes": [
    "长输入往往先撞 KV 显存而非算力——容量瓶颈常在显存不在 FLOPs。",
    "长输出拉长 decode 总时长，但 TPOT 可能不变，要看「总时长」与「连接占用」。",
    "可叠加 prefix cache：相同系统前缀只算一次 prefill，显著改善长输入评测与线上。"
  ],
  "complexity": "评测成本 O(长度档数 × 压测样本)，长档位尤其费卡（显存大、单请求久）；但相比线上事故代价，覆盖多档的评测成本可忽略。统计处理与短样本一致为线性。",
  "followUps": [
    {
      "question": "长输入和长输出哪个更伤吞吐？",
      "answer": "各有伤害面：长输入伤 prefill（TTFT 变长）与 KV 显存（限并发），长输出伤 decode 总时长（单请求更久、占连接）。综合看，长输入更易先撞显存上限、直接砍并发，对系统吞吐打击更大；长输出主要拉长单请求、降低连接周转。优化上长输入靠 prefix cache/KV 量化/稀疏注意力，长输出靠投机解码/更高速 decode。"
    },
    {
      "question": "如何缓解长上下文成本？",
      "answer": "多管齐下：prefix cache 复用公共前缀的 prefill；KV cache 量化（如 INT8/FP8）压缩显存、抬升并发；稀疏/滑动窗口注意力降低长输入计算；或截断+RAG 控制上下文长度；以及对齐业务——并非所有请求都需要 128k，按需分配长度档位。"
    },
    {
      "question": "评测时怎么设长度档位才不漏？",
      "answer": "按线上真实输入/输出长度分布分位取档（p50/p90/p99），至少覆盖短/中/长三档并含一个超过训练上下文的边界档；同时固定并发看显存拐点、固定长度看并发拐点。这样既有代表性，又能暴露极限与 OOM 风险。"
    }
  ],
  "followUpAnswers": [
    "各有伤害面：长输入伤 prefill（TTFT 变长）与 KV 显存（限并发），长输出伤 decode 总时长（单请求更久、占连接）。综合看，长输入更易先撞显存上限、直接砍并发，对系统吞吐打击更大；长输出主要拉长单请求、降低连接周转。优化上长输入靠 prefix cache/KV 量化/稀疏注意力，长输出靠投机解码/更高速 decode。",
    "多管齐下：prefix cache 复用公共前缀的 prefill；KV cache 量化（如 INT8/FP8）压缩显存、抬升并发；稀疏/滑动窗口注意力降低长输入计算；或截断+RAG 控制上下文长度；以及对齐业务——并非所有请求都需要 128k，按需分配长度档位。",
    "按线上真实输入/输出长度分布分位取档（p50/p90/p99），至少覆盖短/中/长三档并含一个超过训练上下文的边界档；同时固定并发看显存拐点、固定长度看并发拐点。这样既有代表性，又能暴露极限与 OOM 风险。"
  ],
  "pitfalls": [
    "用短样本评测得出乐观结论，线上长文直接翻车（OOM/超时）。",
    "忽略 KV 显存随长度膨胀，误判可并发数，容量规划崩溃。",
    "只看平均延迟不看长输出尾延迟，SLA 实际不达标。"
  ],
  "beginnerSummary": "读书比喻：短文章一眨眼读完（prefill 快）、写两句话就完（decode 短）；长篇小说读得久（prefill 慢）而且书占满桌面（KV 显存），同时写长篇耗时长（decode 久）。只测「读一条短信」就以为很快，等真读长篇小说（长文档问答）就傻眼——要么读半天，要么桌面放不下书（OOM）。",
  "prerequisites": [
    "理解 prefill/decode 分解：输入决定 prefill，输出决定 decode。",
    "理解 KV cache 显存随 batch×seq 线性增长，是并发的物理上限。",
    "了解 prefix cache、KV 量化、稀疏注意力等缓解手段，评测时需明确是否启用。",
    "模型与框架支持目标上下文长度（rope/窗口配置）。"
  ],
  "workedExample": [
    "32k 输入：TTFT 由 0.3s 升到 4.5s，KV 显存 18→72GB，最大并发 64→8。",
    "输出 2048：单请求总时长 5s→40s，尾延迟显著拉长但 TPOT 基本不变。",
    "prefix cache：复用 2k 系统前缀后，32k 输入的 prefill 降约 30%，并发可略回升。"
  ],
  "lineByLine": [
    "设长度档位（如 1k/8k/32k 输入，256/2048 输出）——覆盖真实分布。",
    "逐档测 TTFT/TPS/显存——分别暴露 prefill 与 KV 瓶颈。",
    "记录最大并发——由显存上限反推容量。",
    "画随长度变化曲线——找拐点定 SLA 与扩容线。"
  ],
  "diagram": "长度↑ → 输入长: prefill↑, KV↑(限并发)\n           → 输出长: decode 总时长↑"
};

export default {
  "kind": "concept",
  "id": "perf-traps",
  "category": "服务性能评测",
  "difficulty": "Medium",
  "title": "评测中的陷阱（缓存干扰 / 数据污染）",
  "prompt": "LLM 服务性能评测中有哪些常见陷阱会导致结论失真？",
  "quickAnswer": "常见陷阱：①Prefix/KV 缓存命中让重复前缀请求异常快，不清缓存会高估性能；②训练数据污染/固定样本导致过拟合式乐观；③客户端或网络成瓶颈，服务端没压满就先到顶；④时钟不同步使间隔失真；⑤warmup 不足、⑥只看均值忽略 p99。严谨评测要清缓存、随机化负载、控变量、校时钟、报全分位。",
  "approach": "系统性排查清单：①每次评测前重启或清 KV 缓存，做「冷/热」两档对照；②用独立随机、去重且与训练集不重叠的输入；③校准客户端时钟、用多线程/多连接消除客户端瓶颈；④充分 warmup 后再计时；⑤报告 p50/p95/p99 而不仅是均值；⑥控制单一变量做 A/B。",
  "explanationFocus": "是什么：评测陷阱指会让性能指标虚假变好或变坏的因素，常见有缓存命中干扰、样本数据污染、客户端/网络成瓶颈、时钟不同步、warmup 不足与只报均值忽略 p99。这些因素会让「实验室数字」与「线上真实体验」严重错位，必须主动识别并排除，才能得到可信结论。",
  "bruteForce": "跑一遍就信：缓存与样本巧合让数字好看，客户端先瓶颈却误判服务端慢，上线即翻车。或者单跑一个固定 prompt 反复测，完全脱离真实多样本分布。",
  "derivation": [
    "为什么需要：未识别的陷阱会使评测结论与线上真实不符，误导模型选型、容量规划与 SLO 设定，代价可能是大规模扩容或错误下线。",
    "怎么实现：每次评测重启或清 KV 缓存；用独立随机输入；校准时钟与客户端并发；warmup；报告 p99 与错误率。对可疑项做「清 vs 命中」「单样本 vs 随机集」对照。",
    "有什么代价：清缓存与随机化增加评测时间与算力成本；要设计对照实验来区分变量，实验设计本身有门槛。",
    "怎么评测：做「清缓存 vs 命中缓存」「固定样本 vs 随机样本」对照，确认差异来源后再下结论；多 seed 复跑验证稳定性。"
  ],
  "invariant": "清缓存后指标应一致且更保守（更慢/更低吞吐）；随机样本下结果可复现且不依赖特定前缀；对照实验中除被控变量外其余条件一致。",
  "walkthrough": "不清缓存跑同前缀 1000 次，TTFT 全部命中=20ms；执行清 KV 缓存后再跑，TTFT=220ms，差距 10 倍，原结论虚高。进一步：在并发 100 下，命中缓存吞吐可达 500 tok/s，清缓存后仅 60 tok/s。若客户端单线程发请求，服务端 GPU 利用率仅 30% 就先到瓶颈，测得延迟偏高却不是服务端问题——换多线程客户端后 GPU 利用率到 95%，真实延迟才暴露。",
  "edgeCases": [
    "多轮对话天然命中前缀缓存，热路径评测无法避免，需显式标注并分档报告。",
    "benchmark 样本被模型训练见过（数据污染），分数虚高，需用私有/最新样本。",
    "多 worker/多机时钟漂移使间隔与吞吐失真，需统一时钟源。",
    "GPU 利用率未打满就到「瓶颈」，可能是客户端或网络而非服务端。"
  ],
  "code": "# Python\ndef cache_effect(hit_ttft, miss_ttft):\n    return miss_ttft / hit_ttft                # 缓存加速比\ndef dedupe(samples):\n    return list(dict.fromkeys(samples))         # 去重防污染",
  "codeNotes": [
    "做命中/未命中对照，缓存加速比 = miss/hit，但必须标注场景。",
    "样本去重并随机化，dict.fromkeys 保序去重，避免污染。"
  ],
  "complexity": "去重 O(样本)，对照实验 O(样本×档位数)。主要额外成本在清缓存导致的重复预热与更大样本量，而非计算本身。统计全分位需维护直方图，O(样本×约束)。",
  "followUps": [
    {
      "question": "缓存命中算不算作弊？",
      "answer": "不算作弊，但评测必须区分「冷前缀」与「热前缀」两档并分别报告、明确标注，否则会把热路径的 20ms 当成普遍水平误导读者。真实线上既有冷请求也有热请求，应给出两档数字与占比，才能反映真实体验。"
    },
    {
      "question": "数据污染如何发现？",
      "answer": "用模型从未见过的随机或私有样本重测；用多个随机 seed 复跑观察稳定性，若某基准异常一致地好（如所有变体都 +5 分）要警惕。也可对比不同来源/年份的同类基准，分数虚高往往只在被污染的那一个上出现。"
    }
  ],
  "followUpAnswers": [
    "不算作弊，但评测必须区分「冷前缀」与「热前缀」两档并分别报告、明确标注，否则会把热路径的 20ms 当成普遍水平误导读者。真实线上既有冷请求也有热请求，应给出两档数字与占比，才能反映真实体验。",
    "用模型从未见过的随机或私有样本重测；用多个随机 seed 复跑观察稳定性，若某基准异常一致地好（如所有变体都 +5 分）要警惕。也可对比不同来源/年份的同类基准，分数虚高往往只在被污染的那一个上出现。"
  ],
  "pitfalls": [
    "不清缓存高估性能，把命中前缀的 20ms 当常态，真实 220ms 被掩盖。",
    "只看均值忽略 p99 与客户端瓶颈，误判服务端能力。",
    "样本与训练集重叠却当「泛化」，污染导致乐观。"
  ],
  "beginnerSummary": "考试：缓存干扰像「考题正好背过」考分虚高；数据污染像「练习册和考试卷一模一样」；只看平均分像「班级平均 90 但有人交白卷」；客户端瓶颈像「用算盘计时却怪运动员慢」。严谨评测就是换套从没见过的卷子、清掉小抄、用准的秒表、看最差那几个分。",
  "prerequisites": [
    "prefix/KV 缓存机制：理解命中为何加速。",
    "统计分位与实验可复现性：p99、对照、seed。",
    "客户端与服务端瓶颈区分：如何判断谁先到顶。"
  ],
  "workedExample": [
    "清缓存 TTFT 由 20ms→220ms，差距 10 倍，原结论虚高。",
    "单线程客户端 GPU 利用率仅 30%，换多线程后到 95%，真实延迟才暴露。",
    "随机私有样本复跑，结果波动 <2%，确认稳定可信。"
  ],
  "lineByLine": [
    "清/隔离缓存：重启或清 KV cache，确保冷路径。",
    "随机化去重样本：用未见过的输入，去重防污染。",
    "校准客户端与时钟：多线程发压，统一时间源。",
    "warmup 并全分位报告：预热后测 p50/p95/p99。"
  ],
  "diagram": "陷阱: 缓存命中✓ 样本污染✓ 客户端瓶颈✓ 只看均值✓\n  └─▶ 对照+随机+分位 排除"
};

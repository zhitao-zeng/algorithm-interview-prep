export default {
  "id": "perf-script-design",
  "kind": "concept",
  "category": "服务性能评测",
  "difficulty": "Hard",
  "title": "如何设计一份评测脚本",
  "prompt": "设计一份严谨的 LLM 服务性能评测脚本，应该包含哪些模块？",
  "code": "# Python\ndef run_eval(config):\n    warmup(config); samples = []\n    for _ in range(config.rounds):\n        r = async_press(config)                 # 并发加压+流式埋点\n        samples += r\n    return report(samples, config.sla)          # 分位+达标率",
  "diagram": "配置→warmup→加压采集→统计→对照→报告\n   (可控)(稳态)(埋点)(分位)(清缓存)(JSON)",
  "explanationFocus": "是什么：评测脚本设计是把『负载生成、流式埋点、异常处理、分位统计、对照与报告』组织成一条可复现流水线的工程方法，目标是让 LLM 服务的性能结论（吞吐、时延分位、达标率）可信、可比、可回归。它强调的是『可控、可复现、可对照』，而非跑一次看个平均数。",
  "quickAnswer": "严谨脚本应包含 7 模块：①可配置负载（模型/并发/输入输出长度分布/时长）②带 warmup 的阶段控制③流式客户端逐 token 埋点（TTFT/TPOT）④错误与超时处理⑤分位统计与达标率（p50/p95/p99）⑥清缓存对照与随机样本（seed 固定）⑦结构化报告（JSON/CSV）。核心是『可控、可复现、可对照』。",
  "beginnerSummary": "评测脚本像一套标准体检流程：先填基本信息（配置：测谁、多大并发、多长文本），热身（warmup 让系统进稳态），逐项检查并打点（流式埋点记首字延迟、每字间隔），最后出结构化报告。流程固定了，不同人、不同时间去测才有可比性，不会出现『我测的快、你测的慢』的口水仗。",
  "walkthrough": "脚本读 config.yaml：并发档位 [1, 16, 64] × 输出长度 [256, 1024] token，warmup=50 请求让系统进稳态，正式跑 rounds=3 轮每轮 200 请求。流式客户端记录每个请求 TTFT（首 token 延迟）与 TPOT（后续每 token 间隔），结束后输出报告含 p50/p95/p99 TTFT、平均 TPS、错误率与达标率（SLA: p99 TTFT<2s）。",
  "approach": "按『配置 → warmup → 加压采集 → 统计 → 对照 → 报告』流水线组织，所有参数外置到 config、结果可重跑；对照实验只改一个变量（如只换并发），其余（模型版本、seed、硬件）锁死。",
  "bruteForce": "临时拼个脚本跑一次：并发写死、长度随机、无 warmup、只打印均值，结论既不可复现也不可与其他配置对比，CI 里一跑就因环境不同翻车。",
  "invariant": "相同配置（模型/负载/seed/硬件）多次运行应得一致指标（在合理抖动内）；对照实验之间只差一个变量，否则分不清是谁导致差异。",
  "complexity": "主循环复杂度 O(并发 × 轮次 × 平均 token 数)：需并发发起请求并逐 token 计时；统计是 O(样本数) 排序算分位。整体受限于服务端吞吐，客户端打点开销可忽略（微秒级）。",
  "derivation": [
    "为什么需要：手工跑不可控、易踩陷阱（冷启动、缓存、随机负载），脚本化才能标准化、可对比、可放进 CI 做回归，避免『凭感觉说快』。",
    "怎么实现：配置驱动并发与长度分布；async 客户端做流式计时（TTFT/TPOT）；集中收集样本；处理异常/超时/重试与资源清理；最后算分位与达标率并出 JSON/CSV。",
    "有什么代价：要处理异常、超时、重试与资源泄漏；多框架/多配置组合使脚本变复杂；warmup 与对照设计不当会误导结论。",
    "怎么评测：用同一脚本对不同目标跑出可比报告，支持 CI 回归——新版本合入后自动比对新老指标，p99 劣化超阈值就报警。"
  ],
  "edgeCases": [
    "异常请求（服务端 500/超时）需计入错误率而非让脚本崩，否则统计样本缺失、结论偏乐观。",
    "配置项缺失要有默认值与校验，避免『undefined 并发』导致脚本跑飞。",
    "多轮对话评测需维持上下文（把上一轮输出作为下一轮输入），否则测的是单轮而非真实多轮。",
    "服务端有缓存时，重复 prompt 会被秒回，必须在对照实验前清缓存，否则虚低时延。"
  ],
  "pitfalls": [
    "参数写死在脚本里，换负载要改代码，不可复现、易改错。",
    "异常/超时未处理，个别请求挂起拖垮整体或崩溃，统计不完整。",
    "只看均值不看分位（p99），结果平均很快但长尾用户卡死，体验崩。"
  ],
  "prerequisites": [
    "异步压测客户端（如 aiohttp/httpx 异步）：能并发发请求并流式读 token。",
    "分位统计知识：理解 p50/p95/p99 与均值差异，以及达标率概念。",
    "配置驱动思想：把可变参数外置，懂得『只改一个变量做对照』。",
    "了解 TTFT/TPOT/TPS 等 LLM 服务指标含义。"
  ],
  "workedExample": [
    "config 驱动并发×长度扫描：并发 [1,16,64] × 输出 [256,1024]，自动产出 6 组对比数据。",
    "输出 JSON 报告含 p99 TTFT=1.8s、TPS=42、错误率 0.2%、达标率 99.1%，可直接进 CI 比对。",
    "对照实验：清缓存后重跑同一配置，p99 从 1.8s 变 2.4s，暴露缓存虚低问题。"
  ],
  "lineByLine": [
    "读取配置（负载/SLA）：从 config 拿并发、长度分布、轮次、达标阈值，全程不写死。",
    "warmup 后正式加压：先发 50 个预热请求让系统进稳态，再开始计指标。",
    "流式埋点收集样本：每个请求记 TTFT、TPOT、是否错误，append 到 samples。",
    "统计分位并出报告：对 samples 排序算 p50/p95/p99 与达标率，写成 JSON/CSV。"
  ],
  "codeNotes": [
    "参数全部外置到 config，保证可复现、可对比、可回归。",
    "异常不要中断整体统计，计入错误率，否则样本缺失结论失真。",
    "warmup 与清缓存是『稳态/对照』的关键，漏了结论不可信。"
  ],
  "followUps": [
    {
      "question": "为什么非要结构化报告（JSON/CSV）？",
      "answer": "因为结构化输出才能被程序聚合、跨配置对比、接入 CI 做回归。人读日志容易漏看某次 p99 劣化；而 JSON 报告能被脚本自动比对新老版本指标，p99 涨超阈值就自动报警。CSV 也方便画时间序列、贴到文档。一句话：让人看的报告会出错，让机器比的报告才稳。"
    },
    {
      "question": "怎么保证评测可复现？",
      "answer": "四件套锁死：①固定随机种子（seed），让随机负载/采样可重放；②固定负载分布（输入长度、并发曲线写进 config）；③固定硬件与模型版本（记录环境元数据到报告头）；④清缓存 + 固定 warmup 让系统进同一稳态。这样同样配置多次跑，指标应在合理抖动内一致，否则就是实验设计有问题。"
    }
  ],
  "followUpAnswers": [
    "因为结构化输出才能被程序聚合、跨配置对比、接入 CI 做回归。人读日志容易漏看某次 p99 劣化；而 JSON 报告能被脚本自动比对新老版本指标，p99 涨超阈值就自动报警。CSV 也方便画时间序列、贴到文档。一句话：让人看的报告会出错，让机器比的报告才稳。",
    "四件套锁死：①固定随机种子（seed），让随机负载/采样可重放；②固定负载分布（输入长度、并发曲线写进 config）；③固定硬件与模型版本（记录环境元数据到报告头）；④清缓存 + 固定 warmup 让系统进同一稳态。这样同样配置多次跑，指标应在合理抖动内一致，否则就是实验设计有问题。"
  ]
};

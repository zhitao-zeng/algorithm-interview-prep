export default {
  "kind": "concept",
  "id": "perf-framework-bench",
  "category": "服务性能评测",
  "difficulty": "Hard",
  "title": "不同推理框架基准对比（vLLM / TRT-LLM / SGLang）",
  "prompt": "如何在 vLLM、TRT-LLM、SGLang 之间做公平的推理性能基准对比？",
  "quickAnswer": "公平对比需固定同一模型/精度/硬件/负载（输入输出长度分布、并发数、batch 上限），在各自默认且合理的优化配置下测 TTFT、TPS、显存峰值与单位 token 成本，并开启连续批处理与相应的注意力后端。结论会随负载形态（长文本还是短请求、是否大量复用前缀/KV）明显变化，没有绝对赢家，必须贴合自身真实流量来选。建议以 p99 延迟和饱和吞吐双指标看，而非只看均值，并用单位成本（元/百万 token）做商务侧权衡。",
  "approach": "统一硬件与负载，分框架逐一部署并开启各自的推荐优化配置，控制变量只留\"框架\"差异，跑同一套压测脚本采集多维指标与成本，最后按自身流量画像给出选型。具体步骤：①固定基座模型（如 7B/70B FP16 或 INT8）、同一张卡（如 A100 80G）、同一权重；②固定负载生成器（输入/输出长度分布、并发阶梯 1→64→256）；③每个框架按官方文档开启连续批处理、对应注意力后端与量化；④采集 TTFT p50/p99、端到端 TPS、显存峰值、最大稳定并发；⑤换算单位成本并标注负载特征。",
  "explanationFocus": "是什么：框架基准对比是在严格控制变量下，对 vLLM、TRT-LLM、SGLang 等推理引擎测延迟、吞吐、显存占用与单位推理成本，给出贴合自身流量的选型建议。三者优化路径不同：vLLM 主打 PagedAttention 显存高效、TRT-LLM 主打编译图与内核级极致优化、SGLang 主打 RadixAttention 前缀复用，因此没有放之四海皆准的\"最快框架\"。公平对比的关键不是跑一个官方脚本，而是把模型、精度、硬件、负载形态全部钉死，只留\"框架\"这一变量。",
  "bruteForce": "只比官方宣传数字或跑一次默认配置：不同框架宣传时的模型、卡型、并发、序列长度完全不同，数字不可比，极易误选。更糟的是用\"单并发、长输出\"这种偏态负载做唯一裁决，会掩盖框架在真实高并发短请求下的显存碎片与调度差异，上线后 p99 爆炸。",
  "derivation": [
    "为什么需要：各框架优化点不同（vLLM 的分页注意力省显存、TRT-LLM 的编译图压内核、SGLang 的前缀复用降 prefill），官方数字来自各自最有利负载，必须针对自身真实流量实测才能决策。直接沿用宣传指标等于把选型交给别人设定的场景。",
    "怎么实现：同一模型、同一张卡、同一精度，固定并发与长度分布，依次部署三框架并开启各自推荐配置，跑统一压测脚本采集 TTFT/TPS/显存/成本。负载生成器要可复现，建议用固定随机种子生成请求序列，并对每个并发点采样 p50/p99。",
    "有什么代价：每框架的调优与编译成本高，TRT-LLM 需编译耗时且配置更繁琐，SGLang 在前缀复用场景占优但通用短请求未必最佳。人力与时间投入不小，且框架版本升级会改变特性，结论有保质期。",
    "怎么评测：报告 p99 TTFT、饱和 TPS、最大稳定并发、显存峰值与单位成本，并明确标注负载特征（序列长度、前缀复用率、并发）。用 Pareto 思维看\"延迟-成本\"权衡，而非单一指标冠军。"
  ],
  "invariant": "在严格控制变量的前提下，指标差异只来自框架实现；同一框架在不同负载形态（前缀复用比例、序列长度、并发）下排名可能变化。因此任何\"XX 框架最快\"的结论都必须附带负载特征，否则不可外推。",
  "walkthrough": "同 7B fp16、单卡 A100 80G，固定输入 512 / 输出 128 的短请求、并发 64 的压测下：vLLM TPS≈520、SGLang≈560、TRT-LLM≈600，三者差距约 15%。切换为\"多轮对话 + 共享系统提示\"负载（前缀复用率约 60%）、并发 100 时，SGLang 凭借 RadixAttention 把 prefill 显存与时间砍掉近 40%，TPS 反超到 720，TRT-LLM 因未做前缀复用为 610、vLLM 约 580。可见排名随负载翻转，选型必须绑定流量画像。",
  "edgeCases": [
    "TRT-LLM 编译时间长（大模型数十分钟）严重影响迭代节奏，需预编译或缓存构建产物。",
    "前缀/系统提示大量复用场景（多轮对话、Few-shot）下 SGLang 的 RadixAttention 占优，与短请求结论可能反转。",
    "量化支持度各框架不同（如某版本 TRT-LLM 对 FP8 更成熟、vLLM 对 AWQ 更顺），量化开关会改变排名。",
    "长上下文（32K+）下显存管理策略差异放大，vLLM 的 PagedAttention 与 SGLang 的分页 radix 表现分化。"
  ],
  "code": "# Python\ndef bench_matrix(frameworks, load, run):\n    return {f: run(f, load) for f in frameworks}   # {框架: 指标}\ndef rank_by(metric_dict, key):\n    return sorted(metric_dict, key=lambda f: -metric_dict[f][key])",
  "codeNotes": [
    "务必固定硬件/模型/负载，否则 bench_matrix 产出的字典毫无可比性——对比的前提是控制变量。",
    "rank_by 只做单一指标排序，真实选型应多指标加权（延迟权重 + 成本权重），且标注负载特征，避免误导。"
  ],
  "complexity": "时间上是 O(框架数 × 压测轮次 × 并发阶梯) 的重复跑，每轮次需等系统稳定再采样 3~5 分钟以减少抖动；TRT-LLM 还需额外 O(编译) 的一次性成本（大模型可达数十分钟到小时级）。空间上主要是各框架的权重副本与日志，可控。真正成本是人的调优时间与等待，建议用脚本自动化阶梯压测。",
  "followUps": [
    {
      "question": "SGLang 为什么有时更快？",
      "answer": "SGLang 的 RadixAttention 把请求间共享的前缀 KV 以基数树（radix tree）缓存复用，多轮对话、共享系统提示、Few-shot 等前缀复用率高的场景下，能显著削减重复 prefill 的计算与显存，从而提升吞吐并降低延迟。但若是彼此无关的独立短请求（无前缀可复用），这一优势基本消失，排名可能回归到 vLLM/TRT-LLM 的水平。"
    },
    {
      "question": "TRT-LLM 适合什么场景？",
      "answer": "TRT-LLM 在固定模型、能接受一次编译成本、追求极致延迟与吞吐的生产环境最划算，尤其适合大 batch、稳定负载、长周期服务的场景，其编译图与定制内核能把单卡算力压到很高。代价是编译耗时、配置相对繁琐、模型迭代慢；若负载多变、模型频繁更换，编译与调优成本会吃掉收益。"
    }
  ],
  "followUpAnswers": [
    "SGLang 的 RadixAttention 把请求间共享的前缀 KV 以基数树（radix tree）缓存复用，多轮对话、共享系统提示、Few-shot 等前缀复用率高的场景下，能显著削减重复 prefill 的计算与显存，从而提升吞吐并降低延迟。但若是彼此无关的独立短请求（无前缀可复用），这一优势基本消失，排名可能回归到 vLLM/TRT-LLM 的水平。",
    "TRT-LLM 在固定模型、能接受一次编译成本、追求极致延迟与吞吐的生产环境最划算，尤其适合大 batch、稳定负载、长周期服务的场景，其编译图与定制内核能把单卡算力压到很高。代价是编译耗时、配置相对繁琐、模型迭代慢；若负载多变、模型频繁更换，编译与调优成本会吃掉收益。"
  ],
  "pitfalls": [
    "变量未控制（不同模型/卡/精度/并发）导致数字不可比，最常见是拿 A100 上的 vLLM 数字比 H100 上的 TRT-LLM。",
    "脱离自身负载下结论，比如用单并发长输出压测选了 TRT-LLM，上线却是高并发短请求，p99 反而不如预期。",
    "只看均值吞吐忽略 p99 与显存峰值，生产以尾延迟和 OOM 风险为准。"
  ],
  "beginnerSummary": "租车对比：vLLM、SGLang、TRT-LLM 就像三款车。公平的比法是同一条路（同负载：同样的上下坡、同样的载重）、同一位司机（同硬件、同模型）、同样满油策略（同精度），再比油耗（显存）和速度（吞吐）。但某款车在盘山道快，不代表在平原高速也快——关键在于你常跑哪条路。所以测之前先想清楚自己流量是\"短平快\"还是\"长文本 + 多轮复用\"，再选适合的框架。",
  "prerequisites": [
    "连续批处理（continuous batching）：理解为什么它比静态 batch 更能榨干 GPU，是对比吞吐的前提。",
    "分页注意力 / 前缀注意力（PagedAttention / RadixAttention）：理解显存管理与前缀复用的机制，才能解释框架差异。",
    "控制变量实验法：能设计可复现负载、固定随机种子、对并发阶梯采样，是得出可信结论的基础。",
    "推理关键指标（TTFT/TPS/p99/显存峰值）：知道每个指标代表什么，才不会只看\"快不快\"。"
  ],
  "workedExample": [
    "场景 A（短请求高并发）：7B FP16、A100、输入 512/输出 128、并发 64。vLLM TPS≈520、SGLang≈560、TRT-LLM≈600；选型看饱和吞吐与单位成本，TRT-LLM 略优但需编译投入。",
    "场景 B（多轮前缀复用）：70B、并发 100、系统提示复用率 60%。SGLang 借 RadixAttention 把 prefill 降约 40%，TPS 720 反超 TRT-LLM 的 610；说明结论随负载翻转，须绑定流量。"
  ],
  "lineByLine": [
    "固定模型 / 硬件 / 精度 / 负载：把除框架外一切变量钉死，确保差异只来自框架本身。",
    "逐框架部署并开启推荐优化配置（连续批处理、对应注意力后端、量化）：用各框架在目标负载下的\"合理最优\"而非裸默认。",
    "跑统一压测脚本采集 TTFT/TPS/显存/成本，并对每个并发点采样 p50/p99：用脚本化阶梯压测减少人为抖动。",
    "多维对比（延迟、吞吐、显存、单位成本）并结合自身流量画像给选型：不追单一冠军，看 Pareto 与真实场景匹配度。"
  ],
  "diagram": "同负载 ──▶ [vLLM][TRT-LLM][SGLang]\n               延迟 吞吐 显存 成本\n               └─▶ 按自身流量选型"
};

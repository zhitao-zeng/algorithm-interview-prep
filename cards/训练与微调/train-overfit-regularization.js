export default {
  "kind": "concept",
  "id": "train-overfit-regularization",
  "category": "训练与微调",
  "difficulty": "Easy",
  "title": "微调过拟合与正则（早停、dropout、权重衰减）",
  "prompt": "微调时如何防止过拟合，常用正则手段有哪些？",
  "quickAnswer": "防过拟合核心是约束可训空间与早停：用早停（监控验证损失拐点）、dropout（随机置零防神经元共适应）、权重衰减 AdamW 的 weight_decay（L2 式约束权重规模），以及参数高效方法 LoRA 本身限制可训参数来抗过拟合。小数据场景优先 LoRA + 轻正则，全量微调则必须配早停与 dropout，否则极易死记训练样本。",
  "approach": "监控验证集早停；全微调时叠加 dropout 与 AdamW 的权重衰减；小数据优先用 LoRA 冻结主体、只训低秩适配，从源头降过拟合风险。实操上先用强正则 + 早停拿到基线，再逐步放松看验证集反应，避免一上来就暴力全微调。",
  "explanationFocus": "是什么：微调过拟合指模型在训练集上把噪声也拟合进去，导致验证/泛化指标下降；常用正则手段包括早停（监控验证损失、回升即停）、dropout（训练时随机置零部分神经元防共适应）、权重衰减（L2 约束权重规模），以及用 LoRA 这类参数高效方法从源头限制可训容量。本质上都是在\"拟合训练数据\"和\"保持泛化\"之间找平衡。",
  "bruteForce": "小数据（如几百条）上全量微调多 epoch、不加任何正则与早停：模型很快把训练样本逐字记住，训练损失趋近 0，但验证损失先降后飙升、下游泛化崩塌，且对训练集里的标注噪声也照单全收。",
  "derivation": [
    "为什么需要：微调数据常远小于预训练语料（可能差几个数量级），全量更新整网极易过拟合，需要用正则约束可训空间、保留泛化。",
    "怎么实现：早停用验证损失拐点触发（patience 容忍短暂波动）；dropout 设 p=0.05~0.1 随机置零；AdamW 设 weight_decay（如 0.01）解耦权重衰减；或只用 LoRA 冻结主体、只训低秩矩阵。",
    "有什么代价：正则过强会欠拟合（dropout 太大学不动、WD 太高梯度被压死）；早停需要干净可靠的验证集，否则拐点误判；LoRA 容量有限，极难任务可能欠拟合。",
    "怎么评测：画训练/验证损失曲线看两者差距（差距拉大即过拟合信号）与早停点，再在独立测试集与下游任务实测泛化，而非只看训练损失。"
  ],
  "invariant": "小数据首选 LoRA + 轻正则；早停看验证集拐点。注意部分基座已内含 dropout，重复叠加需谨慎；权重衰减对 LoRA 的 A/B 矩阵通常不设或设很小，避免压制本就稀薄的可训信号。",
  "walkthrough": "1K 条指令微调：全量微调 5 个 epoch，训练损失降到 0.2 但验证损失在第 3 epoch 后从 0.9 升到 1.5（典型过拟合），改用 LoRA（r=16，只训约 0.5% 参数）+ 早停（patience=3，第 3 epoch 触发），验证损失稳定在 0.9 且下游任务泛化最佳。相比全微调，显存占用从 24G 降到约 9G。",
  "edgeCases": [
    "验证集与训练同源/泄露：早停会误判\"还没过拟合\"，上线即翻车，需保证验证集独立分布。",
    "基座已含 dropout，重复加需注意：可能叠加过量导致欠拟合，应核对基座配置。",
    "WD 对 LoRA 参数通常不设或很小：LoRA 参数量本就少，过大的衰减反而抑制学习。",
    "类别极不均衡的小数据集：过拟合会先发生在少数类，需结合采样/加权。"
  ],
  "code": "def should_stop(train_loss, val_loss, best, patience=3):\n    if val_loss < best:\n        return False, val_loss  # 更新最优\n    patience -= 1\n    return patience <= 0, best",
  "codeNotes": [
    "早停需记录最优 val_loss 与计数：真正回滚到最优权重，而非停在最后一步。",
    "patience 防止短暂波动误停：但 patience 过大等于晚停，仍会过拟合，需按验证稳定性调。",
    "建议把 early-stop 与 checkpoint 联动，停止时加载 best checkpoint。"
  ],
  "complexity": "早停判定 O(1) 无训练开销；dropout 在训练时引入微小随机化开销（可忽略）；LoRA 把可训参数量从全量（如 7B）降到约 r×(d_in+d_out) 量级（r=16 时百万级），训练显存与过拟合风险同步下降。整体正则成本是\"免费或负成本\"（省显存）。",
  "followUps": [
    {
      "question": "LoRA 本身为什么抗过拟合？",
      "answer": "LoRA 冻结预训练主体权重，只训练注入的低秩矩阵（A、B），可训参数量极少（常仅占全量 0.1%~1%）。假设空间因此被严格限制，模型无法大幅偏离基座、难以逐字死记小训练集，天然降低过拟合。同时冻结部分保留了预训练学到的通用知识，相当于自带强先验。代价是容量有限，极难任务可能欠拟合。"
    },
    {
      "question": "权重衰减和 L2 正则完全一样吗？",
      "answer": "在纯 SGD 优化下，AdamW 式的权重衰减等价于 L2 正则（都给梯度加 −λw）。但 AdamW 把权重衰减从梯度动量中解耦出来，直接对参数做 λw 的衰减，而朴素 L2 会把衰减项混进动量/二阶矩估计，导致衰减强度随自适应学习率被扭曲。实践中 AdamW 的衰减更稳、对大学习率更鲁棒，效果优于把 L2 直接写进损失。"
    }
  ],
  "followUpAnswers": [
    "LoRA 冻结预训练主体权重，只训练注入的低秩矩阵（A、B），可训参数量极少（常仅占全量 0.1%~1%）。假设空间因此被严格限制，模型无法大幅偏离基座、难以逐字死记小训练集，天然降低过拟合。同时冻结部分保留了预训练学到的通用知识，相当于自带强先验。代价是容量有限，极难任务可能欠拟合。",
    "在纯 SGD 优化下，AdamW 式的权重衰减等价于 L2 正则（都给梯度加 −λw）。但 AdamW 把权重衰减从梯度动量中解耦出来，直接对参数做 λw 的衰减，而朴素 L2 会把衰减项混进动量/二阶矩估计，导致衰减强度随自适应学习率被扭曲。实践中 AdamW 的衰减更稳、对大学习率更鲁棒，效果优于把 L2 直接写进损失。"
  ],
  "pitfalls": [
    "早停无干净验证集导致误判：用训练集子集当验证，等于没早停，过拟合照常发生。",
    "对 LoRA 参数设过大 WD 反而抑制学习：误以为\"正则越强越好\"，实则该处信号本就稀薄。",
    "只看训练损失下降就以为训好了，忽视验证曲线。"
  ],
  "beginnerSummary": "微调过拟合像\"背题不解题\"：把例题答案死记硬背，考试换道题就懵。早停是\"见好就收\"——验证分开始掉头就停，别贪心多练；dropout 是\"随机让部分神经元请假\"，逼大家别老抱团作弊（共适应）；权重衰减是\"给参数戴紧箍咒\"，别让某些权重长得太野太极端。LoRA 则是\"只改薄薄一页笔记\"而非重写整本书，改得少自然不易跑偏。",
  "prerequisites": [
    "过拟合与泛化：理解偏差-方差权衡，知道\"训练好\"不等于\"泛化好\"。",
    "Dropout：理解其训练随机置零、推理恢复的机理与防共适应作用。",
    "权重衰减 / AdamW：理解 L2 与解耦权重衰减的区别及其对优化的影响。",
    "参数高效微调（LoRA）：理解低秩适配如何冻结主体、限制容量。"
  ],
  "workedExample": [
    "在 1K 数据上全微调 5 epoch：训练损失 0.2 但验证升到 1.5，差距拉大，过拟合信号明确。",
    "改 LoRA(r=16) + 早停(patience=3)：验证稳定在 0.9，显存从 24G 降到 9G，泛化最佳。",
    "dropout=0.1 时验证损失比 dropout=0.5 更低，说明后者过抑制。"
  ],
  "lineByLine": [
    "def should_stop(...)：早停判定函数，输入当前训练/验证损失与历史最优。",
    "if val_loss < best：验证创新低则继续训练并更新最优值，返回 False（不停止）。",
    "patience -= 1：否则消耗一次容忍额度，容忍短暂波动避免误停。",
    "return patience <= 0, best：额度耗尽即停，返回最优 val_loss 用于回滚权重。"
  ],
  "diagram": "epoch: 训练↓ 验证↓→↑(过拟合) 早停在拐点"
};

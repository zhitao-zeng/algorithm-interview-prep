export default {
  "kind": "concept",
  "id": "train-lora-principle",
  "category": "训练与微调",
  "difficulty": "Medium",
  "title": "LoRA 原理（低秩分解 ΔW=BA）",
  "prompt": "LoRA 如何用低秩分解实现高效微调？",
  "quickAnswer": "LoRA 的做法是：冻结原始权重 W0，旁路注入一对可训练的低秩矩阵 A、B，前向时 y = W0·x + B·A·x，仅 A、B 参与梯度更新。以 d=k=4096、r=16 为例，全量微调需更新 1678 万参数，而 LoRA 只需训练 4096×16 + 16×4096 = 13.1 万参数，约为原规模的 1/128。由于 B 初始化为 0，训练起点处 ΔW=0，模型行为与基座完全一致、训练稳定；部署阶段可把 B·A 合并进 W0，因此推理延迟为零额外开销。",
  "approach": "标准实现分三步：① 选层——对目标线性层（通常是注意力的 q_proj、v_proj，强适配再加 k_proj、o_proj 与 MLP）旁路加 LoRA；② 注入——权重 W0 冻结，新建 A~N(0,σ²)（σ 常用 0.01）、B=0；前向计算 y = W0·x + scaling·(B·A·x)，其中 scaling=α/r 控制增量幅度，α 为超参；③ 训练——反向时只有 A、B 的梯度非零，优化器只更新这两块。由于 r≪d，参数量从 dk 降至 r(d+k)，显存里的优化器状态（通常是参数的 12 倍于 fp32 训练）也同步缩减，这是 LoRA 省资源的关键。",
  "explanationFocus": "是什么：LoRA（Low-Rank Adaptation，低秩适配）的核心假设是——大模型在下游任务上微调时，权重的更新量 ΔW 具有很低的\"内在秩\"（intrinsic rank），因此可拆成两个小矩阵的乘积 ΔW = B·A，其中 B∈ℝ^{d×r}、A∈ℝ^{r×k}，且 r ≪ min(d,k)。训练时把原始预训练权重 W0 冻结（requires_grad=False），只让 A、B 接收梯度，从而把可训练参数量从 d×k 降到 r×(d+k)。这样既大幅节省显存与存储，又能在部署时把 B·A 直接加回 W0 合并成一个权重矩阵，使推理路径与原模型完全一致、没有任何额外延迟。",
  "bruteForce": "朴素做法是全量微调（Full Fine-Tuning）：解冻全部参数，每个参数都需存储梯度与 Adam 的两份动量（m、v），因此优化器状态约为参数本身的 12 倍。以一个 7B 模型为例，参数约 14 GB（fp16），优化器状态约 84 GB（fp32 动量），梯度约 14 GB，合计远超单张 80 GB 卡的可用显存，必须多卡或 ZeRO 切分。多任务场景下更糟：每个任务都要保存一整份微调后的完整权重副本（7B 即 14 GB/任务），十几个任务就是上百 GB 的冗余存储，而 LoRA 把每份任务权重缩减为 13 万～数百万参数的 A、B，多任务部署极为轻量。",
  "derivation": [
    "为什么需要：大模型全量微调的参数量与显存占用都过于庞大——7B 模型全微调仅优化器状态就需数十 GB，70B 级几乎无法在单卡完成；且每来一个下游任务就要保存一份完整权重副本，多任务场景下存储与切换成本极高。LoRA 用低秩假设把\"任务差异\"压缩成极小矩阵，让单卡也能微调、多任务只需存小增量，这是它在工程上流行的最直接动机。",
    "怎么实现：把 W0 冻结（requires_grad=False），在其旁路注入 A~N(0,σ)、B=0（保证初始 ΔW=0，模型行为不变）。前向叠加 BA 分支：y = W0·x + α/r · B·A·x，反向时梯度只经 A、B 流动。缩放因子 α/r 用来平衡新增分支的相对幅度，α 通常与 r 联动调节，避免改 r 就得重调学习率。",
    "有什么代价：首先是\"低秩假设不总成立\"——当任务需要的适配信息维度很高时，过小的 r 会成为容量瓶颈，部分任务必须放大 r 甚至回退全量微调才能达标。其次是层选择敏感，注入层（通常 q、v 投影）选错会欠适配。此外合并虽无损，但若训练用了 scaling，合并需把 α/r 折进 B，否则部署数值会漂移。",
    "怎么评测：在下游基准（如 GLUE、MMLU、特定业务指标）上与全量微调对比，关注\"可训练参数占比\"与\"精度差距\"两项。同时要验证合并后推理输出与训练期输出逐位一致（否则合并公式有误）。工程上还看显存峰值、吞吐与单卡能否跑下，作为是否采用 LoRA 的决策依据。"
  ],
  "invariant": "三条不变量：① B 初始化为 0，确保训练第一步 ΔW=B·A=0，模型输出与冻结的基座严格一致，避免随机扰动破坏已学能力；② 整个训练过程中 W0 始终 requires_grad=False，所有梯度只流向 A、B，否则退化成全量微调、失去省参意义；③ 秩 r 须远小于 d、k（实践 r=8/16/32/64），并应二次核对\"注入哪些层、r 取多大\"——错误的层选择或失衡的 r 会导致欠适配或浪费。",
  "walkthrough": "具体算一笔账：对某个注意力投影层 W0 形状 4096×4096（约 1678 万参数）。全量微调要把这 1678 万参数都更新，且 Adam 优化器还需为它们各存 2 份动量（m、v）和 1 份梯度，仅该层的优化器状态就约 1678 万 × (4+4+4) 字节 ≈ 204 MB。改用 LoRA（r=16）：A 形状 16×4096、B 形状 4096×16，共 13.1 万可训练参数，仅全量的约 1/128；若该层原需 204 MB 优化器状态，现可下降一个数量级以上。初始化时令 B=0，则任意输入 x 都有 B·A·x = 0，前向输出 y = W0·x 与基座逐位相等，保证训练从\"原模型\"出发。",
  "edgeCases": [
    "r 取值失衡：r 过小（如 1~2）容量严重不足，复杂任务学不动；r 过大（如接近 d）则参数效率退化、接近全量微调，失去 LoRA 意义且更占显存。",
    "B 初始化非 0：若误把 B 初始化成随机非零，则初始 ΔW=B·A≠0，前向输出从第一步起就偏离基座，训练初期不稳定、可能破坏已学能力。",
    "注入层不全或选错：只注入 q 而忽略 v，或把 LoRA 加在 LayerNorm/embedding 上往往收效甚微；反之无差别注入所有层会增加不必要的参数与过拟合风险。",
    "合并公式错误：部署时若忘记把 scaling=α/r 折进 B（应 B' = B·α/r），合并后的权重与原训练输出不一致，导致精度掉点。"
  ],
  "code": "class LoRALinear:\n    def __init__(self, weight, r=16):\n        self.W = weight.requires_grad_(False)   # 冻结\n        self.A = torch.randn(r, weight.shape[1])\n        self.B = torch.zeros(weight.shape[0], r) # 初始 ΔW=0\n    def forward(self, x):\n        return x @ self.W.T + (x @ self.A.T) @ self.B.T",
  "codeNotes": [
    "B 置零是 LoRA 收敛稳定的关键：它保证训练第 1 步的输出严格等于原基座模型，模型从\"已学会的起点\"出发逐步学增量，避免随机扰动破坏既有能力。",
    "草图中缺少 scaling=α/r：真实实现（如 HuggingFace PEFT）会在 BA 分支乘上 α/r，α 是可调超参，用于解耦\"秩 r\"与\"增量幅度\"，使调 r 时不必同时重调学习率。",
    "批量与前向形状：实际推理 x 多为 (batch, seq, hidden)，需相应处理矩阵维度；合并部署时执行 W_merged = W0 + (α/r)·B·A 后再丢弃 A、B。"
  ],
  "complexity": "训练期可训练参数量为 O(r·(d+k))，相比全量的 O(d·k) 当 r≪min(d,k) 时下降约 d/r 倍（d=k=4096、r=16 即约 1/128）。前向计算只额外增加一次形状 (batch, seq, r)↔(batch, seq, d) 的瓶颈矩阵乘，计算量约为原前向的 r/d 比例，开销极小。存储上每个任务只需保存 A、B（如 13 万参数 ≈ 0.5 MB/fp16），多任务可近乎零成本共存。注意反向传播仍需对冻结的 W0 保存或重算激活，故激活显存与全量微调相当，真正省的只是\"参数+优化器状态\"这部分。",
  "followUps": [
    {
      "question": "为什么 B 要初始化为 0 而 A 用随机初始化？",
      "answer": "B=0 使得起点处 ΔW=B·A=0，模型输出与冻结的基座严格一致，从而从\"已学会的状态\"平稳出发、避免随机增量破坏既有能力；A 用随机正态是为了打破对称、让增量方向可在训练中自由演化。若把 A 也置零，则 B·A 始终是 0、学不到任何东西（对称死区）。"
    },
    {
      "question": "LoRA 一般注入哪些层，r 怎么选？",
      "answer": "实践中最常注入注意力里的 q_proj 与 v_proj（经验上这两个投影的适配收益最高），强适配可加 k_proj、o_proj 以及 MLP 的各线性投影；r 通常取 8/16/32/64，数据或任务越复杂越大，但要二次核对——r 太小容量不足，r 太大失去参数效率且更占显存。也可用秩自适应方法（如 DoRA、AdaLoRA）动态调整每层的有效秩。"
    },
    {
      "question": "LoRA 微调后如何部署，合并会不会有损？",
      "answer": "合并是无损的：把 W' = W0 + α/r·B·A 直接写回原权重矩阵即可，推理时网络结构与原模型完全一致、零额外延迟。注意必须正确把缩放因子 α/r 折进 B（即 B' = (α/r)·B），否则合并后数值与原训练输出不一致会掉点；合并后 A、B 可丢弃，只保留一个合并权重，多任务则各存一份极小的 A、B。"
    }
  ],
  "followUpAnswers": [
    "B=0 使得起点处 ΔW=B·A=0，模型输出与冻结的基座严格一致，从而从\"已学会的状态\"平稳出发、避免随机增量破坏既有能力；A 用随机正态是为了打破对称、让增量方向可在训练中自由演化。若把 A 也置零，则 B·A 始终是 0、学不到任何东西（对称死区）。",
    "实践中最常注入注意力里的 q_proj 与 v_proj（经验上这两个投影的适配收益最高），强适配可加 k_proj、o_proj 以及 MLP 的各线性投影；r 通常取 8/16/32/64，数据或任务越复杂越大，但要二次核对——r 太小容量不足，r 太大失去参数效率且更占显存。也可用秩自适应方法（如 DoRA、AdaLoRA）动态调整每层的有效秩。",
    "合并是无损的：把 W' = W0 + α/r·B·A 直接写回原权重矩阵即可，推理时网络结构与原模型完全一致、零额外延迟。注意必须正确把缩放因子 α/r 折进 B（即 B' = (α/r)·B），否则合并后数值与原训练输出不一致会掉点；合并后 A、B 可丢弃，只保留一个合并权重，多任务则各存一份极小的 A、B。"
  ],
  "pitfalls": [
    "误把 W0 也设为可训练：一旦 W0.requires_grad 为 True，就退化成全量微调，优化器状态与显存暴涨，LoRA 的省参省显存优势荡然无存，却还背着两套权重。",
    "缺失 A/B 缩放（scaling=α/r）：不用缩放时，ΔW 幅度由随机初始化 σ 决定，可能过大淹没原输出或过小学不动，训练难收敛；α 应与 r 协同调参。",
    "把 LoRA 当万能：对需要大幅改写知识的任务（如学全新语言/领域），低秩增量可能容量不够，应先用实验确认 r 是否够用，必要时回退全量或加大 r。"
  ],
  "beginnerSummary": "LoRA 就像给已经训练好、脑子很大的大模型装一个\"轻量小外挂\"：原本的脑子（权重 W0）完全冻住不动，只在旁边接两块薄薄的小矩阵 A、B，它们拼起来专门表达\"这次任务相对原模型要做的那点改动\"。因为改动通常很小、能用很窄（r 很小）的\"纸条\"记下来，所以这两块小矩阵参数量极少。训练时只改这个小外挂，省钱又省显存；训完把小外挂的改动直接并回原脑子，对外看起来还是同一个模型，速度一点不慢。如果不用 LoRA 而全量微调，等于每次任务都要把整本大词典重写一遍，既贵又占地方。",
  "prerequisites": [
    "矩阵乘法与低秩分解：理解 ΔW≈B·A 的含义，知道\"秩\"衡量矩阵信息维度的多少，r 越小表示能用越窄的子空间近似变化。",
    "梯度与优化器状态：明白冻结（requires_grad=False）如何让某参数不接收梯度，以及 Adam 为何需要为可训练参数存 m/v 两份动量，从而理解 LoRA 省显存的根源。",
    "Transformer 注意力层结构：知道 q/k/v/o 投影与 MLP 各是线性层，才能判断\"该把 LoRA 注入哪一层\"。",
    "PyTorch 自动求导基础：会用 nn.Module 的 requires_grad_()、参数分组（把 A、B 单独放进优化器），是落地的工程前提。"
  ],
  "workedExample": [
    "场景：在单张 24 GB 消费卡上微调 7B 模型做领域问答。全量微调需约 112 GB 显存（参数14+优化器84+梯度14），卡根本放不下；改用 LoRA（r=16，仅注入 q/v）后，可训练参数约 0.8%~1%，优化器状态随可训练参数同步缩小，整模型可在 24 GB 内训练。",
    "数值例子：取 W0(4096×4096) 形状，注入 A(16×4096)、B(4096×16)。前向 y = W0·x + α/r·B·A·x，α 取 32、r=16 则 scaling=2。初始 B=0 ⇒ B·A=0 ⇒ y=W0·x，与基座逐位相同；训练 1 步后 B 略更新，ΔW 仍极小，模型平滑偏离原分布。",
    "部署例子：训练完把合并权重 W' = W0 + α/r·B·A 写回，推理时网络结构与原始完全一致，单请求延迟无变化；多任务则各存一份 13 万参数的 A/B（约 0.5 MB），切换仅替换小矩阵。"
  ],
  "lineByLine": [
    "self.W = weight.requires_grad_(False)：显式冻结底座权重 W0，使其不进入计算图的反向梯度，是 LoRA 省参数的根本。",
    "self.A = torch.randn(r, weight.shape[1])：A 用随机正态初始化（含非零信息），其第二维是输入维度 k=weight.shape[1]。",
    "self.B = torch.zeros(weight.shape[0], r)：B 初始化为全 0，保证 B·A 在起点恒为 0，前向输出 ≡ 原模型。",
    "forward 中 return x @ self.W.T + (x @ self.A.T) @ self.B.T：先算原路径 W0·x，再算低秩增量 B·A·x 并相加；实际工程还应乘 scaling=α/r 控制幅度。",
    "注：该草图省略了 scaling 与批量维度处理，落地时需在 B 分支乘以 α/r，并保证 x 形状为 (batch, seq, k)。"
  ],
  "diagram": "x ─▶[W 冻结]─┐\n    └▶[A]▶[B]─┴─▶ + ─▶ y"
};

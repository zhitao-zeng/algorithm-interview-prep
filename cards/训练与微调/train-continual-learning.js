export default {
  "kind": "concept",
  "id": "train-continual-learning",
  "category": "训练与微调",
  "difficulty": "Hard",
  "title": "持续学习策略（replay / EWC / 模型编辑）",
  "prompt": "缓解灾难性遗忘的持续学习策略有哪些？",
  "quickAnswer": "缓解灾难性遗忘主要有三路策略。第一是经验回放(replay)：维护一个旧样本缓冲池，训练新任务时混合少量旧样本，直接「复习」。第二是正则化方法，代表是 EWC，用 Fisher 信息矩阵给重要权重加二次惩罚，抑制其变动。第三是参数隔离或模型编辑（如 ROME），只动与某知识相关的局部参数。实际部署通常按成本与场景组合：回放效果最好但有存储/隐私成本，EWC 轻量但有近似误差，编辑适合点状事实。",
  "approach": "replay 思路是维护一个固定大小的样本缓冲池，每个新任务训练时按一定比例从池中采样旧样本与当前新数据混合；EWC 思路是在损失里加一项 λ·Σ F_i(θ_i-θ*_i)²，F_i 是旧任务下参数 i 的 Fisher 信息对角，越大表示越重要；模型编辑则是先用因果中介分析定位「知识神经元」，再只更新相关参数。三者可叠加：以 replay 为主、EWC 兜底、对明确错误事实用编辑修正。",
  "explanationFocus": "是什么：持续学习是在不重训全部历史的前提下，让模型按顺序学会新任务同时不遗忘旧任务的能力。其核心矛盾是「学新」与「记旧」的权衡：完全只训新任务会灾难性遗忘，而每来一个任务就全量重训又不可行。主流策略分三类——经验回放（保留并重练旧样本）、正则约束（如 EWC 给重要参数加保护）、以及模型编辑（精准修改特定知识），实践中常组合使用。",
  "bruteForce": "最朴素的做法是每来一个新任务，就把模型在所有历史任务数据上从头重训一遍——这在任务流很长、数据量巨大时算力完全不可行，而且历史原始数据往往不能长期保留。另一个极端是只拿新任务数据微调，模型会迅速覆盖旧知识，导致之前任务精度断崖式下跌，也就是灾难性遗忘。这两种极端都不可取，才需要上面那些折中策略。",
  "derivation": [
    "为什么需要：真实业务里任务往往是流式、陆续到来的（例如对话模型要持续接入新领域），我们既无法永远存下全量历史数据，也不可能每来一个任务就全量重训。必须在「学新任务」与「不忘旧任务」之间做权衡，这就是持续学习要解决的问题。",
    "怎么实现：replay 维护一个样本缓冲池，训练新任务时把旧样本和新样本混合；EWC 在损失里加 λ·Σ F_i(θ_i-θ*_i)²，F_i 用旧任务数据估计的 Fisher 信息对角；模型编辑则通过因果定位找到承载某知识的神经元，再只做局部更新（如 ROME 用秩一更新）。",
    "有什么代价：replay 需要存储旧数据，涉及隐私合规与分布偏移，且缓冲池有限时难覆盖长序列；EWC 的 Fisher 是训练末点的一阶近似，跨任务可能失准，超参 λ 需在验证集上仔细调；编辑只适用于局部事实，滥用会牵一发而动全身。",
    "怎么评测：常用「前向迁移」（学新任务时能否借旧知识更快收敛）与「后向迁移」（学新任务后旧任务保持度），以及遗忘率指标；具体看多任务序列跑到末端时各任务精度的保持情况，越接近「全量联合训练」越好。"
  ],
  "invariant": "回放通常效果上限最高但必须存数据且有隐私顾虑；EWC/正则类方法无需存数据但依赖 Fisher 近似、有偏且超参 λ 难调；模型编辑只适合局部、点状的事实性知识，对能力型更新无能为力（建议二次核对各法适用边界再选型）。",
  "walkthrough": "以 EWC 为例，设旧任务收敛参数 θ*，先在旧任务数据上算每个参数的 Fisher 对角 F_i（损失对参数梯度平方的期望，衡量该参数对旧任务的重要性）。新任务训练时损失 = 当前任务损失 + λ·Σ F_i(θ_i-θ*_i)²。假设新旧任务各 1000 条样本、λ=1000，重要权重变动被显著抑制，经验上旧任务掉分可从 40% 降到约 20%（接近减半）。若改用 replay，维护 2000 条旧样本缓冲池、每个 batch 混 10% 旧样本，旧任务保持度通常能到 90% 以上，但需额外存储与采样开销。",
  "edgeCases": [
    "回放缓冲池大小有限（例如只存 2000 条），面对几十个任务的长序列仍会逐渐遗忘早期任务，出现「末任务偏向」。",
    "EWC 的 λ 过大（如 >5000）会把模型焊死在旧参数附近，新任务学不动；λ 过小则保护不足，遗忘依旧。",
    "模型编辑可能改了目标事实，却意外改变了与之共享神经元的其他知识，产生未预料的副作用。",
    "任务间差异极大（如从文本分类切到图像生成）时，单一共享网络难以用以上任一方法兼顾，需考虑参数隔离/子网络。"
  ],
  "code": "def ewc_loss(base_loss, theta, theta_star, fisher, lam=1000):\n    reg = sum(f * (t - ts)**2 for f, t, ts in zip(fisher, theta, theta_star))\n    return base_loss + lam * reg",
  "codeNotes": [
    "fisher 是旧任务在收敛点 θ* 处对每个参数的 Fisher 信息对角（梯度平方的期望），衡量参数重要性；数值上常用 E[∇L²] 估计。",
    "lam 控制保护强度，过大抑制学习、过小遗忘加剧，需用验证集在 {100, 500, 1000, 3000} 间网格搜索。",
    "theta_star 必须冻结为旧任务最优参数，不能用当前 θ 代替，否则正则项失去「锚点」意义。"
  ],
  "complexity": "EWC 每次前向/反向只需多算一项 O(参数) 的正则梯度，存储上需多保留 θ* 和 F 两份参数规模，开销为常数倍；replay 则每个 batch 多一次旧样本前向，且需维护缓冲池与采样逻辑；模型编辑一次性的定位+局部更新成本较低，但不计入持续训练循环。三者总体都比「全量重训」省几个数量级。",
  "followUps": [
    {
      "question": "replay 和 EWC 到底哪个更好，该怎么选？",
      "answer": "replay 通常在保持度上更好（接近全量联合训练），但必须存储旧数据，带来存储、隐私与分布偏移成本；EWC 无需存数据、轻量，但 Fisher 近似有偏且 λ 难调，单独用保护有限。实践中常见组合：以 replay 为主力（如 10% 旧样本混合），EWC 作为额外兜底正则，对明确的事实错误再用模型编辑修正。选型要看数据能否留存、任务长度与可接受的成本。"
    },
    {
      "question": "模型编辑（如 ROME）适合什么场景，有什么坑？",
      "answer": "适合纠正或注入少量、点状的事实性知识，比如改一个错日期、补一条新事实，成本低（秒级、只动极小比例参数）。它不适合大规模能力更新或风格迁移，因为编辑是局部手术，滥用会改变共享神经元牵连的其他知识，产生副作用；编辑后建议做回归测试，确认未破坏相关旧知识。"
    },
    {
      "question": "长任务序列下回放缓冲池不够怎么办？",
      "answer": "可采用基于重要性的样本选择（只留对旧任务最有代表性的样本）、生成式回放（用旧任务的生成模型合成伪旧样本，省存储但引入合成偏差），或结合 EWC/参数隔离减少对缓冲池的依赖；也可按任务优先级做缓冲池淘汰。"
    }
  ],
  "followUpAnswers": [
    "replay 通常在保持度上更好（接近全量联合训练），但必须存储旧数据，带来存储、隐私与分布偏移成本；EWC 无需存数据、轻量，但 Fisher 近似有偏且 λ 难调，单独用保护有限。实践中常见组合：以 replay 为主力（如 10% 旧样本混合），EWC 作为额外兜底正则，对明确的事实错误再用模型编辑修正。选型要看数据能否留存、任务长度与可接受的成本。",
    "适合纠正或注入少量、点状的事实性知识，比如改一个错日期、补一条新事实，成本低（秒级、只动极小比例参数）。它不适合大规模能力更新或风格迁移，因为编辑是局部手术，滥用会改变共享神经元牵连的其他知识，产生副作用；编辑后建议做回归测试，确认未破坏相关旧知识。",
    "可采用基于重要性的样本选择（只留对旧任务最有代表性的样本）、生成式回放（用旧任务的生成模型合成伪旧样本，省存储但引入合成偏差），或结合 EWC/参数隔离减少对缓冲池的依赖；也可按任务优先级做缓冲池淘汰。"
  ],
  "pitfalls": [
    "误以为 EWC 的 Fisher 永远准确：它只是旧任务末态的局部近似，任务分布漂移大时失准，需配合 replay 而非单用。",
    "回放数据若与当前分布不一致（采集时间、用户群体不同），会引入分布偏移甚至隐私泄露风险，须做匿名化与去偏。"
  ],
  "beginnerSummary": "持续学习就像「边学新课边复习旧课」。三种办法：replay（回放）是每次学新课都重做几道旧题，防止忘记；EWC 是正则化，相当于给重要的「脑细胞」贴上「别乱改」的标签，学新东西时这些细胞尽量不动；模型编辑是精准手术，只改某个记错的具体知识点（比如把错日期改对），不动整体。现实里复习（回放）效果最好但需要存旧题本，贴标签（EWC）省地方但有误差，手术（编辑）只适合改个别事实。",
  "prerequisites": [
    "灾难性遗忘：神经网络在分布变化的数据上顺序训练时，会迅速覆盖旧知识，导致旧任务性能崩塌，这是持续学习的根本动机。",
    "Fisher 信息矩阵：描述损失曲面在参数空间的曲率，其对角近似可衡量每个参数对任务的重要程度，是 EWC 的理论基础。",
    "正则化与二次惩罚项：理解 L2/权重衰减这类惩罚如何约束参数偏离，有助于看懂 EWC 的正则设计。"
  ],
  "workedExample": [
    "维护 1000 条旧样本缓冲池（reservoir sampling），每新任务训练时每个 batch 混 10% 旧样本（64 条新 + 约 7 条旧），5 个任务后首任务精度仍保持约 88%。",
    "或用 EWC：在任务 1 收敛后存 θ* 与 F，任务 2~5 训练损失加 λ=1000 的惩罚，首任务精度从纯微调的 55% 提升到约 80%。",
    "模型编辑案例：把「埃菲尔铁塔高 300 米」纠错为 330 米，用 ROME 只更新约 0.1% 的参数，单次编辑耗时数秒而非重训数小时。"
  ],
  "lineByLine": [
    "def ewc_loss(base_loss, theta, theta_star, fisher, lam=1000): 定义带 EWC 正则的损失函数；base_loss 是当前新任务的损失，theta 是当前参数，theta_star 是旧任务锚点，fisher 是重要性，lam 是强度。",
    "reg = sum(f * (t - ts)**2 for f, t, ts in zip(fisher, theta, theta_star)): 逐项累加，每个参数重要性 f 乘以其相对锚点的偏移平方，偏移越大、越重要，惩罚越重。",
    "return base_loss + lam * reg: 把正则项按强度并入总损失，反向传播时重要参数若想大幅移动就会受到梯度阻力，从而被「保护」。"
  ],
  "diagram": "新任务梯度 ─┬─ 学新\n              └─ EWC 惩罚 ─ 护旧权重"
};

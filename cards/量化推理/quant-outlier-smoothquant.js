export default {
  "kind": "concept",
  "id": "quant-outlier-smoothquant",
  "category": "量化推理",
  "difficulty": "Hard",
  "title": "Outlier 问题与 SmoothQuant",
  "prompt": "大模型激活里的 outlier 是什么，SmoothQuant 怎么解决？",
  "quickAnswer": "LLM 激活里存在极少数幅度极大的 outlier 通道(常见比正常值大几十倍)，直接均匀量化时 scale 被这些 outlier 拉到极大，导致绝大多数正常值被舍入成 0、信息全丢、精度崩。SmoothQuant 的核心思路是对激活逐通道除以一个平滑系数 s_c、同时对权重逐通道乘以同样的 s_c，由于 Y=X·W=(X/s)·(s·W) 在数学上完全等价，这种重分配不改变输出。效果是激活的动态范围被压缩、权重的动态范围被放大，两者都落到 INT8 能容纳的区间，于是激活也能安全量化(W8A8)。相比单纯量化权重(W8A16)，W8A8 在 Tensor Core 上几乎翻倍吞吐并显著省显存，是部署端最实惠的加速手段之一。",
  "approach": "整体做法是\"先校准、再变换、后量化\"三步：① 用一小批校准数据(几十到几百条样本)统计每个线性层输入激活的逐通道最大幅值 a_c，以及权重逐输入通道的最大幅值 w_c；② 按 s_c=(a_c^α / w_c^(1-α)) 计算平滑系数(α 常取 0.5)，并对激活除 s、权重乘 s；③ 对变换后的激活和权重分别做 INT8 量化(per-tensor 或 per-channel)。关键是 s 的选择要在\"激活被压下来\"和\"权重不被推溢出\"之间取得平衡，因此 α 和校准数据质量都会直接影响最终精度，通常需要逐层校准而非用全局统一系数。",
  "explanationFocus": "是什么：SmoothQuant 是一种训练后量化(PTQ)方法，通过一道数学上完全等价的对角缩放变换，把激活中难以量化的 outlier 通道的\"量化难度\"迁移到分布更平滑、更耐量化的权重上，从而让激活和权重都能安全地以 INT8 表示，实现 W8A8 推理。它不改变模型的计算结果，只是重新分配了两端的动态范围，使原本被 outlier 撑爆的激活量化尺度回落到合理区间。这样既能享受 INT8 接近两倍的吞吐与显存收益，又把精度损失压到接近 FP16 的水平，成为 LLM 推理加速的常用基线。",
  "bruteForce": "最朴素的办法是对激活直接做对称 INT8 量化：scale=max|X|/127，然后 round(X/scale)。但 LLM 中某些通道的 outlier 幅值可达正常值的 50~100 倍，于是整个张量的 scale 被它拉到极大，正常通道里 99% 的值落在 [−scale/50, scale/50] 区间，除以 scale 后都小于 1/127、被舍入成 0。结果是一整层几乎变成全零，输出分布坍塌、困惑度暴涨。这就是\"不处理 outlier 直接量化\"的失效模式，也凸显了必须重分配量化难度的必要性。",
  "derivation": [
    "为什么需要：LLM 激活存在明显的 outlier 通道，其幅值常是正常通道的几十倍甚至上百倍。若对激活做均匀 INT8 量化，量化 scale 由最大绝对值决定，正常通道的数值除以巨大 scale 后几乎全部落入同一个量化桶、被舍入为 0，导致该层信息近乎全丢、下游精度雪崩。W8A16 虽能躲过激活量化但又放弃了激活侧的加速，因此需要一种方法让激活也能安全走进 INT8。",
    "怎么实现：基于 Y=X·W=(X·S^{-1})·(S·W)，引入对角平滑系数 S=diag(s_c)。s_c 的选取目标是让 X/s_c 与 s_c·W 的动态范围尽量均衡，工程上常用 s_c=(act_max_c)^α / (weight_max_c)^(1−α)、再 clamp 到下限防止除零，α∈[0,1] 控制难度在激活/权重间的分配(常 0.5)。校准时只需统计激活和权重的逐通道最大幅值即可确定 s，无需梯度或训练。",
    "有什么代价：权重被乘以 s_c 后动态范围放大，若 s_c 选得过大可能超出 INT8 上限造成权重溢出，需配合权重量化与合理 clamp。s 依赖校准数据，若校准分布与真实推理分布偏差大，最优 s 会失准。此外 SmoothQuant 只解决激活侧 outlier，激活内部仍可能残留细粒度 outlier，必要时需叠加 per-group 量化。",
    "怎么评测：在标准下游任务(困惑度 WikiText2/C4、以及 MMLU 等零样本基准)上对比 W8A8(有/无 Smooth)与 FP16 的精度差，通常平滑后困惑度回升到接近 FP16、零样本掉分 <1。速度侧用端到端 token/s 与显存占用对比，W8A8 一般达 FP16 的 1.8~2.0 倍吞吐、显存约减半。还会在 outlier 剧烈的层单独做敏感度分析，确认平滑没有把误差集中到某几层。"
  ],
  "invariant": "对任意对角阵 S=diag(s_c)>0，有 Y=X·W=X·S·S^{-1}·W=(X·S^{-1})·(S·W)，即先对激活右乘 S^{-1}(除 s_c)、再对权重左乘 S(乘 s_c)，矩阵乘积完全不变——这是 SmoothQuant 成立的理论基础。在量化视角下，不变的是\"数值结果\"，变的是\"难量化的范围从激活转移到了权重\"；只要两端量化误差之和可控，端到端精度就不会明显劣化。实现时还需保证 s_c>0，且该变换对后续归一化/激活函数仍成立(通常作用在线性层前后)。",
  "walkthrough": "以 OPT-13B(13B 参数)为例：在不做平滑的直接 W8A8 下，由于激活 outlier 把量化 scale 撑大，WikiText2 困惑度从 FP16 的约 14.5 飙到 100+，基本不可用；引入 SmoothQuant(逐层校准 α=0.5 求 s_c)后，困惑度回落到约 14.7，几乎与 FP16 持平。推理端：在 A100 上 W8A8 的 GEMM 吞吐约为 FP16 的 1.8~2.0 倍，显存从约 26GB(FP16 权重)降到约 13GB(INT8 权重+INT8 激活)。部署经验上，SmoothQuant 常与 per-channel/per-group 量化以及高效 INT8 kernel(如 FasterTransformer)搭配，是 LLM 推理加速的入门首选。",
  "edgeCases": [
    "s 选得太极端(如 α 偏向激活侧令 s_c 过大)会把权重推到 INT8 上限之外，造成权重溢出与反量化误差激增。",
    "不同层、不同模块的 outlier 分布差异很大，最优 s 必须逐层校准，用全局统一 s 会顾此失彼。",
    "SmoothQuant 与 AWQ/GPTQ 等权重量化方法正交，可叠加使用(如 SmoothQuant+W4 做更激进的 W4A8)，叠加时 s 与量化顺序需协调。",
    "当校准数据量极少或分布与线上不一致时，统计出的通道 max 不可靠，平滑系数会失真。",
    "某些结构(如 LayerNorm 后的激活)outlier 模式特殊，需确认变换作用在正确的位置(通常在线性层输入)才有收益。"
  ],
  "code": "# Python (SmoothQuant 系数)\ndef smooth_scales(act, w, alpha=0.5):\n    # act: [tokens, ic], w: [ic, oc]\n    a = act.abs().max(0)                         # 每输入通道激活范围\n    wmax = w.abs().amax(0)                       # 每输入通道权重范围\n    s = (a.pow(alpha) / wmax.pow(1-alpha)).clamp(min=1e-4)\n    return s                                      # X/=s, W*=s 后各自量化",
  "codeNotes": [
    "α 调节激活/权重之间的难度分配：α 越大越把难度压到权重侧，α 越小越留在激活侧，常用 0.5 取折中；注意它与 AWQ 的 α 含义不同(一个在激活/权重间、一个在重要性保护强度)。",
    "变换在数学上完全等价，可把 1/s 烘焙进前一层权重、把 s 烘焙进当前层权重，使得在线推理不引入任何额外算子。",
    "clamp(min=1e-4) 防止某些通道 act 极小导致 s 趋近 0、造成除零或数值爆炸，是工程实现里必加的保护。"
  ],
  "complexity": "离线部分：校准统计只需对少量样本前向一遍，复杂度约 O(校准样本数 × 通道数) 求各通道 max，几乎可忽略；随后对全部参数做逐元素缩放 O(参数量)，也是一次性的。在线推理部分：因为缩放已经被\"烘焙\"进权重(X 的 1/s 也可烘焙进上一层权重或融合进量化参数)，真正的前向计算与标准 INT8 GEMM 完全一致，零额外算子、零额外计算开销。因此 SmoothQuant 的代价几乎全在离线准备，部署后收益(吞吐/显存)与纯 INT8 等价。",
  "followUps": [
    {
      "question": "为什么能把难度移到权重？",
      "answer": "因为矩阵乘法满足 Y=X·W=X·S·S^{-1}·W=(X·S^{-1})·(S·W)，只要在激活和权重两侧乘同一个可逆对角阵，输出不变。而权重的数值分布通常比激活更平滑、离群点更少，乘上 s_c 后虽被放大但多数仍落在 INT8 可表示区间；相比之下激活里的 outlier 才是量化杀手。于是通过把\"动态范围\"从难量的激活挪到好量的权重，整体就更容易压到 INT8，且端到端等价、精度几乎不损。"
    },
    {
      "question": "SmoothQuant 和 AWQ 冲突吗？",
      "answer": "不冲突，二者解决不同阶段的问题：SmoothQuant 处理激活侧 outlier(让激活能进 INT8)，AWQ 保护权重侧的重要通道(让权重能安全进 INT4)。因此常见组合是 SmoothQuant+W4/AWQ 用于更激进的 W4A8 部署。注意顺序：一般先用 SmoothQuant 平滑激活，再对权重做 AWQ/GPTQ 量化；二者可叠加，互补而非互斥，叠加时协调好缩放作用的位置即可。"
    }
  ],
  "followUpAnswers": [
    "因为矩阵乘法满足 Y=X·W=X·S·S^{-1}·W=(X·S^{-1})·(S·W)，只要在激活和权重两侧乘同一个可逆对角阵，输出不变。而权重的数值分布通常比激活更平滑、离群点更少，乘上 s_c 后虽被放大但多数仍落在 INT8 可表示区间；相比之下激活里的 outlier 才是量化杀手。于是通过把\"动态范围\"从难量的激活挪到好量的权重，整体就更容易压到 INT8，且端到端等价、精度几乎不损。",
    "不冲突，二者解决不同阶段的问题：SmoothQuant 处理激活侧 outlier(让激活能进 INT8)，AWQ 保护权重侧的重要通道(让权重能安全进 INT4)。因此常见组合是 SmoothQuant+W4/AWQ 用于更激进的 W4A8 部署。注意顺序：一般先用 SmoothQuant 平滑激活，再对权重做 AWQ/GPTQ 量化；二者可叠加，互补而非互斥，叠加时协调好缩放作用的位置即可。"
  ],
  "pitfalls": [
    "直接对激活做 INT8 量化却不处理 outlier，导致 scale 被 outlier 撑大、正常通道全舍入为 0、整层信息丢失。",
    "平滑系数 s 过度放大权重，使权重超出 INT8 表示范围造成溢出；或 s 选得太小没能压住激活 outlier。",
    "误以为 SmoothQuant 能解决权重侧量化，实际上它只重分配激活/权重的难度，权重自身的低位量化仍需配合 AWQ/GPTQ 或 per-group 方案。",
    "用与线上分布差异大的校准集，导致逐通道 max 统计失真、平滑失效。"
  ],
  "beginnerSummary": "班里有个身高两米三的巨高个(outlier 通道)，老师按他的身高做一把\"身高尺\"，结果其他同学全被量成\"矮子\"(舍入成 0)。SmoothQuant 把尺\"折中\"：让高个稍微蹲一点、矮个稍微垫一点(等价对角变换，两边都乘除以同一个数)，于是所有人用同一把 INT8 尺都能量准。代价是矮个(权重)被垫高后也可能接近尺子上限，所以 SmoothQuant 还会小心选\"蹲多少\"，并配合权重量化防止溢出。一句话：把难量的部分分摊给好量的部分，整体就能用更省的精度跑起来。",
  "prerequisites": [
    "LLM 激活存在极端 outlier 通道：研究表明 OPT、LLaMA 等模型在固定若干通道上会出现幅值异常大的值，这是量化的主要难点。",
    "矩阵乘法满足 Y=X·W 可在两侧同乘可逆对角阵而保持等价，这是 SmoothQuant 变换成立的理论前提。",
    "权重分布通常比激活更平滑、更耐量化，因此把难度转移到权重侧是\"划算\"的。",
    "了解 INT8 对称量化的 scale/zero-point 机制，才能理解为何 outlier 会撑爆 scale、为何重分配有效。"
  ],
  "workedExample": [
    "假设某线性层输入激活中，第 7 通道存在 outlier：max=60，而其余通道正常幅值约 2。若直接量化，scale=60/127≈0.472，正常通道值 2/0.472≈4.2 个量化级、尚可；但若 outlier 达 200，则 scale≈1.575，正常 2 仅约 1.3 个量化级、几乎全被压成 0。",
    "取 α=0.5，该通道 activ≈60、权重通道 max≈3，则 s=√(60/3)≈4.47；激活除以 s 后 outlier 由 60 降到约 13.4，正常 2 降到约 0.45(仍需配合 per-group)；权重乘 s 后由 3 升到约 13.4，落在 INT8 区间。两端动态范围被\"折中\"，整体可 INT8。",
    "端到端示例：OPT-13B 在 WikiText2 上 FP16 困惑度≈14.5；直接 W8A8 暴涨到 >100；SmoothQuant W8A8 回落到≈14.7，几乎无损且显存减半。"
  ],
  "lineByLine": [
    "a = act.abs().max(0)：对校准激活在 token 维取逐输入通道的最大绝对值，得到向量 a(长度=输入通道数 ic)。",
    "wmax = w.abs().amax(0)：对权重矩阵在输入通道维取最大绝对值，得到每输入通道的权重范围 wmax。",
    "s = (a.pow(alpha) / wmax.pow(1-alpha)).clamp(min=1e-4)：按 α 平衡两侧难度；clamp 防止某些通道 a 极小导致 s→0 引发除零/爆炸。",
    "返回 s 后，部署时令 X' = X / s.unsqueeze(0)，W' = W * s.unsqueeze(1)，再分别对 X'、W' 做 INT8 量化；由于 X'/W' 的动态范围更均衡，量化误差显著下降。",
    "可选地把 1/s 烘焙进上一层输出、把 s 烘焙进本层权重，使在线前向无额外算子、与标准 INT8 GEMM 完全一致。"
  ],
  "diagram": "X(有outlier)·W  ─▶ (X/s)·(sW)\n 激活范围↓   权重范围↑  → 都可 INT8"
};

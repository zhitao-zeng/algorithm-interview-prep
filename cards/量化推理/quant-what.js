export default {
  "kind": "concept",
  "id": "quant-what",
  "category": "量化推理",
  "difficulty": "Medium",
  "title": "模型量化是什么",
  "prompt": "大模型推理里的量化是什么，为什么推理要做量化？",
  "quickAnswer": "量化把模型权重/激活从 FP16/BF16 映射到 INT8/INT4/FP8 等低精度表示，用更小的存储与更快的低精度计算换得显存下降与吞吐提升。代价是引入舍入误差，设计不当会掉点，因此需配合校准（calibration）与合适的量化粒度（per-tensor/per-channel/per-group）。常见的 W4A16 只量化权重、保留激活 FP16，是性价比很高的推理配置。",
  "approach": "核心思路是\"低精度存储 + 低精度（或反量化后）计算\"：对权重/激活做线性缩放量化（如对称量化 x_q = round(x/s) + z），只存储低精度整数与少量缩放参数；计算时要么先反量化回高精度再算，要么直接用低位 kernel（如 INT8 GEMM、FP8 Tensor Core）在低位完成矩阵乘，最后再把结果缩放回原范围。粒度上从粗到细有 per-tensor、per-channel、per-group，越细越能抵抗 outlier 但 overhead 略增。",
  "explanationFocus": "是什么：量化（Quantization）是用更低比特的数值格式（如 INT8、INT4、FP8）来表示模型权重和激活，以\"精度\"换取\"显存占用下降\"与\"计算速度提升\"的技术。它的本质是把原本用 FP16/BF16 记录的浮点参数，映射成占用更少 bit 的低精度表示，并在计算时配合缩放因子反量化回合理的数值范围。推理场景做量化，是因为大模型权重量巨大、KV Cache 随上下文线性膨胀，FP16 的显存与带宽已成硬瓶颈，而低精度既省显存又能用专用低精度 kernel 提升吞吐。",
  "bruteForce": "不做量化、全 FP16 推理是最直接的基线，但显存与算力都紧：7B 模型权重 14 GB 只是起步，长上下文下 KV Cache（每 token 每层若干 KB）随 seq 线性暴涨，并发和长上下文都受限于显存；同时 FP16 矩阵乘无法充分利用新 GPU 的整数/低精度算力单元，吞吐上不去。这也是为什么线上推理几乎必做量化的原因。",
  "derivation": [
    "为什么需要：LLM 权重量巨大、KV Cache 随上下文线性暴涨，FP16 的显存与带宽成为推理硬瓶颈；单纯堆显存成本高，而低精度表示可同时降显存、提算力利用率，是性价比最高的解法。",
    "怎么实现：对权重/激活做线性缩放量化（如 INT8 对称 x_q=round(x/s)，z=0），存储低精度整数与 scale；计算前/中反量化，或直接使用低位 kernel（INT8 GEMM、FP8 Tensor Core）。激活比权重更敏感时，用 per-channel/per-group 或 SmoothQuant 把难度迁移到权重。",
    "有什么代价：舍入误差可能掉点，尤其碰到 outlier 通道；需校准集与专用 kernel；并非所有层都耐量化（如 lm_head、最终投影常需留高精度）；细粒度缩放虽抗 outlier 但引入额外 scale 存储与计算。",
    "怎么评测：对比量化前后显存、吞吐与下游精度（困惑度、MMLU、任务准确率），并重点看 P99 延迟是否劣化——均值不掉点不代表长尾不掉，需分位数核对。"
  ],
  "invariant": "两条经验不变量：① 量化误差随比特数下降而上升——INT8 约 1/2 显存、误差很小，INT4 约 1/4 显存、误差明显增大，需靠细粒度缩放兜底；② 激活通常比权重更敏感，所以\"只量化权重（W4A16）\"往往比\"权重激活都量化（W4A4）\"更稳。任何量化方案都应保证反量化后的数值范围与原 FP16 大致对齐。",
  "walkthrough": "具体算一笔账：一个 7B 模型，FP16 权重每项 2 字节，共约 70 亿×2 = 14 GB。量化为 INT4（每参数 0.5 字节）后约 3.5 GB，显存降到约 1/4，于是原本放不下的模型能塞进单张 24 GB 卡，并留出空间给 KV Cache 以支撑更大 batch 或更长上下文（如 4096 token 序列）。若只是 INT8（每参数 1 字节）则约 7 GB，省一半显存——直观看到\"比特数减半、显存减半\"的线性关系。",
  "edgeCases": [
    "outlier 通道：少数通道数值极大，全局缩放会被这些 outlier 压扁、正常通道精度崩，需 per-channel/per-group 缩放或 SmoothQuant  smoothing。",
    "敏感层（如 lm_head、最终分类头）：宜保留 FP16/BF16 高精度，否则输出分布偏移明显、掉点严重。",
    "激活比权重更敏感：常需更高精度（如 W4A16 只量化权重），或对激活做细粒度量化与 SmoothQuant。",
    "动态形状/变长输入：激活量化需要运行时统计 max/min，若输入分布漂移大，固定校准 scale 会失准。"
  ],
  "code": "# Python\ndef linear_quantize(tensor, bits=8):\n    qmin, qmax = -(2**(bits-1)), 2**(bits-1)-1\n    scale = tensor.abs().max() / qmax          # 对称缩放\n    q = (tensor / scale).round().clamp(qmin, qmax)\n    return q, scale                             # 存 q, 用前 q*scale 反量化",
  "codeNotes": [
    "对称量化 z=0，公式简单、硬件友好，是 INT8/FP8 推理最常用的方案。",
    "per-group 缩放（把通道再细分成组分别定 scale）比 per-tensor 更耐 outlier，是 INT4 接近无损的关键。",
    "草图中只演示了权重对称量化；真实推理还要处理激活量化与反量化 placement（在 GPU kernel 内完成以省带宽）。"
  ],
  "complexity": "量化本身是 O(元素数) 的逐点缩放与取整，预处理开销可忽略；节省的显存与精度下降近似线性于比特数的减少（INT8→1/2，INT4→1/4）。真正的速度收益取决于底层是否有对应低精度 kernel：有则 GEMM 吞吐随精度下降成倍提升，无（仅存储省）则只是省显存、算力不变。校准与 per-group 缩放会带来少量额外统计开销。",
  "followUps": [
    {
      "question": "权重量化和激活量化哪个更难？",
      "answer": "激活含 outlier 且随输入动态变化，比静态的权重更难量化，所以常见 W4A16（只量化权重、激活留 FP16）或用 SmoothQuant 把激活的量化难度迁移到权重侧。权重是固定的，可离线精细校准；激活每步都变，难以用单一 scale 覆盖全部分布。"
    },
    {
      "question": "INT4 一定掉点吗？",
      "answer": "不一定。若用细粒度 group 缩放（如每 128 个权重一组一个 scale）配合校准集，多数任务可接近无损；粗粒度全局缩放则极易因 outlier 崩掉。实践中 W4A16 + group-wise 是当前最稳的 4bit 方案之一。"
    },
    {
      "question": "怎么判断量化是否可接受？",
      "answer": "不能只看均值精度，要同时比较显存、吞吐与下游指标，并重点看 P99 延迟与困惑度是否劣化；若长尾任务掉点明显，应回退敏感层到高精度或减小量化范围。"
    }
  ],
  "followUpAnswers": [
    "激活含 outlier 且随输入动态变化，比静态的权重更难量化，所以常见 W4A16（只量化权重、激活留 FP16）或用 SmoothQuant 把激活的量化难度迁移到权重侧。权重是固定的，可离线精细校准；激活每步都变，难以用单一 scale 覆盖全部分布。",
    "不一定。若用细粒度 group 缩放（如每 128 个权重一组一个 scale）配合校准集，多数任务可接近无损；粗粒度全局缩放则极易因 outlier 崩掉。实践中 W4A16 + group-wise 是当前最稳的 4bit 方案之一。",
    "不能只看均值精度，要同时比较显存、吞吐与下游指标，并重点看 P99 延迟与困惑度是否劣化；若长尾任务掉点明显，应回退敏感层到高精度或减小量化范围。"
  ],
  "pitfalls": [
    "以为量化一定掉点：INT8/FP8 在合理粒度（per-channel/per-group）与校准下常接近无损，但非绝对，敏感任务仍可能掉点，需实测而非想当然。",
    "忽略 outlier 导致全局缩放崩：不做 per-group 或 SmoothQuant，遇到 outlier 通道时量化误差急剧放大。",
    "只量化权重却用错 kernel：若硬件不支持对应低位计算，量化只省了显存、没提速，收益打折扣。"
  ],
  "beginnerSummary": "模型参数原本用\"高清\"数字（FP16）记录，占地方又算得慢。量化好比把高清笔记改成速记缩写（INT8/INT4），地方省一大半、翻得也快，代价是偶尔写错一两个字。只要缩写规则（缩放因子 scale）设计好——比如按小组分别定刻度（per-group）而不是整本笔记统一一个刻度——大部分内容不丢。激活比权重更敏感，常常还得保留较高精度或用 SmoothQuant 把难度转移给权重，才算真正\"又快又准\"。",
  "prerequisites": [
    "模型由大量浮点参数组成，且每个参数在 FP16 下占 2 字节、FP32 下占 4 字节，数量级决定显存。",
    "低精度计算有硬件加速：现代 GPU 的 Tensor Core/INT 单元对 INT8、FP8 有数倍于 FP16 的吞吐。",
    "量化 = 低精度存储 + 缩放/反量化：理解 scale、zero-point 与对称/非对称量化的区别是前提。"
  ],
  "workedExample": [
    "7B 模型 FP16 权重约 14 GB → INT4 约 3.5 GB（省 3/4），可塞进 24 GB 卡并留出 KV Cache 空间；INT8 则约 7 GB（省 1/2）。",
    "对称量化具体算：scale = max(|tensor|)/127，量化值 q = round(x/scale) 再 clamp 到 [-128,127]，反量化用 x≈q·scale。",
    "W4A16 实战：权重压到 4bit、激活留 16bit，既大幅省显存又避免激活量化掉点，是很多部署的默认选择。"
  ],
  "lineByLine": [
    "选缩放因子（对称 z=0 或非对称带 zero-point）：决定低精度格子怎么对齐原数值范围。",
    "浮点乘 scale 后 round 到整数：完成\"高清→速记\"的映射，round 引入的舍入误差即量化误差。",
    "存整数权重/激活（如 INT8 占 1 字节、INT4 占 0.5 字节）：显存随比特数线性下降。",
    "计算前/中乘回 scale 反量化（或直接在低位 kernel 算）：保证结果回到合理数值范围，供下一层使用。"
  ],
  "diagram": "FP16(2B) ─▶ INT8(1B) 省1/2 ─▶ INT4(0.5B) 省3/4\n代价: 舍入误差 → 需校准/细粒度缩放"
};

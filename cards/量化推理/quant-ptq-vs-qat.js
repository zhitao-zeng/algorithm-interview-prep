export default {
  "kind": "concept",
  "id": "quant-ptq-vs-qat",
  "category": "量化推理",
  "difficulty": "Medium",
  "title": "PTQ 与 QAT 的区别",
  "prompt": "训练后量化(PTQ)和量化感知训练(QAT)有什么区别，怎么选？",
  "quickAnswer": "PTQ 在训练完成后用一小批校准数据（如 128 条）直接量化，零重训、成本低，是部署首选；QAT 在训练时插入伪量化（fake_quant）节点让网络提前适应低精度，精度更好但需训练数据与算力。经验法则：先 PTQ 看精度，掉点严重（尤其 INT4 或敏感小模型）再上 QAT 微调兜底。",
  "approach": "默认先 PTQ（校准→量化→测精度）；若目标比特下掉点超标（如 INT4 掉 >2 点），再在训练末端插入伪量化节点做 QAT 微调，通常只训 1~3 epoch、小学习率。",
  "explanationFocus": "是什么：PTQ（训练后量化）是模型训练完成后，用一小批校准数据直接统计范围并量化，零重训、成本极低，是部署首选；QAT（量化感知训练）是在训练/微调时插入伪量化节点，让网络「提前适应」低精度，精度更好但需训练数据与算力。二者解决「落地成本 vs 低比特精度」的权衡。",
  "bruteForce": "直接把 FP16 权重截断成低精度 INT8（无校准、无训练）：scale 取错导致大量溢出/舍入，精度崩。",
  "derivation": [
    "为什么需要：PTQ 想零成本把模型落地到低精度，QAT 想在低比特（INT4）或敏感模型下保精度，二者解决「成本 vs 精度」的权衡。",
    "怎么实现：PTQ 统计校准集的 min/max（或百分位数）求缩放因子并量化；QAT 在前向插 STE 伪量化（quant-dequant 模拟精度损失），反向仍用 FP 梯度更新权重，使网络适应量化噪声。",
    "有什么代价：PTQ 对极低位（INT3/4）或 outlier 多的权重/激活易掉点；QAT 需重训、数据/算力开销大、流程复杂，且学习率过大易把权重推到饱和区。",
    "怎么评测：同一测试集比较 PTQ 与 QAT 的精度差、显存占用与推理时延，结合部署 ROI（省下的显存/时延是否值得训练成本）决策。"
  ],
  "invariant": "同样比特下 QAT 精度 ≥ PTQ；PTQ 成本远低于 QAT。PTQ 只需前向，QAT 需前向+反向但用 STE 直通梯度。",
  "walkthrough": "以 7B 模型为例：PTQ INT8 通常掉点 <1%（困惑度几乎不变），耗时仅几分钟、无需 GPU 训练；压到 INT4 时若 PTQ 掉 5 分，引入 QAT 在 1~3 epoch 内微调可拉回约 3 分，最终掉点收窄到 ~2 分，但需训练数据与数小时训练。小模型（如 0.5B）对量化更敏感，常需 QAT 兜底。",
  "edgeCases": [
    "校准数据分布偏离推理分布：PTQ 的 scale/min/max 失真，激活溢出，精度大幅掉。",
    "QAT 需冻结/解冻策略：全量微调易遗忘，常只训部分层或加小学习率，否则 STE 把权重推到饱和区。",
    "小模型对量化更敏感，常需 QAT；大模型（>7B）PTQ INT8 往往足够。",
    "激活 outlier（如某通道值极大）：需 per-channel 或平滑量化（SmoothQuant）处理，否则 PTQ 掉点严重。"
  ],
  "code": "# Python (伪量化 STE)\ndef fake_quant(x, bits=8):\n    qmin, qmax = -(2**(bits-1)), 2**(bits-1)-1\n    scale = x.abs().max() / qmax\n    x_q = (x / scale).round().clamp(qmin, qmax)   # 前向量化\n    return x_q * scale                              # 反量化; 反向 STE 直通梯度",
  "codeNotes": [
    "STE（Straight-Through Estimator）：反向时把 round/clamp 的梯度当作 1 直通回传，使权重能在「被量化」的约束下继续学习。",
    "QAT 最终只保留量化后的整型权重用于部署，伪量化节点仅训练期存在；scale/zero_point 可训练或固定。"
  ],
  "complexity": "PTQ 仅前向 + 统计：O(校准样本数 × 参数量)，CPU 即可；QAT 多一次训练：O(epoch × 数据量 × 参数量)，需 GPU，且伪量化节点增加少许计算图复杂度。",
  "followUps": [
    {
      "question": "什么时候必须从 PTQ 升级到 QAT？",
      "answer": "当 PTQ 在目标比特（如 INT4）下精度不达标、或超过可接受掉点阈值（如 >2 点）时；模型很小/很敏感（如 <1B）对量化噪声容忍低时也常需 QAT；以及需要端到端联合优化缩放因子（可训练 scale）的场景。升级代价是要有训练/微调数据与相应算力，通常只微调 1~3 epoch。"
    },
    {
      "question": "QAT 会不会破坏已经训好的权重？",
      "answer": "会做小幅微调让权重适应量化噪声，但通常从小学习率恢复、或只动伪量化参数与部分层，整体变化可控；若学习率过大或全量微调，确实可能把权重「洗坏」、遗忘原能力。所以 QAT 强调小学习率 + 短训练 + 必要时冻结主干只训量化参数。"
    }
  ],
  "followUpAnswers": [
    "当 PTQ 在目标比特（如 INT4）下精度不达标、或超过可接受掉点阈值（如 >2 点）时；模型很小/很敏感（如 <1B）对量化噪声容忍低时也常需 QAT；以及需要端到端联合优化缩放因子（可训练 scale）的场景。升级代价是要有训练/微调数据与相应算力，通常只微调 1~3 epoch。",
    "会做小幅微调让权重适应量化噪声，但通常从小学习率恢复、或只动伪量化参数与部分层，整体变化可控；若学习率过大或全量微调，确实可能把权重「洗坏」、遗忘原能力。所以 QAT 强调小学习率 + 短训练 + 必要时冻结主干只训量化参数。"
  ],
  "pitfalls": [
    "认为 PTQ 永远够用：低比特（INT4 及以下）或敏感小模型常不够，需 QAT 兜底。",
    "QAT 学习率过大把权重洗坏：伪量化下梯度被 STE 直通，大学习率会让权重剧烈跳动、收敛不稳。"
  ],
  "beginnerSummary": "PTQ 像把写好的稿子直接缩写印刷，快但可能漏字（精度小损）；QAT 像边写边用缩写方式练习，最后成稿更顺、但要重写一遍（费时费算力）。没钱没时间先 PTQ，要极致低比特精度再 QAT。好比想省钱用现成缩印，想保证质量就重排一版。",
  "prerequisites": [
    "了解量化的基本缩放：知道 scale/zero_point 如何把浮点映射到整型，以及 round/clamp 的作用。",
    "知道反向传播与梯度：理解 STE 为何需要——量化不可导，要用直通估计让梯度流过。",
    "有（或没有）现成训练数据：PTQ 只需少量校准，QAT 需要完整训练集/微调集。"
  ],
  "workedExample": [
    "PTQ：取 128 条代表样本跑前向，统计每层 min/max 求 scale，几分钟完成 INT8 量化，掉点 <1%。",
    "QAT：在 Linear 层前后插 fake_quant（权重+激活），用 1~3 epoch 小学习率微调，INT4 掉点从 5 分拉回 2 分。"
  ],
  "lineByLine": [
    "PTQ：加载 FP 权重，准备校准集（少量代表样本）。",
    "PTQ：跑校准集统计每层范围（min/max 或百分位数）。",
    "PTQ：计算 scale 并取整存储为 INT8/INT4 权重，部署即用。",
    "QAT：插伪量化节点，前向量化反量化模拟损失、反向 STE 直通梯度，微调后只留整型权重。"
  ],
  "diagram": "PTQ: 训练完成 ─▶ 校准 ─▶ 量化部署 (零重训)\nQAT: 训练完成 ─▶ 插伪量化 ─▶ 微调 ─▶ 量化部署 (更高精度)"
};

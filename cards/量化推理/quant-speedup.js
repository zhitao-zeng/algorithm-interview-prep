export default {
  "kind": "concept",
  "id": "quant-speedup",
  "category": "量化推理",
  "difficulty": "Easy",
  "title": "量化加速比计算",
  "prompt": "量化带来的加速比应该怎么估算与实测？",
  "quickAnswer": "加速比 = 基线端到端时延 / 量化端到端时延,需端到端实测(prefill+decode 都算)并报告均值与 P99。理论上限由两项瓶颈取 min:算力倍数(低精度每 cycle 能算更多 MAC)与带宽倍数(权重字节更少、搬运更快);实际加速 = min(二者) − 各类开销。要分清\"仅省显存\"(无算速收益)与\"真加速\",prefill 阶段大矩阵受算力瓶颈、decode 阶段小矩阵受带宽瓶颈,二者加速比不同,不能混为一谈。",
  "approach": "两步法:先算 Roofline 理论上下限(min(峰值 MAC 比, 带宽比))作为乐观上界;再在目标硬件上固定输入/批大小端到端计时(含预热、prefill 与 decode),取均值与 P99 作实测值。报告时拆开 prefill/decode 各自的加速比,并扣除 dequant/调度开销,避免用\"字节比\"冒充加速。",
  "explanationFocus": "是什么：量化加速比是同一模型在量化后相对 FP16/BF16 的端到端时延缩减比，由\"算力瓶颈\"与\"带宽瓶颈\"双重约束共同决定(取二者较小者),且必须通过目标硬件端到端实测才作数。理论加速来自低精度 MAC 更多(算力倍数)与数据字节更少(带宽倍数),实际还要扣除 dequant、kernel 启动与混精度调度开销。",
  "bruteForce": "只看字节数减半就宣称 2× 加速:这是最常见的夸大。低比特只减了\"数据大小\",但若瓶颈在算力(没打满 Tensor Core)或开销在 dequant/kernel 启动,实际加速远不到字节比。",
  "derivation": [
    "为什么需要：量化宣称\"更快\"必须可验证,而不同阶段瓶颈不同——prefill 是大矩阵乘、受算力限制;decode 是小矩阵、受显存带宽限制;且 dequant/调度会侵蚀收益。不拆开测就会得出误导的\"2×\"。",
    "怎么实现：算力比=峰值低精度 MAC / 峰值 FP16 MAC;带宽比=FP16 字节 / 低精度字节(如 2× for INT8);理论加速取二者 min。实测:固定 batch/seq,预热后端到端计时(含 prefill+decode),取均值与 P99;并分别记 prefill、decode 耗时。",
    "有什么代价：kernel 启动、dequant(反量化回高精度计算或融合)、混精度调度、未打满的占用率都会侵蚀理论峰值;测量若漏掉 decode 或冷启动会虚高。",
    "怎么评测：固定输入规模重复计时取稳定值,报告\"理论上限 / prefill 加速 / decode 加速 / 端到端加速 / 显存降幅\"五联指标;端到端加速必须 ≤ 理论上限,否则查测量漏洞。"
  ],
  "invariant": "实际端到端加速 ≤ min(算力倍数, 带宽倍数) − 开销。任何宣称超过该上限的加速比都是测量方法有误(如未含 decode、未预热、对比的不是同硬件)。",
  "walkthrough": "以 7B 模型在 A100(INT8 Tensor Core 算力 2× FP16)为例。理论:算力倍数=2、带宽倍数=2(字节 2× 少),故理论上限≈2×。实测:prefill(处理 2048 token 提示、大矩阵乘)FP16=0.42s、INT8=0.22s→约 1.9×,接近算力上限;decode(逐 token、小矩阵受带宽瓶颈)FP16=18ms/tok、INT8=11ms/tok→约 1.6×,受带宽与 dequant 拖累低于 2×。端到端(seq=4096、生成 256 token)FP16 总 5.1s、INT8 总 3.3s→综合 1.55×。结论:实测远低于\"字节比\"宣称的 2×。",
  "edgeCases": [
    "小 batch decode 受带宽非算力：batch=1 时矩阵极小,瓶颈在搬权重而非算,INT8 的算力倍数用不上,加速≈带宽比且被 dequant 压到 1.3~1.6×。",
    "kernel 开销侵蚀加速：未融合 dequant 的 naive kernel,启动+反量化占 20% 时间,实际加速砍半。",
    "不同序列长度瓶颈切换：seq=128 偏算力、seq=8192 偏带宽,加速比随 seq 浮动,需报多档。",
    "仅权重量化(W8A16)：权重省带宽但激活仍 FP16,decode 加速有限,常只有 1.2~1.4×。"
  ],
  "code": "# Python\ndef speedup(t_fp16, t_quant):\n    return t_fp16 / t_quant                     # 端到端实测\n# 理论: min(peak_mac_ratio, bw_ratio) 再扣 overhead",
  "codeNotes": [
    "prefill 受算力瓶颈、decode 受带宽瓶颈,务必分开报加速比,否则端到端被 prefill 虚高掩盖。",
    "务必排除冷启动与预热:首轮含 CUDA 初始化,应从第 2 轮起计时,并重复取中位数/P99。",
    "理论公式 min(peak_mac_ratio, bw_ratio) 是乐观上界,实测一般低 20~40%,别直接拿去汇报。"
  ],
  "complexity": "计时本身 O(重复次数),取重复 N 次(如 100 轮)的稳定中位数/P99;理论比值是硬件常数(峰值 TFLOPS 比、显存带宽比)。但复杂点在\"公平对比\":必须相同批大小、相同 seq、相同硬件与相同框架;decode 阶段还受 KV cache 带宽影响,INT8 权重省下的带宽可能被激活/KV 的搬运吃掉一部分,故 decode 实际加速常低于 prefill。",
  "followUps": [
    {
      "question": "为什么实际加速常低于理论？",
      "answer": "三个侵蚀源:①dequant——很多 kernel 先反量化再算或做融合,这部分不在\"低精度更快\"里;②kernel 启动与调度开销,尤其小 batch 时启动时间占比高;③占用率未打满,矩阵形状没对齐到 Tensor Core 最优分块。综合下来 INT8 实测常比理论 2× 低 20~40%,典型 1.5~1.7×。"
    },
    {
      "question": "prefill 和 decode 加速一样吗？",
      "answer": "不一样,因为瓶颈不同。prefill 处理整段提示是大矩阵乘,受算力瓶颈,低精度 Tensor Core 收益充分,加速接近算力倍数(如 1.9×);decode 逐 token 生成是小矩阵,受显存带宽瓶颈(搬权重慢),且 KV cache/激活搬运吃掉部分带宽收益,加速只有 1.4~1.6×。端到端是两者加权平均,所以整体低于 prefill 单项。"
    },
    {
      "question": "只量化权重(W8A16)有加速吗？",
      "answer": "有但有限。权重字节减半省了\"读权重\"的带宽,但激活仍是 FP16、计算仍是 FP16 MAC,算力倍数=1,故加速受\"读权重带宽\"一项限制,decode 通常只 1.2~1.4×。真正想接近 2× 要做 W8A8(权重+激活都 INT8),让算力也翻倍。报加速时一定要说明量化类型。"
    }
  ],
  "followUpAnswers": [
    "三个侵蚀源:①dequant——很多 kernel 先反量化再算或做融合,这部分不在\"低精度更快\"里;②kernel 启动与调度开销,尤其小 batch 时启动时间占比高;③占用率未打满,矩阵形状没对齐到 Tensor Core 最优分块。综合下来 INT8 实测常比理论 2× 低 20~40%,典型 1.5~1.7×。",
    "不一样,因为瓶颈不同。prefill 处理整段提示是大矩阵乘,受算力瓶颈,低精度 Tensor Core 收益充分,加速接近算力倍数(如 1.9×);decode 逐 token 生成是小矩阵,受显存带宽瓶颈(搬权重慢),且 KV cache/激活搬运吃掉部分带宽收益,加速只有 1.4~1.6×。端到端是两者加权平均,所以整体低于 prefill 单项。",
    "有但有限。权重字节减半省了\"读权重\"的带宽,但激活仍是 FP16、计算仍是 FP16 MAC,算力倍数=1,故加速受\"读权重带宽\"一项限制,decode 通常只 1.2~1.4×。真正想接近 2× 要做 W8A8(权重+激活都 INT8),让算力也翻倍。报加速时一定要说明量化类型。"
  ],
  "pitfalls": [
    "只按字节比宣称加速：权重 16→8 位就写 2×,无视算力/带宽瓶颈与开销,实测常仅 1.5×。",
    "没排除预热/冷启动：首 token 含 CUDA 上下文初始化,计时含它就虚低基线、虚高加速。",
    "漏掉 decode 阶段：只测 prefill 大矩阵(接近算力上限 1.9×)当整体,隐瞒 decode 仅 1.6×,端到端被夸大。"
  ],
  "beginnerSummary": "加速好比\"抄近路省时间\":你省下的时间,被两条路里更慢那条卡着(木桶短板)——要么算得不够快(算力),要么数据搬得不够快(带宽),取较慢那个当上限。还得扣掉\"换鞋(dequant 反量化)\"耽误的时间,才是真正快了多少。只说\"权重从 16 位变 8 位、字节减半所以 2 倍快\"是忽悠,因为可能瓶颈在搬运而不是计算。",
  "prerequisites": [
    "Roofline 模型：理解算力瓶颈(compute-bound)与带宽瓶颈(memory-bound),性能受二者较小者限制。",
    "INT8/FP8 字节更少：知道低精度权重/激活占用字节减半或更多,带来带宽与算力潜力。",
    "端到端时延可测：会用计时工具、区分 prefill 与 decode、会预热与取 P99。",
    "清楚量化类型：W8A8 / W8A16 / W4A16 的加速来源不同,别混为一谈。"
  ],
  "workedExample": [
    "理论:INT8 在 A100 上算力比 2×、带宽比 2×,理论上限 min=2×。",
    "实测:prefill 0.42→0.22s(1.9×),decode 18→11ms/tok(1.6×),端到端(seq4096+gen256)5.1→3.3s(1.55×)。",
    "W8A16 反例:仅权重量化,decode 18→14ms(1.29×),证明激活不量化时加速有限,别按 W8A8 报。"
  ],
  "lineByLine": [
    "算算力与带宽理论比：峰值 MAC 比(如 INT8=2×FP16)、字节比(INT8 权重=1/2),取 min 作上限。",
    "取瓶颈较小者：compute-bound 看算力、memory-bound 看带宽,prefill/decode 各自不同。",
    "端到端计时(含预热+prefill+decode)：固定 batch/seq 重复 N 次,取均值与 P99。",
    "扣开销得实际加速：减去 dequant/kernel 启动/调度,报告五联指标,验证 ≤ 理论上限。"
  ],
  "diagram": "加速 ≤ min(算力倍数, 带宽倍数) − 开销"
};

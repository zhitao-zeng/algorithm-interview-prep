export default {
  "kind": "concept",
  "id": "quant-mixed-precision",
  "category": "量化推理",
  "difficulty": "Medium",
  "title": "混合精度量化",
  "prompt": "混合精度量化是什么，怎么决定哪些层留高精度？",
  "quickAnswer": "混合精度对不同层/模块用不同比特（如敏感层 FP16、其余 INT4/INT8），在显存与精度间精细权衡。判断敏感度常用「量化前后输出差异」或「对下游精度边际贡献」做搜索（如 Hessian 迹、逐层回放评估），把有限的精度预算留给最敏感的少数层，从而在几乎不掉点的前提下最大化压缩。",
  "approach": "先全量量化到低位得到基线；再逐层回升精度（INT4→INT8 或 INT8→FP16）看对下游指标的边际收益；按显存预算从最敏感层开始贪心地抬升精度；最终形成「哪些层高精度、哪些低位」的部署方案。",
  "explanationFocus": "是什么：混合精度量化（Mixed-Precision Quantization）按层/模块对量化误差的敏感度，给不同权重分配不同比特宽度（如敏感层留 FP16/INT8，耐量化层压到 INT4/INT2）。它在「显存/算力节省」与「精度保持」之间做精细权衡，是部署大模型时最常用的压缩手段之一。",
  "bruteForce": "全部统一 INT4，敏感层崩了再全回 INT8，既浪费显存又没保住关键点；或凭经验拍脑袋挑层，缺乏数据支撑。",
  "derivation": [
    "为什么需要：不同层对量化误差敏感度差异巨大，统一低位会无谓牺牲关键层、又没省到该省的；混合精度让预算用在刀刃上。",
    "怎么实现：用敏感指标（逐层量化前后输出 MSE、余弦相似度，或 Hessian 对角迹）排序，按显存预算从低位起逐步把最敏感层升精度。也可整数规划求最优分配。",
    "有什么代价：需离线的逐层评估与搜索，耗时；部署时要支持多种精度 kernel 与调度，工程复杂度上升。",
    "怎么评测：在固定显存预算下比较混合方案与均匀方案的精度，看拐点；在真实推理负载上测延迟/显存是否达标。"
  ],
  "invariant": "少数敏感层占精度损失大头，保护它们收益最大；在固定显存预算下，混合方案精度应不低于同预算的均匀方案。",
  "walkthrough": "以 70B 模型为例，全部 INT4 显存约省 4×但精度掉 4 分。用逐层回放测敏感度，发现约 10% 的层（多集中在某些注意力投影与 lm_head）占精度损失大头，把它们留 INT8、其余 INT4，结果比全 INT4 精度只掉 1 分、显存仅多 5%，性价比最高。进一步，embedding 表保留 FP16 后困惑度再降 0.3。",
  "edgeCases": [
    "敏感层跨模块分布不均：可能 80% 预算集中在 10% 层，需支持非均匀切分。",
    "lm_head/embedding 通常对量化极敏感，常整体留高精度。",
    "多精度调度增加 kernel 复杂度与算子碎片，可能抵消硬件加速收益。",
    "激活值量化与权重量化耦合，仅混合权重量化可能不够。"
  ],
  "code": "# Python (敏感度=量化前后输出差异)\ndef layer_sensitivity(layer, x, bits=4):\n    y_fp = layer(x)\n    y_q  = layer(quantize_weights(layer.weight, bits))(x)\n    return (y_fp - y_q).pow(2).mean().item()     # 越大越敏感→留高精度",
  "codeNotes": [
    "常用余弦相似度或 MSE 作敏感指标，越大越敏感→留高精度。",
    "预算分配可用贪心或整数规划，权衡精度与显存。"
  ],
  "complexity": "逐层评估 O(层数 × 样本数 × 单次前向)；敏感度排序 O(层数 log 层数)；搜索分配 O(层数)。对 70B 模型，逐层回放需在数百样本上跑前向，成本可控但非零。",
  "followUps": [
    {
      "question": "哪些层通常要留高精度？",
      "answer": "embedding、lm_head 以及少数对输出 logits 影响大的中间层（如某些注意力投影、归一化附近层）常留 FP16/INT8。经验上越靠近输出、参数越「不可替代」的层越敏感，但具体仍以逐层回放数据为准，不应纯靠直觉。"
    },
    {
      "question": "怎么自动化选层？",
      "answer": "逐层量化回放测敏感指标（MSE/余弦/下游精度下降量），按显存预算贪心地把最敏感层从低位升到高位；也可用整数规划在预算约束下求最优分配。关键是先定义「敏感度度量」与「预算」两个量，再让搜索自动出方案。"
    }
  ],
  "followUpAnswers": [
    "embedding、lm_head 以及少数对输出 logits 影响大的中间层（如某些注意力投影、归一化附近层）常留 FP16/INT8。经验上越靠近输出、参数越「不可替代」的层越敏感，但具体仍以逐层回放数据为准，不应纯靠直觉。",
    "逐层量化回放测敏感指标（MSE/余弦/下游精度下降量），按显存预算贪心地把最敏感层从低位升到高位；也可用整数规划在预算约束下求最优分配。关键是先定义「敏感度度量」与「预算」两个量，再让搜索自动出方案。"
  ],
  "pitfalls": [
    "凭直觉选层而非数据驱动，导致保护错层、预算浪费。",
    "低估混合精度部署复杂度（多 kernel、调度、校准），上线踩坑。",
    "只看权重敏感度忽略激活，末端仍崩。"
  ],
  "beginnerSummary": "全班合影，脸小的人（耐量化层）用缩略图就行，主角（敏感层）得给高清。混合精度就是「谁重要谁高清」，把有限的存储预算花在刀刃上——既省了整体空间，又不让关键人物糊掉。",
  "prerequisites": [
    "层间量化敏感度不同：理解为何不能统一低位。",
    "显存/算力预算约束：压缩的目标边界。",
    "能逐层回放评估：前向与指标度量。"
  ],
  "workedExample": [
    "全 INT4：精度掉 4 分、显存省 4×。",
    "10% 敏感层回 INT8：只掉 1 分、显存仅多 5%。",
    "embedding 留 FP16 后困惑度再降 0.3。"
  ],
  "lineByLine": [
    "逐层量化并测输出差异：量化前后前向对比。",
    "按敏感度排序：MSE/余弦/迹排序。",
    "按预算回升最敏感层精度：贪心抬升。",
    "混合部署：生成 per-layer 精度表。"
  ],
  "diagram": "层敏感度: 高●●● 低○○○○○○\n精度:      FP16 INT4 INT4 INT4 ..."
};

export default {
  "kind": "concept",
  "id": "quant-mixed-precision",
  "category": "量化推理",
  "difficulty": "Medium",
  "title": "混合精度量化",
  "prompt": "混合精度量化是什么，怎么决定哪些层留高精度？",
  "quickAnswer": "混合精度量化的核心是『不同层给不同比特』：敏感层留 FP16/INT8，耐量化层压到 INT4/INT2，在显存与精度间精细权衡。判断敏感度常用『量化前后输出差异』（MSE/余弦相似度）或『对下游精度的边际贡献』做搜索，例如逐层回放评估、或用 Hessian 对角迹近似；把有限的精度预算留给最敏感的少数层，从而在几乎不掉点的前提下最大化压缩比。落地上一般先全量低位得到基线，再逐层回升精度看边际收益，按显存预算贪心分配，必要时用整数规划求最优。",
  "approach": "落地步骤：①先用目标低位（如 INT4）全量量化得到精度基线；②逐层把精度回升一级（INT4→INT8 或 INT8→FP16），在验证集上测下游指标，记录每层的边际收益；③按显存预算，从最敏感层开始贪心地抬升精度，直到预算用尽或精度达标；④形成『哪些层高精度、哪些低位』的部署方案，并可进一步用整数规划在预算约束下求全局最优分配。关键是先定义清楚『敏感度度量』与『预算』两个量，再让搜索自动出方案。",
  "explanationFocus": "是什么：混合精度量化（Mixed-Precision Quantization）按层/模块对量化误差的敏感度，给不同权重分配不同比特宽度——敏感层保留 FP16/INT8，耐量化的层压到 INT4/INT2。它解决的正是『统一低位量化会无谓牺牲关键层』的痛点，在『显存/算力节省』与『精度保持』之间做精细权衡，是部署大模型时最常用的压缩手段之一。与均匀量化相比，混合精度把有限的精度预算精准地投在『最舍不得降精度』的少数层上。",
  "bruteForce": "最朴素的『暴力』做法是所有层统一量化到 INT4：显存是省了，但敏感层（如 lm_head、部分注意力投影）一旦崩了，整体精度掉 4 分，得不偿失。另一种是凭经验拍脑袋挑层（比如『前几层留高精度』），缺乏数据支撑，往往保护错了层、浪费了预算。这两种都回避了『不同层敏感度差异巨大』这一事实。",
  "derivation": [
    "为什么需要：不同层对量化误差的敏感度差异巨大——有的层压到 INT2 几乎无损，有的层（如 lm_head）压到 INT4 就崩。若统一低位，会无谓牺牲关键层、又没省到该省的层；混合精度让有限的精度预算用在刀刃上，避免『一刀切』的浪费与损失。",
    "怎么实现：先定义敏感度指标——逐层量化前后输出的 MSE/余弦相似度，或用 Hessian 对角迹近似该层对损失的贡献；按显存预算从低位起，贪心地把最敏感层一级级升精度（INT4→INT8→FP16）；也可建模为预算约束下的整数规划求全局最优分配。",
    "有什么代价：需要离线的逐层评估与搜索，耗时不菲；部署时要支持多种精度 kernel 与对应的调度逻辑，工程复杂度上升；精度档过多还可能造成算子碎片、抵消硬件加速。另外仅混合『权重量化』可能不够，激活值量化常与权重耦合。",
    "怎么评测：在固定显存预算下，比较混合方案与均匀方案的精度，看是否以更低/相同显存达到更高精度（找拐点）；并在真实推理负载上测延迟与显存是否达标。还应验证敏感性排序的稳定性——换一批校准样本，敏感层集合不应剧烈变动。"
  ],
  "invariant": "混合精度必须满足一条经验不变量：少数敏感层占了精度损失的绝大部分（如 10% 的层占 80% 损失），保护它们收益最大。因此在固定显存预算下，混合方案的精度应不低于同预算的均匀方案；若混合方案反而更差，说明敏感度度量或分配算法有问题，应回退检查。",
  "walkthrough": "以 70B 模型、上下文 batch=32 的部署为例。全部量化到 INT4 时显存约省 4×，但精度掉 4 分，且 lm_head 附近输出 logits 抖动明显。改用混合精度：先用逐层回放测敏感度（在 512 条校准样本上跑前向，记录每层量化前后输出的 MSE），发现约 10% 的层（多集中在部分注意力投影与 lm_head）占了精度损失的 80% 大头。把这 10% 回退到 INT8、其余保持 INT4，结果比全 INT4 只掉 1 分、显存仅多 5%，性价比最高。进一步把 embedding 表保留 FP16 后，困惑度再降 0.3。最终方案在单卡 80GB 上从『装不下』变成『可服务 4k 上下文』。",
  "edgeCases": [
    "敏感层跨模块分布不均：可能 80% 的精度预算集中在 10% 的层，量化方案必须支持非均匀、per-layer 的精度切分，而非整组统一。",
    "lm_head/embedding 通常对量化极敏感，常整体保留高精度（FP16），单独处理而非参与搜索。",
    "多精度调度增加 kernel 复杂度与算子碎片，可能在小 batch 下抵消硬件加速收益，需实测端到端延迟。",
    "激活值量化与权重量化耦合：只做混合权重量化、忽略激活，末端输出仍可能崩，需联合考虑。"
  ],
  "code": "# Python (敏感度=量化前后输出差异)\ndef layer_sensitivity(layer, x, bits=4):\n    y_fp = layer(x)\n    y_q  = layer(quantize_weights(layer.weight, bits))(x)\n    return (y_fp - y_q).pow(2).mean().item()     # 越大越敏感→留高精度",
  "codeNotes": [
    "敏感度指标常用余弦相似度或 MSE：值越大说明该层量化前后输出差异越大→越敏感→应留高精度。",
    "预算分配可用贪心（实现简单）或整数规划（全局最优），核心是『先定义敏感度度量与预算，再搜索分配』。",
    "quantize_weights 需指定 bits，真实实现还要处理缩放因子（scale）与零点（zero-point）的校准。"
  ],
  "complexity": "逐层评估需在 O(层数 × 校准样本数 × 单次前向) 上跑回放，对 70B 模型在数百样本上做前向，成本可控但非零（约数小时 GPU 时）。敏感度排序是 O(层数 log 层数)，分配是 O(层数)（贪心）或 O(层数 × 预算档)（整数规划）。更重要的是部署侧：支持多种精度 kernel 与调度会增加工程复杂度，且若精度档过多可能带来算子碎片，反而抵消硬件加速收益。",
  "followUps": [
    {
      "question": "哪些层通常要留高精度？",
      "answer": "经验上，embedding、lm_head 以及少数对输出 logits 影响大的中间层（如某些注意力投影、归一化附近的层）常保留 FP16/INT8。直觉是：越靠近输出、参数越『不可替代』的层越敏感。但具体仍以逐层回放数据为准，不应纯靠直觉——不同模型、不同任务的敏感层集合可能不同，盲目按『经验名单』挑层常常保护错层。"
    },
    {
      "question": "怎么自动化选层？",
      "answer": "核心是先定义两个量：敏感度度量（如逐层量化回放的 MSE/余弦/下游精度下降量）与显存预算。然后按预算贪心地把最敏感层从低位一级级升到高位；也可用整数规划在预算约束下求全局最优分配。自动化选层的好处是可复现、可随模型/校准集变化重新搜索，而不是写死一套经验规则。关键是搜索出的方案要在真实负载上验证端到端延迟与精度。"
    }
  ],
  "followUpAnswers": [
    "经验上，embedding、lm_head 以及少数对输出 logits 影响大的中间层（如某些注意力投影、归一化附近的层）常保留 FP16/INT8。直觉是：越靠近输出、参数越『不可替代』的层越敏感。但具体仍以逐层回放数据为准，不应纯靠直觉——不同模型、不同任务的敏感层集合可能不同，盲目按『经验名单』挑层常常保护错层。",
    "核心是先定义两个量：敏感度度量（如逐层量化回放的 MSE/余弦/下游精度下降量）与显存预算。然后按预算贪心地把最敏感层从低位一级级升到高位；也可用整数规划在预算约束下求全局最优分配。自动化选层的好处是可复现、可随模型/校准集变化重新搜索，而不是写死一套经验规则。关键是搜索出的方案要在真实负载上验证端到端延迟与精度。"
  ],
  "pitfalls": [
    "凭直觉选层而非数据驱动：用『前 N 层留高精度』这类经验拍脑袋，往往保护错层、浪费预算，敏感层反而被压低。",
    "低估混合精度部署复杂度：多 kernel、调度、校准都需工程投入，上线才发现某些精度档没有高效算子支撑。",
    "只看权重敏感度忽略激活：仅混合权重量化而激活仍低位，末端 logits 仍可能崩，精度没保住。"
  ],
  "beginnerSummary": "全班合影，脸小的人（耐量化层）用缩略图就够认，主角（敏感层）得给高清。混合精度就是『谁重要谁高清』，把有限的存储预算花在刀刃上——既省了整体空间，又不让关键人物糊掉。反过来，要是给所有人统一发缩略图，主角也糊了；要是全发高清，空间又白浪费。所以关键不是『统一多少位』，而是『哪几层值得多花比特』。",
  "prerequisites": [
    "层间量化敏感度不同：理解『为什么不能统一低位』，以及敏感层集中分布的现象。",
    "显存/算力预算约束：明确压缩的目标边界（如单卡 80GB 要装下 70B）。",
    "能逐层回放评估：会用校准样本跑前向、计算输出差异指标。"
  ],
  "workedExample": [
    "全 INT4：70B 显存省 4×，但精度掉 4 分、lm_head 附近 logits 抖动明显。",
    "10% 敏感层（注意力投影+lm_head）回退 INT8：只掉 1 分、显存仅多 5%，性价比最高。",
    "embedding 保留 FP16 后困惑度再降 0.3，单卡从『装不下』变『可服务 4k 上下文』。"
  ],
  "lineByLine": [
    "y_fp = layer(x)：用全精度权重对该层做前向，得到『基准输出』。",
    "quantize_weights(layer.weight, bits)：把权重量化到指定位宽（如 INT4），引入量化误差。",
    "y_q = layer(量化权重)(x)：用量化权重再过一次前向，得到『量化后输出』。",
    "return (y_fp - y_q).pow(2).mean()：计算两层输出均方误差，值越大说明该层越敏感、越该留高精度。"
  ],
  "diagram": "层敏感度: 高●●● 低○○○○○○\n精度:      FP16 INT4 INT4 INT4 ..."
};

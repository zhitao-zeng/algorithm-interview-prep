export default {
  "kind": "concept",
  "id": "quant-weight-act-kv",
  "category": "量化推理",
  "difficulty": "Medium",
  "title": "权重/激活/KV 量化对比",
  "prompt": "权重量化、激活量化、KV 量化分别量化什么，为何要分开看？",
  "quickAnswer": "权重量化压缩的是模型静态参数，离线即可完成、主要省显存且最容易做；激活量化压缩每层前向产生的中间输出，需处理逐样本变化的 outlier、通常保留较高精度或用细粒度/SmoothQuant；KV 量化压缩注意力缓存，它随序列长度线性增长，是长上下文场景的显存主因。三者动态特性不同，所以粒度与敏感度策略不同：权重可激进(INT4)、激活偏保守或细粒度、KV 按 head/group 量化。常见组合是 W4A16(权重 4 位/激活 16 位)+KV INT8/INT4，分别拿下\"模型大小\"与\"长上下文\"两道显存瓶颈。",
  "approach": "权重离线激进量化(INT4/INT8)省显存最划算；激活因含 outlier 且逐样本变，偏向保留较高精度或细粒度/per-group/SmoothQuant；KV 随序列线性增长，用 INT8/INT4 按 head 或 group 量化。组合策略上常用 W4A16+KV8：压权重、保激活精度、量化 KV 解长上下文。需要时可再上 W4A8(配 SmoothQuant)进一步压激活，换取更高吞吐。",
  "explanationFocus": "是什么：权重量化、激活量化、KV 量化分别压缩\"静态参数 / 中间激活 / 注意力缓存\"三类对象。由于它们的动态特性(是否随样本变、是否随序列长度增长)和敏感度差异很大，必须采用不同的量化粒度与策略，而不能用同一套参数套全 model。常见落地组合是 W4A16(权重 4 位、激活留 16 位)再加 KV 的 INT8/INT4 量化，以兼顾收益与精度。",
  "bruteForce": "最朴素是只量化权重、KV 与激活全留 FP16。短期省了权重显存，但一旦上下文变长，KV 缓存随序列线性膨胀、成为显存杀手，长文本推理仍会 OOM；而且激活若完全不量化也错失了激活侧 GEMM 的加速。说明单点量化无法解决全部瓶颈，必须分对象施策，针对权重与 KV 各自下手。",
  "derivation": [
    "为什么需要：权重是固定的，好压；激活逐样本变化且含 outlier，统一量化易掉点；KV 随上下文长度线性增长，在长文本时才是显存主因。若用同一粒度套三者(比如都 INT8 或都不量化)，要么精度崩要么显存爆。因此必须按各自的动态特性分别设计量化策略。",
    "怎么实现：权重用离线 PTQ(INT4/INT8，per-channel 或 group)；激活用 per-token/per-group 动态量化或 SmoothQuant 把 outlier 难度转移；KV 用 INT8/INT4 按 head 或 group 量化，并在解码时按需反量化参与注意力。三者可自由组合，例如 W4A16 只压权重、KV8 只压缓存。",
    "有什么代价：激活量化因 outlier 与逐样本变化，需要更细粒度或动态 scale，kernel 更复杂、易掉点，故常保留 FP16(如 W4A16)；KV 量化在短序列时收益不明显，且过粗会损伤长程注意力精度，部分层对 KV 精度极敏感需豁免。",
    "怎么评测：分别消融：① 只量化权重看显存与精度；② 加激活量化看吞吐与掉点；③ 加 KV 量化重点在长上下文(如 32k/128k)看显存下降与长程任务精度。最终用显存上限、端到端 token/s、长上下文基准(如长文档 QA)综合判断。"
  ],
  "invariant": "KV Cache 显存占用 ∝ 序列长度 × batch × 层数 × hidden；权重显存 ∝ 参数量(与序列无关)。这个不变关系决定了\"长上下文的瓶颈在 KV、模型大小的瓶颈在权重\"，因此二者的量化收益与策略天然不同，不能互相替代。",
  "walkthrough": "以 13B 模型为例：仅做权重量化 W4 时权重约 26GB→6.5GB，但 KV 与激活仍 FP16，长上下文仍爆显存；当上下文从 4k 增到 32k，KV 缓存可从约 2GB 涨到约 16GB，此时做 INT8 KV 量化可将其减半到约 8GB、直接延长可服务上下文长度。端到端经验上，W4A16+KV8 能在精度几乎不变的前提下把显存从 26GB+ 压到 7GB 量级，是长上下文部署的标配。",
  "edgeCases": [
    "激活 outlier 使整 token 量化失真：某 token 单通道 outlier 拉大 per-token scale，导致该 token 其余值被压成 0。",
    "KV 量化过粗(如 per-tensor)损伤长程注意力：远距离 token 的细微差异被抹平，长文档召回下降。",
    "部分层(尤其浅层或 attention 输出)对 KV 精度极敏感，需混合精度或 exempt，不能一刀切。",
    "短序列场景 KV 量化收益可忽略甚至因反量化开销略变慢，应按序列长度动态决定是否量化。"
  ],
  "code": "# Python (KV 量化示意)\ndef quantize_kv(k, bits=8):\n    # 按 head/group 量化 key/value 缓存\n    s = k.abs().amax(dim=-1, keepdim=True) / (2**(bits-1)-1)\n    k_q = (k / s).round().clamp(-(2**(bits-1)), 2**(bits-1)-1)\n    return k_q, s                                 # 注意时重算 s 反量化",
  "codeNotes": [
    "KV 量化常 per-head 或 per-group：把 scale 放在 head 维，能更好保留注意力中各 head 的相对关系，比 per-tensor 精度好。",
    "W4A16 不量化激活，所以前向里激活仍是 FP16，只有权重的 GEMM 享受低位收益——这是精度/收益的常见折中。",
    "反量化时需用同一 scale 把 k_q 还原：注意推理时要保存 scale 并在取用时重算 s，避免精度偏移。"
  ],
  "complexity": "权重量化离线 O(参数量)、一次性；激活量化与 KV 量化在线发生，复杂度约 O(序列长度 × 维度数)，需要低开销的量化/反量化 kernel 才不会拖累前向。整体显存收益由三者分别贡献，其中 KV 的边际收益随序列长度递增、权重的收益随参数量固定，所以长上下文优化的重心在 KV。",
  "followUps": [
    {
      "question": "为什么 KV 量化对长上下文最关键？",
      "answer": "因为 KV 缓存大小与序列长度成正比(每多一个 token 就多一份 K/V)，在短文本时权重才是显存主因，但一旦上下文拉长到 32k、128k，KV 会线性膨胀成显存杀手。量化 KV(INT8/INT4)能近乎按比例削减这部分显存，直接延长可服务的最大上下文长度，而权重量化对长上下文没有额外边际收益，所以长上下文场景下 KV 量化收益最显著，是绕不开的一步。"
    },
    {
      "question": "W4A16 为什么不量化激活？",
      "answer": "激活含 outlier 且逐样本变化，统一低位量化容易把正常通道压成 0、精度掉得厉害；而权重是静态的、好压。W4A16 的权衡是：把最划算、最稳的权重压到 INT4 省下大部分显存，激活保留 FP16 保住精度，只在权重侧拿加速。若再对激活量化(做 W4A8)收益更大但需配合 SmoothQuant 等处理 outlier，风险更高，所以常用 W4A16 作为稳妥基线。"
    }
  ],
  "followUpAnswers": [
    "因为 KV 缓存大小与序列长度成正比(每多一个 token 就多一份 K/V)，在短文本时权重才是显存主因，但一旦上下文拉长到 32k、128k，KV 会线性膨胀成显存杀手。量化 KV(INT8/INT4)能近乎按比例削减这部分显存，直接延长可服务的最大上下文长度，而权重量化对长上下文没有额外边际收益，所以长上下文场景下 KV 量化收益最显著，是绕不开的一步。",
    "激活含 outlier 且逐样本变化，统一低位量化容易把正常通道压成 0、精度掉得厉害；而权重是静态的、好压。W4A16 的权衡是：把最划算、最稳的权重压到 INT4 省下大部分显存，激活保留 FP16 保住精度，只在权重侧拿加速。若再对激活量化(做 W4A8)收益更大但需配合 SmoothQuant 等处理 outlier，风险更高，所以常用 W4A16 作为稳妥基线。"
  ],
  "pitfalls": [
    "把三者混为一谈、用同一粒度与同一 scale 套权重/激活/KV，导致要么 OOM 要么精度崩。",
    "忽略 KV 量化只在长序列才显现收益，短序列上白加开销；或在长序列上却忘了量化 KV 而 OOM。",
    "对激活直接激进量化却不处理 outlier，整 token 信息丢失，输出质量骤降。"
  ],
  "beginnerSummary": "权重像书架上固定的藏书，压成小开本就省地方(静态、好压)；激活像每次对话临时写的便签，内容多变还有重点词(动态、要小心)；KV 像越聊越长的备忘录，聊得越久越长(随序列线性涨)。三样东西\"压缩难度\"不同：书最省事、便签要留神重点词、备忘录在长聊时才最占空间，所以得分开处理、用不同力度，否则要么 OOM 要么精度崩。",
  "prerequisites": [
    "模型参数静态、激活逐样本动态变化：这决定了权重可离线压、激活需在线小心压。",
    "注意力 KV 缓存随序列长度线性增长，是长上下文显存主因——理解这点才能定位瓶颈。",
    "量化粒度(per-tensor/per-channel/per-group/per-head)直接影响精度，是分对象施策的工具。",
    "了解 W4A16 等常见组合的含义与权衡，才能选择合适的落地方案。"
  ],
  "workedExample": [
    "权重 INT4：13B 模型约 26GB→6.5GB(省 3/4 显存)。",
    "KV INT8：32k 上下文下 KV 约 16GB→8GB，可服务上下文长度近乎翻倍。",
    "组合 W4A16+KV8：整体显存从 26GB+ 降到约 7GB，精度几乎不变，是长上下文部署标配。"
  ],
  "lineByLine": [
    "权重: 离线用 scale/zero 做 INT4/INT8 量化，部署时直接低位 GEMM，最简单收益最稳。",
    "激活: 在线用 per-token 动态 scale 或 SmoothQuant 处理 outlier，W4A16 下则完全保留 FP16。",
    "KV: 按 head/group 求 scale 量化 key/value，解码时反量化参与注意力，随序列增长持续省显存。",
    "组合: 通常 W4A16(压权重)+KV8(压缓存)，激活视精度需求决定，三者叠加得到综合显存/吞吐收益。"
  ],
  "diagram": "权重(静态) ─▶ INT4 省显存\n激活(动态) ─▶ 细粒度/留16\nKV(随长度) ─▶ INT8 解长上下文"
};

export default {
  "id": "quant-deploy-pitfalls",
  "kind": "concept",
  "category": "量化推理",
  "difficulty": "Hard",
  "title": "部署量化模型的坑",
  "prompt": "把量化模型部署上线有哪些常见坑，怎么规避？",
  "code": "# Python (部署前断言)\ndef sanity(quant_model, sample):\n    assert not torch.isnan(quant_model(sample)).any()   # 防溢出NaN\n    # 校验 scale/零点形状、kernel 实际精度路径",
  "diagram": "量化模型 ─▶ [溢出?][回退?][变慢?] ─▶ 实测 ─▶ 灰度上线",
  "explanationFocus": "是什么：部署量化模型的工程陷阱，集中在数值正确性（溢出/精度损失）、性能（反融合导致变慢）与框架兼容性（精度不被支持而静默回退）三大类；它们往往在测试集上显形不出来，只在线上高并发、长尾输入、分布漂移时才爆发，所以必须靠实测与灰度来规避。",
  "quickAnswer": "量化部署常见坑有六类：①数值溢出（INT32 累加溢出、FP8/E4M3 超 ±448 未 clamp 出 NaN）；②dequant 反量化解码未与计算融合，反而比 FP16 更慢；③推理框架不支持某精度组合，静默回退到高精度，『省了显存却没加速』；④scale/零点（zero-point）形状或轴对齐错误，结果整体偏移；⑤校准集（calibration）分布与线上分布漂移，导致量化后精度掉点；⑥混精度（部分层 INT8、部分 FP16）调度复杂易配错。规避靠：显存/时延/精度三维实测、用成熟 kernel、校准数据贴近线上、逐步灰度 + 监控 P99 与异常输出。",
  "beginnerSummary": "把量化上线想成把一辆大货车（FP16 大模型）换成一队小卡车（INT8）来省油（省显存）。但有三道坎：一是小卡车装超重会翻车（数值溢出出 NaN）；二是改装时没把装卸流程合并，反而比大货车还慢（dequant 未融合）；三是有些桥（框架）根本不让小卡车过，偷偷让你还开大货车（静默回退到 FP16），你以为省了油其实没省。所以上线前得在真实路上试跑、逐步放行，别一股脑全换。",
  "walkthrough": "某对话服务把 13B 模型从 FP16 量到 INT8 后，单请求 P99 时延反而涨了 30%——排查发现把 MatMul 输出先 dequant 回 FP16 再进下一层（未融合），多了一次大张量转换。把 dequant 融合进后续算子的 kernel 后，P99 反降 15%。随后发现校准只用公开通用语料，线上多是客服短句，部分层 scale 估偏，补了线上采样语料重新校准后，精度回到与 FP16 基本持平（掉 <0.5%）。",
  "approach": "上线前按『显存/时延/精度』三维实测，配合灰度 checklist：先确认框架对该 (精度, 算子, 形状) 组合有原生 kernel（否则会回退）；再确认 dequant 已融合、无多余类型转换；再用线上分布的数据做校准并验证精度；最后小流量灰度，监控 P99、错误率、NaN/Inf 出现率，达标再全量。",
  "bruteForce": "量化完直接替换上线，只在一两条样例上目测输出正常就切流，不测 P99 也不看框架是否真走低精度路径。结果要么被回退导致『假量化』白做，要么某长尾输入触发溢出 NaN 污染整批回复，事故上线后才发现。",
  "invariant": "当且仅当『线上分布 ≈ 校准分布』且『框架确有对应低精度 kernel 且已融合』时，量化才是安全且真正加速的；任一不满足都应暂停或回退。",
  "complexity": "排查成本主要是 O(问题定位时间)：需要 profiling、对照实验、长尾样本复现，往往比量化本身更耗时。正确部署后推理开销与正常量化一致（一次融合 kernel 完成量化计算+反量化），不产生额外延迟；校准本身是一次前向统计，成本可忽略。",
  "derivation": [
    "为什么需要：量化用低比特换省显存/提速，但引入了『溢出、回退、变慢、漂移』等全新失败模式，且这些模式在离线小测里常不暴露，线上出问题代价高，必须系统化规避。",
    "怎么实现：逐项核对——用 profiler 确认 kernel 真走低精度路径（防回退）；把 dequant 融合进相邻算子（防变慢）；对累加/激活做 clamp 防溢出；校验 scale/zero-point 的轴(shape)对齐；用贴近线上分布的数据校准；最后灰度并监控 P99 与异常输出。",
    "有什么代价：排查需投入工程时间，且部分框架对混合精度支持差，可能要自写或等社区 kernel；为保精度有时只能对敏感层保留 FP16，损失部分收益。",
    "怎么评测：A/B 灰度对比精度与时延，监控 P99、错误率、NaN/Inf 率；用线上采样语料做校准集再验一遍，确保精度掉点在可接受阈值（如 <1%）内。"
  ],
  "edgeCases": [
    "框架发现某算子无 INT8 kernel，会静默回退到 FP16 计算，表面显存省了实则没加速，需用 profiler 抓出。",
    "长尾/异常输入（超长序列、极端数值）触发 INT32 累加溢出或 FP8 超范围，输出变 NaN/Inf 污染后续 token。",
    "多卡/多节点 kernel 实现对齐不一致，A 卡结果正常、B 卡溢出，问题只在特定机器出现，极难复现。",
    "权重接近 0 的通道，scale 估得极小，量化后舍入误差占比暴增，该通道输出整体偏移。"
  ],
  "pitfalls": [
    "只测平均时延和均值指标，不看 P99，结果大部分请求快了但长尾用户卡死，体验崩坏。",
    "忽略框架的精度回退（silent fallback），以为部署了量化，实际全是 FP16 在跑，显存和钱都白省。",
    "用与线上分布差异大的公开数据集做校准，导致 scale/零点估偏，精度静默掉点。"
  ],
  "prerequisites": [
    "量化会引入新失败模式（溢出、回退、变慢），不能当成『无代价优化』。",
    "框架是否原生支持某精度组合，决定了量化到底加不加速。",
    "线上数据分布需与校准分布匹配，否则量化参数失效。",
    "会用 profiler/监控看真实执行路径与时延分位。"
  ],
  "workedExample": [
    "dequant 未融合：INT8 部署后 P99 涨 30%；融合后 P99 降 15%，吞吐翻倍。",
    "校准补线上客服语料：精度从掉 3 分回到掉 <0.5 分，达上线标准。",
    "某算子静默回退 FP16：显存省 40% 但时延零变化，profiler 发现后换支持 INT8 的实现才真正提速。"
  ],
  "lineByLine": [
    "核对 kernel 精度支持：确认目标 (精度, 算子, 形状) 有原生低精度 kernel，否则会回退。",
    "融合 dequant 防变慢：把反量化算进相邻算子，避免一次大张量类型转换。",
    "clamp 防溢出 NaN：对 INT32 累加和 FP8 范围做截断，避免极端值出 Inf。",
    "灰度实测三维指标：小流量对比显存/时延/精度，监控 P99 与异常输出再全量。"
  ],
  "codeNotes": [
    "务必断言模型输出无 NaN/Inf，这是溢出最直观的信号，能拦住大部分线上事故。",
    "检查框架是否真走低精度路径（看 profiler 或显存/时延是否达预期），没达即可能回退。",
    "sanity 检查后还应跑一小批线上分布样本比对精度，单看断言不够。"
  ],
  "followUps": [
    {
      "question": "怎么发现框架偷偷回退到 FP16？",
      "answer": "用 profiler（如 PyTorch Profiler、TensorRT/TRT-LLM 的层执行日志）看是否真调用了 INT8/FP8 的 kernel；或者对比『量化前后显存占用』和『量化前后时延』是否达到理论预期——如果显存没怎么降、时延几乎不变，基本就是回退了。更稳的是在部署前对照框架的 supported-ops 文档逐算子核对。"
    },
    {
      "question": "溢出通常出现在哪里，怎么防？",
      "answer": "主要两处：一是 INT32 累加器在长序列/大 batch 的矩阵乘里累加项数过多溢出，需做分块累加（tiled accumulation）或用更大累加类型；二是 FP8/E4M3 的动态范围仅 ±448，激活值超过就饱和成 Inf/NaN，需在量化前对激活做 clamp（如 clip 到 ±448 范围）。两者都靠『分块 + clamp』解决，并在上线前用长尾样本做溢出压测。"
    }
  ],
  "followUpAnswers": [
    "用 profiler（如 PyTorch Profiler、TensorRT/TRT-LLM 的层执行日志）看是否真调用了 INT8/FP8 的 kernel；或者对比『量化前后显存占用』和『量化前后时延』是否达到理论预期——如果显存没怎么降、时延几乎不变，基本就是回退了。更稳的是在部署前对照框架的 supported-ops 文档逐算子核对。",
    "主要两处：一是 INT32 累加器在长序列/大 batch 的矩阵乘里累加项数过多溢出，需做分块累加（tiled accumulation）或用更大累加类型；二是 FP8/E4M3 的动态范围仅 ±448，激活值超过就饱和成 Inf/NaN，需在量化前对激活做 clamp（如 clip 到 ±448 范围）。两者都靠『分块 + clamp』解决，并在上线前用长尾样本做溢出压测。"
  ]
};

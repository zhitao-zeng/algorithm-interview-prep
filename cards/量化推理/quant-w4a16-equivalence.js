export default {
  "kind": "concept",
  "id": "quant-w4a16-equivalence",
  "category": "量化推理",
  "difficulty": "Medium",
  "title": "W4A16 的等价性",
  "prompt": "W4A16 为什么常说\"等价\"于某种计算，权重 4 位激活 16 位意味着什么？",
  "quickAnswer": "W4A16 指权重存为 INT4、激活保持 BF16/FP16。其\"等价性\"指：计算时权重可反量化回 16 位再与激活做普通 GEMM，数学结果等价于 FP16 权重(仅差量化舍入)；也可在整数域算后乘 scale 还原。它只压权重显存、激活不压，故最省显存且对精度最友好，是 LLM 部署性价比首选；但计算量本身基本不降，速度提升主要来自\"能装下更大模型/更长 batch\"带来的吞吐增益。",
  "approach": "权重离线量化为 INT4（常 per-group 带 scale），激活留 16 位；推理时 dequant 回 16 位与激活相乘，或在整数域做 QMM 后乘 scale 还原。",
  "explanationFocus": "是什么：W4A16 是权重 4 位(INT4)、激活 16 位(BF16/FP16)的混合精度配置，它强调\"权重可在计算时被等价反量化回 16 位、再与 16 位激活做普通 GEMM\"，其数学结果仅差量化舍入误差，因此在精度上近似无损。它不是\"加速\"配置，而是\"省显存\"配置。",
  "bruteForce": "权重和激活都压成 INT4（W4A4）：显存更省但激活对量化极敏感，小数值波动被放大，精度断崖式下跌，常出现乱码或事实错误。",
  "derivation": [
    "为什么需要：LLM 推理时权重占显存大头（70B 模型 FP16 权重就 140GB），而激活相对小且对量化极敏感；只压权重就能拿到绝大部分显存收益，又不伤精度。W4A16 正是\"抓主要成本\"的取舍：用最小的精度代价换最大的部署自由。",
    "怎么实现：权重按 per-group（如每 128 个元素一组）量化成 INT4 并存储 group scale/zero；推理时把 INT4 反量化回 BF16 再与 16 位激活做普通 GEMM，或在整数域做 INT4×INT16 矩阵乘后乘回 scale 还原。per-group 的小 scale 能纠正该组内的分布偏移，使反量化后近无损。",
    "有什么代价：dequant 有额外开销，若每次都先 materialize 出完整 FP16 权重再乘，既慢又占瞬时显存；必须有高效的 INT4 存储/反量化 kernel，把 dequant 融合进 GEMM（权重边读边解包边算）才能既省显存又快。",
    "怎么评测：对比 W4A16 与 FP16 在显存占用、困惑度(PPL)与下游任务准确率上的差距，看是否近无损（通常 PPL 升 < 1%）；同时压测吞吐，确认能服务更大模型或更长的 batch。若某层敏感，可在该层混合留高精度(W8 或 FP16)做补偿。"
  ],
  "invariant": "W4A16 显存 ≈ FP16 的 1/4（仅权重部分）；激活路径保持 16 位、精度无损；反量化后参与的计算与 FP16 GEMM 仅差 INT4 舍入误差。",
  "walkthrough": "以 70B 模型部署为例：FP16 权重 140GB、激活与 KV 另算，单卡 80GB 根本装不下；改 W4A16 后权重约 35GB（70B×4bit÷8 + group scale 开销≈5%），单卡 80GB 轻松装下并留足 KV cache。实测 PPL 从 FP16 的 2.85 升到 2.87（<1%），人评几乎无感；由于能跑 batch=32 而非被迫 batch=1，总吞吐从 ~20 tokens/s 升到 ~180 tokens/s。反量化在融合 kernel 内完成，单步延迟与 FP16 仅差约 5%。",
  "edgeCases": [
    "dequant 开销侵蚀速度：未融合的 kernel 把反量化做成独立步骤，吞吐量可能反不如 FP16，必须上融合 kernel。",
    "极敏感层（如部分 attention 投影）：可在 W4A16 基础上对该层混合留 W8/FP16，做局部补偿而不整体退精度。",
    "group 大小影响等价精度：group 越小（如 32）越准但 scale 存储越多；group 越大（如 256）越省存储但误差越大，需权衡。",
    "校准集选择：量化 scale 依赖校准数据统计，校准集与真实分布偏差大时等价性下降，需覆盖典型输入。"
  ],
  "code": "# Python (W4A16 推理)\ndef w4a16_forward(x_bf16, w_q_int4, scales):\n    w = dequant_int4(w_q_int4, scales).bfloat16()  # 权重反量化回16位\n    return x_bf16 @ w                               # 普通 BF16 GEMM",
  "codeNotes": [
    "W4A16 也可整数域算（INT4×BF16 再乘 scale）再还原，与反量化回 16 位做 GEMM 数学等价，选实现更简单/更快的那种。",
    "group 缩放（per-group scale）保证反量化后近无损，是等价性的关键；group 越小越准但存储越多。",
    "生产用融合 kernel：权重从 INT4 存储边读边解包边参与 GEMM，避免 materialize 完整 FP16 权重。"
  ],
  "complexity": "存储省 4×（权重），但计算退化为\"BF16 GEMM + 反量化开销\"：反量化 O(参数) 量级，若未融合进 kernel 会成为瓶颈。以 70B 模型为例，FP16 权重 140GB、W4A16 权重约 35GB（每权重 4bit + 少量 group scale），显存从\"装不下\"变成\"单卡 80GB 可装\"；FLOPs 与 FP16 基本持平，故单步延迟相近，收益体现在能上更大 batch、提总吞吐。",
  "followUps": [
    {
      "question": "W4A16 和 W8A8 怎么选？",
      "answer": "目标不同：W4A16 主打\"最大省显存\"，权重压到 1/4，激活留 16 位保精度，适合显存吃紧但要保质量的场景；W8A8 主打\"算力加速\"，权重激活都 INT8、走整数 GEMM，吞吐更高但精度余量更小。实务常先上 W4A16 保精度与可部署性，当算力成为瓶颈、且模型对 INT8 够鲁棒时，再切 W8A8 提速度。也可混合：多数层 W4A16，少数敏感层留高精度。"
    },
    {
      "question": "反量化会不会很慢？",
      "answer": "取决于 kernel：若先 materialize 出完整 FP16 权重再乘，既慢又可能 OOM，确实成瓶颈；但好的实现把 dequant 融合进 GEMM——权重从 INT4 存储边读边解包边参与计算，额外开销很小（实测单步延迟仅多几个百分点）。所以选对支持融合反量化的推理框架（如 AWQ/GPTQ 的专用 kernel）是关键，而不是反量化本身慢。"
    }
  ],
  "followUpAnswers": [
    "目标不同：W4A16 主打\"最大省显存\"，权重压到 1/4，激活留 16 位保精度，适合显存吃紧但要保质量的场景；W8A8 主打\"算力加速\"，权重激活都 INT8、走整数 GEMM，吞吐更高但精度余量更小。实务常先上 W4A16 保精度与可部署性，当算力成为瓶颈、且模型对 INT8 够鲁棒时，再切 W8A8 提速度。也可混合：多数层 W4A16，少数敏感层留高精度。",
    "取决于 kernel：若先 materialize 出完整 FP16 权重再乘，既慢又可能 OOM，确实成瓶颈；但好的实现把 dequant 融合进 GEMM——权重从 INT4 存储边读边解包边参与计算，额外开销很小（实测单步延迟仅多几个百分点）。所以选对支持融合反量化的推理框架（如 AWQ/GPTQ 的专用 kernel）是关键，而不是反量化本身慢。"
  ],
  "pitfalls": [
    "以为 W4A16 也加速计算：它主要省显存，单步算力与 FP16 接近；速度提升只来自\"装得下→更大 batch→更高吞吐\"，别指望单请求变快。",
    "dequant 未融合导致慢且占瞬时显存：先把整份权重解包成 FP16 再算，既慢又可能 OOM，必须用按块解包融合计算的 kernel。",
    "忽略敏感层混合精度：对所有层一刀切 W4，个别层崩了整体质量掉，应允许 per-layer 升级。"
  ],
  "beginnerSummary": "W4A16 好比把厚厚的词典(权重)缩印成巴掌大的小册子(INT4)带着走——地方一下子省了四分之三，用的时候把小册子摊开还原成原样(反量化)再查，内容基本不变，只是携带轻便了。而\"查询时写的笔记\"(激活)仍然用原版不缩印，保证查得准。所以 W4A16 最大的好处是\"装得下\"：一本 140GB 的大词典缩到 35GB，单张显卡就能揣走，还能顺手多服务几个请求。但它并不让\"查一次\"变快——计算量和原来差不多，速度提升只是因为装得下所以能同时服务更多人。要是你以为它能加速单请求，就理解错了。",
  "prerequisites": [
    "理解权重占显存大头、激活较敏感，因此\"只压权重\"是高性价比切入点。",
    "具备反量化可还原权重的数学认知：INT4 + group scale 能近似重建 FP16 权重，仅差舍入。",
    "有支持 INT4 存储与融合反量化 GEMM 的推理框架/kernel（如 AWQ、GPTQ 配套 kernel），否则省了显存却丢了速度。"
  ],
  "workedExample": [
    "70B：FP16 权重 140GB → W4A16 约 35GB，单卡 80GB 可装下并近无损推理，PPL 仅升 0.7%。",
    "权重 INT4 + 激活 BF16：dequant 后做普通 BF16 GEMM，输出分布与 FP16 几乎重合，下游准确率差 < 0.5%。",
    "吞吐对比：因能上 batch=32，总吞吐从 20 tok/s 升到 180 tok/s，速度收益来自更大 batch 而非单步更快。"
  ],
  "lineByLine": [
    "权重按 per-group INT4 存储（含 group scale/zero），把 FP16 权重量化压缩到 1/4 显存。",
    "推理时把 INT4 权重反量化回 16 位（融合 kernel 内边读边解包，不 materialize 完整 FP16）。",
    "与 16 位激活做普通 GEMM（或在整数域算后乘 scale 还原），两种路径数学等价、仅差舍入。",
    "输出层保持 16 位精度，因此激活路径无损，整体近似等价于 FP16 计算。"
  ],
  "diagram": "权重 INT4 ─▶ dequant ─┐\n激活 BF16 ───────────┴─▶ GEMM (近无损)"
};

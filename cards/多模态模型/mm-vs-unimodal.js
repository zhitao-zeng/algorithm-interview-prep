export default {
  "kind": "concept",
  "id": "mm-vs-unimodal",
  "category": "多模态模型",
  "difficulty": "Medium",
  "title": "多模态 vs 单模态性能权衡",
  "prompt": "引入多模态能力相比纯文本模型，在性能和成本上要权衡什么？",
  "quickAnswer": "收益：多模态带来图文/视频理解、OCR、具身等新能力，在多模态任务上显著优于纯文本。代价：多一条视觉/音频链路 → 更多参数、更长序列（视觉 token 放大 KV 与注意力）→ 更高训练/推理成本，且若对齐不足反而引入幻觉拖累纯文本能力。权衡点：是否真需多模态、视觉 token 数、是否分离部署、是否按需加载模态。",
  "approach": "列收益（新能力/多模态任务）vs 代价（参数/序列/幻觉/成本）→ 按需启模态。能用适配器/MoE 轻量接入就别全参膨胀，能按需跳过视觉就别无图也付费。",
  "explanationFocus": "是什么：多模态 vs 单模态权衡，是在获得看图/听声等新能力的同时，承担更长的序列、更高的训练推理成本与潜在幻觉风险之间做取舍。核心问题是\"这份额外开销带来的价值，是否值得\"。",
  "bruteForce": "无脑把所有模态都接上、全参数训练：成本高、训练慢，且模态冲突可能让原本擅长的文本能力回退，还徒增每次请求的视觉开销（即便没图）。",
  "derivation": [
    "为什么需要：资源有限，需判断多模态带来的价值是否值得额外开销与风险，避免为用而用。",
    "怎么实现：评估任务是否真含非文本信号；控制视觉 token 数；用适配器/MoE 轻量接入避免全参膨胀；分离或按需加载编码器；监控纯文本能力回退并保留文本数据。",
    "有什么代价：序列变长使注意力/KV 成本上升、推理更慢更费显存；多模态数据与训练投入大；弱对齐致幻觉、可能拖累文本。",
    "怎么评测：多模态任务增益（vs 纯文本上界）、纯文本基准回退幅度、单位成本准确率（Pareto 前沿），综合决定要不要、怎么接。"
  ],
  "invariant": "引入模态后纯文本能力不应显著回退（除非刻意取舍）；按需路由保证无图请求不付视觉代价。",
  "walkthrough": "在 7B 基座上加 ViT-B + 轻量 MLP 连接器约 +0.3B 参数，视觉 token（如 256 张图 token）使每请求 KV 缓存增约 30%，但图文 VQA 任务准确率从纯文本的 0（根本看不到图）跃升到可用（如 75%）。纯文本基准（如 MMLU）若训练配比不当可能回退 1~2 个点，需用文本数据保底。",
  "edgeCases": [
    "纯文本请求也带视觉开销：需按需路由跳过编码器，否则每次都白付 ViT 成本。",
    "小模型接多模态易过载：可用适配器轻量接入，避免视觉塔挤占本就紧张的容量。",
    "模态冲突致某任务回退：如加了图后某些纯文本推理题变弱，需文本数据保底。",
    "训练数据里图文配比失衡：图太多压文本，文本能力滑坡。"
  ],
  "code": "def route(inputs, model):\n    if 'image' in inputs:\n        vt = model.vision_encoder(inputs['image'])   # 按需才编码\n        return model.generate(concat(vt, inputs['text']))\n    return model.generate(inputs['text'])            # 纯文本跳过视觉",
  "codeNotes": [
    "按需编码避免无图也付费：'image' in inputs 决定要不要跑视觉塔，是控制成本的关键分支。",
    "监控文本能力回退：多模态训练后要回测纯文本基准，发现回退及时加文本数据。",
    "真实系统还可分离部署视觉服务，与 LLM 解耦伸缩。"
  ],
  "complexity": "多模态请求额外 O(V²) 编码 + 放大 LLM 注意力（序列多了 V 个 token）；参数增量取决于接入方式（适配器仅百万级、全参则整个视觉塔 + 对齐层）。纯文本请求走路由跳过编码，成本同单模态。",
  "followUps": [
    {
      "question": "多模态会拖累纯文本能力吗？",
      "answer": "对齐不足或训练配比不当会回退。原因有二：一是容量被视觉塔/连接器分走，小模型尤甚；二是多模态数据稀释了文本数据的训练占比。缓解办法：用适配器/LoRA 轻量接入（不全参）、保留充足纯文本数据继续训练、训练后回测 MMLU 等纯文本基准，发现回退就补文本样本或冻结部分文本能力。"
    },
    {
      "question": "什么时候不该用多模态？",
      "answer": "当任务纯文本、对延迟/成本极敏感、且输入根本不含图像/音频时，强行多模态只增负担——每次请求多付视觉编码、序列更长、显存更紧，还可能引入额外幻觉。此时应保持纯文本模型，或用按需路由确保无图请求完全跳过视觉链路。多模态是\"为能力付费\"，没对应需求就别付。"
    }
  ],
  "followUpAnswers": [
    "对齐不足或训练配比不当会回退。原因有二：一是容量被视觉塔/连接器分走，小模型尤甚；二是多模态数据稀释了文本数据的训练占比。缓解办法：用适配器/LoRA 轻量接入（不全参）、保留充足纯文本数据继续训练、训练后回测 MMLU 等纯文本基准，发现回退就补文本样本或冻结部分文本能力。",
    "当任务纯文本、对延迟/成本极敏感、且输入根本不含图像/音频时，强行多模态只增负担——每次请求多付视觉编码、序列更长、显存更紧，还可能引入额外幻觉。此时应保持纯文本模型，或用按需路由确保无图请求完全跳过视觉链路。多模态是\"为能力付费\"，没对应需求就别付。"
  ],
  "pitfalls": [
    "认为多模态只增不损：对齐不足会引入幻觉、还可能拖累纯文本，不是免费午餐。",
    "无图请求也跑视觉链路：路由缺失导致每次请求都付编码代价，成本高且可能引入噪声。",
    "盲目全参微调视觉塔，训练成本与灾难性遗忘风险双高。"
  ],
  "beginnerSummary": "给只会写字的人加\"眼睛\"（多模态）能让他看图答题，很值；但眼睛、翻译、额外记忆都要花钱（算力），图看太多还容易说胡话（幻觉）。所以值不值，要看你到底要不要他看图。纯聊天的活儿就别让他睁眼，省电；真要看图再开眼睛，并盯着他别退化了原本的文笔（纯文本能力）。",
  "prerequisites": [
    "多模态增加参数与序列：理解视觉塔 + 连接器带来额外参数，视觉 token 拉长序列。",
    "视觉 token 放大注意力成本：序列每多 V 个 token，注意力 O(n²) 与 KV 同步上升。",
    "弱对齐可能引入幻觉：视觉与文本空间若没对齐好，模型易\"看错\"并自信胡说。"
  ],
  "workedExample": [
    "加 ViT-B + MLP 约 +0.3B 参数：图文 VQA 从 0（看不到图）到 ~75% 可用。",
    "纯文本请求走路由跳过编码：成本同单模态，且 MMLU 回退控制在 1~2 点内。",
    "小模型用 LoRA 适配器接视觉：避免全参膨胀致容量爆炸。"
  ],
  "lineByLine": [
    "评估是否真需多模态：先确认任务含非文本信号，避免为用而用。",
    "控制视觉 token 数降本：token 越少序列越短、越便宜，但别少到看不清。",
    "按需/分离加载模态：route 函数无图就跳视觉，省成本也防噪声。",
    "监控纯文本能力回退：保留文本数据与评测，防止\"睁眼瞎了文笔\"。"
  ],
  "diagram": "纯文本模型 ─▶ +视觉链路 ─▶ 多模态\n   收益:新能力 / 代价:序列↑·成本↑·幻觉风险"
};

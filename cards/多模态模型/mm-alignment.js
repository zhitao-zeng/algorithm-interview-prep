export default {
  "kind": "concept",
  "id": "mm-alignment",
  "category": "多模态模型",
  "difficulty": "Medium",
  "title": "跨模态对齐 alignment",
  "prompt": "多模态模型里说的\"跨模态对齐\"具体指什么，有哪些做法？",
  "quickAnswer": "跨模态对齐常见三类做法。(1) 表示对齐——用线性层/MLP/Q-Former 把视觉特征投影到文本嵌入空间；(2) 对比对齐——CLIP 式 InfoNCE，用海量图文对把匹配对拉近、非匹配对推远；(3) 指令对齐——用多模态指令数据微调，让模型按指令做跨模态推理。三者常组合：投影/Q-Former 做结构对齐，对比损失做语义对齐，指令 SFT 做行为对齐。对齐质量直接决定模型能否「看懂」并正确使用图像信息，对齐不足则幻觉泛滥。",
  "approach": "先选投影/对比/指令三种手段之一或组合，把视觉流拉到文本流同一空间；再用对比损失或指令信号监督对齐质量，使视觉 token 与文本 token 在空间中相邻且可互相替代参与推理。",
  "explanationFocus": "是什么：跨模态对齐是让图像、文本等不同模态的表示在语义与行为上保持一致，使模型能把「看到的」和「说出的」对应起来。对齐不是简单的维度拼接，而是要让同一语义在不同模态的 token 落在可互相替代、可联合推理的同一坐标空间里。对齐质量直接决定多模态模型能否真正「看懂」并使用图像信息，而不是只会机械拼接。",
  "bruteForce": "两模态各训各的编码器、最后只在输出层拼结果：语义空间各自为政，模态间存在漂移，无法做联合推理，模型答非所问。",
  "derivation": [
    "为什么需要：模态异构且各自分布不同（图像是连续像素网格、文本是离散 token 流），不对齐则 LLM 读不懂视觉 token，也答非所问，跨模态推理无从谈起。",
    "怎么实现：表示对齐用线性/MLP 投影、Q-Former 把图像压缩成少数查询 token；对比对齐用图文 InfoNCE 把匹配对拉近、非匹配对推远；指令对齐用多模态 SFT 让输出服从指令、学会按图推理。",
    "有什么代价：对齐不足则幻觉多（凭语言先验编造）；过度对齐可能抹平模态特有信息（如图像里的精细纹理）；指令对齐需要高质量多模态标注，获取成本高。",
    "怎么评测：跨模态检索（图文互搜）、grounding（指代表达理解）、幻觉率（POPE 等基准）、以及 MMBench 对齐相关子项的准确率。"
  ],
  "invariant": "对齐后，同一语义的视觉 token 与文本 token 在表示空间中相邻，且可以互相替代地参与后续推理（例如用文本「红苹果」能检索到图中红苹果的视觉 token）。",
  "walkthrough": "以 LLaVA-1.5 为例：ViT-L 把 336×336 图像切成 24×24=576 个 patch，输出 576 个 1024 维视觉 token；两层 MLP 把它投影到 LLM 的 4096 维空间，得到 576 个 4096 维视觉 token；再拼接文本 token 一起送进 Vicuna，用多模态指令数据做 SFT 完成行为对齐。投影后视觉 token 与文本 token 处在同一 4096 维坐标，LLM 才能直接「读」图。",
  "edgeCases": [
    "细粒度对齐（物体局部/属性）比全局语义对齐难得多，常需 region-level 标注或 grounding 辅助。",
    "多物体、复杂场景里不同物体视觉 token 易混淆，投影后区分度下降。",
    "引入新模态（如音频、视频）需重新设计对齐器，旧投影不能直接复用。",
    "低资源语言缺少图文对，对比对齐效果差，只能靠指令对齐补。"
  ],
  "code": "def align_modalities(visual_feat, text_feat, projector):\n    v_aligned = projector(visual_feat)        # 视觉 → 文本空间\n    sim = cos_sim(v_aligned, text_feat)       # 对齐质量可测\n    return v_aligned, sim",
  "codeNotes": [
    "projector 参数少时只做线性对齐，参数多（如两层 MLP 或 Q-Former）时能学非线性映射，表达力更强。",
    "对齐后可接对比损失（如 InfoNCE）监督质量，否则投影容易学到平凡解（所有视觉 token 塌缩到一个点）。"
  ],
  "complexity": "投影器参数规模 O(d_visual·d_text)，视 patch 数线性；对比对齐（InfoNCE）对 N 个图文对需 O(N²) 相似度计算，常借大 batch 或队列缓解；指令对齐成本取决于 SFT 数据量与训练步数。",
  "followUps": [
    {
      "question": "表示对齐和对比对齐具体区别是什么，能不能只用一个？",
      "answer": "表示对齐（投影）是直接把特征映射到同维度同空间，本身没有显式正负对监督，容易学到平凡解；对比对齐用正负图文对拉近距离，提供语义监督但不改结构维度。二者常互补：先投影做结构对齐，再 InfoNCE 做语义对齐。只做投影不接对比/指令损失，模型容易把视觉 token 全塌缩到一个点。"
    },
    {
      "question": "对齐不够会有什么现象，怎么发现？",
      "answer": "典型现象是视觉幻觉：模型无视图像内容、凭语言先验编造答案（如问图里有没有某物，它乱答）。可用 POPE、CHAIR 等幻觉基准量化，或做跨模态检索看图文能否互搜；若检索准确率很低，说明对齐没做好。"
    }
  ],
  "followUpAnswers": [
    "表示对齐（投影）是直接把特征映射到同维度同空间，本身没有显式正负对监督，容易学到平凡解；对比对齐用正负图文对拉近距离，提供语义监督但不改结构维度。二者常互补：先投影做结构对齐，再 InfoNCE 做语义对齐。只做投影不接对比/指令损失，模型容易把视觉 token 全塌缩到一个点。",
    "典型现象是视觉幻觉：模型无视图像内容、凭语言先验编造答案（如问图里有没有某物，它乱答）。可用 POPE、CHAIR 等幻觉基准量化，或做跨模态检索看图文能否互搜；若检索准确率很低，说明对齐没做好。"
  ],
  "pitfalls": [
    "把对齐简单等同于「能拼接」：拼接只是空间对齐，语义一致性还需对比/指令损失来保证。",
    "对齐过度反而丢失模态特有细节，例如把图像中细微但关键的文字/数字信息抹平。"
  ],
  "beginnerSummary": "对齐就像把中文和英文都翻成同一种「世界语」，让说不同语言的人能互相理解。多模态里就是让「图的意思」和「词的意思」落在同一套坐标上，这样模型既能看图也能用词去指图里的东西。没对齐的话，模型只是把图和词硬拼在一起，其实根本没「看懂」。",
  "prerequisites": [
    "不同模态表示空间不同：图像连续、文本离散，直接拼接无法联合推理，必须先映射到共享空间。",
    "需可计算的语义相似度：如余弦相似度，用以衡量图文表示是否对齐。",
    "对齐影响下游推理正确性：对齐差的模型会视觉幻觉，因此对齐是质量的瓶颈而非装饰。"
  ],
  "workedExample": [
    "MLP 把视觉 1024 维投影到文本 4096 维：576 个图像 patch → 576 个 4096 维 token，与文本 token 直接拼接进 LLM。",
    "指令微调让模型按图回答「图里有几只猫」而非凭语言先验瞎编：训练时用 15 万条图文指令对，幻觉率从 30% 降到 12%。"
  ],
  "lineByLine": [
    "选对齐方式（投影/对比/指令）：先决定结构对齐（投影器）与语义对齐（对比/指令）的组合。",
    "把视觉特征映射到文本空间：通过 projector 把 ViT 输出变换到 LLM 嵌入维度。",
    "用对比或指令信号监督对齐：InfoNCE 拉近距离、SFT 让输出服从指令，避免投影塌缩。",
    "验证视觉与文本 token 可互换参与推理：用检索/grounding 任务检验同一语义是否相邻。"
  ],
  "diagram": "视觉特征 ─▶ 投影器/对比/指令 ─▶ 文本空间(同坐标)\n                                  ↓\n                           LLM 联合推理"
};

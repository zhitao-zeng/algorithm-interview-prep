export default {
  "kind": "concept",
  "id": "mm-deploy",
  "category": "多模态模型",
  "difficulty": "Hard",
  "title": "部署：视觉编码器与 LLM 协同",
  "prompt": "线上部署多模态模型时，视觉编码器和 LLM 该怎么协同部署才高效？",
  "quickAnswer": "部署要点：视觉编码器与 LLM 可分开服务——编码器（常更小）做图像→token 的前置/离线计算并缓存；LLM 负责生成。用连续批处理、KV 缓存、编码器量化（INT8/FP8）、Tokenizer 并行，以及把视觉 token 预计算后复用（同图多问）来降本。还要处理变长视觉 token 的 padding 与动态 batch，以及缓存的键管理与失效。",
  "approach": "编码器前置+缓存 → LLM 连续批生成+KV 缓存 → 编码器量化/分离部署 → 同图多问复用视觉 token；用动态 padding/packing 处理变长。",
  "explanationFocus": "是什么：视觉编码器与 LLM 协同部署是把「图像转视觉 token」的前置计算与「语言生成」解耦，通过分离服务、缓存、量化与连续批处理实现高吞吐低延迟。核心思想是：图像编码是确定且可复用的，应前置并缓存；LLM 生成是主要算力消耗，应独立扩缩容与批处理。",
  "bruteForce": "每次请求都重跑整条链路（重新编码同一张图、重新算视觉 token），重复算力浪费，延迟与成本都高。",
  "derivation": [
    "为什么需要：线上要并发、低延迟、低成本，重复编码与同步阻塞会拖垮吞吐；且视觉编码器与 LLM 算力特性不同（编码器短平快、LLM 生成长），适合分别优化。",
    "怎么实现：图像编码前置（可异步/队列），视觉 token 缓存供多轮/多问复用；LLM 独立扩缩容并开 KV 缓存与连续批处理；编码器做 INT8/FP8 量化降显存与耗时。",
    "有什么代价：分离带来一致性/版本管理复杂度（缓存键须含版本）；缓存需键管理与失效策略；变长视觉 token 需动态 padding/packing 以免被最长序列拖慢。",
    "怎么评测：看吞吐（QPS）、P99 延迟、单请求成本、缓存命中率，以及编码前置后首 token 延迟（TTFT）是否下降。"
  ],
  "invariant": "同一图像（同分辨率、同编码器版本）的编码结果可缓存复用，且复用不影响最终生成一致性（缓存键须含分辨率与版本号）。",
  "walkthrough": "同图被问 10 个不同问题：图像编码只做 1 次（约 30ms、消费一份算力），视觉 token 缓存后供 10 次 LLM 生成复用，编码成本降为 1/10；LLM 侧用连续批处理把这 10 个请求合并，KV 缓存命中前缀，p99 延迟从单请求的 3.5s 降到批的 2.1s，单请求成本下降约 40%。",
  "edgeCases": [
    "同图多分辨率（如先缩略后高清）需按分辨率分别缓存，否则命中错版本。",
    "动态 batch 中视觉 token 长度不一（有的图切 6 块、有的切 2 块），直接 padding 到最长浪费算力。",
    "模型版本升级需清缓存：旧视觉 token 与新 LLM 不兼容，必须按版本号失效。",
    "缓存击穿：热点图并发到来，编码未完成前大量请求同时穿透到编码器。"
  ],
  "code": "def serve(image, question, cache, encoder, llm):\n    key = hash(image, enc_version)\n    vt = cache.get(key) or cache.set(key, encoder(image))   # 编码缓存\n    return llm.generate(concat(vt, question))               # 复用视觉 token",
  "codeNotes": [
    "编码缓存是同图多问的关键优化：key = hash(image, enc_version, tile_config)，命中即复用视觉 token。",
    "版本号纳入缓存键防不一致：模型升级后旧 token 必须失效，避免静默错误。"
  ],
  "complexity": "编码 O(V²) 只算一次（V 为视觉 token 数）；LLM 生成随问数近线性但共享视觉 token 前缀，配合 KV 缓存前缀复用可进一步省；变长视觉 token 需动态 padding，padding 比例影响有效吞吐。",
  "followUps": [
    {
      "question": "为什么要把视觉编码器前置并缓存，难道不能每次现编码？",
      "answer": "图像编码是确定性的（同图同输出），且同图常被多轮/多问复用，每次现编码是纯浪费。前置缓存后，同图只编码一次，后续问题直接复用视觉 token，既降低首 token 延迟（TTFT）又省下成倍算力；缓存键必须含分辨率与编码器版本，避免命中错误版本。"
    },
    {
      "question": "变长视觉 token（有的图切 6 块、有的切 2 块）怎么高效批处理？",
      "answer": "用动态 padding 或 packing（把多个序列拼接并记长度）配合连续批处理（continuous batching），避免被 batch 内最长序列拖慢；也可对视觉 token 做轻量池化压缩，或在预处理阶段统一 tile 数。关键是让有效计算集中在真实 token 上，而非 padding 上。"
    }
  ],
  "followUpAnswers": [
    "图像编码是确定性的（同图同输出），且同图常被多轮/多问复用，每次现编码是纯浪费。前置缓存后，同图只编码一次，后续问题直接复用视觉 token，既降低首 token 延迟（TTFT）又省下成倍算力；缓存键必须含分辨率与编码器版本，避免命中错误版本。",
    "用动态 padding 或 packing（把多个序列拼接并记长度）配合连续批处理（continuous batching），避免被 batch 内最长序列拖慢；也可对视觉 token 做轻量池化压缩，或在预处理阶段统一 tile 数。关键是让有效计算集中在真实 token 上，而非 padding 上。"
  ],
  "pitfalls": [
    "每请求重编码浪费算力：没把确定性的图像编码前置缓存，是最常见也最亏的部署错误。",
    "缓存键忽略分辨率/版本致错答：只以 image hash 为键，忽略 enc_version 或 tile 数，命中旧/错视觉 token 产生答非所问。"
  ],
  "beginnerSummary": "部署就像餐厅后厨：看图翻译（编码）这活儿可提前做好并存着，同一张图被问十次只翻一次（缓存）；写答案的大厨（LLM）单独排班、能同时接多单。把「翻译」和「写答案」分开、复用、批量，餐厅才能又快又省。如果每次问都重新翻译同一张图，大厨就得干等，又慢又费。",
  "prerequisites": [
    "图像编码确定可缓存：同一图经同一编码器输出固定，适合前置与复用。",
    "LLM 生成可批处理：多个请求共享前缀（视觉 token）时，连续批处理与 KV 缓存能显著提效。",
    "需降延迟与成本：编码前置+复用直接降 TTFT 与单位成本，是线上多模态服务的标配。"
  ],
  "workedExample": [
    "同图 10 问编码仅 1 次：编码 30ms 被 10 次生成复用，编码成本降为 1/10。",
    "编码器 FP8 量化：显存从 2GB 降到 1GB，吞吐提升约 1.6×，精度掉点 <0.5%。"
  ],
  "lineByLine": [
    "图像编码前置并缓存：把确定性的视觉 token 计算移到请求前/异步队列，命中缓存直接取值。",
    "视觉 token 供多问复用：同一图的多个问题共享同一份视觉 token，省去重复编码。",
    "LLM 独立扩缩 + KV 缓存：生成服务单独弹性，复用前缀 KV 加速。",
    "量化与动态批处理降本：编码器 INT8/FP8，变长 token 用 packing/连续批避免 padding 浪费。"
  ],
  "diagram": "图像 ─▶ 编码器(前置/缓存) ─▶ 视觉token ─┐\n                                          ├─▶ LLM批生成(复用)\n提问 ───────────────────────────────────┘"
};

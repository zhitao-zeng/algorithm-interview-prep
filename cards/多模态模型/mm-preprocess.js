export default {
  "kind": "concept",
  "id": "mm-preprocess",
  "category": "多模态模型",
  "difficulty": "Easy",
  "title": "推理时图像预处理流水线",
  "prompt": "多模态模型在推理时，一张图进模型前要经过怎样的预处理流水线？",
  "quickAnswer": "推理流水线通常是：解码图像 → resize/pad 到模型输入尺寸 → 归一化(均值方差) → 转 tensor → 切 patch → 视觉编码器 → 连接器对齐 → 拼入 LLM 提示。高分辨率还需切图/动态分辨率。关键是预处理必须与训练严格对齐：训练/推理归一化不同、resize 策略不同都会掉点，因此推理端要复用训练端的同一套 transform。",
  "approach": "解码 → resize/pad → 归一化 → tensor → patch → 编码 → 对齐 → 入 LLM；高分辨率插入切图。全部参数（尺寸、均值、方差、插值方式）与训练一致。",
  "explanationFocus": "是什么：推理时图像预处理流水线是图像从原始文件变成模型可消费的视觉 token 的一系列固定步骤，必须与训练时严格一致。它包含解码、resize/pad、归一化、转 tensor、切 patch、视觉编码、连接器对齐、拼入 LLM 提示。任何一步在训练/推理间不一致（如归一化均值不同）都会让编码器表征整体偏移、精度明显下滑。",
  "bruteForce": "直接把任意尺寸原图喂编码器：尺寸不匹配报维度错误，或插值/值域不同导致分布漂移、精度崩。",
  "derivation": [
    "为什么需要：模型只在固定尺寸、固定分布（如 ImageNet 归一化）上训练过，原始图尺寸不一、像素值域 [0,255] 与训练分布不同，必须先标准化否则编码器罢工或表征漂移。",
    "怎么实现：统一解码为 RGB；按策略 resize/pad 到模型输入尺寸；用训练同款均值方差归一化；转 CHW float tensor；再走 patch 切分 + 视觉编码器 + 连接器对齐。",
    "有什么代价：resize 下采样丢细节（高分辨率任务需切图补）；pad 引入无效 token 拖慢后续；高分辨率切图进一步增加序列长度与延迟。",
    "怎么评测：对齐性测试（同图同配置输出应完全一致）、端到端精度对比、以及预处理耗时占比（通常 <5% 总时延）。"
  ],
  "invariant": "同一图像在相同配置（尺寸、均值方差、插值、padding 方式）下得到确定且可复现的预处理结果与视觉 token。",
  "walkthrough": "原图 1000×800（RGB）→ resize 最短边到 336（得 420×336）→ pad 到 336×336 正方形（上下补 42 行无效像素）→ 用 ImageNet 均值(0.485,0.456,0.406)方差(0.229,0.224,0.225)归一化 → 转 CHW tensor → 切 14×14 patch → 576 个视觉 token → ViT 编码。",
  "edgeCases": [
    "非 RGB（含 alpha 通道的 PNG）：必须转 RGB，否则 4 通道与训练 3 通道不匹配。",
    "极端长宽比（如长条截图）：pad 出大量无效 token，浪费算力且可能干扰模型。",
    "训练/推理归一化不一致（如推理漏了减均值）：输入分布整体偏移，编码器表征崩，精度断崖下跌。",
    "EXIF 方向信息：手机照片可能带旋转，解码若不读 EXIF 会把图读反。"
  ],
  "code": "def preprocess(img, size=336, mean=(0.485,0.456,0.406), std=(0.229,0.224,0.225)):\n    img = decode_rgb(img)\n    img = resize_shortest(img, size)\n    img = pad_to_square(img, size)\n    t = to_tensor(img).normalize(mean, std)     # 与训练一致\n    return t",
  "codeNotes": [
    "均值方差必须和训练完全一致（包括用哪套统计、插值方式），否则分布偏移直接掉点。",
    "pad 会增加无效视觉 token：长图建议用动态切片而非暴力 pad 正方形，减少浪费。"
  ],
  "complexity": "预处理本身 O(像素)（解码+resize 主导），远小于视觉编码器与 LLM 的 O(N²)；但 pad 会引入无效视觉 token，浪费后续注意力算力，需在精度与 token 数间权衡。",
  "followUps": [
    {
      "question": "为什么训练推理归一化必须一致，差一点不行吗？",
      "answer": "差一点就会让输入分布整体偏移，编码器的表征不再落在训练时学到的流形上，下游精度明显下滑（可能掉几十个点）。归一化的均值方差、甚至插值方式都必须和训练同款，所以推理端应直接复用训练配置，不要凭记忆重写。"
    },
    {
      "question": "高分辨率图在预处理阶段怎么处理？",
      "answer": "在 resize/pad 之后插入切图（把大图分成若干子图各编码）或动态分辨率：先出一张全局缩略保整体，再切 K 块局部图补细节，最后带位置合并视觉 token。这样既不超序列上限，又保留 OCR/小物体所需的细节。"
    }
  ],
  "followUpAnswers": [
    "差一点就会让输入分布整体偏移，编码器的表征不再落在训练时学到的流形上，下游精度明显下滑（可能掉几十个点）。归一化的均值方差、甚至插值方式都必须和训练同款，所以推理端应直接复用训练配置，不要凭记忆重写。",
    "在 resize/pad 之后插入切图（把大图分成若干子图各编码）或动态分辨率：先出一张全局缩略保整体，再切 K 块局部图补细节，最后带位置合并视觉 token。这样既不超序列上限，又保留 OCR/小物体所需的细节。"
  ],
  "pitfalls": [
    "忽视训练/推理预处理差异：这是最常见掉点根因，推理端务必复用训练端的 transform 配置而非重写。",
    "pad 引入大量无效 token 拖慢：长图 pad 成正方形会塞一堆空白 patch，既费算力又可能让模型注意力被空白分散。"
  ],
  "beginnerSummary": "预处理就像照相前的统一化妆标准：每个人都先调成同样的尺寸、同样的亮度色调，再进场。如果训练和考试时的「化妆标准」不一样，模型就会认不出人。高个子（高分辨率图）还得裁成几张标准照分别处理。漏掉任何一步（比如忘了调亮度），模型看到的就是「陌生脸」，答案自然错。",
  "prerequisites": [
    "模型只在固定尺寸/分布训练：ViT 通常吃固定 patch 网格，输入尺寸必须对齐。",
    "归一化影响表征分布：减均值除方差把输入拉到训练同分布，是精度前提。",
    "训练推理须一致：推理端的 transform 应直接复用训练配置，避免隐性分布偏移。"
  ],
  "workedExample": [
    "1000×800 → resize 最短边 336（420×336）→ pad 正方形 336×336，多出的行是无效 padding。",
    "归一化后切 14 patch → 576 个视觉 token；若不做归一化，同一图 top-1 可能从 78% 掉到 40%。"
  ],
  "lineByLine": [
    "解码为 RGB：decode_rgb 统一处理 PNG/JPG，并把带 alpha 的图转成 3 通道。",
    "resize 最短边并 pad 方形：保证输入尺寸符合模型，pad 补的是无效像素。",
    "按训练均值方差归一化：to_tensor 后 .normalize(mean, std)，与训练同款配置。",
    "转 tensor 后切 patch 编码：按 patch_size=14 切块送视觉编码器，得到视觉 token。"
  ],
  "diagram": "文件 ─▶ 解码RGB ─▶ resize/pad ─▶ 归一化 ─▶ tensor ─▶ patch ─▶ 编码"
};

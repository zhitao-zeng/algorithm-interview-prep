export default {
  "kind": "concept",
  "id": "mm-sft",
  "category": "多模态模型",
  "difficulty": "Medium",
  "title": "多模态指令微调",
  "prompt": "多模态大模型的指令微调(多模态 SFT)和普通文本 SFT 有什么不同？",
  "quickAnswer": "多模态 SFT 在文本指令数据基础上混入「图文/视频-指令-回答」三元组，让模型学会按指令使用视觉信息（描述、计数、定位、推理）。训练时通常冻结视觉编码器、只训连接器与 LLM（或全参），损失仍是自回归 next-token；关键是数据要覆盖多样指令与难例，否则易过拟合语言先验、产生幻觉（不看图就答）。",
  "approach": "构造多模态对话数据 → 把图像编码为视觉 token 拼入对话模板 → 冻结（或 LoRA）视觉塔、训练连接器 + LLM → 自回归交叉熵损失 → 对齐「按指令用图」的能力。数据上要覆盖描述/计数/定位/推理/拒答多类指令与难例，避免模型退化为只依赖语言先验。",
  "explanationFocus": "是什么：多模态指令微调是用「图像（或视频）+ 指令 + 回答」三元组去微调模型，让它学会听从指令去正确利用视觉内容（描述、计数、定位、推理），而不仅仅会「看图说话」。它与普通文本 SFT 的区别在于数据多了一路视觉输入、训练目标是「按指令用图」而非「仅续写文本」。",
  "bruteForce": "只用纯文本指令数据做 SFT：模型完全忽略图像，凭语言先验作答，出现「图里明明是猫却说狗」的幻觉，且不会执行「数一数」「框出来」等视觉指令。",
  "derivation": [
    "为什么需要：预训练（如 CLIP/ViT + 语言模型对齐）只给「看图说话」的粗能力，不一定服从具体指令（如「数图里有几个、框出红色物体」）。需要用指令数据教它按令行事，把视觉能力「指挥」起来。",
    "怎么实现：把图像经 ViT 编码成视觉 token，拼入对话模板；构造多轮图文对话（含指令-回答）；优化因果 LM 损失；常冻结 ViT、训投影 + LLM，或上 LoRA/全参。高分辨率可用动态分辨率或切片（如把图切成多块）增加视觉 token。",
    "有什么代价：高质量多模态指令数据贵且难标（尤其定位/推理类）；数据分布偏斜会让模型仍偏语言先验（不看图）；多任务需平衡权重，否则某类能力被挤掉；高分辨率带来显存压力。",
    "怎么评测：看指令遵循准确率、幻觉率（如 POPE）、各能力维度（MMBench 的感知/认知），以及「换图后输出是否变化」的反事实测试，确认模型真在用图。"
  ],
  "invariant": "给定相同的图文指令，微调后的模型输出应当稳定且真正利用图像内容（可通过「改写图像后输出是否随之改变」来检验）；若换张图回答不变，说明没在看图。",
  "walkthrough": "以 LLaVA-1.5 为例：用约 1.2M 条混合指令数据（含图文问答、OCR、计数、定位、推理），冻结 ViT-H（约 300M 参数），只训一个两层 MLP 投影 + LLM（7B/13B），单卡 A100 训练约 1 天。评测显示：在 MMBench 上指令遵循与幻觉率显著优于只用文本 SFT 的对照；若把视觉 token 数从 256 提到 576，小字 OCR 准确率提升约 8 个点，但显存与 prefill 成本上升。",
  "edgeCases": [
    "指令要求定位/框选但训练数据缺 grounding（坐标）样本：模型会说「在左上」却给不出坐标，需补 bbox 指令数据。",
    "负样本/拒答数据不足：遇到无关或无法回答的图，模型硬编答案而非拒答，需加「图中无此物」类样本。",
    "多轮对话中图像上下文引用易丢：第 3 轮忘了第 1 轮图的细节，需做多轮图文对话数据。",
    "低分辨率图：小字 OCR/细粒度计数失败，需提高视觉 token 数或图像分辨率。"
  ],
  "code": "def sft_step(batch, model, opt):\n    logits = model(batch['tokens'])            # 含视觉 token 的对话\n    loss = causal_lm_loss(logits, batch['labels'])\n    loss.backward(); opt.step()                # 冻结 ViT, 训其余",
  "codeNotes": [
    "常冻结视觉编码器（ViT）以防用少量 SFT 数据破坏已学好的视觉表征，也省显存；只调投影与语言部分。",
    "损失仍是下一 token 预测（causal LM），与文本 SFT 一致，便于复用训练框架。",
    "batch 中图文样本需与纯文本样本混合，防止模型退化成只处理文本、忽视图像。"
  ],
  "complexity": "与文本 SFT 同量级，额外开销是视觉 token 的前向/反向（∝ 视觉 token 数 × ViT/投影算力）。冻结 ViT 时这部分不反向，省显存；视觉 token 数越多（更高分辨率）显存与 prefill 越贵，需在分辨率与成本间权衡。",
  "followUps": [
    {
      "question": "为什么要冻结视觉编码器？",
      "answer": "两个原因：一是多模态 SFT 数据量远小于视觉预训练，若开放训练容易用少量数据破坏 ViT 已学好的通用视觉表征（灾难性遗忘）；二是冻结可大幅省显存与算力，使 7B/13B 级别训练能在单卡完成。若数据量极大且想进一步适配，也可解冻 ViT 做全参，但风险与成本更高。"
    },
    {
      "question": "数据质量比数量重要吗？",
      "answer": "非常关键。多模态里少量高质量、覆盖多样指令（描述/计数/定位/推理/拒答）的数据，远胜大量同质「看图说话」数据，能显著降低语言先验导致的幻觉。尤其要包含难例与拒答样本，否则模型遇到答不上来的图也会硬编。数据工程往往比模型改动更影响最终效果。"
    },
    {
      "question": "怎么判断模型真的「在看图」而不是凭语言先验？",
      "answer": "用反事实测试：固定指令、替换图像内容（如把猫换成狗、改数量），观察输出是否随之改变；若输出不变，说明模型没用图、在靠先验。再配合 POPE 等幻觉基准、MMBench 各维度，以及人为构造「图中无此物」的拒答题，综合判定。"
    }
  ],
  "followUpAnswers": [
    "两个原因：一是多模态 SFT 数据量远小于视觉预训练，若开放训练容易用少量数据破坏 ViT 已学好的通用视觉表征（灾难性遗忘）；二是冻结可大幅省显存与算力，使 7B/13B 级别训练能在单卡完成。若数据量极大且想进一步适配，也可解冻 ViT 做全参，但风险与成本更高。",
    "非常关键。多模态里少量高质量、覆盖多样指令（描述/计数/定位/推理/拒答）的数据，远胜大量同质「看图说话」数据，能显著降低语言先验导致的幻觉。尤其要包含难例与拒答样本，否则模型遇到答不上来的图也会硬编。数据工程往往比模型改动更影响最终效果。",
    "用反事实测试：固定指令、替换图像内容（如把猫换成狗、改数量），观察输出是否随之改变；若输出不变，说明模型没用图、在靠先验。再配合 POPE 等幻觉基准、MMBench 各维度，以及人为构造「图中无此物」的拒答题，综合判定。"
  ],
  "pitfalls": [
    "只用文本指令数据做 SFT，模型学会「说话」却不「看图」，产生幻觉。",
    "忽视难例与拒答数据，模型对答不上来的图也硬编，幻觉率高。",
    "视觉 token 数过低导致细粒度任务（OCR/计数）崩，却误以为是模型不行。"
  ],
  "beginnerSummary": "普通 SFT 是教模型「听人话」（你问啥它按格式答）；多模态 SFT 是额外教它「边看图片边听人话」。比如教它「数图里有几个苹果」而不是凭印象乱说。训练时通常保留它已经学会的「看图能力」（冻结眼睛/ViT），只训练它如何按指令把看到的 organise 成回答。如果不喂图文数据，它就会「睁眼瞎」——图就在眼前却凭语言习惯瞎编。",
  "prerequisites": [
    "模型已具备基础「看图能力」（如经视觉-语言对齐预训练），多模态 SFT 是锦上添花而非从零学视觉。",
    "SFT 用自回归（next-token）损失，需构造「指令-回答」监督信号。",
    "需图文指令数据：至少含图像、指令、期望回答三元组，最好覆盖多类指令与难例。",
    "了解连接器（projector）如何将视觉 token 映射到语言空间。"
  ],
  "workedExample": [
    "冻结 ViT 训练：LLaVA-1.5 冻结 ViT-H，只训两层 MLP + 7B LLM，约 1.2M 数据单卡 A100 训 1 天，MMBench 准确率较纯文本 SFT 对照提升明显。",
    "分辨率影响：视觉 token 从 256 提至 576，小字 OCR 准确率 +8 个点，但 prefill 显存升约 30%。",
    "幻觉测试：混入 10% 拒答样本后，POPE 幻觉率从 18% 降到 9%。"
  ],
  "lineByLine": [
    "`logits = model(batch['tokens'])`：输入是含视觉 token 的对话序列，模型前向得 logits。",
    "`loss = causal_lm_loss(logits, batch['labels'])`：只对回答部分计算自回归交叉熵（问题/图像部分通常 mask）。",
    "`loss.backward(); opt.step()`：反向更新，但 ViT 被冻结，仅投影 + LLM 参数更新。",
    "循环 batch 完成多模态 SFT，逐步对齐「按指令用图」的能力。"
  ],
  "diagram": "图文指令 ─▶ 模板拼接(含视觉token) ─▶ 模型 ─▶ 自回归损失 ─▶ 微调"
};

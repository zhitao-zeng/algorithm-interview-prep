export default {
  "kind": "concept",
  "id": "mm-multi-image",
  "category": "多模态模型",
  "difficulty": "Medium",
  "title": "多图与多轮多模态对话",
  "prompt": "模型怎么支持一次性看多张图、并在多轮对话里持续引用它们？",
  "quickAnswer": "多图时把每张图各自编码成视觉 token，用特殊标记（如 <img0>/<img1>）和位置编码区分，拼接进同一序列；多轮对话则把历史图文消息按对话模板累积（含之前图像 token 与回答），模型靠注意力跨轮引用。挑战是序列随图数与轮数线性增长、跨图/跨轮指代易混、KV 缓存暴涨，需要清晰的位置与角色标记，以及对超长历史做截断或摘要。",
  "approach": "逐图独立编码 → 用占位符/序号/位置编码区分 → 拼入按模板组织的对话历史 → 依靠注意力跨图跨轮引用。关键是给每张图稳定的唯一标识，并在指令里显式点名\"图1/图2\"，训练数据也要覆盖多图指代，否则模型分不清。",
  "explanationFocus": "是什么：多图多轮对话支持是让模型同时消费多张图像，并在连续问答中跨轮引用它们，靠\"图像标记（如 <img0>/<img1>）+ 位置编码 + 对话历史拼接\"实现。本质是把多张图各自编码成视觉 token、用唯一标识区分，再按顺序拼进同一序列，让注意力能跨越图与轮次建立关联。",
  "bruteForce": "每轮只重发当前单张图、丢弃历史图像：模型无法跨图比较（\"图1和图2哪张更亮？\"答不出），上下文断裂，多轮追问体验崩坏。这是早期单图模型的典型局限。",
  "derivation": [
    "为什么需要：真实场景常需对比多张图、连续追问（看图说话、文档多页、电商多图），单图单轮无法满足。",
    "怎么实现：每张图经同一编码器得 token，插入 <image> 占位与序号；按多轮模板把用户/助手消息（含图 token）顺序拼成序列；用位置/模态标记避免混淆，指令中显式指明图序号。",
    "有什么代价：图数×轮数使序列极长、KV 暴涨、显存吃紧；跨图指代与\"第几张\"易错；长历史需截断或摘要，可能丢信息。",
    "怎么评测：多图推理/比较基准（如多图 VQA）、多轮一致性、指代准确率（模型能否正确回答\"图2 中…\"），以及与单图基线的质量对比。"
  ],
  "invariant": "相同多图多轮输入应得到稳定、且能正确指代各图（\"第几张\"不混淆）的输出；图顺序变化应合理改变答案（如比较题）。",
  "walkthrough": "3 张图，每张经 ViT 编码成 256 个视觉 token，加上 2 轮文本（每轮约 50 token）。总序列约 3×256 + 100 = 868 token，注意力要对全部 token 做 O(n²) 计算；若再加 5 轮对话，序列逼近 1.2K token，KV 缓存线性增长，显存与延迟同步上升。",
  "edgeCases": [
    "指代\"左边那张/图2\"需空间/序号对齐：模型若把序号搞混，比较题直接错。",
    "历史过长需摘要/截断：截断掉含图 token 的早期轮可能丢失关键视觉上下文。",
    "图顺序变化应改变答案：比较类问题对顺序敏感，需测试顺序鲁棒性。",
    "图数超过训练见过的最大数：泛化可能崩，需限制或分页处理。"
  ],
  "code": "def multi_image_dialog(images, history, encoder, sep='<image>'):\n    img_toks = [encoder(im) for im in images]\n    seq = []\n    for turn in history:\n        if turn.has_image:\n            seq.append(sep + img_toks[turn.img_id])\n        seq.append(turn.text_tokens)\n    return concat(seq, dim=1)",
  "codeNotes": [
    "用占位符（sep）绑定图像序号：turn.img_id 决定插入哪张图的 token，避免错位。",
    "历史累积但需控长度：concat 前应对总 token 数设上限，超限做截断/摘要。",
    "真实系统还要处理图像分辨率切分（tile），单图可能不止 256 token。"
  ],
  "complexity": "注意力为 O((Σ图token + 文本)²)，随图数与轮数增长；KV 缓存随序列线性膨胀。3 图 5 轮可能让序列从数百涨到数千 token，显存受限时需截断早期轮次或摘要图像 token。",
  "followUps": [
    {
      "question": "多图怎么避免混淆？",
      "answer": "核心是给每张图唯一且稳定的标记：在序列中用 <img0>/<img1> 这类占位符，并配合位置编码与模态类型编码，让模型清楚\"哪段 token 属于第几张图\"。同时在用户指令里显式点名\"图1/图2\"，且训练数据要大量覆盖多图指代样本（比较、计数、跨图推理）。推理时若发现序号与指令描述不一致，最容易出\"张冠李戴\"。"
    },
    {
      "question": "多轮历史太长怎么办？",
      "answer": "对早期轮次做摘要或丢弃其图像 token、仅保留文本摘要，在上下文长度与信息保真间取舍。也可采用滑动窗口只保留最近 N 轮，或对图像 token 做下采样/聚类压缩。更进阶的是分层记忆：文本细节留近期、图像语义压成摘要存长期记忆，按需召回。"
    }
  ],
  "followUpAnswers": [
    "核心是给每张图唯一且稳定的标记：在序列中用 <img0>/<img1> 这类占位符，并配合位置编码与模态类型编码，让模型清楚\"哪段 token 属于第几张图\"。同时在用户指令里显式点名\"图1/图2\"，且训练数据要大量覆盖多图指代样本（比较、计数、跨图推理）。推理时若发现序号与指令描述不一致，最容易出\"张冠李戴\"。",
    "对早期轮次做摘要或丢弃其图像 token、仅保留文本摘要，在上下文长度与信息保真间取舍。也可采用滑动窗口只保留最近 N 轮，或对图像 token 做下采样/聚类压缩。更进阶的是分层记忆：文本细节留近期、图像语义压成摘要存长期记忆，按需召回。"
  ],
  "pitfalls": [
    "图像标记混乱致跨图指错：占位符/序号与指令描述不一致，模型张冠李戴。",
    "无脑累积历史致序列爆炸：不截断不摘要，显存 OOM、延迟失控。",
    "训练数据缺多图指代样本，导致推理时分不清图。"
  ],
  "beginnerSummary": "多图多轮就像把几张照片摊在桌上连续讨论。每张照片贴个编号（图1/图2/图3），对话时你说\"图2里那只猫\"，模型靠编号和位置记住哪张是哪张。照片和对话越多，桌上东西越多越占地方（算力/KV 缓存），所以太久的对话要适当收尾或摘要，否则桌子被堆满、模型也记混。",
  "prerequisites": [
    "单图可独立编码：每张图经 ViT/视觉编码器得到固定维 token，是拼多图的前提。",
    "对话需历史拼接：多轮要靠把历史消息按模板累积成一段序列来保持上下文。",
    "跨图指代需明确标记：用序号/位置/占位符给图唯一身份，模型才能正确引用。"
  ],
  "workedExample": [
    "3 图各 256 token 带序号 <img0/1/2>，两轮文本约 100 token，总序列约 868 token 进 LLM。",
    "用户问\"图1 和图3 有什么区别\"，模型靠序号定位两图 token 做跨图注意力比对。",
    "超长对话（10 轮）触发历史摘要，早期图像 token 压缩为文本摘要以控长度。"
  ],
  "lineByLine": [
    "逐图编码带序号标记：img = encoder(im)，每个图得到一组带位置/序号的视觉 token。",
    "按模板拼接对话历史：遍历 history，遇到带图轮次插入对应 img_tok，否则插文本。",
    "位置/模态编码区分图与轮：让模型知道哪些 token 是图、属于哪一轮、第几张。",
    "注意力跨图跨轮引用：在统一序列上做自注意力，建立图-图、轮-轮关联。"
  ],
  "diagram": "图1─▶tok1  图2─▶tok2\n   └─▶ 对话模板(含<img1><img2>+文本) ─▶ LLM"
};

export default {
  "kind": "concept",
  "id": "mm-hires",
  "category": "多模态模型",
  "difficulty": "Hard",
  "title": "高分辨率图像处理",
  "prompt": "多模态模型怎么在不让视觉 token 爆炸的前提下看清高分辨率图像？",
  "quickAnswer": "核心是「动态分辨率/切图」：把大图切成若干子图（每块按模型尺寸编码）得到局部 token，再配一张下采样的全局图保留整体，最后合并所有视觉 token 喂 LLM。这样既保留细节又不让单图 token 线性爆炸。代表如 LLaVA-NeXT / InternVL 的动态切片。代价是序列更长、需位置编排，且切片边界信息割裂需补偿。",
  "approach": "全局下采样图（保整体）+ 局部切图编码（补细节）→ 给每块打空间位置 → 合并全部 token → 送 LLM。动态按原图长宽比决定切片数。",
  "explanationFocus": "是什么：高分辨率处理是用「全局缩略 + 局部切片」的方式，在可控的视觉 token 数量下保留图像细节，避免直接把整张高分辨率图切块导致 token 随像素平方爆炸。它解决的是「细粒度任务（OCR/小物体）要看清细节」与「序列长度/算力 O(N²) 受控」之间的根本矛盾。",
  "bruteForce": "把整张高分辨率图直接切成 14×14 的小 patch：token 数 = (H/14)·(W/14)，随像素平方暴涨，注意力 O(N²) 直接算爆。",
  "derivation": [
    "为什么需要：OCR/文档/细粒度识别需要看清细节，但整体高分辨率会让 token 数 (H/p)(W/p) 随像素平方暴涨，注意力 O(N²) 与显存失控，单卡根本放不下。",
    "怎么实现：生成一张低分辨率全局图保留布局；把原图按网格切成 K 块各编码；给每块打二维空间位置（如二维 RoPE 或绝对位置），合并全部 token；动态按原图比例决定切片数（长边越长切越多）。",
    "有什么代价：切片多 → 总 token 仍涨、注意力变贵；切片边界把物体切断，信息割裂需重叠裁剪或上下文补偿；预处理与位置编排更复杂。",
    "怎么评测：在 OCR/文档/细粒度基准（如 DocVQA、TextVQA）上对比「直切 vs 动态切片」的精度，并同时看 token 数与端到端延迟的权衡曲线。"
  ],
  "invariant": "同一高分辨率图按相同切片策略（tile 大小、网格数、位置编码）得到可复现的合并 token 集，多次推理结果一致。",
  "walkthrough": "以 1344×1008 图为例：先 resize 出一张 336×336 全局缩略图（编码得 576 token）；再按网格切 3×2=6 块（每块 448×504，各自缩到 336 编码），共 6×576=3456 token；合计 7 组 × 576 ≈ 4032 token。相比直接整图切块 (1344/14)·(1008/14)=96·72≈6912 token，且避免了把 1344 维长边直接铺开带来的注意力爆炸。",
  "edgeCases": [
    "极端长宽比（如长截图）：不能简单切正方形，需按长边比例切更多列，否则细节丢失。",
    "切片边界把物体切断（如被切一半的文字）：需重叠切片或把上下文相邻块拼读，否则 OCR 出错。",
    "token 上限：超过模型最大序列（如 4096/8192）时需截断、再池化或只编码关键区域（先做区域检测）。",
    "极小图：本就不需要切片，强行切会引入大量重复 padding token 浪费。"
  ],
  "code": "def dynamic_slice(img, tile=336, max_tiles=9):\n    global_img = resize(img, tile)\n    tiles = split_into_grid(img, tile, max_tiles)   # 按需网格\n    toks = [encode(global_img)] + [encode(t) for t in tiles]\n    return concat_with_pos(toks)                    # 带空间位置合并",
  "codeNotes": [
    "全局图保整体语义（布局/大物体），局部切片补细节（OCR/小物体），二者互补缺一不可。",
    "切片数随原图比例动态调整（max_tiles 限制上限），避免长图无限切导致序列爆掉。"
  ],
  "complexity": "token 数 ≈ (1 + 切片数) × (tile/p)²，远小于整体直切的 (H/p)(W/p)；但切片越多，序列仍线性增长，注意力成本 O(N²) 随总 token 上升，需配合 token 上限与位置补偿。",
  "followUps": [
    {
      "question": "全局图和局部切片各自解决什么问题，能只用一个吗？",
      "answer": "全局图提供整体布局与上下文（图里大致有什么、物体在哪），但分辨率低、细节糊；局部切片补细节（OCR、小物体、细文字），但丢整体关系。只给全局图 = 看不清字，只给切片 = 不知整体、易误读局部。二者互补，现代多模态模型（LLaVA-NeXT、InternVL）都是「1 张全局 + K 张局部」一起用。"
    },
    {
      "question": "切片太多导致 token 爆了怎么办？",
      "answer": "设最大切片数上限 max_tiles（如 9 或 12）；超限则提高 tile 尺寸、或对图像先做区域检测只编码关键区（如先检测文字区再切）；也可对切片 token 做轻量池化压缩。原则是落在「精度-延迟」帕累托前沿上，而不是盲目加切片。"
    }
  ],
  "followUpAnswers": [
    "全局图提供整体布局与上下文（图里大致有什么、物体在哪），但分辨率低、细节糊；局部切片补细节（OCR、小物体、细文字），但丢整体关系。只给全局图 = 看不清字，只给切片 = 不知整体、易误读局部。二者互补，现代多模态模型（LLaVA-NeXT、InternVL）都是「1 张全局 + K 张局部」一起用。",
    "设最大切片数上限 max_tiles（如 9 或 12）；超限则提高 tile 尺寸、或对图像先做区域检测只编码关键区（如先检测文字区再切）；也可对切片 token 做轻量池化压缩。原则是落在「精度-延迟」帕累托前沿上，而不是盲目加切片。"
  ],
  "pitfalls": [
    "只看全局图丢失细节：OCR、小物体、细文字在缩略图里直接糊掉，必须靠局部切片补。",
    "无脑加切片数导致 token/算力失控：切片不是越多越好，要在精度-延迟帕累托前沿上选 tile 与上限。"
  ],
  "beginnerSummary": "看清高清大图但不能把图拆成天文数字的小块。办法是「先拍一张小全景照记住全貌，再把大图裁成几张标准小照片分别细看」，最后把这些照片（带位置标签）一起交给大脑。这样既看得细又不至于信息多到炸。如果只拍全景照，小字就糊了；如果无脑狂裁，照片多到大脑读不过来。",
  "prerequisites": [
    "整体编码 token 随像素平方涨：patch 数 = (H/p)(W/p)，高分辨率图直接编码不可行。",
    "细节任务需高分辨率：OCR、文档、小物体识别依赖局部清晰像素。",
    "需位置编排合并切片：不同块的 token 必须带空间位置，LLM 才知道它们拼回原图的哪个位置。"
  ],
  "workedExample": [
    "全局 336×336（576 token）+ 6 个 336 局部切片（每块 576 token）：合计约 4032 token 喂 LLM。",
    "一张 4000×3000 发票图：直切需约 6 万 token（超限），动态切片限 max_tiles=9，总 token 压到约 5760，DocVQA 准确率从 52% 升到 81%。"
  ],
  "lineByLine": [
    "生成低分辨率全局图：resize(img, tile) 保留整体布局，编码得到一份 token。",
    "按网格切局部图：split_into_grid 按原图比例切 K 块，每块缩到 tile 尺寸编码。",
    "各块独立编码：每块经同一视觉编码器得 token 序列。",
    "带位置合并后入 LLM：concat_with_pos 给每块打空间位置，拼成最终视觉序列，保证 LLM 能还原空间关系。"
  ],
  "diagram": "高清图 ─┬─▶ 全局缩略(整体)\n              └─▶ 切 K 块(细节)\n      两路编码 ─▶ 合并(带位置) ─▶ LLM"
};

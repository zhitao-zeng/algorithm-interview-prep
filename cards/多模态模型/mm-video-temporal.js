export default {
  "kind": "concept",
  "id": "mm-video-temporal",
  "category": "多模态模型",
  "difficulty": "Hard",
  "title": "视频理解处理时序",
  "prompt": "多模态模型处理视频时，怎么把\"时间\"这一维有效地建模进去？",
  "quickAnswer": "视频 = 多帧图像 + 时间维。常见三类做法：① 逐帧独立提视觉 token，再全部拼接交给注意力去学时序(最常用)；② 时空 patch，把时间当作第三维直接切 token(如 ViT-3D)；③ 先每帧编码成特征，再接时序注意力/池化/GRU 等模块。关键工程点是帧采样率与每帧 token 数：二者乘法式决定序列长度，必须控制以免算力与 KV 爆炸，并用时间位置编码(M-RoPE)保留帧序，否则模型会把视频当无序图集。",
  "approach": "采样帧 → 逐帧提 token(带时间位置) → 拼接 / 时空切分 → 时序注意力或池化 → 送 LLM。具体：先用均匀或关键帧采样降低帧数，逐帧 ViT 提 token 并用时间位置编码(如 M-RoPE 时间维)标注帧序，帧间用自注意力或轻量时序模块交互，必要时对帧 token 做池化压缩以控序列长度。预算紧张时对长视频先做分段+摘要再拼接。",
  "explanationFocus": "是什么：视频理解的时序建模是把\"多帧画面 + 先后顺序\"转换成模型可消费的结构。常见做法是先对视频采样若干帧、逐帧用图像编码器(如 ViT)提取视觉 token，并给每帧打上时间位置编码(如 M-RoPE 的时间维)，再把所有帧 token 拼接或做时空联合切分，最后用注意力或轻量时序模块让模型学会帧间关系。核心是控制帧数与每帧 token 数，避免序列长度爆炸。",
  "bruteForce": "最朴素是把整段视频每帧都提 token 全拼进去：一秒 30 帧、每帧 256 token，10 秒就是 76800 token，注意力 O(n^2) 算力与 KV 缓存都不可承受，且大量相邻帧高度冗余。不采样、不压缩直接全量拼接在工程和精度上都不可行，既算不动也浪费在重复信息上。",
  "derivation": [
    "为什么需要：视频除了空间内容(单帧画面)还有时序内容(动作、先后、因果)，只看单帧会丢失\"发生了什么变化\"。要把视频喂给 Transformer/LLM，就必须把\"多帧+顺序\"变成有序的 token 序列，否则模型分不清前后、答不对时序问题。",
    "怎么实现：先做帧采样(均匀或关键帧)控制帧数；逐帧用 ViT 提 token 并打时间位置(M-RoPE 时间维 0..T-1)；把各帧 token 沿序列维拼接，交给自注意力学帧间关系；必要时在帧间插轻量时序模块或池化压缩。时间位置编码保证顺序不被注意力打乱。",
    "有什么代价：帧数 × 每帧 token 是乘法式增长，注意力 O(n^2) 下算力与 KV 压力巨大；采样过少又会漏掉关键动作帧。需要在\"信息保留\"与\"算力预算\"间权衡，常用帧 token 池化、分段+摘要来降本。",
    "怎么评测：在视频 QA(如 MSVD-QA、ActivityNet-QA)、动作识别、时序排序等任务上看准确率；同时测端到端延迟与显存，确认在预算内。消融\"去掉时间位置编码\"应能观察到时序任务明显掉点，验证时序建模生效。"
  ],
  "invariant": "相同帧序列经固定时间编码得到确定且有序的表示：只要帧采样位置与时间位置编码一致，模型对\"先后\"的感知是确定且可复现的，不会因注意力置换不变性而丢失时序。也就是说，时间位置编码给\"无序的 token 序列\"注入了一个稳定可复现的顺序信号。",
  "walkthrough": "以 16 帧、每帧 256 token 为例，共 4096 个视觉 token；用 M-RoPE 的时间维对帧编号 0..15，使注意力能区分\"第几帧\"。若把帧率从每秒 1 帧提到 8 帧、每段 8 秒，则 64 帧 × 256 = 16384 token，注意力 O(n^2) 下算力翻 16 倍——因此实际部署常在\"帧数 × 每帧 token\"上做预算封顶(如总视觉 token ≤ 几千)。时序位置保证即使打乱帧顺序，模型也能还原正确先后，时序问答才答得对。",
  "edgeCases": [
    "长视频：整段提 token 不现实，需分段处理或只取关键帧/镜头边界帧。",
    "高帧率快速动作：采样过疏会漏掉转瞬即逝的动作，需提高采样率或用光流辅助。",
    "静态镜头/静止画面：多帧高度冗余，可去重或降低该段帧率节省算力。",
    "视频与文本交错(如带字幕)：时间位置需与文本位置对齐编排，避免注意力混淆。"
  ],
  "code": "def video_to_tokens(frames, encoder, sampler, temporal_pos):\n    picked = sampler(frames)                       # 采关键帧\n    toks = [encoder(f) for f in picked]            # 逐帧提 token\n    for t, tk in enumerate(toks):\n        tk = add_temporal_pos(tk, temporal_pos(t)) # 打时间位置\n    return concat(toks, dim=1)                     # 拼成时序序列",
  "codeNotes": [
    "帧采样(sampler)是第一道降本手段：均匀采样保覆盖、关键帧采样保重点，直接影响算力与信息。",
    "add_temporal_pos 把时间维写进位置编码，保证注意力即使置换不变也能感知帧序。",
    "concat(dim=1) 把各帧 token 沿序列维拼接，后续统一进 Transformer；也可先池化再 concat 控长度。"
  ],
  "complexity": "主干复杂度 O(帧数 × 每帧 token 数)^2 的自注意力；通过帧 token 池化、分段摘要可降到 O(T·V')。每帧的 ViT 编码是 O(帧数 × 图像 token 数) 的线性开销，整体受\"帧数×每帧 token\"乘积主导，因此降本的第一杠杆永远是帧采样与每帧 token 数。",
  "followUps": [
    {
      "question": "帧太多 token 爆炸怎么办？",
      "answer": "几条路：① 减少帧数(降采样率)与降低每帧分辨率(少 patch)，从源头砍 token；② 对帧 token 做池化/聚类压缩，把 256 token/帧压到比如 64；③ 分段处理+段落摘要，每段提要点再拼接；④ 用时空联合切分减少冗余。核心是在\"时序信息保留\"与\"算力预算\"间取平衡，通常把总视觉 token 控制在几千以内最稳，否则注意力 O(n^2) 与 KV 都扛不住。"
    },
    {
      "question": "为什么不能只看单帧？",
      "answer": "因为大量语义只存在于帧间变化中：动作(举手/落子)、因果(先开门后进入)、顺序(哪件事先发生)。单帧丢掉了\"变化\"与\"先后\"，模型只能看到静态快照，答不对\"视频里发生了什么\"\"下一步会怎样\"这类时序问题。时间维正是视频区别于图像的关键，所以必须把帧序显式编码进模型。"
    }
  ],
  "followUpAnswers": [
    "几条路：① 减少帧数(降采样率)与降低每帧分辨率(少 patch)，从源头砍 token；② 对帧 token 做池化/聚类压缩，把 256 token/帧压到比如 64；③ 分段处理+段落摘要，每段提要点再拼接；④ 用时空联合切分减少冗余。核心是在\"时序信息保留\"与\"算力预算\"间取平衡，通常把总视觉 token 控制在几千以内最稳，否则注意力 O(n^2) 与 KV 都扛不住。",
    "因为大量语义只存在于帧间变化中：动作(举手/落子)、因果(先开门后进入)、顺序(哪件事先发生)。单帧丢掉了\"变化\"与\"先后\"，模型只能看到静态快照，答不对\"视频里发生了什么\"\"下一步会怎样\"这类时序问题。时间维正是视频区别于图像的关键，所以必须把帧序显式编码进模型。"
  ],
  "pitfalls": [
    "忽视时间位置编码，导致帧顺序被自注意力的置换不变性抹掉，模型把视频当无序图集。",
    "无脑堆帧数/每帧 token 造成序列爆炸、KV 与算力不可承受，却不加池化或分段。",
    "采样率与任务不匹配：静态任务过采样浪费、动态任务欠采样漏动作。"
  ],
  "beginnerSummary": "视频就是一连串照片外加\"先后顺序\"。模型先挑几张关键照片(帧采样)，每张翻译成 token 并贴上时戳(时间位置编码)，再把所有带时戳的 token 排好队交给大脑(LLM/注意力)。这样它既能看画面、也能懂\"先发生后发生\"。照片太多就挑重点、能压缩就压缩(帧 token 池化)，否则大脑(算力/KV)算不过来，序列一长注意力 O(n^2) 就爆了。",
  "prerequisites": [
    "视频本质是多帧有序图像：理解\"帧\"与\"时序\"是两个独立维度，缺一不可。",
    "逐帧可复用图像编码器(ViT)：单帧处理与图像理解共用一套表征。",
    "时间顺序需显式编码(位置编码/RoPE)：注意力本身不含顺序感，必须外部注入。",
    "了解 Transformer 自注意力的 O(n^2) 成本，才能理解为何要控序列长度。"
  ],
  "workedExample": [
    "16 帧 × 每帧 256 token = 4096 视觉 token，配 M-RoPE 时间维 0..15 标帧序。",
    "若 8 秒 @ 8fps = 64 帧 × 256 = 16384 token，注意力算力是 16 帧的 16 倍，故需池化或降帧。",
    "时序排序任务：打乱帧序后模型答错率显著上升，说明时间位置编码确实在起作用。"
  ],
  "lineByLine": [
    "picked = sampler(frames)：按策略采关键帧，第一道降本，决定总帧数 T。",
    "toks = [encoder(f) for f in picked]：逐帧用 ViT 提视觉 token，复用图像编码器。",
    "add_temporal_pos(tk, temporal_pos(t))：给第 t 帧 token 打时间位置(如 M-RoPE 时间维 t)，保留顺序。",
    "concat(toks, dim=1)：沿序列维拼成一条带时戳的时序序列，送后续注意力/池化/LLM。"
  ],
  "diagram": "视频 ─▶ 采样帧 ─▶ 逐帧编码 ─(时戳)─▶ 拼接 ─▶ 时序注意力 ─▶ LLM"
};

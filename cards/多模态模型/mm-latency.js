export default {
  "kind": "concept",
  "id": "mm-latency",
  "category": "多模态模型",
  "difficulty": "Medium",
  "title": "端到端多模态推理延迟",
  "prompt": "多模态模型端到端推理里，图像编码开销占多大，怎么分析？",
  "quickAnswer": "端到端延迟 = 图像预处理 + 视觉编码器前向 + 连接器 + LLM 自回归（含视觉 KV）。视觉编码器是一次性固定开销，LLM 自回归随输出长度增长；视觉 token 多会拉长 LLM 每步注意力（KV 缓存）。分析时要分段打点计时，优化常落在减小视觉 token 数、量化编码器、缓存重复图像编码、或并行预处理与首 token 生成。",
  "approach": "拆段计时（预处理/编码/连接/LLM）→ 定位瓶颈 → 减 token/量化/缓存/并行。先有数据再优化，避免凭感觉改。",
  "explanationFocus": "是什么：端到端多模态推理延迟分析，是把一次看图问答的总耗时拆成图像预处理、视觉编码、连接器（projector）与 LLM 自回归生成几段，分别计时找出瓶颈并优化。它回答\"慢在哪\"这个工程问题，而非只报一个总秒数。",
  "bruteForce": "只测总时长不拆分：得到一个\"1.2s\"却不知该优化哪，可能花大力气量化编码器（只省 0.25s）却忽略真正的 LLM 大头，投入产出比极差。",
  "derivation": [
    "为什么需要：延迟决定用户体验与成本，多模态比纯文本多了视觉链路，必须量化各部分占比才能有的放矢。",
    "怎么实现：对各阶段打点计时（含 warm-up 去冷启动）；视觉编码是一次 O(V²) 固定成本，LLM 是输出长度相关的自回归成本，视觉 token 还增大每步 KV 与注意力。",
    "有什么代价：减视觉 token 可能损精度（看不清细节）；量化编码器略降质量；并行受数据依赖限制（编码须先于 LLM，但预处理可与首 token 预取并行）。",
    "怎么评测：分段 P50/P99 延迟、吞吐（req/s）、质量-延迟 Pareto（如精度掉 1% 换 30% 提速是否划算）。"
  ],
  "invariant": "相同输入与配置下各段耗时稳定可复现；分段计时应在 warm-up 后、排除首调度的冷启动抖动。",
  "walkthrough": "总 1.2s 的问答拆开：预处理（resize/归一化）0.05s + ViT 视觉编码 0.25s + 连接器 0.01s + LLM 自回归生成 100 token 0.89s。可见本例 LLM 占 ~74%；若改为输出 500 token，LLM 段升到 ~4.4s，视觉占比被稀释到 ~6%，瓶颈彻底转移到生成侧。",
  "edgeCases": [
    "长输出时 LLM 主导，视觉占比变小：优化重点应从编码器转移到投机解码/更短输出。",
    "高分辨率切图（tile）使编码占比上升：图切成 16 块时编码可能反超 LLM 成瓶颈。",
    "batch 推理时共享编码降本：同一图被多请求复用，摊薄编码成本。",
    "首 token 冷启动（kernel 编译/显存分配）需 warm-up 排除。"
  ],
  "code": "def profile(model, img, prompt):\n    t0 = now(); v = encode_image(img)          # 视觉编码\n    t1 = now(); h = connector(v)               # 连接\n    t2 = now(); out = llm_generate(h, prompt)  # 自回归\n    t3 = now()\n    return {'enc': t1-t0, 'conn': t2-t1, 'llm': t3-t2}",
  "codeNotes": [
    "编码与 LLM 是两段主要成本：先量化这两段再动其他。",
    "视觉 token 数放大 LLM 每步：减 V 能同时降每步注意力与 KV 缓存。",
    "真实部署还要测 P99 与 batch 共享编码收益。"
  ],
  "complexity": "编码 O(V²)（ViT 对视觉 token 的自注意力），与图分辨率/切片数相关；LLM 自回归约 O(L·(T+V)²)（L 为输出长度，T 文本、V 视觉 token），随输出与视觉 token 增长。优化主要压 V 与 L。",
  "followUps": [
    {
      "question": "视觉编码和 LLM 哪个更慢？",
      "answer": "取决于输出长度。短输出（如一句话描述）时，视觉编码（ViT 对视觉 token 的 O(V²)）占比明显，可能占一半以上；长输出（如详细长答）时，LLM 自回归随 token 数线性增长，稳稳主导，但视觉 token 仍会放大 LLM 每步的注意力成本。所以\"谁慢\"没有定论，必须分段实测你的真实负载。"
    },
    {
      "question": "怎么降延迟？",
      "answer": "四个方向：①减视觉 token 数（降分辨率/动态 token、用更紧的 encoder）；②量化或蒸馏视觉编码器（INT8 编码小幅提速）；③缓存重复图像的编码结果（相同图多请求复用）；④并行——预处理与首 token 预取重叠、用投机解码加速 LLM 生成。优先做性价比高的（减 token + 缓存），再做重活（量化）。"
    }
  ],
  "followUpAnswers": [
    "取决于输出长度。短输出（如一句话描述）时，视觉编码（ViT 对视觉 token 的 O(V²)）占比明显，可能占一半以上；长输出（如详细长答）时，LLM 自回归随 token 数线性增长，稳稳主导，但视觉 token 仍会放大 LLM 每步的注意力成本。所以\"谁慢\"没有定论，必须分段实测你的真实负载。",
    "四个方向：①减视觉 token 数（降分辨率/动态 token、用更紧的 encoder）；②量化或蒸馏视觉编码器（INT8 编码小幅提速）；③缓存重复图像的编码结果（相同图多请求复用）；④并行——预处理与首 token 预取重叠、用投机解码加速 LLM 生成。优先做性价比高的（减 token + 缓存），再做重活（量化）。"
  ],
  "pitfalls": [
    "只盯总延迟不拆分瓶颈：凭直觉优化错环节，事倍功半。",
    "为降延迟过度减视觉 token 损精度：盲目砍 token 让模型看不清图，问答质量崩。",
    "忽略 P99 只看均值，线上尾延迟炸。"
  ],
  "beginnerSummary": "一次看图问答像做菜：洗切（预处理）、炒菜（视觉编码）、装盘（连接）、慢慢摆盘解说（LLM 一字字生成）。洗切炒菜是一次性固定功夫，解说越长越费时；图越清楚（高分辨率切片多）炒菜越久。要先分别掐表才知道该优化哪步——是菜炒得慢，还是解说太啰嗦。",
  "prerequisites": [
    "多模态比文本多视觉链路：知道延迟多了预处理/编码/连接三段。",
    "LLM 自回归随输出增长：输出越长，生成段越主导延迟。",
    "需分段定位瓶颈：会用计时打点把总延迟拆到各组件。"
  ],
  "workedExample": [
    "总 1.2s 拆为 enc 0.25 / conn 0.01 / llm 0.89：LLM 占 74%，优先优化生成。",
    "长输出（500 token）时 LLM 升到 ~4.4s，视觉占比降到 6%：瓶颈转移。",
    "高分辨率切 16 tile 时编码升到 ~1.0s：此时轮到优化编码器。"
  ],
  "lineByLine": [
    "分段打点计时：t0/t1/t2/t3 把预处理、编码、连接、生成四段切分，返回各段耗时。",
    "视觉编码是固定开销：encode_image 一次算完，不随输出长度变。",
    "LLM 随输出长度增长：llm_generate 自回归逐 token，耗时 ≈ 输出长度 × 每步成本。",
    "按瓶颈优化：本例 LLM 最大，则减 token/量化/并行；若编码大则减视觉 token。"
  ],
  "diagram": "输入 ─▶ 预处理 ─▶ 视觉编码 ─▶ 连接 ─▶ LLM生成\n   计时拆分各段 → 定位瓶颈"
};

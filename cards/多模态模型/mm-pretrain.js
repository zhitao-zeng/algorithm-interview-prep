export default {
  "id": "mm-pretrain",
  "kind": "concept",
  "category": "多模态模型",
  "difficulty": "Medium",
  "title": "多模态预训练目标",
  "prompt": "多模态大模型在预训练阶段都会用哪些训练目标？",
  "code": "def multimodal_loss(img, txt, model, w=(1.0, 0.5, 1.0)):\n    lc = contrastive(img, txt)        # 对齐\n    lm = itm(img, txt)                # 配对判别\n    lg = lm_loss(img, txt, model)     # 以图生文\n    return w[0]*lc + w[1]*lm + w[2]*lg",
  "diagram": "语料 ─▶ [对比 + 匹配 + 生成] 多目标 ─▶ 多模态表征",
  "explanationFocus": "是什么：多模态预训练目标指训练时用来逼模型学会『跨模态对齐与生成』的『考题』，主流有三类——①图文对比（Image-Text Contrastive，用 InfoNCE 拉齐匹配图文、推开不匹配的）；②图文匹配（ITM，二分类判断图文是否配对）；③图像条件文本生成（以图生文，自回归或 MLM）。BLIP-2 等用『对比+匹配+生成』联合，LLaVA 类先对齐再做指令式生成微调。",
  "quickAnswer": "常见多模态预训练目标有四类：(1) 图文对比（ITC/InfoNCE）把匹配图文拉近、非匹配推开，做语义对齐；(2) 图文匹配（ITM）二分类判别图文是否配对；(3) 图像条件文本生成（以图生文，自回归或 MLM）练『看图说话/答题』；(4) 视觉问答/指令式生成。BLIP-2 用『对比+匹配+生成』联合，LLaVA 类先对齐后用指令数据做生成式微调，目标是让模型『看图能对齐、能判别、能生成』。",
  "beginnerSummary": "预训练就像给学生出三种题：配对题（这句描述配不配这张图）、找相近题（哪句话和哪张图意思最近）、作文题（看着图写一段话/答一个问题）。三种题一起练，学生既懂图意、又能判断图文关系、还能开口描述或回答问题。只做一种题，能力就会有短板。",
  "walkthrough": "以 BLIP-2 为例，它在 Q-Former 上联合训练三个目标：ITC（图文对比，batch 内负样本、温度系数 τ≈0.1）、ITM（图文是否配对、二分类）、以图生文（给定图像生成描述，因果 LM 损失）。三目标加权联合，使 Q-Former 既学会把图投影到文本空间、又能判别配对、还能生成。训练数据约数百万图文对，单卡十几小时。",
  "approach": "按『对齐（对比）→判别（匹配）→生成（以图生文/答）』组织多目标，可联合加权或分阶段：先对比+ITM 把视觉表征对齐到文本空间，再冻结大部分做生成微调；权重需调，避免生成把对齐拉歪。",
  "bruteForce": "只在图文对上训一个分类头做单任务（如只做 ITM）：模型能判别配对却不会开放生成描述，泛化差、下游零样本能力弱，遇到『描述这张图』就傻眼。",
  "invariant": "无论目标怎么组合，最终学到的视觉-语言表征应同时支撑『对齐』与『生成』两种能力，不能为了生成把对齐搞坏。",
  "complexity": "各目标前向成本与其对应任务一致（对比是双塔编码、匹配是单塔、生成是自回归）；联合训练为各损失加权和，总前向约 3 倍单目标，但可共享视觉编码器前向、实际开销低于 3 倍。训练数据规模决定总体成本。",
  "derivation": [
    "为什么需要：要让模型既『对齐语义』又会『开放生成』，单一目标（如只分类）覆盖不了这两种能力，必须用多目标分别逼出对齐、判别、生成。",
    "怎么实现：对比损失（图文相似度、InfoNCE）、ITM（是否配对、二分类）、LM 损失（给定图生成描述或答案，因果交叉熵）；可加权联合或分阶段（先对齐后生成）。",
    "有什么代价：多目标权重难调，生成目标需要高质量图文/QA 语料且易过拟合到训练分布；对齐与生成可能互相拉扯（生成追求流畅、对齐追求保真）。",
    "怎么评测：看各目标在验证集上的损失，以及下游零样本/微调表现（如 COCO caption、VQAv2、图像检索召回），确认两种能力都没塌。"
  ],
  "edgeCases": [
    "噪声图文对（网上抓的图注不匹配）会损害对比目标，把错误配对当正样本，需用过滤或弱化权重。",
    "生成目标若不做去重/采样，模型会『背』训练样本而非真正理解，下游泛化差。",
    "多目标权重很敏感：生成权重过高会牺牲对齐，导致图像检索变差。",
    "纯文本语料若完全不混入，模型文本能力会退化，需在多模态训练时保留一定比例纯文本。"
  ],
  "pitfalls": [
    "以为一个目标（如只做对比）就能同时覆盖『理解』与『生成』，结果模型不会开放描述。",
    "忽视噪声图文对对对比目标的破坏，不做数据清洗直接训，表征被污染。",
    "生成与对齐权重拍脑袋定，没做 ablation，最终某能力塌方才发现。"
  ],
  "prerequisites": [
    "模型需同时具备『对齐』与『生成』能力，这是多模态的基本要求。",
    "对比/判别/生成是三种不同性质的能力，对应不同损失函数。",
    "需要大规模多模态语料（图文对、QA）支撑各目标。",
    "了解 InfoNCE、因果 LM 损失等基础训练目标。"
  ],
  "workedExample": [
    "对比目标：同 batch 内图文相似度最大化（正样本），与其他图/文相似度最小化（负样本），温度 τ=0.1。",
    "生成目标：给定一张猫的图片，自回归生成描述『A cat sitting on a couch』，算 token 级交叉熵。",
    "ITM：给 (图, 句) 判是否配对，输出匹配概率，难负样本（同图错句）提升判别力。"
  ],
  "lineByLine": [
    "设计对比目标（ITC）拉对齐：双塔编码图文，InfoNCE 拉近正对、推远负对。",
    "加 ITM 判别配对：单塔拼接图文，二分类输出匹配概率，用难负样本增强。",
    "加生成目标练以图生文：冻结/共享视觉编码器，因果 LM 损失训文本解码。",
    "加权联合或分阶段训练：先对齐后生成，调权重防互相拉扯。"
  ],
  "codeNotes": [
    "权重 w 需实验调，常用 w=(1.0, 0.5, 1.0) 这类经验值，但要按任务 ablation。",
    "生成目标常用因果 LM 损失（next-token），ITM 用二分类交叉熵，ITC 用 InfoNCE。",
    "视觉编码器前向可在三目标间共享，避免重复计算、省开销。"
  ],
  "followUps": [
    {
      "question": "对比目标和生成目标不冲突吗？",
      "answer": "会互相拉扯：对比追求『表征保真对齐』，生成追求『文本流畅合理』，二者优化方向不完全一致。所以常分阶段处理——先对比/ITM 把视觉对齐到文本空间，再冻结视觉侧、只微调生成侧做生成微调；或小心加权（生成权重别太高）。BLIP-2 正是用 Q-Former 做中介，缓解直接拉扯。"
    },
    {
      "question": "LLaVA 用什么预训练目标？",
      "answer": "LLaVA 的预训练阶段主要是『视觉-语言对齐』（把图像特征投影进 LLM 词空间），随后用指令/对话数据做自回归生成（next-token prediction）微调。它没有显式 ITM/对比头，核心是靠一个 projector 把视觉编码器输出对齐到 LLM，再用大量视觉指令对话数据练『看图对话』的生成能力。"
    }
  ],
  "followUpAnswers": [
    "会互相拉扯：对比追求『表征保真对齐』，生成追求『文本流畅合理』，二者优化方向不完全一致。所以常分阶段处理——先对比/ITM 把视觉对齐到文本空间，再冻结视觉侧、只微调生成侧做生成微调；或小心加权（生成权重别太高）。BLIP-2 正是用 Q-Former 做中介，缓解直接拉扯。",
    "LLaVA 的预训练阶段主要是『视觉-语言对齐』（把图像特征投影进 LLM 词空间），随后用指令/对话数据做自回归生成（next-token prediction）微调。它没有显式 ITM/对比头，核心是靠一个 projector 把视觉编码器输出对齐到 LLM，再用大量视觉指令对话数据练『看图对话』的生成能力。"
  ]
};

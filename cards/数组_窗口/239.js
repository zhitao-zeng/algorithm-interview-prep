export default {
  "kind": "code",
  "id": "239",
  "category": "数组/窗口",
  "difficulty": "Hard",
  "title": "滑动窗口最大值",
  "prompt": "返回每个长度 k 窗口的最大值。",
  "diagram": "nums=[1,3,-1,-3,5], k=3\n窗[1,3,-1]→max 3   队[3]\n窗[3,-1,-3]→max 3  队[3,-1,-3]\n窗[-1,-3,5]→max 5  队[5]\n输出 [3,3,5]\n队首=当前窗最大, 队列值递减",
  "quickAnswer": "单调递减双端队列保存下标；队首是最大值，先淘汰过期下标，再从尾部淘汰更小候选。",
  "approach": "单调递减双端队列保存下标；队首是最大值，先淘汰过期下标，再从尾部淘汰更小候选。",
  "explanationFocus": "滑动窗口最大值：单调递减双端队列保存下标；队首是最大值，先淘汰过期下标，再从尾部淘汰更小候选。",
  "bruteForce": "《滑动窗口最大值》可枚举所有子数组或窗口再计算指标，复杂度常为 O(n²) 或更高。",
  "invariant": "当前窗口始终满足 滑动窗口最大值：单调递减双端队列保存下标；队首是最大值，先淘汰过期下标，再从尾部淘汰更小候选。 的约束，辅助结构准确反映窗口内元素。",
  "walkthrough": "演练《滑动窗口最大值》时逐项移动左右边界，并记录哈希表、队列或计数器变化。",
  "derivation": [
    "每个窗口重新调用 max 需要 O(nk)。",
    "关键观察：新值比队尾的旧值更大时，那些旧值既更小又更早过期，永远不可能再成为最大值，可以淘汰。每个下标最多入队、出队一次，故线性。",
    "队列存下标还能用 i-k 判断过期，保证队首始终是「窗口内」的最大值。"
  ],
  "edgeCases": [
    "k<=0、空数组或 k 大于数组长度返回空列表。",
    "k=1 时每个窗口只有一个元素，输出原数组副本。",
    "重复最大值保留较新的下标，旧下标可安全淘汰（值相等不影响答案）。"
  ],
  "code": "# Python\nfrom collections import deque\n\ndef max_sliding_window(nums, k):\n    if not nums or k <= 0 or k > len(nums):\n        return []\n    window = deque()\n    result = []\n    for i, value in enumerate(nums):\n        while window and window[0] <= i - k:\n            window.popleft()\n        while window and nums[window[-1]] <= value:\n            window.pop()\n        window.append(i)\n        if i >= k - 1:\n            result.append(nums[window[0]])\n    return result",
  "codeNotes": [
    "left 指针只能右移，避免重复扫描。",
    "队列中优先保存下标以便淘汰过期元素。"
  ],
  "complexity": "时间 O(n)，空间 O(k)。每个下标最多入队、出队一次；队列大小不超过窗口长度 k。",
  "followUps": [
    {
      "question": "为什么队列要存下标而不是值？",
      "answer": "值本身无法说明元素是否已经离开窗口；存下标就能用 i-k 精确判断过期并淘汰，保证队首永远是窗口内的候选。"
    },
    {
      "question": "若要返回最大值及其下标怎么办？",
      "answer": "队首本来就是下标，直接输出 (nums[window[0]], window[0]) 即可，无需额外结构。"
    }
  ],
  "followUpAnswers": [
    "更新最优答案时同时保存左右端点。",
    "维护固定大小窗口和可淘汰的增量统计。"
  ],
  "pitfalls": [
    "只存值不存下标，导致无法判断队首是否已滑出窗口。",
    "从队尾淘汰时逻辑写错，把与当前值相等但更晚过期的候选过早删掉。"
  ],
  "beginnerSummary": "滑动窗口最大值：数组每次右移一格（窗口长度固定为 k），要快速知道每个窗口的最大值。暴力地每窗重算 max 是 O(nk)；用「单调递减双端队列」可以在 O(n) 内解决——队列里保存的是下标，对应的值从队首到队尾递减，因此队首永远是当前窗口的最大值。",
  "prerequisites": [
    "deque 两端都能 O(1) 加删，适合维护「候选最大值」。",
    "保存下标而不只是值，才能判断某个元素是否已经滑出窗口（下标 <= i-k 即过期）。",
    "队列始终保持「值递减」：新来的大值会把队尾所有更小或相等的旧候选淘汰掉。"
  ],
  "workedExample": [
    "nums=[1,3,-1,-3,5]、k=3。加入 1、3 时从队尾删除 1（因为 3 更大且更晚过期），队首为 3，第一个完整窗口答案是 3。",
    "i=4 加入 5：先淘汰过期下标 1（值 3，已不在窗口），再从队尾淘汰 -1、-3 等更小候选，窗口 [-1,-3,5] 的最大值为 5。"
  ],
  "lineByLine": [
    "先移除 i-k 之前的过期下标（window[0] <= i-k 时 popleft）。",
    "从队尾删除所有不大于当前值的下标，维护递减性。",
    "当前下标入队后，队首就是窗口最大值。",
    "i 达到 k-1 才开始输出完整窗口的答案。"
  ]
};

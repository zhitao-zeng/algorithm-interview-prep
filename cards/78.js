export default {
  "kind": "code",
  "id": "78",
  "category": "搜索/图",
  "difficulty": "Medium",
  "title": "子集",
  "prompt": "返回数组的全部子集。",
  "quickAnswer": "每个元素只有选或不选两种分支，也可按起点枚举组合。",
  "approach": "每个元素只有选或不选两种分支，也可按起点枚举组合。",
  "explanationFocus": "子集：每个元素只有选或不选两种分支，也可按起点枚举组合。",
  "bruteForce": "《子集》可不加剪枝地枚举所有路径或状态，分支数会快速爆炸。",
  "derivation": [
    "逐一枚举长度再组合会重复且繁琐。",
    "每个元素独立「选/不选」天然覆盖全部子集且不重；回溯按数组顺序决策保证 [1,2] 只以一种顺序出现。",
    "递归深度 = 数组长度，每片叶子对应一个子集。"
  ],
  "invariant": "搜索前沿只包含 子集：每个元素只有选或不选两种分支，也可按起点枚举组合。 下尚未扩展且状态合法的候选。",
  "walkthrough": "演练《子集》时画出一层搜索树或队列，标记访问和回溯恢复的位置。",
  "edgeCases": [
    "空数组：只有空集 []（一个子集）。",
    "含重复元素：本实现会产出重复子集，需要排序 + 同层同值跳过去重（进阶）。",
    "子集顺序无要求，DFS 产出的顺序即可。"
  ],
  "code": "# Python\ndef subsets(nums):\n    result = []\n    path = []\n\n    def dfs(start):\n        result.append(path[:])\n        for i in range(start, len(nums)):\n            path.append(nums[i])\n            dfs(i + 1)\n            path.pop()\n\n    dfs(0)\n    return result",
  "codeNotes": [
    "BFS 入队时标记访问，避免重复入队。",
    "回溯函数退出前恢复现场。"
  ],
  "complexity": "时间 O(n·2^n)（2^n 个子集，每个构造 O(n)），空间 O(n)（递归深度 + 路径）。",
  "followUps": [
    {
      "question": "为什么递归参数是 i+1？",
      "answer": "当前元素只能使用一次，下一层必须从它右边开始；若允许重复使用才会传 i。"
    },
    {
      "question": "如何生成固定大小 k 的子集？",
      "answer": "只有 len(path)==k 时收集，并在剩余元素不足以凑满 k 时剪枝。"
    }
  ],
  "followUpAnswers": [
    "无权最短步数使用 BFS；枚举组合或深度结构常用 DFS。",
    "保存 parent 或在 path 中记录选择。"
  ],
  "pitfalls": [
    "只在「叶子」收集子集而忘记把中间路径（如 [1]）也加入，会漏掉非满子集。",
    "加入结果用 path 引用而非副本，回溯后被改空。"
  ],
  "beginnerSummary": "给定无重复数组，输出所有子集（幂集）。每个元素面对一个二叉树式的抉择：「选」或「不选」，沿这条路走到底就得到一个子集。用回溯：每到一个位置，先「不选当前元素」往下一层走，再「选当前元素」往下一层走，到数组末尾就把当前路径作为一个子集收下。共 2^n 个子集。",
  "prerequisites": [
    "子集不要求顺序，[1,2] 和 [2,1] 算同一个，故按数组顺序「从左到右决策」即可避免重复。",
    "每个位置两种分支：跳过该元素 / 把该元素加入当前路径。",
    "当决策完所有元素（到达数组末尾），当前路径就是一个合法子集。"
  ],
  "workedExample": [
    "nums=[1,2,3]。从空路径开始：不选1→不选2→不选3 = []；不选1→不选2→选3 = [3]；不选1→选2→不选3 = [2]……直至选1选2选3 = [1,2,3]。",
    "共 2^3=8 个：[], [1], [2], [3], [1,2], [1,3], [2,3], [1,2,3]。"
  ],
  "lineByLine": [
    "result=[]，path=[]。",
    "dfs(start)：把 path 副本加入 result（每个节点都是子集，含空集）。",
    "从 start 遍历 i：path.append(nums[i])，dfs(i+1)，再 path.pop()（回溯撤销）。",
    "起始 dfs(0)，遍历完即得全部子集。"
  ],
  "diagram": "[1,2,3] 子集 (每个元素 选/不选)\n[]  [1]  [1,2]  [1,2,3]\n    [1,3] [2]    [2,3]   [3]\n共 2^3 = 8 个子集"
};

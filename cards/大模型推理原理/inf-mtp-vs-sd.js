export default {
  "kind": "concept",
  "id": "inf-mtp-vs-sd",
  "category": "大模型推理原理",
  "difficulty": "Hard",
  "title": "Multi-Token Prediction（MTP）与 Speculative Decoding 有什么区别？",
  "prompt": "Multi-Token Prediction（MTP）与 Speculative Decoding 有什么区别？MTP 本身一定带来推理加速吗？",
  "quickAnswer": "MTP 是训练目标：让模型在训练时同时预测多个未来 token（多个预测头/层），使隐藏状态对未来更敏感、学习信号更丰富；SD 是推理手段：用 Draft 模型提议、Target 模型并行验证，以无损方式减少大模型的自回归步数。二者都能产出「候选 token」，但 MTP 首先是一种训练策略，要转化成实际推理加速，还需配套的候选生成、并行验证与推理 Runtime（常复用 SD 的验证框架）；单有 MTP 训练目标本身并不保证推理更快。可结合使用：用 MTP 头做 Draft，再借 SD 框架无损验证。",
  "approach": "区分「训练期目标(MTP)」与「推理期机制(SD)」是关键。落地加速的组合路径是：①用 MTP（或 Medusa/EAGLE 这类候选生成方案）在每一步产出 K 个候选 token；②用 Target 大模型对这些候选做一次并行前向验证；③按目标分布无损接受/拒绝。MTP 提供「结构化候选」，SD 提供「无损验证框架」，二者可结合——但必须明确：没有第②③步的推理 Runtime，MTP 训练目标本身零加速。",
  "explanationFocus": "是什么：MTP（Multi-Token Prediction，多 token 预测）改的是「模型怎么训练」——让模型在训练时同时预测多个未来 token，使隐藏状态对未来更敏感；而 SD（Speculative Decoding，投机解码）改的是「模型怎么推理」——用 Draft 模型提议、Target 模型并行验证，以无损方式减少大模型的自回归步数。二者常被混为一谈，但处在完全不同的阶段：一个在训练期、一个在推理期，解决的问题也不同。",
  "bruteForce": "最朴素做法是「只做标准下一 token 训练 + 仅 Target 自回归推理（无 MTP、无 SD）」：训练时模型只学预测下一个字，隐藏状态对更远未来不敏感；推理时严格逐 token 串行生成，每生成一个 token 都要等一次完整前向。这是基线，没有任何多步前瞻或步数压缩，延迟完全等于 token 数×单步耗时。",
  "derivation": [
    "为什么需要：标准语言模型只预测「下一个 token」，隐藏状态主要编码「刚发生的 context」，对更远的未来不敏感，学到的信号较窄；而推理又受自回归步数限制，逐 token 串行天生慢。MTP 从训练侧补「多步前瞻」的信号，SD 从推理侧补「步数压缩」的机制。",
    "MTP 怎么实现：在主干上增加若干预测模块（MTP module），第 k 个模块负责预测第 k+1 个未来 token，训练损失为各步预测交叉熵之和，越远步可设越小权重（见代码 weights）。这迫使隐藏状态携带更多未来信息。",
    "SD 怎么实现：由轻量 Draft 模型（或草稿头）一次提议 K 个候选 token，Target 大模型对这 K 个候选做一次并行前向，按目标分布逐位接受/拒绝，保证输出分布与纯自回归无损一致（详见 SD 专题）。",
    "怎么评测：MTP 单独看训练收益——下游任务精度、困惑度是否改善；MTP 结合 SD 后看端到端加速比与接受率（acceptance rate）。接受率越高加速越明显；若候选频繁被拒，加速收益趋零，此时要回头调 Draft/候选生成质量。"
  ],
  "invariant": "MTP 是训练目标，不自带推理加速——要加速必须再接候选生成与并行验证（推理 Runtime）。更形式化地说：仅训练 MTP 而不改变推理路径时，端到端延迟不变；只有当 MTP 产出的候选被 Target 模型并行验证并接受，才算真正减少自回归步数。任何声称「加了 MTP 就更快」的说法，都必须指明它配套了怎样的推理验证框架。",
  "walkthrough": "以 DeepSeek-V3 为例，它把 MTP 作为训练目标（同时配合 MLA 与 MoE），在主干上挂若干 MTP 模块，使模型在训练时就对齐多步预测，隐藏状态对未来 token 更敏感。但这本身不改变推理方式——如果推理仍是标准逐 token 自回归，那 MTP 一点加速都没有。要真正加速，需要：用 MTP 模块在每一步产出未来 K 个候选 token，再由大模型（或自身）对这些候选做一次并行前向验证，按目标分布无损地接受/拒绝（借用 SD 的验证框架）。接受率高时，原本 K 步自回归被压缩成 1 步验证，端到端加速比可达 1.5~2×；接受率低（如长尾难样本）则收益有限。",
  "edgeCases": [
    "只训练 MTP 但不改推理：推理仍是逐 token 自回归，零加速——这是最常被误解的坑，MTP≠加速技术。",
    "MTP 候选被大模型大量拒绝：接受率低时加速收益极低，与 SD 的接受率问题同源；需提升 Draft 质量。",
    "与 Medusa/EAGLE 混淆：Medusa 加多个独立预测头、EAGLE 预测高层特征，都是「候选生成」方案，不等于 MTP 训练目标本身，但都能给 SD 供草稿。",
    "K 过大：预测头越多训练越贵，且越远未来越难预测，边际收益递减，K 一般取 1~4。"
  ],
  "code": "# Python\n# 概念示意: MTP 训练时多步预测头\ndef mtp_loss(logits_per_step, targets_per_step, weights=None):\n    # logits_per_step[k]: 预测第 k+1 个未来 token 的 logits\n    loss = 0.0\n    for k, (logit, tgt) in enumerate(zip(logits_per_step, targets_per_step)):\n        w = (weights or [1.0] * (k + 1))[k]\n        loss += w * cross_entropy(logit, tgt)  # 越远权重可调\n    return loss",
  "codeNotes": [
    "这是训练损失示意：logits_per_step[k] 是第 k+1 个未来 token 的预测，循环把它们加权求和成总损失。",
    "weights 让越远的预测权重越小（k 越大权重越小），避免远处噪声主导梯度；推理加速还需用这些头做候选 + 并行验证(Runtime)。",
    "MTP 与 MLA/MoE 常一起出现(如 DeepSeek-V3)，但概念相互独立，可单独理解。"
  ],
  "complexity": "训练侧：每步多 K 个预测头的前向与反向，训练成本随 K 线性增加（如 K=2~4 时训练算力增 10%~30%，取决于实现），但换来更丰富的学习信号。推理侧：仅当接 SD 类验证才影响步数——此时一次验证前向处理 K 个候选，若接受率高则等效把 K 步压缩成约 1 步，单步算力略增但总步数大降；若不接验证，推理复杂度与标准自回归完全相同。",
  "followUps": [
    {
      "question": "MTP 与 Speculative Decoding 核心区别？",
      "answer": "核心区别在阶段与目的：MTP 是训练目标，让模型在训练时学会多步前瞻（预测多个未来 token），改变的是『模型学到了什么』；SD 是推理机制，用 Draft 模型提议、Target 模型并行验证，以无损方式减少自回归步数，改变的是『生成怎么跑』。一个在训练期、一个在推理期，解决的是不同环节的问题，只是二者都能产出『候选 token』因而常被放一起讨论。"
    },
    {
      "question": "Medusa / EAGLE 属于哪类？",
      "answer": "二者都属于『候选生成方案』，不是 MTP 训练目标本身。Medusa 在主干上挂多个独立预测头一次性产出多候选；EAGLE 则基于已生成 token 的高层特征来预测下一个，也是为了给 SD 提供草稿。它们解决的是『SD 里 Draft 从哪来』的问题，可配合 MTP 或直接配合 SD 使用。"
    },
    {
      "question": "MTP 本身一定带来推理加速吗？",
      "answer": "不一定。MTP 首先是一个训练目标，它改变模型的表征与学习信号，但本身不改变推理路径——如果推理仍是标准逐 token 自回归，那加了 MTP 也零加速。要把它变成实际加速，还需要候选生成、并行验证和对应的推理 Runtime（通常复用 SD 的验证框架）。所以评估 MTP 价值时，要区分『训练收益』与『推理加速』，后者必须另接验证机制才成立。"
    },
    {
      "question": "Target Verification 指什么？",
      "answer": "Target Verification 即 SD 的核心验证步骤：用大模型（Target）对一批候选 token 做一次并行前向，得到目标分布后，按该分布逐位接受或拒绝候选——接受的片段与纯自回归采样在分布上完全无损一致。这一步保证了『加速但不改变输出分布』，是投机解码之所以『无损』的关键。"
    }
  ],
  "followUpAnswers": [
    "核心区别在阶段与目的：MTP 是训练目标，让模型在训练时学会多步前瞻（预测多个未来 token），改变的是『模型学到了什么』；SD 是推理机制，用 Draft 模型提议、Target 模型并行验证，以无损方式减少自回归步数，改变的是『生成怎么跑』。一个在训练期、一个在推理期，解决的是不同环节的问题，只是二者都能产出『候选 token』因而常被放一起讨论。",
    "二者都属于『候选生成方案』，不是 MTP 训练目标本身。Medusa 在主干上挂多个独立预测头一次性产出多候选；EAGLE 则基于已生成 token 的高层特征来预测下一个，也是为了给 SD 提供草稿。它们解决的是『SD 里 Draft 从哪来』的问题，可配合 MTP 或直接配合 SD 使用。",
    "不一定。MTP 首先是一个训练目标，它改变模型的表征与学习信号，但本身不改变推理路径——如果推理仍是标准逐 token 自回归，那加了 MTP 也零加速。要把它变成实际加速，还需要候选生成、并行验证和对应的推理 Runtime（通常复用 SD 的验证框架）。所以评估 MTP 价值时，要区分『训练收益』与『推理加速』，后者必须另接验证机制才成立。",
    "Target Verification 即 SD 的核心验证步骤：用大模型（Target）对一批候选 token 做一次并行前向，得到目标分布后，按该分布逐位接受或拒绝候选——接受的片段与纯自回归采样在分布上完全无损一致。这一步保证了『加速但不改变输出分布』，是投机解码之所以『无损』的关键。"
  ],
  "pitfalls": [
    "把 MTP 直接当成「推理加速技术」：忽略它只是训练目标，没有配套的候选生成与验证 Runtime 就零加速。",
    "把 Medusa/EAGLE 与 MTP 混为一谈：前者是候选生成方案、后者是训练目标，二者可结合但不可等同。",
    "以为多训练几个预测头就自动更快：训练目标要变成实际加速，必须再接 SD 类推理验证框架，否则只是多花了训练算力。"
  ],
  "beginnerSummary": "这俩名字都带「多 token」，但不在同一阶段。MTP（多 token 预测）是「训练时的目标」：让模型读书时不仅猜下一个字，还顺手猜下下个、下下下个字，逼模型想得更远、学得更深。投机解码（SD）是「推理时的技巧」：先用小车模（Draft）猜一长串字，再让大车模（Target）一次校验，对的就全收，省去逐字等待。MTP 让模型「更会猜」，SD 让生成「更快且不失真」。关键提醒：光训练 MTP 不会自动变快——你得再接一套「猜了之后怎么验」的推理流程（常复用 SD 框架），才能真的加速。",
  "prerequisites": [
    "区分训练目标与推理机制：理解「训练期做的事」和「推理期做的事」是两回事，不能混用。",
    "SD 用草稿+并行验证实现无损加速：理解接受/拒绝与分布无损的原理。",
    "DeepSeek-V3 用 MTP+MLA+MoE（但三者概念独立）：知道 MTP 常与这些技术结伴出现，便于读论文时不混淆。"
  ],
  "workedExample": [
    "MTP 训练：主干 + K 个预测头，损失含各步 CE，模型学会多步前瞻；此时若推理不变，延迟无变化。",
    "要加速：用 MTP 头产出 K 个候选 + 大模型并行验证（借 SD 框架），接受率高时 K 步压成 1 步验证，加速比 1.5~2×。",
    "反例：若候选接受率仅 30%，等效步数压缩有限，加速比掉到 1.1× 左右，说明 Draft/候选质量是关键。"
  ],
  "lineByLine": [
    "for k, (logit, tgt): 遍历每一步的未来 token 预测，k 从 0 开始对应「下一个 token」。",
    "w = (weights or [...])[k]：取第 k 步权重，越远（k 大）权重越小，抑制远处噪声。",
    "loss += w * cross_entropy(logit, tgt)：把各步预测损失按权重累加，得到 MTP 总损失。",
    "返回 loss 用于反向传播：模型因此被迫让隐藏状态编码多步未来信息——但注意这只是训练，推理加速还需另接验证框架。"
  ],
  "diagram": "对比:\nMTP  -- 训练目标 --> 模型多步前瞻\nSD   -- 推理机制 --> 草稿+并行验证(无损)\n结合: MTP 头产候选 -> SD 验证 -> 加速\n关键: MTP 不自带加速, 需推理 Runtime"
};

# 知识脉络梳理 · algorithm-interview-prep

> 这份题库现有 **29 个分类、380+ 题**，在刷题应用里是平铺成一排分类按钮的，打开没有"先学哪块、块之间怎么连"的主线。
> 本文把 29 个分类归成 **7 个主题域**，给出推荐学习路径和每类的一句话定位，并给**每个分类补充了"本类主线"**——即这一类下的题应该按什么逻辑顺序串起来学，帮你建立"地图感"——先知道全貌，再逐域刷，避免只见树木不见森林。
>
> 适用场景：临场复习前先过一遍脉络树建立框架；跨域交叉题（如"推理原理 + KV Cache + 量化"）单独串起来练；每个分类内部按"本类主线"顺序刷，而不是随机点。

---

## 0. 怎么读这份脉络

- **主题域** = 把平铺的 29 类按"知识依赖关系"归的组（A→G）。
- **学习路径** = 建议的复习先后顺序（不是强制，是按"前面是后面的地基"排的）。
- **一句话定位** = 每类到底考什么、在岗位里占多重。
- **本类主线** = 这一类下的题应该按什么逻辑顺序串（最重要的补充）。
- 标注 **【岗重】** 的是大模型算法 / 多模态岗的高权重域（对应 `社招三轮面试策略补充.md` 里强调的硬核块）。

---

## 1. 主题域总览（脉络树）

```
algorithm-interview-prep 知识地图（29 类 → 7 域）
│
├── A. 算法与数据结构基础 ──────────── coding 入场券，先保手撕不死
│   ├── 链表 / 二叉树 / 数组·窗口 / 二分·TopK / 搜索·图 / 动态规划
│   └── 模型手写（ML 经典算子的代码实现：attention / softmax / beam / RoPE…）
│
├── B. Transformer 与序列建模核心 ───── 理解大模型底座
│   ├── Transformer 架构（MHA/MQA/GQA、RoPE、RMSNorm、Pre/Post-Norm、SwiGLU…）
│   └── 长上下文与位置编码（RoPE 外推、多维 RoPE、相对位置…）
│
├── C. 大模型训练体系 ──────────────── 模型怎么训出来
│   ├── 训练与微调（SFT / LoRA / QLoRA / 蒸馏 / scaling laws / 数据配比）
│   ├── RL 后训练（RLHF / PPO / DPO / GRPO / 奖励模型）
│   └── 分布式训练（TP/PP/DP/EP/SP、ZeRO、1F1B、通信与气泡）
│
├── D. 大模型推理与部署【岗重】──────── 岗位最硬核：怎么跑得快、跑得省
│   ├── 大模型推理原理（prefill/decode、arithmetic intensity、roofline、TTFT/TPOT）
│   ├── KV Cache（为何缓存、增长规律、MHA/MQA/GQA、量化、prefix cache）
│   ├── Continuous Batching（连续批处理、调度、preemption、padding 浪费）
│   ├── PagedAttention（分页 KV、block 分配、COW 前缀、vLLM 实现）
│   ├── 流式推理工程（chunked prefill、PD 分离、speculative decoding、MTP vs SD）
│   ├── 多GPU并行（TP/PP/DP/EP/SP 推理侧、通信、策略选择）
│   ├── 量化推理（PTQ/QAT、INT8/INT4/FP8、AWQ/GPTQ、smoothquant、部署陷阱）
│   ├── ONNX/TensorRT（导出、opset、图优化、动态 shape、INT8 校准、插件）
│   └── 服务性能评测（QPS/并发、延迟分位、吞吐vs延迟、GPU 利用率、SLA、e2e 拆解）
│
├── E. 多模态与语音【岗重】─────────── 岗位特性：PGC/OGC 多模态 + 语音
│   ├── 多模态模型（CLIP / Q-Former / ViT、对齐、融合、幻觉、视频 token 压缩）
│   ├── ASR 专项（CTC / RNNT / VAD / 流式识别 / 解码）
│   └── 语音大模型（speech token、语义vs声学 token、thinker-talker、语音 LLM pipeline）
│
├── F. 应用层与系统设计 ────────────── 软硬结合，三轮面重点
│   ├── 搜索推荐（召回/粗排/精排、双塔、MMOE、偏差、冷启动、LLM 召回）
│   ├── RAG（检索/重排/索引、chunk、混合检索、评估、vs 微调）
│   ├── Agent Workflow（agent 定义、react、planning、tool calling、MCP vs FC、可观测/安全）
│   └── 系统设计（推荐架构、多模态理解服务、推理服务、AB 平台、特征 pipeline、容量规划）
│
└── G. 系统与底层 ─────────────────── 查漏补缺，压底层原理
    └── 计算机系统基础（进程/线程/协程、锁与原子、pinned memory、内存模型）
```

> 注：**KV Cache** 在原理上紧接 Transformer，但归属推理部署域（D）更顺——它解决的是"推理时为什么要存、怎么存、怎么省"，和 prefill/decode、量化是一体的。脉络上 B 之后直接接 D，逻辑连贯。

---

## 2. 推荐学习路径（按"地基→上层"排）

| 阶段 | 主题域 | 为什么这个顺序 | 建议投入 |
|---|---|---|---|
| 1 | **A. 算法与数据结构基础** | 手撕是入场券，一二轮就考；先保 coding 不死，才有后面 | 持续每天 1 题边写边讲 |
| 2 | **B. Transformer 与序列建模核心** | 所有大模型题的底座，不先吃透架构和位置编码，训练/推理都说不清 | 重点，配"底层深度自检" |
| 3 | **C. 大模型训练体系** | 理解模型怎么从数据变成权重，承接 B | 重点 |
| 4 | **D. 大模型推理与部署【岗重】** | 岗位最硬核、权重最高；且和 B/C 直接咬合（架构→训练→推理） | **最大头**，反复刷 |
| 5 | **E. 多模态与语音【岗重】** | 岗位多模态+语音特性，建立在 B/D 之上 | 重点（按 JD 倾斜） |
| 6 | **F. 应用层与系统设计** | 搜推/Agent/RAG/系统设计，把前面的硬核用进业务闭环，三轮面重点 | 重点，配策略文档第 1–2 节 |
| 7 | **G. 系统与底层** | 查漏补缺、压底层原理，面试被追问到实现时的兜底 | 按需补漏 |

**跨域交叉线（最容易被考"综合"的地方，建议单独串练）：**
- 推理全链路：`大模型推理原理 → KV Cache → Continuous Batching → PagedAttention → 量化推理 → 服务性能评测`（一条"让模型跑起来"的线）。
- 训练全链路：`Transformer 架构 → 训练与微调 → 分布式训练 → RL 后训练`。
- 多模态落地：`多模态模型 → 大模型推理原理 → 流式推理工程 → 系统设计`（PGC/OGC 视频理解系统的线）。
- Agent 生产化：`Agent Workflow → 系统设计 → 服务性能评测`（Agent 上线怎么扛量、怎么观测）。

---

## 3. 各分类一句话定位 + 本类主线

### A. 算法与数据结构基础

- **链表**
  - 定位：线性结构增删查、快慢指针、反转、环检测、合并/分割类手撕。
  - 本类主线：从最基础的"反转 / 合并" → "环形检测（快慢指针）" → "LRU Cache（哈希 + 双向链表的综合应用）" → "K 组翻转 / 区间反转"等困难变形。顺序：单链表操作 → 双指针技巧 → 结合哈希/虚拟头的中等题 → 困难变形。

- **二叉树**
  - 定位：遍历（前中后层）、递归/迭代互转、BST、序列化、最近公共祖先。
  - 本类主线：DFS/BFS 遍历打底（层序 / 锯齿层序） → 递归构造（前中序构造） → 路径/最值类（最大路径和，带 DP 思维） → 序列化（序列化与反序列化、验证前序序列化，树 ↔ 字符串）。顺序：遍历 → 构造 → 路径 → 序列化。

- **数组·窗口**
  - 定位：双指针、滑动窗口、前缀和、单调栈/队列，高频 Medium 主战场。
  - 本类主线：哈希 O(1) 查找（两数之和） → 滑动窗口（最大值 / 无重复子串，定长与变长） → 单调结构与前后缀（递增三元组 / 接雨水）。顺序：哈希 → 窗口 → 单调栈。

- **二分·TopK**
  - 定位：二分查找边界、堆/快选求第 K 大、TopK 与频率统计。
  - 本类主线：先掌握"在有序上二分找边界"（旋转数组搜索 / 最小值） → 再掌握"堆 / 快选求 TopK"（第 K 大 / 前 K 高频 / 两数组中位数）。顺序：二分边界 → TopK。

- **搜索·图**
  - 定位：DFS/BFS、拓扑排序、最短路、并查集、回溯，图论与状态搜索。
  - 本类主线：回溯穷举（子集 / 全排列 / 组合总和） → 网格图遍历（DFS 连通域：岛屿 / 单词搜索；BFS 最短路：腐烂橘子）。顺序：回溯 → 图遍历。

- **动态规划**
  - 定位：状态定义与转移、背包、序列/区间 DP、买卖股票类，重在"定义对"。
  - 本类主线：线性 DP（最大子数组 / 打家劫舍 / 股票） → 序列 DP（LCS / 编辑距离） → 背包与贪心边界（分割等和子集 / 单词拆分 / 跳跃游戏）。顺序：状态定义 → 序列/字符串 DP → 背包。

- **模型手写**
  - 定位：attention / 数值稳定 softmax / beam search / RoPE 等 ML 经典算子的**代码实现**，原理+代码双考。
  - 本类主线：归一化与损失（RMSNorm / BatchNorm / Dropout / CrossEntropy / BCE，训练基石） → 卷积与几何（1D / 2D 输出尺寸） → 检测后处理（IoU / NMS / KMeans） → 生成采样（Top-K / Beam Search） → 注意力（缩放点积 Attention，通向 Transformer）。顺序：训练组件 → 几何 → 后处理 → 采样 → 注意力。

### B. Transformer 与序列建模核心

- **Transformer 架构**
  - 定位：MHA/MQA/GQA、RoPE、RMSNorm/LayerNorm、Pre/Post-Norm、SwiGLU、因果掩码、注意力复杂度——大模型所有题的词汇表。
  - 本类主线：单头注意力怎么算（K/Q/V 投影、head 维度、复杂度 O(N²d)） → 三种变体 MHA/MQA/GQA 的取舍 → 位置编码演进（Sinusoidal→RoPE→ALiBi，重点 RoPE 原理 + 高频低频外推 + 多维推广） → 归一化（RMSNorm vs LayerNorm、Pre/Post-LN 为何普遍 Pre-LN+RMSNorm） → 前馈（FFN 与 SwiGLU 门控激活） → 因果掩码与"注意力 vs 卷积的归纳偏置" → FlashAttention（计算/显存层面收尾）。顺序：注意力计算 → 变体 → 位置 → 归一化 → 前馈 → 掩码 → FlashAttention。

- **长上下文与位置编码**
  - 定位：RoPE 外推（NTK/线性缩放）、多维 RoPE、相对位置、其他位置编码，长序列训练/推理的根。
  - 本类主线：先理解"外推 vs 插值"本质区别 → 线性缩放 PI → NTK-aware（调基频而非线性缩放） → YaRN（NTK-by-parts + 注意力温度缩放） → ALiBi（偏置替代编码） → 方案对比 → 训练侧（序列并行 / 上下文并行、跨块位置编号） → 评测（Passkey / Needle-in-a-Haystack）。顺序：问题定义 → 经典解法演进 → 训练落地 → 评测。

### C. 大模型训练体系

- **训练与微调**
  - 定位：SFT 数据构造与 loss mask、LoRA 秩/alpha 选择、QLoRA、蒸馏、灾难性遗忘、数据配比、scaling laws、课程学习。
  - 本类主线：先定位"预训练 vs 微调 vs 对齐"三者关系 → SFT（数据构造 / loss mask / 多轮 mask） → LoRA（原理 / 秩 α / merge / QLoRA） → 蒸馏（KD / teacher-student / KD vs RLHF） → 正则与过拟合 → 灾难性遗忘与持续学习 → 数据工程（配比 / 质量 / 去重 / Chinchilla / token 估算） → 课程学习。顺序：范式定位 → 指令微调 → 知识压缩 → 稳定性 → 数据工程。

- **RL 后训练**
  - 定位：RLHF/PPO/DPO/GRPO 分别解决什么、奖励模型、奖励 hacking、采样温度。
  - 本类主线：RL 基础概念（MDP / 贝尔曼 / Q-Learning） → 策略优化（策略梯度 / REINFORCE → Actor-Critic、V/Q/A） → PPO → GRPO → RLHF 流程 → SFT/DPO/RLHF 对比。顺序：RL 基础 → 策略算法演进 → 对齐到 LLM。

- **分布式训练**
  - 定位：多卡通信（NCCL）、TP/PP/DP/EP/SP 维度切分、ZeRO 三阶段、1F1B、流水线气泡。
  - 本类主线：先解决"放得下"（混合精度 / Gradient Checkpointing / ZeRO-FSDP 分片） → 再解决"算得快"（TP 切分 / PP 1F1B 与 bubble / EP / SP 与 Ring Attention） → Megatron 3D 并行组合 → 通信计算重叠与 bubble 优化 → MoE 训练。顺序：显存 → 多维并行 → 3D 组合 → 通信优化。

### D. 大模型推理与部署【岗重】

- **大模型推理原理**
  - 定位：prefill（compute-bound）vs decode（memory-bound）、arithmetic intensity、roofline、TTFT/TPOT/TPS、为什么慢。
  - 本类主线：从"模型推理为什么慢"切入 → Prefill vs Decode 区别（compute vs memory bound） → Arithmetic Intensity / Roofline → TTFT/TPOT 指标 → 首 token 为何慢 / Decode GPU 利用率低 / Batch Size 影响 → 进阶优化（Chunked Prefill / PD 分离 / Speculative Decoding / MTP vs SD） → 与架构呼应（MLA vs MHA/MQA/GQA、MoE Router）。顺序：瓶颈认知 → 两阶段本质 → 量化指标 → 工程优化 → 架构呼应。

- **KV Cache**
  - 定位：为何缓存、随 batch/上下文增长规律、MHA/MQA/GQA 对 KV 的影响、量化、prefix cache、共享前缀。
  - 本类主线：为什么需要（避免重复算） → 是什么 / 大小参数 / 随 batch 与上下文增长 / 不使用的后果 → MHA/MQA/GQA 影响（GQA 降显存） → 量化（能否量化 / 精度损失） → Prefix Cache / 多请求共享 System Prompt 复用。顺序：动机 → 原理与增长 → 变体影响 → 压缩（量化/复用）。

- **Continuous Batching**
  - 定位：连续批处理 vs 离散批、调度与 preemption、padding 浪费、吞吐对比。
  - 本类主线：静态 batching 的问题 → 为什么需要 → iteration-level 调度 → in-flight KV 动态分配 / 完成即释放 → 可变长与 padding 浪费 → 长短负载均衡 → 优先级与抢占 → 收益衡量 → 与 PagedAttention 协同 → 框架实现（vLLM / TRT-LLM）。顺序：问题 → 原理 → 资源管理 → 工程权衡 → 框架落地。

- **PagedAttention**
  - 定位：KV 分页（像虚拟内存）、block 分配/表、COW 前缀共享、碎片化降低、vLLM 实现。
  - 本类主线：为什么需要（KV 显存碎片） → 是什么 / 受 OS 分页启发 → block 分配 / 逻辑物理映射 / block table → block size 权衡 → copy-on-write 共享前缀 → 与 Continuous Batching 协同 → 评测（碎片减少 / 吞吐） → vLLM 实现细节与局限。顺序：碎片问题 → 分页机制 → 工程参数 → 协同与评测。

- **流式推理工程**
  - 定位：chunked prefill、PD 分离、speculative decoding、MTP vs SD、首 token 慢的根因与解法。
  - 本类主线：首字延迟 TTFT 工程优化 → 流式 tokenizer/detokenizer 增量对齐 → 流式停止判定与截断 → 背压与取消 → 多头多 token 预测（Medusa / EAGLE / Lookahead，配合投机解码）。顺序：延迟优化 → 流式正确性 → 流式控制 → 多 token 加速。

- **多GPU并行**
  - 定位：推理侧 TP/PP/DP/EP/SP、通信操作（all-reduce/all-gather）、部署策略选择。
  - 本类主线：为什么需要多 GPU → DP 及其局限 / ZeRO → TP（切分原理 / 矩阵乘切分 / all-reduce 通信 / 推理侧 TP） → PP（原理 / 1F1B / bubble） → EP（MoE / All-to-All） → SP / Ring Attention → 通信后端 NCCL / NVLink / IB → 并行策略选择 / 通信量对比。顺序：动机 → 数据并行 → 张量并行 → 流水线 → 专家/序列并行 → 通信与选型。

- **量化推理**
  - 定位：PTQ vs QAT、INT8/INT4/FP8、AWQ/GPTQ、granularity、outlier 与 smoothquant、部署陷阱与精度评估。
  - 本类主线：模型量化是什么 → PTQ vs QAT → 动态 vs 静态 → 对称/非对称 INT8 → INT4 AWQ/GPTQ → FP8 → 粒度(per-tensor/channel/group) → outlier 与 SmoothQuant → 权重/激活/KV 量化对比 → W4A16 等价 → 反量化 QMM → 校准流程 → 精度评测 → 部署坑 → 加速比。顺序：概念 → 两类范式 → 具体格式 → 粒度与 outlier → 三处量化对象 → 评测与部署。

- **ONNX/TensorRT**
  - 定位：导出与 opset、图优化/算子融合、动态 shape、INT8 校准、plugin、部署最佳实践。
  - 本类主线：ONNX 是什么 / 导出问题 → 多框架统一到 ONNX → 算子集与融合 / 图优化与常量折叠 → ONNX→TRT 转换链路 → TRT 构建引擎 / 主要优化 / plugin → 动态 shape 与动态 batch → FP16/INT8（校准） → 评测加速 / 延迟对比 → 部署最佳实践 / 痛点。顺序：中间表示 → 图优化 → 引擎构建 → 动态与量化 → 部署落地。

- **服务性能评测**
  - 定位：QPS/并发、延迟分位（P50/P99）、吞吐 vs 延迟权衡、饱和容量、GPU 利用率、SLA、e2e 拆解、常见陷阱。
  - 本类主线：为什么需要评测 → 吞吐 vs 延迟区别 → QPS/并发 → p50/p95/p99 → TTFT/TPOT/TPS → 批大小曲线 → GPU 利用率(MFU/SM) → 显存占用 → 端到端分解 → 饱和度/容量 → 冷启动/预热 → 流式评测 → 线上 vs 离线 → 长上下文影响 → 陷阱（缓存干扰/数据污染） → 工具 / 脚本设计 → SLA → 框架对比 → 成本。顺序：动机 → 核心指标 → 资源维度 → 工程细节 → 方法与陷阱。

### E. 多模态与语音【岗重】

- **多模态模型**
  - 定位：CLIP/Q-Former/ViT、特征对齐与融合、幻觉、视频 token 压缩、hi-res、benchmark、部署。
  - 本类主线：视觉编码器 ViT → 视觉 token 化(patch→token) → 跨模态对齐 CLIP → 连接器 Q-Former → 主流架构 → 融合方式 → 音频接入 / M-RoPE → 高分辨率 / 多图多轮 → 预训练目标 / 指令微调 → 幻觉 → 评测基准 → 部署（视觉编码器与 LLM 协同）/ 端到端延迟 → 视频（时序 / Token Explosion 压缩） → 设计短视频理解系统 → 多模态 vs 单模态权衡。顺序：编码器 → token 化 → 对齐 → 连接器 → 融合 → 进阶（高分辨率/视频/音频） → 训练与评测 → 部署与系统设计。

- **ASR 专项**
  - 定位：CTC/RNNT 原理、VAD、流式分段、partial/final、解码与语言模型融合。
  - 本类主线：CTC（Greedy / Prefix Beam，路径对齐） → RNN-T（前向递推 / Greedy，流式建模） → 流式 ASR 缓存（工程落地）。顺序：CTC → RNN-T → 流式工程。

- **语音大模型**
  - 定位：speech/semantic/acoustic token、thinker-talker、语音 LLM pipeline、训练阶段。
  - 本类主线：连续 vs 离散表示 → 语义 Token vs 声学 Token → RVQ → Audio Encoder 与 Adapter → 整体架构 → Thinker-Talker 双塔 → 训练阶段 → 全双工交互。顺序：表示形式 → token 类型 → 编码器/适配器 → 架构 → 训练与全双工。

### F. 应用层与系统设计

- **搜索推荐**
  - 定位：多路召回/融合、粗排+精排、双塔、MMOE/ESMM、偏差（位置/选择）、冷启动、LLM 召回、实时、指标。
  - 本类主线：工业整体架构 → 召回（目标 / ANN·Faiss / 多路融合 / 评测） → 粗排 → 精排（DIN/DeepFM/序列） → 重排（多样性/打散） → 多目标（CTR/CVR/时长） → CTR 预估 → 偏差（位置/选择） → 冷启动 → 特征工程 / 特征存储 → 实时流处理 → LLM 应用 → 视频配乐专项（生成 vs 检索 / 系统设计）。顺序：全栈架构 → 多层级漏斗（召回→粗排→精排→重排） → 多目标与偏差 → 特征与实时 → 专项（配乐）。

- **RAG**
  - 定位：检索/重排/索引、chunk 策略、混合检索、评估、与微调的取舍。
  - 本类主线：三段范式 → 文档切块 → 向量库 / ANN 选型 → 混合检索(BM25+向量) → 查询改写 / HyDE → 重排 → 元数据过滤 → 上下文压缩 → Self-RAG / CRAG 自省 → embedding 选型 → 失败模式 → 评测 → RAG vs 微调。顺序：基础范式 → 检索增强 → 重排与压缩 → 自省纠错 → 选型与评测。

- **Agent Workflow**
  - 定位：agent 定义、ReAct、planning、tool calling 与失败恢复、memory、MCP vs FC、orchestration、可观测、安全、生产部署。
  - 本类主线：定义与边界（什么是 Agent / Workflow 区别 / 什么不该用自主 Agent） → 核心范式（ReAct / planning / 反思 / CoT） → 手写实现（ReAct 循环 / tool 路由 / function caller / 记忆 buffer / 流式 SSE / 状态机） → 记忆与上下文管理 → 多步推理与任务调度 → 多 Agent 协作 / A2A / 编排框架 → 工具与安全（MCP vs FC / Guardrails / 权限 / human-in-loop） → 评测（最终答案不够 / 轨迹级 / 失败模式） → 生产化（最小骨架 / 部署挑战 / 可观测 / 成本控制） → 框架选型 / 多模态 Agent / RL 优化。顺序：定义 → 范式 → 手写 → 记忆/多步/多Agent → 工具与安全 → 评测 → 生产化。

- **系统设计**
  - 定位：推荐系统架构、多模态理解服务、推理服务架构、AB 实验平台、特征 pipeline（防 leakage）、向量检索、容量规划。
  - 本类主线：内容理解 Pipeline → 在线推理服务架构 → 多模态模型服务架构 → 高并发推荐架构 → 向量检索系统 → 实时流处理 ETL → 特征工程 Pipeline / 一致性 → A/B 实验平台 → 容量规划 / 限流降级 → 内容安全与版权合规。顺序：按"理解 → 推理 → 推荐 → 检索 → 流式 → 特征 → 实验 → 容量 → 合规"的业务闭环串。

### G. 系统与底层

- **计算机系统基础**
  - 定位：进程/线程/协程、锁与原子操作、pinned memory、内存模型——被追问到实现细节时的底层兜底。
  - 本类主线：执行单元模型（进程/线程/协程，Python GIL 影响） → 同步原语（Mutex / Spinlock / Atomic / CAS 场景） → 内存层级（Pageable / Pinned Memory / GPU 显存区别）。顺序：执行单元 → 同步原语 → 内存层级。

---

## 4. 针对"大模型算法 / 多模态"岗的优先级提示

如果你面的是 TikTok PGC/OGC 大模型算法这类岗位（多模态 + 语音 + Agent + 训练推理加速 + 搜推），权重建议：

1. **第一梯队（必须滚瓜烂熟）**：D 域全类（推理部署是岗位直接匹配项）、B 域（Transformer 与位置编码）、E 域（多模态与语音，岗位特性）。
2. **第二梯队（要能讲闭环）**：C 域（训练体系）、F 域（搜推/Agent/系统设计，三轮面重点）。
3. **第三梯队（查漏补缺）**：A 域（保手撕）、G 域（底层兜底）。

对应复习材料：`社招三轮面试策略补充.md` 第 1–4 节补的是 F 域的"软模块"和 D 域的深度自检；本题库补的是全部硬核题卡。

---

## 5. 怎么用这份脉络复习

1. **建立地图**：复习前先扫一遍第 1 节脉络树和第 3 节定位，知道自己有哪些盲区。
2. **按路径刷**：按第 2 节阶段 1→7 推进；进入某个分类后，**严格按第 3 节"本类主线"的顺序刷**，而不是随机点题——主线就是这一类内部的"最小知识依赖路径"。
3. **串交叉线**：单独抽时间走第 2 节的四条跨域线（推理全链路 / 训练全链路 / 多模态落地 / Agent 生产化），这是综合题高发区。
4. **按岗倾斜**：对照第 4 节优先级，把时间压在第一梯队，第三梯队只补缺不深抠。
5. **闭环验证**：每类挑 1 题，按"澄清→复杂度→边界→实现/讲解"四步自测（见策略文档第 3.6 节），卡壳的就回到对应域补。
